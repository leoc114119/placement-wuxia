/* battle_demo bundle —— 由 proto/battle_demo/build.mjs 生成，勿手改 */
(function () {
  var __mods = Object.create(null);
  function __def(id, fn) { __mods[id] = { fn: fn, exp: null }; }
  function __resolve(from, spec) {
    var dir = from.split("/").slice(0, -1);
    var parts = spec.split("/");
    var stack = dir.slice();
    for (var i = 0; i < parts.length; i++) {
      var p = parts[i];
      if (p === "." || p === "") continue;
      if (p === "..") stack.pop();
      else stack.push(p);
    }
    return stack.join("/");
  }
  function __req(from, spec) {
    var id = __resolve(from, spec);
    var m = __mods[id];
    if (!m) throw new Error("module not found: " + spec + " (from " + from + ")");
    if (m.exp === null) { m.exp = { exports: {} }; m.fn(function (s) { return __req(id, s); }, m.exp, m.exp.exports); }
    return m.exp.exports;
  }
  var __mod_proto_battle_demo_main = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst battle_hex_1 = require(\"../../config/battle-hex\");\nconst battle_input_1 = require(\"../../ui/battle-input\");\nconst battle_hex_render_1 = require(\"../../ui/battle-hex-render\");\nconst battle_session_1 = require(\"../../systems/battle-session\");\n// ===== 画布（逻辑分辨率自适应窗口实际比例，L 环反馈④；dpr 放大保真） =====\nlet W = 375;\nlet H = 667;\nconst canvas = document.getElementById('cv');\nconst dpr = Math.min(3, window.devicePixelRatio || 1);\nfunction resize() {\n    const r = canvas.getBoundingClientRect();\n    const w = Math.max(280, Math.round(r.width));\n    const h = Math.max(420, Math.round(r.height));\n    if (w === W && h === H)\n        return;\n    W = w;\n    H = h;\n    canvas.width = Math.round(W * dpr);\n    canvas.height = Math.round(H * dpr);\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n}\nconst ctx = canvas.getContext('2d');\nresize();\nwindow.addEventListener('resize', resize);\n// ===== toast =====\nconst toastEl = document.getElementById('toast');\nlet toastTimer = 0;\nfunction toast(msg) {\n    toastEl.textContent = msg;\n    toastEl.style.opacity = '1';\n    window.clearTimeout(toastTimer);\n    toastTimer = window.setTimeout(() => (toastEl.style.opacity = '0'), 1400);\n}\n// ===== 资源加载（版本号防缓存 + 帧预解码，失败降级 null 不崩） =====\n// config 路径=小游戏包根相对（生产唯一真值）；file:// 预览页位于 proto/battle_demo/，需回退到仓库根\nconst FILE_PREFIX = location.protocol === 'file:' ? '../../' : '';\nfunction loadImg(url) {\n    const im = new Image();\n    im.src = FILE_PREFIX + url;\n    return im.decode().then(() => im).catch(() => null);\n}\nasync function loadAssets() {\n    const q = (p) => `${p}?v=${battle_hex_1.BATTLE_HEX_RES.ver}`;\n    const frameJobs = [];\n    const frames = new Map();\n    for (const kind of battle_hex_1.BATTLE_HEX_RES.spriteKinds) {\n        const jobs = [];\n        for (let i = 0; i < battle_hex_1.BATTLE_HEX_RES.frameCount; i++)\n            jobs.push(loadImg(q(battle_hex_1.BATTLE_HEX_RES.frameSrc(kind, i))));\n        frameJobs.push(Promise.all(jobs).then((arr) => {\n            frames.set(kind, arr);\n        }));\n    }\n    // T23：ctrl 三钮独立脸 + 状态图标三枚（key 词表 poison/blood/skull = config BATTLE_HEX_RES.statusIcons）\n    const faceEntries = Object.entries(battle_hex_1.BATTLE_HEX_RES.ctrlFaces);\n    const faceJobs = faceEntries.map(async ([key, path]) => [key, await loadImg(q(path))]);\n    const iconEntries = Object.entries(battle_hex_1.BATTLE_HEX_RES.statusIcons);\n    const iconJobs = iconEntries.map(async ([key, path]) => [key, await loadImg(q(path))]);\n    const [env, topbar, plaque, facePairs, iconPairs] = await Promise.all([\n        loadImg(q(battle_hex_1.BATTLE_HEX_RES.env)),\n        loadImg(q(battle_hex_1.BATTLE_HEX_RES.topbar)),\n        loadImg(q(battle_hex_1.BATTLE_HEX_RES.plaque)),\n        Promise.all(faceJobs),\n        Promise.all(iconJobs),\n        ...frameJobs,\n    ]);\n    const ctrlFaces = { tuoguan: null, jiasu: null, flee: null };\n    for (const [key, img] of facePairs) {\n        if (key === 'tuoguan' || key === 'jiasu' || key === 'flee')\n            ctrlFaces[key] = img;\n    }\n    const statusIcons = new Map(iconPairs);\n    const ok = (i) => (i ? 'ok' : 'MISS');\n    console.log(`[battle_demo] 资源：env=${ok(env)} topbar=${ok(topbar)} plaque=${ok(plaque)} ` +\n        `ctrlFaces=[${facePairs.map(([k2, v]) => `${k2}:${ok(v)}`).join(' ')}] ` +\n        `statusIcons=[${iconPairs.map(([k2, v]) => `${k2}:${ok(v)}`).join(' ')}] ` +\n        `帧=[${[...frames.entries()].map(([k2, v]) => `${k2}:${v.filter(Boolean).length}/${v.length}`).join(' ')}]`);\n    return { env, topbar, plaque, ctrlFaces, statusIcons, frames };\n}\n// ===== 对局构造（联调：真 session；演示阵容=主角四技 vs 山贼双敌（Leo 09-04 裁定摘狼：设计无狼 NPC），R-07 档位语义占位） =====\n/** 演示技能表（id 与 ui ARC_BTNS.ids 对齐；数值走 SkillDef 结构由 core 结算，此处非真值来源） */\nconst DEMO_SKILLS = [\n    { id: 'te', name: '特', kind: 'special', weapon: 'fist', grade: 1.3, growth: 1, level: 20, cooldownTurns: 2, neiliCost: 20 },\n    { id: 'jue', name: '绝', kind: 'ultimate', weapon: 'fist', grade: 1.7, growth: 1, level: 20, cooldownTurns: 5, neiliCost: 35 },\n    { id: 'qing', name: '轻', kind: 'qingGong', weapon: null, grade: 1.0, growth: 1, level: 20, cooldownTurns: 3, neiliCost: 15 },\n    { id: 'du', name: '毒', kind: 'hiddenWeapon', weapon: 'hidden', grade: 1.0, growth: 1, level: 20, cooldownTurns: 1, neiliCost: 10 },\n];\nfunction demoUnit(over) {\n    return {\n        hp: 100,\n        maxHp: 100,\n        neili: 60,\n        maxNeili: 100,\n        atk: 12,\n        def: 3,\n        neigongLevel: 5,\n        jimin: 8,\n        danshi: 0,\n        shizhan: 60, // 演示高命中（保普攻可观测；命中率真值在 core F-04）\n        pos: { x: 0, y: 0 }, // 出生位由 session 按 O3 随机覆盖\n        weapon: 'fist',\n        skills: [],\n        ...over,\n    };\n}\nlet session = makeSession();\nlet speedOn = false;\nlet evCursor = 0; // session.events 消费游标（累积数组）\nfunction makeSession() {\n    // 敌方 name=configId（F3 约定：spriteKey=configId → 帧表键；名字牌暂显模板名，美化留后续）\n    return (0, battle_session_1.createHexBattle)({\n        player: demoUnit({ id: 'hero', side: 'player', name: '小虾米', skills: DEMO_SKILLS }),\n        enemies: [\n            demoUnit({ id: 'e1', side: 'enemy', name: 'npc-shanzei', hp: 70, maxHp: 70, atk: 8, jimin: 5 }),\n            demoUnit({ id: 'e2', side: 'enemy', name: 'npc-shanzei', hp: 60, maxHp: 60, atk: 9, jimin: 6 }),\n        ],\n        mode: 'manual',\n        seed: 42,\n    });\n}\nconst view = (0, battle_hex_render_1.createView)();\nlet assets = {\n    env: null,\n    topbar: null,\n    plaque: null,\n    ctrlFaces: { tuoguan: null, jiasu: null, flee: null },\n    statusIcons: new Map(),\n    frames: new Map(),\n};\nconst input = (0, battle_input_1.createBattleInput)({\n    dispatch: (req) => {\n        const ok = session.submit(req);\n        if (ok && req.type === 'toggleSpeed')\n            speedOn = !speedOn; // 演出态：加速中可视反馈\n    },\n    onBlocked: (msg) => toast(msg),\n    // T23 开放点③默认：onPlaque 回调移除——牌面点击由 input 层静默吞掉（不穿透、无提示，纯占位）\n    mode: () => session._debug.mode(),\n});\nfunction resetDemo() {\n    input.reset(); // A07：重开清输入拖动态（结算瞬间按住/拖镜不跨局残留）\n    session = makeSession();\n    evCursor = 0;\n    speedOn = false;\n    view.anim.clear();\n    view.moveAnims.clear();\n    view.camInit = false; // 重开重新定位镜头\n    view.fx.length = 0;\n    view.pendingHits.length = 0; // T21/E4：重开残留清理（旧对局挂起/震动/错位序号不跨局冒出）\n    view.shakes.clear();\n    view.dmgStagger.clear();\n    view.selectedCell = null;\n    view.skillPop = 0;\n    view.camDrag.x = 0;\n    view.camDrag.y = 0;\n}\nfunction toLogical(e) {\n    const r = canvas.getBoundingClientRect();\n    return { x: ((e.clientX - r.left) / r.width) * W, y: ((e.clientY - r.top) / r.height) * H };\n}\n// ===== 指针生命周期（A07）：同指针配对 + pointercancel/失焦重置 =====\nconst ptr = (0, battle_input_1.createPointerTracker)();\ncanvas.addEventListener('pointerdown', (e) => {\n    if (!ptr.down(e.pointerId))\n        return; // 多指交叉：非活动指忽略（同 id 重复 down=丢失 up 的自愈重锚）\n    const p = toLogical(e);\n    const snap = session.snapshot();\n    if (snap.phase !== 'fighting')\n        return; // 结算遮罩期点击=重开（抬起触发）\n    input.down(view, snap, p.x, p.y, W, H);\n});\ncanvas.addEventListener('pointermove', (e) => {\n    if (!ptr.owns(e.pointerId) || !input.pointer.down)\n        return; // 配对：非活动指的 move 忽略\n    const p = toLogical(e);\n    input.move(view, p.x, p.y);\n});\ncanvas.addEventListener('pointerup', (e) => {\n    if (!ptr.release(e.pointerId))\n        return; // 配对：id 不匹配/无活动指的 up 忽略（非配对释放不产生点击）\n    const p = toLogical(e);\n    const snap = session.snapshot();\n    if (snap.phase !== 'fighting') {\n        resetDemo();\n        return;\n    }\n    input.up(view, snap, p.x, p.y, W, H);\n});\ncanvas.addEventListener('pointercancel', (e) => {\n    if (!ptr.release(e.pointerId))\n        return; // 非活动指的 cancel 忽略\n    input.reset(); // A07：cancel=系统接管异常终止——视作未发生的按下：清拖动态，不产生点击\n});\nwindow.addEventListener('blur', () => {\n    ptr.reset(); // A07：失焦清指针归属\n    input.reset(); // 清拖动态（拖镜中失焦不残留 dragging）\n});\ndocument.addEventListener('dragstart', (e) => e.preventDefault());\nfunction logicalToCss(x, y) {\n    const r = canvas.getBoundingClientRect();\n    return { x: r.left + (x / W) * r.width, y: r.top + (y / H) * r.height };\n}\nfunction sampleHeroDrawPos() {\n    const hero = session.snapshot().actors.find((a) => a.id === 'hero');\n    if (!hero)\n        return { q: 0, r: 0, hop: 0 };\n    const ma = view.moveAnims.get(hero.id);\n    const mp = ma ? Math.min(1, ma.t / ma.duration) : 1;\n    const q = ma ? ma.from.q + (ma.pos.q - ma.from.q) * mp : hero.renderPos.q;\n    const r = ma ? ma.from.r + (ma.pos.r - ma.from.r) * mp : hero.renderPos.r;\n    return { q: +q.toFixed(3), r: +r.toFixed(3), hop: +(0, battle_hex_render_1.pieceHop)(view, hero).toFixed(1) };\n}\nwindow.__demo = {\n    get session() {\n        return session;\n    },\n    getView: () => view,\n    get W() {\n        return W;\n    },\n    get H() {\n        return H;\n    },\n    /** 格 → 页面坐标（自动化点击用） */\n    cellCss(q, r) {\n        const w = (0, battle_hex_1.hexToWorld)(q, r);\n        return logicalToCss(w.x - view.camera.x + W / 2, w.y - view.camera.y + H / 2);\n    },\n    /** 主角演出绘制位置采样（终验：移动帧序列单调性断言用） */\n    sampleHeroDraw() {\n        return sampleHeroDrawPos();\n    },\n    /** 真实 rAF 逐帧位置录制（终验闪烁实证：Performance.now 时间轴） */\n    startFrameLog() {\n        frameLog = [];\n        frameLogOn = true;\n    },\n    stopFrameLog() {\n        frameLogOn = false;\n        return frameLog;\n    },\n    /** 逻辑坐标 → 页面坐标（ctrl 等布局热区换算用） */\n    cssOf(lx, ly) {\n        return logicalToCss(lx, ly);\n    },\n    /** 弧形技能钮 → 页面坐标 */\n    btnCss(id) {\n        const b = view.layout.skillBtns.find((x) => x.id === id);\n        return b ? logicalToCss(b.x, b.y) : null;\n    },\n};\n// ===== 主循环 =====\nlet last = performance.now();\nlet frameLog = [];\nlet frameLogOn = false;\nfunction loop(t) {\n    const dt = Math.min(0.05, (t - last) / 1000 || 0);\n    last = t;\n    resize(); // 每帧检测窗口尺寸变化（resize 事件不可靠场景兜底）\n    if (frameLogOn)\n        frameLog.push({ t: Math.round(performance.now()), ...sampleHeroDrawPos() });\n    session.tick(dt);\n    const snap = session.snapshot();\n    view.uiState = { mode: session._debug.mode(), speed: speedOn };\n    // 事件消费（rejected 冒字 T15 R3 + T21 受击反馈白名单入队，方案 §2.2——其余一切不入队）\n    const evs = session.events;\n    for (; evCursor < evs.length; evCursor++) {\n        const e = evs[evCursor];\n        if (e.type === 'rejected') {\n            if (!e.actorId)\n                continue;\n            const actor = snap.actors.find((a) => a.id === e.actorId);\n            if (!actor)\n                continue;\n            const w = (0, battle_hex_1.hexToWorld)(actor.renderPos.q, actor.renderPos.r);\n            (0, battle_hex_render_1.spawnNoteFx)(view, w.x, w.y, battle_hex_1.REJECT_HINTS[e.reason ?? 'invalid'] ?? '无法执行');\n            continue;\n        }\n        // T21 白名单（§2.2）：basic/skill 且有 targetId 且 damage>0 → 冒数字+震动；\n        // miss 且有 targetId → 冒「闪避」不震（闪避=未受击）。fallback/blocked damage=0、\n        // 空放 skill（无 targetId 无 damage，session:768）、death/move/win/lose 等天然不入队。\n        // 数值铁律：text=String(e.damage) 直读事件字段禁任何换算（真值在 core，UI 只展示）。\n        if ((e.type === 'basic' || e.type === 'skill') && e.targetId && typeof e.damage === 'number' && e.damage > 0) {\n            (0, battle_hex_render_1.enqueueHit)(view, e.actorId ?? '', e.targetId, String(e.damage), true);\n        }\n        else if (e.type === 'miss' && e.targetId) {\n            (0, battle_hex_render_1.enqueueHit)(view, e.actorId ?? '', e.targetId, battle_hex_1.DMG.missText, false);\n        }\n    }\n    (0, battle_hex_render_1.updateView)(view, snap, dt, W, H);\n    (0, battle_hex_render_1.drawFrame)({ ctx, width: W, height: H, dt }, snap, assets, view);\n    requestAnimationFrame(loop);\n}\nvoid loadAssets().then((a) => {\n    assets = a;\n    requestAnimationFrame((t) => {\n        last = t;\n        loop(t);\n    });\n});\n";
  // ---- proto/battle_demo/main ----
  __def("proto/battle_demo/main", new Function("require", "module", "exports", __mod_proto_battle_demo_main));
  var __mod_types = "\"use strict\";\n// 所有数据结构定义（模块间通过类型咬合，AGENTS.md 施工守则 5）\n// T02 骨架阶段：仅放占位类型，玩法数据结构随 T03~T07 任务卡逐步补全\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.EMPTY_SCENE_ASSETS = void 0;\n/** 空资源（加载完成前的初始态） */\nexports.EMPTY_SCENE_ASSETS = { bg: null, heroFrames: [], buttonIcons: [] };\n";
  // ---- types ----
  __def("types", new Function("require", "module", "exports", __mod_types));
  var __mod_config_battle_hex = "\"use strict\";\n// T16 战斗 hex 表现层常量（frontend）——只读展示/几何参数（ADR-004 同口径：本文件禁止放结算公式）。\n// 依据：战场布局规格-六边形战棋.md 96 号（s=31 / 7×7 视口 / 平顶公式）、战斗界面视觉骨架.md v8 定稿\n//（色彩/组件布局）、主架构《战斗界面接入技术方案》§2（渲染分层）。数值真值仍在 battle-core / 云端 settle。\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.BATTLE_HEX_RES = exports.PLAQUE_BUTTONS = exports.CTRL_ACTIVE = exports.CTRL_TEXT = exports.CTRL_BUTTONS = exports.HIT_TOL = exports.PLAQUE_ART = exports.CTRL_ART = exports.COMPONENT_LAYOUT = exports.TOPBAR = exports.FONT_STACK = exports.DMG = exports.FX = exports.REJECT_HINTS = exports.ARC_BTNS = exports.HUD = exports.CHOREO = exports.ANIM_LOOP_GROUPS = exports.ANIM_FRAMES = exports.PIECE = exports.HIGHLIGHT = exports.TILE_NOISE = exports.SHADOW = exports.BOARD_SHAPE = exports.ENV_WORLD = exports.TILE = exports.JUMP = exports.CAMERA = exports.FIELD = exports.BOARD = exports.TILE_SPRITES = exports.SIDE_DEPTH = exports.ROW_H = exports.TILE_H = exports.TILE_W = exports.TILE_SPEC = void 0;\nexports.hexToWorld = hexToWorld;\nexports.jumpParamsFor = jumpParamsFor;\nexports.hexDist = hexDist;\nconst battle_1 = require(\"./battle\");\n// ===== 六边形几何（96 号定值：平顶 flat-top，s=31 → 格 62×54pt） =====\n// ===== 瓦片投影规格（L 环投影改造·Leo 看稿修正：尖角朝上/朝下的压扁六边形——上下尖角、左右竖直边，\n// 宽:高 ≈ 1:0.7；行错位半格 + 行距 0.75H 拼贴出边缘整齐的长方形战区（绘制层战区矩形裁剪）；立体侧面厚 12%） =====\nexports.TILE_SPEC = {\n    w: 88, // 横向平边间距（L 环二反馈①：62→88，560 宽基准每行 6.4 格 ∈ 6-7 格口径）\n    hRatio: 0.7, // 宽:高 = 1:0.7（纵向尖到尖，压扁）\n    rowRatio: 0.75, // 行距 = 压扁格高 × 0.75（奇偶行错位半格宽）\n    sideRatio: 0.12, // 下尖角/下斜边侧面厚 = 格高 × 12%\n};\nexports.TILE_W = exports.TILE_SPEC.w;\nexports.TILE_H = exports.TILE_W * exports.TILE_SPEC.hRatio;\nexports.ROW_H = exports.TILE_H * exports.TILE_SPEC.rowRatio;\nexports.SIDE_DEPTH = exports.TILE_H * exports.TILE_SPEC.sideRatio;\n/** 瓦片 sprite 素材（美术窗口产出：草绿/土黄立体瓦片，透明底；空串=未到位走代码绘制） */\nexports.TILE_SPRITES = {\n    grass: '',\n    dirt: '',\n};\n/** 压扁错位网格像素投影（投影层唯一公式；逻辑格 q/r 不变，col = q + ⌊r/2⌋ odd-r）：\n * px = (col + (row&1 ? 0.5 : 0)) × TILE_W；py = row × ROW_H。 */\nfunction hexToWorld(q, r) {\n    const col = q + Math.floor(r / 2);\n    return { x: (col + (Math.abs(r) % 2 === 1 ? 0.5 : 0)) * exports.TILE_W, y: r * exports.ROW_H };\n}\n// ===== 棋盘（offset odd-r 存储：16×16 地图；可动区=T15 R3 定版 FIELD：12 高 × 8 宽） =====\nexports.BOARD = {\n    cols: 16,\n    rows: 16,\n};\n/** 可动区（战区）边界（T15 R3 定版：col 4..11 / row 2..13，12 高 × 8 宽纵向走廊） */\nexports.FIELD = {\n    colMin: 4,\n    colMax: 11,\n    rowMin: 2,\n    rowMax: 13,\n};\n// ===== 视口与镜头（96 号 7×7 视口 + 旧 T06 已验拖动口径） =====\nexports.CAMERA = {\n    viewportCells: 7, // 以镜头中心 hex 为准的视口宽（96 号）\n    dragThresholdPx: 8, // 拖动生效判定（位移超过=拖镜头非点按，旧 T06 已验证）\n    worldPad: 40, // 全图包围盒四周留白（boardBounds 用；T24 起战区矩形 clip 已撤，包围盒保留作几何基准）\n    followPad: 96, // 镜头跟随聚焦：可动区外扩窄边（L 环二反馈①：土黄外围自然推出视口，绿区铺满主体）\n    smoothingSec: 0.22, // 镜头平滑回拉时长常数（L 环追加③：仅主角条满时回拉，指数平滑 tau）\n};\n// ===== 轻功跳跃演出参数（L 环追加①② + Leo 实测反馈：参数随 hex 距离插值——\n// 基准 2 格=0.6s/88px，每超 1 格 duration+0.15s、height+25%（线性），封顶防浮夸；短距 ≤2 格观感不变） =====\nexports.JUMP = {\n    baseDuration: 0.6, // 基准演出时长（秒，≤2 格）\n    baseHeight: 88, // 基准抛物线顶高（px，≤2 格）\n    baseCells: 2, // 基准距离（格；≤ 此距离不加成）\n    durationPerTile: 0.15, // 每超 1 格时长增量（秒）\n    heightPerTileRatio: 0.25, // 每超 1 格高度增幅（基准的 25%，线性）\n    maxDuration: 1.2, // 时长封顶\n    maxHeight: 176, // 高度封顶（2 倍基准）\n};\n/** 距离插值：hex 距离 → 演出时长/顶高（封顶防浮夸） */\nfunction jumpParamsFor(cells) {\n    const extra = Math.max(0, cells - exports.JUMP.baseCells);\n    const duration = Math.min(exports.JUMP.maxDuration, exports.JUMP.baseDuration + exports.JUMP.durationPerTile * extra);\n    const height = Math.min(exports.JUMP.maxHeight, exports.JUMP.baseHeight * (1 + exports.JUMP.heightPerTileRatio * extra));\n    return { duration, height };\n}\n/** hex 距离（cube 口径，与 systems/hex 同式；渲染表现侧自含） */\nfunction hexDist(a, b) {\n    const dq = a.q - b.q;\n    const dr = a.r - b.r;\n    return (Math.abs(dq) + Math.abs(dr) + Math.abs(dq + dr)) / 2;\n}\n// ===== 瓦片配色（v8：草绿/土黄两族区域化分布，非棋盘交替；光照上暗下亮——底部光源氛围） =====\nexports.TILE = {\n    topGrass: '#7d9b4a', // 草地绿（可移动区）\n    // T24 dirt 两档（方案 §2.2：由内向外渐暗，色值从 env 离线采样对齐——tools/sample_dirt_colors.mjs，\n    // 7×7 均值，禁运行时 getImageData）：inner=土路上段亮面 (705,790)；outer=土路下段暗面 (255,1460)\n    topDirtInner: '#8b703d',\n    topDirtOuter: '#514623',\n    dirtInnerRings: 1, // 距 FIELD 边 ≤ 本 ring 数（格环距）用内档，更远用外档（两档分界，shot 目验可调）\n    side: '#57432a', // 侧面土层（左下段）\n    sideShade: '#3e2f1c', // 侧面暗部（右下段，背光）\n    sideLit: '#6b543a', // 侧面受光段（底缘，底部光源氛围）\n    sideDepth: exports.SIDE_DEPTH, // 立体厚度（压扁规格派生）\n    edgeLight: '#d9c98f', // 顶面受光边描边\n    edgeDark: '#2e2418', // 背光边描边\n    strokeWidth: 1.5,\n};\n// ===== T24 场景融合参数组（方案 §2.3；ADR-004 只读展示参数，无结算公式） =====\n/** env 世界锚定矩形外扩余量：envRect = movableBounds 外扩 (width/2+margin, height/2+margin)，\n * width/height 用当帧实际屏尺寸（预览窗可拉伸，禁写死基准屏——方案 §四易错 1） */\nexports.ENV_WORLD = { margin: 80 };\n/** v8 有机缺角边缘（Leo 09-04 翻案旧「整齐矩形」）：棋盘最外 rings 圈的非可动 dirt 格按\n * 坐标哈希剔除 notchPerMille‰（确定性、逐帧稳定）；FIELD 内/出生锚区格绝不剔除 */\nexports.BOARD_SHAPE = { rings: 2, notchPerMille: 200 };\n/** 台面落地阴影（方案 §2.2）：边缘格逐格下偏软阴影两层——offsetPx 主影 + bottomMul 深偏影（底侧重，\n * 上大半被格体覆盖露出下缘月牙）；rgb=暖深棕（配土黄台面） */\nexports.SHADOW = { offsetPx: 6, alpha: 0.22, alphaDeep: 0.1, bottomMul: 1.6, rgb: '20, 12, 4' };\n/** 顶面静态噪点（方案 §2.2）：同色系 ±amp 明度伪随机点阵，seed=格坐标哈希（静态不闪——易错 5）；\n * step=候选网格间距 px、dotPx=点尺寸、density=候选命中密度（tileset 贴图到位自动替换本回退层，槽位保留） */\nexports.TILE_NOISE = { amp: 0.03, density: 0.18, step: 11, dotPx: 2 };\n// ===== 高亮层（L2：数据来自快照，渲染只画不算；moveKind 换色——绿=普通走位 / 金=轻功跳跃，联调 F1） =====\nexports.HIGHLIGHT = {\n    move: 'rgba(110, 220, 110, 0.38)', // 移动范围（绿）\n    moveEdge: 'rgba(160, 240, 160, 0.8)',\n    jump: 'rgba(245, 205, 70, 0.45)', // 轻功跳跃可达（金）\n    jumpEdge: 'rgba(255, 230, 130, 0.95)',\n    attack: 'rgba(225, 70, 55, 0.42)', // 攻击范围（红）\n    attackEdge: 'rgba(255, 120, 100, 0.85)',\n    selected: 'rgba(245, 205, 70, 0.5)', // 选中格（金）\n    selectedEdge: 'rgba(255, 230, 130, 0.95)',\n};\n// ===== 棋子（L3；占位帧=既有 battle/ 小表，T14 Q 版帧到位换 spriteKey+定尺系数即可） =====\nexports.PIECE = {\n    heightPerTile: 2.0, // 渲染高 = 压扁格高(TILE_H) × 本系数（定尺接口：T14 到位调此系数，O4）\n    bossScale: 1.25, // Boss 放大\n    walkFrameMs: 140, // 战斗步频（沿 config/battle BATTLE_FRAME 口径）\n    moveLerpSec: 0.3, // 普通行走位移表现时长\n    jumpHeightPx: 88, // 轻功抛物线顶高（L 环追加②：44→88 翻倍；=JUMP.height 同源）\n    deadAlpha: 0.45, // 阵亡变灰透明度\n    hitFlashSec: 0.15, // 受击红闪时长\n};\n/** animState → 帧组（帧组播报铁律：组内单播、组间不跨——组切换从组首帧重放，禁 05→06 直切）\n * 帧号与 config/battle.ts BATTLE_FRAME 同源：01~03 行走 / 04 蓄力 / 05 出招挥出 / 06 普攻 / 07 侧身待机 */\nexports.ANIM_FRAMES = {\n    idle: [battle_1.BATTLE_FRAME.idle],\n    walk: [battle_1.BATTLE_FRAME.walkStart, battle_1.BATTLE_FRAME.walkStart + 1, battle_1.BATTLE_FRAME.walkEnd],\n    charge: [battle_1.BATTLE_FRAME.charge],\n    strike: [battle_1.BATTLE_FRAME.strike],\n    basic: [battle_1.BATTLE_FRAME.basic],\n    hit: [0],\n    dead: [0],\n};\n/** 循环型帧组（walk 循环重放；其余单播型：播到组尾帧保持，直到 session 切状态） */\nexports.ANIM_LOOP_GROUPS = ['walk'];\n/** 出招演出时序（🟡 手感项，preview 目验可调；mock 按此驱动 animState 时间线） */\nexports.CHOREO = {\n    chargeSec: 0.1, // 蓄力段（04）\n    strikeSec: 0.3, // 出招挥出（05）\n    basicSec: 0.32, // 普攻全程（06+前冲回位口径）\n    hitSec: 0.18, // 受击段\n};\n// ===== 棋子 HUD（L4：v8 §1 头顶三件套——名字牌→绿行动条→红血条） =====\nexports.HUD = {\n    nameFontPx: 9,\n    nameAlly: '#a8d8a8', // 我方淡绿字（v8）\n    nameEnemy: '#e89a9a', // 敌方淡红字（v8）\n    barW: 46, // 条宽（px）\n    barH: 4,\n    barGap: 2, // 条间距\n    aboveHead: 4, // 三件套距头顶偏移\n    actionBarColor: '#5fd35f', // 绿行动条（v8）\n    hpBarColor: '#e04540', // 红血条（v8）\n    barBg: 'rgba(10, 10, 10, 0.6)',\n    nameBg: 'rgba(10, 10, 10, 0.45)',\n};\n// ===== 主角弧形特绝轻毒四钮（L4；v8：深色底鎏金描边金字；L 环反馈⑥：直径=头宽 2.4 倍放大可点面积） =====\nexports.ARC_BTNS = {\n    ids: ['te', 'jue', 'qing', 'du'], // 特技/绝学/轻功/毒功（与 ActionRequest.selectSkill 的 skillId 对表）\n    labels: ['特', '绝', '轻', '毒'],\n    headWidthRatio: 0.32, // 头宽 ≈ 棋子渲染宽 × 本系数\n    diameterPerHead: 2.4, // 钮直径 ≈ 头宽 × 2.4（L 环⑥：1.8→2.4 放大可点面积）\n    arcRadiusPerHead: 3.6, // 弧排布半径（×头宽）：弧距 = R×Δθ/3 ≈ 头宽×3.15 > 钮径，防重叠\n    angleFromDeg: 195, // 弧起始角（度，屏幕坐标系：180=正左、270=正上）\n    angleToDeg: 345, // 弧终止角\n    popSec: 0.18, // 弹出动画时长\n    colorBg: '#241a10',\n    colorRim: '#d4af37',\n    colorText: '#ffd870',\n    colorTextDisabled: '#8a7a58',\n    rimWidth: 2,\n    rimWidthSelected: 3.5,\n    rimColorSelected: '#fff0b0',\n    disabledAlpha: 0.45, // 内力不足/冷却中置灰\n};\n/** rejected 事件轻提示文案（T15 R3：拒绝可观测；按 reason 映射，头顶冒字） */\nexports.REJECT_HINTS = {\n    bar: '行动条未就绪',\n    range: '目标超出射程',\n    invalid: '无法执行',\n};\n// ===== 特效（L6：纯代码 fx；Q1②裁决——本期由 animState 驱动） =====\nexports.FX = {\n    slashSec: 0.28, // 出招斩击弧\n    hitSec: 0.25, // 受击环\n    slashColor: 'rgba(255, 235, 160, 0.9)',\n    hitColor: 'rgba(230, 80, 60, 0.85)',\n    maxRadius: 34,\n};\n// ===== 受击反馈 DMG 参数组（T21 · 方案 §2.6 全表；ADR-004 只读展示参数，无结算公式——L 环真机手感可调） =====\nexports.DMG = {\n    sec: 0.6, // 冒字寿命（s）\n    risePx: 24, // 上浮量（px）\n    fontPerH: 0.026, // 屏高定尺系数（667 屏 ≈ 17px；数字挂角色走屏高系，与 note 组件屏宽系刻意不同源——裁决①）\n    fillColor: '#ffffff', // 白字\n    strokeColor: '#1c1c1c', // 深描边\n    strokeWidth: 3, // 描边宽（px）\n    shakeSec: 0.2, // 震动时长（s）\n    shakePx: 3, // 震动幅度（±px）\n    shakeFreq: 55, // 震动频率（sin(t×freq)，rad/s）\n    staggerPx: 6, // 同位错位步长（px×命中序）\n    staggerWindowMs: 600, // 同位错位窗口（ms；滑动窗口，at=上一条 spawn 时刻——09-03 PM 裁 Q2）\n    flushDeadlineSec: 1.5, // 挂起冲刷兜底超时（s；> walk 300ms + 余量）\n    missText: '闪避', // miss 冒字文案（miss 不震动只冒字——§六确认点 1）\n};\n// ===== 战斗文字统一字体栈（Leo 09-04 裁定：战斗文字统一宋体族） =====\n/** 战斗界面代码字唯一字体栈（HUD 名字牌/弧形钮/木牌占位/ctrl 占位钮/rejected 冒字/DMG 冒字/结算遮罩）；\n * 与 TOPBAR.fontStack / CTRL_TEXT.fontStack 同族（既有落库保留原位），微信端无系统宋体回退 serif（M4 目验） */\nexports.FONT_STACK = '\"Songti SC\",\"STSong\",\"SimSun\",serif';\n// ===== 顶栏组件（L5；T23 实装：topbar_base.png 无字底图 + 代码条/名字/百分比/状态图标；\n// 常量组按 topbar_meta.json 重标定 2026-09-04——meta 双格式统一转 {x,y,w,h}：\n// bar_*_fill/name_bbox 原为 [x0,y0,x1,y1]、icon_* 原为 [x,y,w,h]、slots 原为 {x0,x1,y0,y1}，禁运行时读 json） =====\nexports.TOPBAR = {\n    artW: 1440,\n    artH: 300,\n    dimAlpha: 0.25, // 代码压暗层（Leo：原稿过亮；目验可调，零行为变更保留）\n    redFill: { x: 321, y: 94, w: 437, h: 43 }, // 血条槽（meta bar_red_fill [321,94,758,137] 实测原稿烘焙红填充严丝合缝）\n    blueFill: { x: 322, y: 153, w: 315, h: 42 }, // 内力条槽（meta bar_blue_fill [322,153,637,195]）\n    nameBox: { x: 322, y: 23, w: 149, h: 47 }, // 名字区（meta name_bbox [322,23,471,70]，左对齐垂直居中）\n    statusSlots: [\n        // 状态图标槽×4（meta slots {x0,x1,y0,y1} 转换；图标按槽中心对齐绘制）\n        { x: 321, y: 206, w: 84, h: 78 },\n        { x: 420, y: 206, w: 83, h: 78 },\n        { x: 517, y: 206, w: 78, h: 78 },\n        { x: 625, y: 206, w: 82, h: 78 },\n    ],\n    hpGradient: ['#e22a23', '#931a15'], // 血条纵向渐变 条顶→条底（开放点①默认：采样原稿，rgb(226,42,35)→rgb(147,26,21)）\n    neiliGradient: ['#1a94f4', '#064faf'], // 内力条纵向渐变（rgb(26,148,244)→rgb(6,79,175)）\n    nameFontPx: 44, // 名字字号（art px，×k 落屏）\n    pctFontPx: 34, // 百分比字号（art px）\n    pctPadRight: 6, // 百分比右对齐锚 = 填充末端 − 本值（art px，随填充末端移动复刻烘焙稿位）\n    textFill: 'rgb(242, 228, 192)', // 奶黄字（ctrl_face_text_meta 同族）\n    textStroke: 'rgb(42, 29, 18)', // 深描边（同族）\n    textStrokeWidth: 5, // 描边宽（art px，strokeText 先描后填）\n    fontStack: '\"Songti SC\",\"STSong\",\"SimSun\",serif', // 宋体栈（微信端无系统宋体回退 serif，M4 目验）\n};\n// ===== 组件布局（L5；L 环反馈④：ctrl 改右下锚定+高度占比上限——任何窗口比例恒贴右下可见） =====\nexports.COMPONENT_LAYOUT = {\n    /** 左侧木牌挂串（左上锚） */\n    plaque: { leftRatio: 0.012, topRatio: 0.075, wRatio: 0.17, maxHRatio: 0.42 },\n    /** 右下托管/加速/逃跑（右下锚）：w=min(wRatio×W, maxHRatio×H÷artH/artW) —— 短边约束防溢出 */\n    ctrl: { rightRatio: 0.02, bottomRatio: 0.025, wRatio: 0.183, maxHRatio: 0.42 },\n};\n/** ctrl 组件美术标定尺寸（锚定/行映射用它，弱化对图片对象尺寸的依赖） */\nexports.CTRL_ART = { w: 223, h: 448 };\n/** plaque 组件美术标定尺寸（T20-FE D-13：pickPlaqueButton 的 art→屏换算用，与 CTRL_ART 对称） */\nexports.PLAQUE_ART = { w: 310, h: 680 };\n/**\n * 组件热区容差（T20-FE D-13 / HIT-1）：标定矩形四边外扩 短边×ratio，分部件定值。\n * 【2026-09-03 Leo/PM 裁决】方案 §4.2 单值 tolRatio=0.15 与 §2.4/§4.3/§九-5 冲突（ctrl 钮容差\n * ≈19px art 会吞没钮间隙 33px 与左右边缘 2~5px，热区=外接矩形全覆盖、收缩量为负）——裁决：\n * ctrl=0（满宽条带实体，标定矩形本体即热区，间隙+边缘自然 fall-through）；plaque=0.15\n *（≈21px art ≈4.5px 屏，与弧钮 1.3 半径容差折算精度同量级）。ADR-004 口径展示参数，preview 手感可调。\n */\nexports.HIT_TOL = { ctrl: 0, plaque: 0.15 };\n/**\n * ctrl_r_alpha.png（=CTRL_ART 尺寸）三钮热区标定矩形（art 坐标系）→ ActionRequest 映射。\n * T20-FE D-13 复量核定（行剖面 alpha>240，measure_hotzones.mjs）：实体 bbox 钮1 {x:3,w:218} /\n * 钮2 {x:2,w:219} / 钮3 {x:2,w:219}，行宽下限 202——落库值取方案 §4.2 参考值（收 2~3px 安全边，\n * 防素材边缘半透明噪点；容差 19px 外扩下手感无差）。装饰性边缘光晕不设热区。\n */\nexports.CTRL_BUTTONS = [\n    { x: 5, y: 2, w: 216, h: 128, action: 'mode' }, // 托管\n    { x: 5, y: 163, w: 213, h: 126, action: 'speed' }, // 加速\n    { x: 5, y: 319, w: 213, h: 127, action: 'flee' }, // 逃跑\n];\n/**\n * ctrl 代码字样式（T23 · ctrl_face_text_meta.json 落库 2026-09-04；ADR-004 只读展示参数）。\n * 比例位以钮本体宽 216 为基准（切图=钮本体 1:1，meta 的 252×164 canvas 为设计画布溯源、代码不使用）；\n * text_center [132,66] 为 Leo 09-04 右移定值（避开骷髅/双刀装饰图标，meta note），需求文档旧值 122 已勘误。\n */\nexports.CTRL_TEXT = {\n    fontStack: '\"Songti SC\",\"STSong\",\"SimSun\",serif', // 宋体（meta font Songti；微信端回退 serif，M4 目验）\n    sizeRatio: 50 / 216, // 字号 50 @钮本体 216 宽（等比 ×btnW）\n    fill: 'rgb(242, 228, 192)', // 奶黄\n    stroke: 'rgb(42, 29, 18)', // 深描边\n    strokeWidthRatio: 5 / 216, // 描边宽 5 @216（strokeText 先描后填）\n    shadowOffsetRatio: 3 / 216, // 阴影偏移 3 @216（深色错位垫底实现）\n    centerRatio: { x: 132 / 216, y: 66 / 216 }, // 文字中心在钮矩形内的比例位（meta text_center [132,66]）\n    normal: { mode: '托管', speed: '加速' }, // 常态字（meta normal_text）\n    active: { mode: '自动', speed: '两倍' }, // 激活字（meta active_text）；逃跑=静态图不叠字\n};\n/**\n * ctrl 激活态样式（判定源=view.uiState 宿主镜像，渲染层禁直调 session._debug）。\n * Leo 09-04 L环拍板：去叠亮/柔光，激活态=金框+换字（meta active_fx 的 brighten/glow 五参数随之失效删除，仅存 gold_frame）。\n */\nexports.CTRL_ACTIVE = {\n    goldFrame: 'rgba(255, 205, 95, 0.95)', // 金框（meta gold_frame [255,205,95]）\n    frameWidthRatio: 4 / 216, // 金框线宽 @216（≈4）\n};\n/**\n * plaque_l_alpha.png（=PLAQUE_ART 尺寸）两块木牌热区标定矩形（牌面占比；文字已烘焙在切图内）。\n * T20-FE D-13 复量核定（最长连续实体 run 口径，系绳孔收腰防全行计数高估）：落库 x/w 取方案 §4.2\n * 参考值 26/273（可点主体，左右透明边+侧穗不设热区）；y/hRatio 沿 2026-09-02 牌面标定不动\n *（0.26/0.55、0.21 ≈ 方案 h:143px 口径；run 复核牌体带 y 177..306/367..504 落在现带内，收腰段保留可点）。\n * 装饰件（顶部横杆/挂绳/流苏）不设热区——HIT-1「可点部件的实际图形」收窄解释（方案 §九-5）。\n */\nexports.PLAQUE_BUTTONS = [\n    { xRatio: 26 / 310, yRatio: 0.26, wRatio: 273 / 310, hRatio: 0.21, label: '装备' },\n    { xRatio: 26 / 310, yRatio: 0.55, wRatio: 273 / 310, hRatio: 0.21, label: '武功' },\n];\n// ===== 素材路径表（资源外置铁律：路径唯一出处=本表；版本号防缓存，preview 换图 bump） =====\nexports.BATTLE_HEX_RES = {\n    ver: 't24v1',\n    env: 'assets/ui/pixel/battle/raw/battle_env_pure.png', // L0 纯环境底图（无格无 UI，1088×1920）\n    topbar: 'assets/ui/pixel/battle/components/topbar_base.png', // T23：无字底图（名字/百分比/条由代码绘制）\n    plaque: 'assets/ui/pixel/battle/components/plaque_l_alpha.png',\n    /** T23：ctrl 三钮独立脸（切图=CTRL_BUTTONS 钮本体 1:1 预裁；缺图逐钮代码占位兜底） */\n    ctrlFaces: {\n        tuoguan: 'assets/ui/pixel/battle/components/ctrl_tuoguan_face.png',\n        jiasu: 'assets/ui/pixel/battle/components/ctrl_jiasu_face.png',\n        flee: 'assets/ui/pixel/battle/components/ctrl_flee.png',\n    },\n    /** T23：状态图标映射表（key 词表 poison/blood/skull，与 types.ts:290 冻结字段注释语义对齐；\n     * 传入 key 命中即点亮，MVP 快照恒空数组=空槽；第 4 槽无素材预留不映射） */\n    statusIcons: {\n        poison: 'assets/ui/pixel/battle/components/icon_status_poison.png',\n        blood: 'assets/ui/pixel/battle/components/icon_status_blood.png',\n        skull: 'assets/ui/pixel/battle/components/icon_status_skull.png',\n    },\n    /** 占位帧表：沿用既有 battle/ 小表（frameSrc 与 ui/assets.ts 同源），T14 Q 版帧到位即换表 */\n    frameSrc: (kind, i) => {\n        const dir = kind === 'hero' ? 'hero' : 'spr_' + kind.replace('npc-', '').replace(/-/g, '_');\n        return `assets/ui/frames/battle/${dir}/${dir}_0${i}_transparent.png`;\n    },\n    frameCount: 8, // 00~07（BATTLE_FRAME.idle=7 需全量 8 帧）\n    /** 预载帧表键（联调 F3：敌方 spriteKey=configId，按 T15 敌型对齐；玩家恒 'hero'。\n     * Leo 09-04 裁定摘狼：设计无狼 NPC，演示阵容全山贼系——spr_lang/ 素材归档保留不删） */\n    spriteKinds: ['hero', 'npc-shanzei'],\n};\n";
  // ---- config/battle-hex ----
  __def("config/battle-hex", new Function("require", "module", "exports", __mod_config_battle_hex));
  var __mod_config_battle = "\"use strict\";\n// 战斗界面演出常量（75 v2.1 + A1-T06 v2 裁决；全部为表现/布局参数，结算公式唯一真值在 systems/battle-core.ts）\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.DEBUG_ENTRY = exports.RESULT_OVERLAY = exports.SPEED_FACTOR = exports.SKILL_BTN = exports.ROUND_BTN = exports.TOP_PANEL = exports.BAR = exports.BATTLE_FRAME = exports.FX = exports.MOVE = exports.BODY_ANCHOR = exports.FOOT_DROP = exports.BODY_CALIB = exports.BOSS_SCALE = exports.SPRITE_HEIGHT_PER_TILE = exports.PLATFORM = exports.CAMERA = exports.TILE_HALF_H = exports.TILE_HALF_W = exports.TILE_SIZE = exports.ENEMY_ROW_Y = exports.PLAYER_ROW_Y = exports.BOARD_ROWS = exports.BOARD_COLS = void 0;\nexports.battleFrameSrc = battleFrameSrc;\n// ===== 棋盘几何（§1/§1b.1 方向 A 等距投影） =====\nexports.BOARD_COLS = 8;\nexports.BOARD_ROWS = 12;\nexports.PLAYER_ROW_Y = 10; // P2-5 我方布阵行\nexports.ENEMY_ROW_Y = 1; // P2-5 敌方布阵行\n/** 菱形格边长（TS）——两层架构代码常量（75 v2.3 §1b.4：几何唯一真源=代码，背景零几何契约）。\n * 初值 77（棋盘世界轮廓 20·THW≈1333 × 20·THH≈770，拖动查看全场）；L 环可调。 */\nexports.TILE_SIZE = 54; // L 环二轮：77→54（格子过大不精致，Leo 08-22 定）\n/** THW = TS·cos30°，THH = TS·sin30°（75 §1b.1 定式） */\nexports.TILE_HALF_W = exports.TILE_SIZE * 0.866;\nexports.TILE_HALF_H = exports.TILE_SIZE * 0.5;\n/** 镜头（75 v2.3 §1b.2）：以主角为中心自动跟随 + 手动拖动偏移；clamp 于台面包围盒+边距（v2.3：环境背景\n * 屏幕空间静态不参与视野夹取）；无缩放 */\nexports.CAMERA = {\n    /** 台面（棋盘）世界包围盒四周留白（px）——拖动/跟随 clamp 边距 */\n    worldPad: 40,\n    /** 拖动生效判定：位移超过此值（物理 px）视为拖镜头而非点按 */\n    dragThresholdPx: 8,\n};\n// ===== 战斗台（Layer1 全代码绘制，75 v2.3 §1b.4：无贴图采样、几何唯一真源=代码常量） =====\nexports.PLATFORM = {\n    /** 台面填充（石面暖调，规格色板系内取色） */\n    fill: 'rgba(163, 177, 138, 0.92)',\n    /** 台面描边 */\n    edge: 'rgba(43, 43, 43, 0.75)',\n    edgeWidth: 3,\n    /** 厚度侧沿（悬浮台侧立面，下缘两条边向下挤出）——L 环五轮c：Leo 定调小+加透明 */\n    side: 'rgba(74, 90, 62, 0.7)',\n    sideDepth: 10, // 侧沿厚度（px）：18→10\n    /** 格线（L 环二轮：调淡调细，棋盘精致化） */\n    grid: 'rgba(43, 43, 43, 0.20)',\n    gridWidth: 1,\n};\n// ===== 棋子比例（§8b.4：renderH = tileVisualH × spriteHeightPerTile × bossScale；按主体占比反推画布绘制高） =====\nexports.SPRITE_HEIGHT_PER_TILE = {\n    humanoid: 1.6, // L 环四轮：1.2→1.35→1.6（Leo 定，观感不够再调）\n    wolf: 0.8, // 四足狼形（区间 0.75~0.85）\n};\nexports.BOSS_SCALE = 1.3; // 狼王 ×（狼基线 0.8×1.3≈1.05）\nexports.BODY_CALIB = {\n    // ground = 00~03（旧构图格）/ attack = 04~07（d2 重画格：攻击三连+待机，狼构图形变主体悬画布上半）\n    hero: {\n        ground: { cx: 0.533, bottom: 0.988, ratio: 0.983 },\n        attack: { cx: 0.521, bottom: 0.951, ratio: 0.929 },\n    },\n    humanoid: {\n        ground: { cx: 0.554, bottom: 0.966, ratio: 0.955 },\n        attack: { cx: 0.505, bottom: 0.94, ratio: 0.87 },\n    },\n    wolf: {\n        ground: { cx: 0.505, bottom: 0.961, ratio: 0.512 },\n        attack: { cx: 0.505, bottom: 0.593, ratio: 0.489 },\n    },\n    boss: {\n        ground: { cx: 0.481, bottom: 0.861, ratio: 0.638 },\n        attack: { cx: 0.572, bottom: 0.782, ratio: 0.613 },\n    },\n};\n/** 脚点垂直微调（格视觉高 2·THH 的比例；正=下移）。\n * =0.2（L 环五轮 Leo 定：0.25 微过冲，回收 0.05）：等距菱形格的几何格心在视觉上偏格后缘（上半），\n * 角色站格心显悬空——整体下压 1/4 格高做视觉补偿，脚踩视觉格心。观感仍偏继续调此数。 */\nexports.FOOT_DROP = 0.2; // L 环五轮b：Leo 复验 0.25 过冲，上提 0.05\nexports.BODY_ANCHOR = {\n    // cx = 脚部中心（主体底部 12% 带的水平中点均值，00~03 标定）——真实站姿落点，比包围盒中心\n    // （0.488，被侧伸的剑/手臂拉偏）更准；L 环四轮b：0.488 锚致脚偏左、躯干显偏右。\n    hero: { cx: 0.527, bottom: 0.986 },\n    humanoid: { cx: 0.546, bottom: 0.963 }, // 山贼\n    wolf: { cx: 0.506, bottom: 0.961 }, // 野狼\n    boss: { cx: 0.447, bottom: 0.977 }, // 狼王（脚部中心实测，构图偏画布左）\n};\n// ===== 移动表现（§8b.3 + A1 Q8 老网金二选一） =====\nexports.MOVE = {\n    lerpSec: 0.3, // 普通移动贴格中心平滑时长\n    qinggongArcTiles: 0.6, // 轻功抛物线顶高（格高）\n    baseRange: 2, // F-06 基础移动力（+装配轻功加成 + ⌊轻功等级/5⌋；MVP 玩家无轻功 → 2）\n    qinggongRangeFactor: 2, // [轻]跳跃范围 = 移动力 × 此系数（金色格）\n    qinggongMpCost: 10, // A1 Q9：轻功内力消耗（R-09 表）；冷却 0\n};\n// ===== 武功光影时序（§8c.2 四段式） =====\nexports.FX = {\n    chargeSec: 0.1, // 蓄势\n    mainSecMin: 0.3, // 主体光效时长下限\n    mainSecMax: 0.5, // 上限（按品阶在区间取值：一阶 0.3 / 二阶 0.4 / 三阶 0.5）\n    hitFlashSec: 0.15, // 命中闪白\n    fadeSec: 0.12, // 收尾消散\n    shakeSec: 0.15, // 绝学/Boss 出招全屏微震时长\n    shakeAmpPx: 5, // 震幅（幅度小）\n    basicLungeSec: 0.16, // 普攻半格前冲 + 回位（各半）——basicTotalSec 的构成基准\n    strikeSec: 0.3, // 出招 05 挥出帧持续（04 蓄力 = chargeSec 0.1）\n    basicTotalSec: 0.32, // 普攻 06 单帧 + 前冲回位全程（两段 basicLungeSec）\n};\n// ===== 帧映射（硬规则：出招 04→05 两帧、普攻 06 单帧+代码动效、移动 01~03 循环、禁 05→06 直切） =====\nexports.BATTLE_FRAME = {\n    idle: 7, // Q4-T06：启用⑧侧身待机（Q3 黑椭圆根因=加载器少第 8 帧，素材已勘误无恙；hero_07 轻微偏暗转观察项）\n    walkStart: 1,\n    walkEnd: 3,\n    charge: 4, // 出招预备（蓄力段）\n    strike: 5, // 出招挥出\n    basic: 6, // 普攻单帧\n    walkFrameMs: 140, // 战斗内步频略快于江湖\n};\n// ===== 行动条 / 顶栏（§1c/§2） =====\nexports.BAR = {\n    max: 100, // 与引擎 BAR_MAX 同值（展示口径）\n    nameColorAlly: '#4A7A6B', // 竹青（§1c 我方字色）\n    nameColorEnemy: '#E2574C', // 朱砂（敌方）\n    actionBarColor: '#D4AF37', // 淡金行动条\n    hpBarColor: '#E2574C', // 朱砂血条\n    mpBarColor: '#4A7A9B', // A1 Q2 定版黛蓝（非竹青系）\n};\n// ===== 顶栏三件套（A1 Q3：头像+朱砂血条+黛蓝内力条一整块面板；右上避胶囊——锚顶复用 getStatusBarBottomPx） =====\nexports.TOP_PANEL = {\n    padRatio: 0.03, // 面板距状态栏底的间距（占屏高）\n    heightRatio: 0.11, // 面板高（占屏高）\n    avatarRatio: 0.075, // 头像直径（占屏宽）\n};\n// ===== 圆钮（左下 [属性][装备][退出] / 右下 [⚙][⏩] / 特轻绝同规格——代码统一常量，A1/工单口径） =====\nexports.ROUND_BTN = {\n    radiusRatio: 0.07, // 半径（占屏宽）\n    gapRatio: 0.025, // 竖排钮间距（占屏高）\n    bottomInsetRatio: 0.03, // 距屏底（占屏高）\n    iconFontR: 0.62, // 图标字号 = r × 系数\n    labelFontR: 0.3,\n};\n// ===== 特/轻/绝悬浮钮（行动条满时主角上方；与圆钮同规格） =====\nexports.SKILL_BTN = {\n    gapRatio: 0.02, // 三钮横排间距（占屏宽）\n    aboveActorPx: 90, // 悬浮于主角头顶上方偏移（逻辑 px）\n};\n// ===== 加速（A1 Q13：全局时间倍率，演出层 dt × 系数） =====\nexports.SPEED_FACTOR = { normal: 1, fast: 2 };\n// ===== 结算遮罩 / 占位（A1 Q5：胜负遮罩+战报统计，奖励与疗伤占位文案） =====\nexports.RESULT_OVERLAY = {\n    fadeInSec: 0.3,\n};\n/** 战斗帧源构建器（Q3-T06）：battle/ 小表 128×256（等比下采样，清晰度修复 F4b 收尾）。\n * kind = 'hero' | NPC configId（npc-shanzei/npc-lang/npc-boss-lang）→ 目录名映射；\n * 江湖场景 heroFrameSrc / NPC appearance.sprite 前缀不动（大表）。 */\nfunction battleFrameSrc(kind, i) {\n    // kind：'hero' | 'npc-shanzei' | 'npc-lang' | 'npc-boss-lang' → battle/ 目录名（npc- 前缀换 spr_，连字符转下划线）\n    const dir = kind === 'hero' ? 'hero' : 'spr_' + kind.replace('npc-', '').replace(/-/g, '_');\n    return `assets/ui/frames/battle/${dir}/${dir}_0${i}_transparent.png`;\n}\n// ===== 调试入口（A1 Q12：preview ?battle=1&seed=N + console wx.__enterBattle(seed)） =====\nexports.DEBUG_ENTRY = {\n    queryFlag: '__BATTLE_DEBUG__', // globalThis 标记（preview 页设置后再加载 game.js）\n};\n";
  // ---- config/battle ----
  __def("config/battle", new Function("require", "module", "exports", __mod_config_battle));
  var __mod_ui_battle_input = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createPointerTracker = createPointerTracker;\nexports.pickSkillButton = pickSkillButton;\nexports.pickCtrlButton = pickCtrlButton;\nexports.pickPlaqueButton = pickPlaqueButton;\nexports.createBattleInput = createBattleInput;\nconst battle_hex_1 = require(\"../config/battle-hex\");\nconst battle_hex_render_1 = require(\"./battle-hex-render\");\nfunction createPointerTracker() {\n    let activeId = null;\n    return {\n        get activeId() {\n            return activeId;\n        },\n        down(id) {\n            if (activeId !== null && activeId !== id)\n                return false; // 多指交叉：非活动指忽略\n            activeId = id; // 首指受理；同 id 重复 down=丢失 up 的自愈（重锚）\n            return true;\n        },\n        owns(id) {\n            return activeId !== null && activeId === id;\n        },\n        release(id) {\n            if (activeId === null || activeId !== id)\n                return false;\n            activeId = null;\n            return true;\n        },\n        reset() {\n            activeId = null;\n        },\n    };\n}\nconst sameCell = (a, b) => a.q === b.q && a.r === b.r;\n/** 命中弧形技能钮（纯函数，导出供用例）：返回命中的 skillId 或 null */\nfunction pickSkillButton(view, x, y) {\n    for (const b of view.layout.skillBtns) {\n        const rr = b.r * 1.3; // 命中容差（圆钮略放大热区）\n        if ((x - b.x) ** 2 + (y - b.y) ** 2 <= rr * rr)\n            return { id: b.id, disabled: b.disabled };\n    }\n    return null;\n}\n/**\n * 命中 ctrl 钮标定矩形（T20-FE D-13 / HIT-1 纯函数，导出供用例）：返回 action 或 null。\n * 热区=每钮标定矩形（art 坐标）+容差（短边×HIT_TOL.ctrl，Leo/PM 裁决 ctrl=0）——外接矩形/行带口径\n * 废弃；图形外（钮间带间隙 art y 130..163/289..319、左右边缘 x<5/>218）返回 null → fall-through\n * 棋盘格（HIT-1：图形内优先、图形外不吞格）。多钮矩形若因容差重叠，沿 CTRL_BUTTONS 数组序首个命中。\n */\nfunction pickCtrlButton(view, x, y) {\n    const rect = view.layout.ctrlRect;\n    if (!rect)\n        return null;\n    const ax = ((x - rect.x) / rect.w) * battle_hex_1.CTRL_ART.w;\n    const ay = ((y - rect.y) / rect.h) * battle_hex_1.CTRL_ART.h;\n    for (const b of battle_hex_1.CTRL_BUTTONS) {\n        const tol = Math.min(b.w, b.h) * battle_hex_1.HIT_TOL.ctrl;\n        if (ax >= b.x - tol && ax <= b.x + b.w + tol && ay >= b.y - tol && ay <= b.y + b.h + tol)\n            return b.action;\n    }\n    return null;\n}\n/**\n * 命中 plaque 木牌标定矩形（T20-FE D-13 / HIT-1 纯函数，导出供用例）：返回 label 或 null。\n * 热区=两牌可点主体标定矩形（xRatio/wRatio）+容差——装饰件（横杆/挂绳/流苏）不设热区，\n * 左右透明边 fall-through 棋盘格（HIT-1）。\n */\nfunction pickPlaqueButton(view, x, y) {\n    const rect = view.layout.plaqueRect;\n    if (!rect)\n        return null;\n    const ax = ((x - rect.x) / rect.w) * battle_hex_1.PLAQUE_ART.w;\n    const ay = ((y - rect.y) / rect.h) * battle_hex_1.PLAQUE_ART.h;\n    for (const b of battle_hex_1.PLAQUE_BUTTONS) {\n        const bx = b.xRatio * battle_hex_1.PLAQUE_ART.w;\n        const bw = b.wRatio * battle_hex_1.PLAQUE_ART.w;\n        const by = b.yRatio * battle_hex_1.PLAQUE_ART.h;\n        const bh = b.hRatio * battle_hex_1.PLAQUE_ART.h;\n        const tol = Math.min(bw, bh) * battle_hex_1.HIT_TOL.plaque;\n        if (ax >= bx - tol && ax <= bx + bw + tol && ay >= by - tol && ay <= by + bh + tol)\n            return b.label;\n    }\n    return null;\n}\nfunction createBattleInput(hooks) {\n    const pointer = { down: false, sx: 0, sy: 0, lx: 0, ly: 0, dragging: false };\n    function down(_view, _snapshot, x, y) {\n        pointer.down = true;\n        pointer.sx = pointer.lx = x;\n        pointer.sy = pointer.ly = y;\n        pointer.dragging = false;\n    }\n    function move(view, x, y) {\n        if (!pointer.down)\n            return;\n        const dxTotal = x - pointer.sx;\n        const dyTotal = y - pointer.sy;\n        if (!pointer.dragging && Math.hypot(dxTotal, dyTotal) > battle_hex_1.CAMERA.dragThresholdPx)\n            pointer.dragging = true;\n        if (pointer.dragging) {\n            // 拖镜头：画面跟手（手指右拖=内容右移 → 相机反向平移，L 环反馈⑤方向修正）；clamp 在 computeCamera 统一处理\n            view.camDrag.x -= x - pointer.lx;\n            view.camDrag.y -= y - pointer.ly;\n        }\n        pointer.lx = x;\n        pointer.ly = y;\n    }\n    function up(view, snapshot, x, y, width, height) {\n        // A07 核实结论：原实现无 down 守卫（move 有）→ 无有效 down 的 up 会真处理点击（体检报告属实），\n        // 本卡按派发口径改为忽略——非配对释放不产生点击；用例锁死该语义。\n        if (!pointer.down)\n            return;\n        const wasDragging = pointer.dragging;\n        pointer.down = false;\n        pointer.dragging = false;\n        if (wasDragging)\n            return; // 拖动结束不触发点选（旧 T06 口径）\n        // ① 主角弧形特绝轻毒钮（UI > 格子；弹出中才可命中）\n        if (view.skillPop > 0.5) {\n            const hitBtn = pickSkillButton(view, x, y);\n            if (hitBtn) {\n                if (hitBtn.disabled) {\n                    hooks.onBlocked?.('内力不足或冷却中');\n                    return;\n                }\n                if (snapshot.selectedSkill === hitBtn.id)\n                    hooks.dispatch({ type: 'cancelSkill' });\n                else\n                    hooks.dispatch({ type: 'selectSkill', skillId: hitBtn.id });\n                return;\n            }\n        }\n        // ② 右下 ctrl 三钮（托管/加速/逃跑）——D-13/HIT-1：逐钮标定矩形+容差，图形外 fall-through 棋盘\n        const ctrlAction = pickCtrlButton(view, x, y);\n        if (ctrlAction) {\n            if (ctrlAction === 'mode')\n                hooks.dispatch({ type: 'setMode', mode: hooks.mode?.() === 'auto' ? 'manual' : 'auto' });\n            else if (ctrlAction === 'speed')\n                hooks.dispatch({ type: 'toggleSpeed' });\n            else\n                hooks.dispatch({ type: 'flee' });\n            return;\n        }\n        // ③ 左侧木牌（装备/武功——占位反馈）——D-13/HIT-1：两牌标定矩形+容差，装饰件不设热区\n        const plaque = pickPlaqueButton(view, x, y);\n        if (plaque) {\n            hooks.onPlaque?.(plaque);\n            return;\n        }\n        // ④ 棋盘格点选（仅主角行动回合等待输入时）\n        if (snapshot.phase !== 'fighting' || !snapshot.pendingInput)\n            return;\n        const cam = view.camera;\n        const cell = (0, battle_hex_render_1.worldToHex)(x - width / 2 + cam.x, y - height / 2 + cam.y);\n        if (!(0, battle_hex_render_1.axialToOffset)(cell)) {\n            if (snapshot.selectedSkill)\n                hooks.dispatch({ type: 'cancelSkill' }); // 点棋盘外=取消施放\n            return;\n        }\n        // 敌棋子命中按逻辑 hex（快照 pos，结算真值）——与 renderPos 动画位解耦（L 环终验根因 A：\n        // 敌移动动画中 renderPos≠pos，按动画位匹配点击落空→误走 cancelSkill）\n        const target = snapshot.actors.find((a) => a.side === 'enemy' && a.animState !== 'dead' && a.pos.q === cell.q && a.pos.r === cell.r);\n        const inMove = snapshot.moveCells.some((c) => sameCell(c, cell));\n        const inAttack = snapshot.attackCells.some((c) => sameCell(c, cell));\n        const skill = snapshot.selectedSkill;\n        if (target) {\n            if (skill === 'qing')\n                return; // 【ATK-4 / T18-D4 · 主架构授权例外】轻功态点敌=无操作（不派发任何请求）\n            if (skill) {\n                // 【T20-FE · 方案 B / ATK-2 v2.0】特/绝选中点敌逻辑格统一派 cast（格上有敌=对敌结算，session 侧\n                // 与 attack 同源四查）。单一决策点收敛：弃「敌格 attack(按逻辑位)/空格 cast(按 legalCells)」双轨——\n                // 敌移动中两口径可分叉（方案 §3.3）。tests/battle-hex-render.test.ts :330 断言随卡改写\n                //（方案 :339 锚实指 :330，行号为写作时快照）。\n                hooks.dispatch({ type: 'cast', to: cell, skillId: skill });\n                return;\n            }\n            // 点敌人：未激活=普攻（ATK-1，零改动）\n            hooks.dispatch({ type: 'attack', targetId: target.id, skillId: null });\n            return;\n        }\n        if (skill === 'qing' && inMove) {\n            // 轻功：点可移动格=跳跃位移\n            view.selectedCell = cell;\n            hooks.dispatch({ type: 'move', to: cell });\n            return;\n        }\n        if (!skill && inMove) {\n            // 点可移动格=移动\n            view.selectedCell = cell;\n            hooks.dispatch({ type: 'move', to: cell });\n            return;\n        }\n        // 【T19/N2 机制② · 方案 §4】普攻态（无技能选中）点中移动中敌的「演出位」（可见位≠逻辑格）\n        // 且该格非合法移动格 → 不派发任何请求（零 dispatch）、onBlocked 可观测反馈。\n        // 分支序：pos 命中/轻功金格/无选中绿格之后、cancelSkill 之前——\n        // 逻辑格可点（D-06 绿锁）与移动意图优先（绿/金格）不受影响；skill 态（特/绝/轻/毒）零改动。\n        // 「移动中」= 双通道：session 位移窗（renderPos≠pos，300ms）∪ FE 演出窗（view.moveAnims.has，\n        // 长演出尾段）；可视位量化与绘制/点击同链 worldToHex(hexToWorld(renderPos))，禁自造取整。\n        if (!skill) {\n            const movingFoe = snapshot.actors.find((a) => {\n                if (a.side !== 'enemy' || a.animState === 'dead')\n                    return false; // 排除 dead 与我方单位\n                const moving = a.renderPos.q !== a.pos.q || a.renderPos.r !== a.pos.r || view.moveAnims.has(a.id);\n                if (!moving)\n                    return false;\n                const w = (0, battle_hex_1.hexToWorld)(a.renderPos.q, a.renderPos.r);\n                const vis = (0, battle_hex_render_1.worldToHex)(w.x, w.y);\n                return vis.q === cell.q && vis.r === cell.r;\n            });\n            if (movingFoe) {\n                hooks.onBlocked?.('目标移动中');\n                return;\n            }\n        }\n        // 【T20-FE · 方案 B / ATK-2 v2.0】攻击技选中（skill && skill!=='qing'，§七-12）点非敌格分流\n        //（N2-① 修复本体）：原「skill && !inAttack → cancelSkill」只覆盖射程外，红格空格（inAttack=true）\n        // 落此零派发=空红格零反馈缺陷。现 castable = 射程内格 ∪ 自己格（Q2 特判并联、不入 legalCells/高亮）\n        // → cast 空放受理（ATK-6/ATK-7）；否则 cancelSkill（SEL-5②）。\n        // qing 态零改动（§七-13）：不进 castable 分流，保持既有 cancelSkill 取消路径（T16 已验收行为）。\n        if (skill) {\n            if (skill === 'qing') {\n                hooks.dispatch({ type: 'cancelSkill' });\n            }\n            else {\n                const me = snapshot.actors.find((a) => a.side === 'player'); // MVP 单人假设：唯一我方单位（快照真值）\n                const castable = inAttack || (me !== undefined && sameCell(cell, me.pos));\n                hooks.dispatch(castable ? { type: 'cast', to: cell, skillId: skill } : { type: 'cancelSkill' });\n            }\n        }\n    }\n    /** A07：异常终止重置（pointercancel/失焦/重开）——只清拖动态，不派发任何请求/点击 */\n    function reset() {\n        pointer.down = false;\n        pointer.dragging = false;\n    }\n    return { pointer, down, move, up, reset };\n}\n";
  // ---- ui/battle-input ----
  __def("ui/battle-input", new Function("require", "module", "exports", __mod_ui_battle_input));
  var __mod_ui_battle_hex_render = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createView = createView;\nexports.worldToHex = worldToHex;\nexports.axialToOffset = axialToOffset;\nexports.isMovableCell = isMovableCell;\nexports.cellHash = cellHash;\nexports.fieldRing = fieldRing;\nexports.isBoardCell = isBoardCell;\nexports.boardBounds = boardBounds;\nexports.computeMovePath = computeMovePath;\nexports.moveAnimDrawPosPx = moveAnimDrawPosPx;\nexports.movableBounds = movableBounds;\nexports.envWorldRect = envWorldRect;\nexports.computeCamera = computeCamera;\nexports.updateCamera = updateCamera;\nexports.spawnNoteFx = spawnNoteFx;\nexports.enqueueHit = enqueueHit;\nexports.updateView = updateView;\nexports.pieceHop = pieceHop;\nexports.drawFrame = drawFrame;\nconst battle_hex_1 = require(\"../config/battle-hex\");\nfunction createView() {\n    return {\n        time: 0,\n        camDrag: { x: 0, y: 0 },\n        lastCamDrag: { x: 0, y: 0 },\n        camera: { x: 0, y: 0 },\n        camInit: false,\n        anim: new Map(),\n        moveAnims: new Map(),\n        moveDone: new Set(),\n        fx: [],\n        pendingHits: [],\n        shakes: new Map(),\n        dmgStagger: new Map(),\n        skillPop: 0,\n        selectedCell: null,\n        uiState: {},\n        topbarHud: {\n            name: '',\n            hpFrac: 0,\n            neiliFrac: 0,\n            hpPctText: '0%',\n            neiliPctText: '0%',\n            statusIcons: [],\n            ctrlActive: { mode: false, speed: false },\n        },\n        layout: { skillBtns: [], ctrlRect: null, plaqueRect: null },\n    };\n}\n// ============ 几何纯函数（导出供 node 用例；FE 自含——T15 hex.ts 仍在演进，不反向耦合） ============\n/** 世界坐标 → 轴向格（压扁错位网格反算；点选拾取用，与 hexToWorld 严格互逆） */\nfunction worldToHex(wx, wy) {\n    const row = Math.round(wy / battle_hex_1.ROW_H);\n    const odd = Math.abs(row) % 2 === 1 ? 0.5 : 0;\n    const col = Math.round(wx / battle_hex_1.TILE_W - odd);\n    return { q: col - Math.floor(row / 2), r: row };\n}\n/** 轴向格 → offset col/row（odd-r：q = col - ⌊row/2⌋）；出界返回 null */\nfunction axialToOffset(p) {\n    const row = p.r;\n    const col = p.q + Math.floor(row / 2);\n    if (row < 0 || row >= battle_hex_1.BOARD.rows || col < 0 || col >= battle_hex_1.BOARD.cols)\n        return null;\n    return { col, row };\n}\n/** 是否可动区（T15 R3 FIELD：col 4..11 / row 2..13；瓦片配色用） */\nfunction isMovableCell(p) {\n    const off = axialToOffset(p);\n    if (!off)\n        return false;\n    return off.col >= battle_hex_1.FIELD.colMin && off.col <= battle_hex_1.FIELD.colMax && off.row >= battle_hex_1.FIELD.rowMin && off.row <= battle_hex_1.FIELD.rowMax;\n}\n/** 格坐标确定性哈希（murmur 风格 finalizer；Math.imul 32 位乘法防超 2^53 精度丢失）。\n * T24 双用：v8 缺角剔除闸门 + 顶面噪点 seed——同格同值、跨帧稳定不闪（方案 §四易错 5）。 */\nfunction cellHash(q, r) {\n    let h = (Math.imul(q, 0x27d4eb2d) ^ Math.imul(r, 0x165667b1) ^ 0x9e3779b9) | 0;\n    h = Math.imul(h ^ (h >>> 15), 0x85ebca6b);\n    h = Math.imul(h ^ (h >>> 13), 0xc2b2ae35);\n    h ^= h >>> 16;\n    return h >>> 0;\n}\n/** 距 FIELD 边的格环距（0=FIELD 内；1 起为外围 dirt 带；用于 dirt 两档色由内向外渐暗） */\nfunction fieldRing(col, row) {\n    return Math.max(battle_hex_1.FIELD.colMin - col, col - battle_hex_1.FIELD.colMax, battle_hex_1.FIELD.rowMin - row, row - battle_hex_1.FIELD.rowMax, 0);\n}\n/** v8 有机缺角边缘（Leo 09-04 翻案旧「整齐长方形战区」，方案 §2.2）：棋盘矩形**最外两圈**的\n * 非可动 dirt 格按坐标哈希剔除（BOARD_SHAPE.notchPerMille ≈20%，确定性→逐帧稳定不闪）；\n * **FIELD 内格绝不剔除**——出生锚（battle-session D1 SP-1：offset(4,13)/(11,2)）与出生带\n *（锚 hex 距 ≤3 的可动区格）⊆ FIELD，一并受保护；纯渲染形状：isMovableCell 与输入命中零改动，\n * 被剔格直接露出 env=齿边咬进地形。 */\nfunction isBoardCell(q, r) {\n    const off = axialToOffset({ q, r });\n    if (!off)\n        return false;\n    if (isMovableCell({ q, r }))\n        return true;\n    const ring = Math.min(off.col, battle_hex_1.BOARD.cols - 1 - off.col, off.row, battle_hex_1.BOARD.rows - 1 - off.row);\n    if (ring >= battle_hex_1.BOARD_SHAPE.rings)\n        return true; // 非最外两圈不剔\n    return cellHash(q, r) % 1000 >= battle_hex_1.BOARD_SHAPE.notchPerMille; // 千分比闸门\n}\n/** 是否台面边缘格（六邻之一非台面格=外缘或缺角豁口，投落地阴影用） */\nfunction isEdgeCell(q, r) {\n    return (!isBoardCell(q + 1, r) ||\n        !isBoardCell(q + 1, r - 1) ||\n        !isBoardCell(q, r - 1) ||\n        !isBoardCell(q - 1, r) ||\n        !isBoardCell(q - 1, r + 1) ||\n        !isBoardCell(q, r + 1));\n}\n/** 棋盘世界包围盒（含六边形 extent 与立体厚度、边距）。\n * 角格：odd-r 下 r=15 行的 q ∈ [-7, 8]（q = col - ⌊15/2⌋）。 */\nfunction boardBounds() {\n    const corners = [\n        { q: 0, r: 0 },\n        { q: battle_hex_1.BOARD.cols - 1, r: 0 },\n        { q: -Math.floor((battle_hex_1.BOARD.rows - 1) / 2), r: battle_hex_1.BOARD.rows - 1 },\n        { q: battle_hex_1.BOARD.cols - 1 - Math.floor((battle_hex_1.BOARD.rows - 1) / 2), r: battle_hex_1.BOARD.rows - 1 },\n    ];\n    let minX = Infinity;\n    let minY = Infinity;\n    let maxX = -Infinity;\n    let maxY = -Infinity;\n    const halfW = battle_hex_1.TILE_W / 2 + 6; // 6 = 错位半格余量\n    const halfH = battle_hex_1.TILE_H / 2 + battle_hex_1.SIDE_DEPTH;\n    for (const c of corners) {\n        const w = (0, battle_hex_1.hexToWorld)(c.q, c.r);\n        if (w.x - halfW < minX)\n            minX = w.x - halfW;\n        if (w.x + halfW > maxX)\n            maxX = w.x + halfW;\n        if (w.y - halfH < minY)\n            minY = w.y - halfH;\n        if (w.y + halfH > maxY)\n            maxY = w.y + halfH;\n    }\n    return {\n        minX: minX - battle_hex_1.CAMERA.worldPad,\n        minY: minY - battle_hex_1.CAMERA.worldPad,\n        maxX: maxX + battle_hex_1.CAMERA.worldPad,\n        maxY: maxY + battle_hex_1.CAMERA.worldPad,\n    };\n}\n/** cube 舍入（hex_lerp 直线取格用） */\nfunction cubeRound(x, y, z) {\n    let rx = Math.round(x);\n    let ry = Math.round(y);\n    let rz = Math.round(z);\n    const dx = Math.abs(rx - x);\n    const dy = Math.abs(ry - y);\n    const dz = Math.abs(rz - z);\n    if (dx > dy && dx > dz)\n        rx = -ry - rz;\n    else if (dy > dz)\n        ry = -rx - rz;\n    else\n        rz = -rx - ry;\n    return { q: rx, r: ry }; // cube y 轴 = axial r（z 轴是 s，勿取）\n}\n/** hex 直线路径（标准 hex_lerp + cube round）：视觉最直、锯齿最小——**优先走此路径**。\n * L 环终验实证：BFS 等距多解的锯齿/L 形路径在错位网格像素投影下每段横向偏移差 20-46px，\n * 即 Leo 观感\"左闪右闪才到\"的根因。 */\nfunction hexLerpPath(from, to) {\n    const n = (0, battle_hex_1.hexDist)(to, from);\n    if (n === 0)\n        return [{ ...from }];\n    const path = [];\n    for (let i = 0; i <= n; i++) {\n        const t = i / n;\n        const q = from.q + (to.q - from.q) * t;\n        const r = from.r + (to.r - from.r) * t;\n        path.push(cubeRound(q, r, -q - r));\n    }\n    return path;\n}\n/** BFS 逐格路径（渲染侧自含，与 session reachable 同参同数学：FIELD 内、6 邻、阻挡=占格）。\n * 仅当 hex 直线路径被占格/出界挡住时作为绕行回退（锯齿是绕行的必要代价）。路径含 from 与 pos。 */\nfunction bfsMovePath(from, to, occupied) {\n    const dirs = [\n        { q: 1, r: 0 },\n        { q: 1, r: -1 },\n        { q: 0, r: -1 },\n        { q: -1, r: 0 },\n        { q: -1, r: 1 },\n        { q: 0, r: 1 },\n    ];\n    const key = (c) => `${c.q},${c.r}`;\n    const inField = (c) => {\n        const off = axialToOffset(c);\n        return !!off && off.col >= battle_hex_1.FIELD.colMin && off.col <= battle_hex_1.FIELD.colMax && off.row >= battle_hex_1.FIELD.rowMin && off.row <= battle_hex_1.FIELD.rowMax;\n    };\n    if (key(from) === key(to))\n        return [from];\n    const seen = new Set([key(from)]);\n    const prev = new Map();\n    const queue = [from];\n    let found = false;\n    while (queue.length && !found) {\n        const cur = queue.shift();\n        for (const d of dirs) {\n            const nb = { q: cur.q + d.q, r: cur.r + d.r };\n            const k = key(nb);\n            if (seen.has(k))\n                continue;\n            if (!inField(nb))\n                continue;\n            if (occupied.has(k) && k !== key(to))\n                continue; // 终点格允许为目标单位所在（攻击前站位）\n            seen.add(k);\n            prev.set(k, key(cur));\n            if (k === key(to)) {\n                found = true;\n                break;\n            }\n            queue.push(nb);\n        }\n    }\n    if (!found)\n        return hexLerpPath(from, to); // 不可达防御：退化为直线\n    const path = [];\n    let cur = key(to);\n    while (cur) {\n        const [q, r] = cur.split(',').map(Number);\n        path.unshift({ q, r });\n        cur = prev.get(cur);\n    }\n    return path;\n}\n/** 移动路径（终验修法二版）：hex_lerp 直线 + **出界格 clamp 进 FIELD 带**——纵向窄走廊下斜向直线的\n * 中间格必然越带，不 clamp 会判 blocked 回退 BFS 锯齿，段间 20-46px 横跳 = Leo\"左闪右闪\"根因；\n * clamp 后去重相邻重复格。walk 仍规避占格（被挡回退 BFS）；jump 凌空无视占格恒直线。 */\nfunction computeMovePath(from, to, occupied, isJump = false) {\n    const line = hexLerpPath(from, to);\n    const clampField = (c) => {\n        const off = axialToOffset(c);\n        if (!off)\n            return c;\n        const col = Math.min(battle_hex_1.FIELD.colMax, Math.max(battle_hex_1.FIELD.colMin, off.col));\n        const row = Math.min(battle_hex_1.FIELD.rowMax, Math.max(battle_hex_1.FIELD.rowMin, off.row));\n        return { q: col - Math.floor(row / 2), r: row };\n    };\n    const clamped = [];\n    for (const c of line) {\n        const cc = clampField(c);\n        const last = clamped[clamped.length - 1];\n        if (last && last.q === cc.q && last.r === cc.r)\n            continue; // 去重相邻重复格\n        clamped.push(cc);\n    }\n    clamped[0] = { ...from };\n    clamped[clamped.length - 1] = { ...to };\n    if (isJump)\n        return clamped; // 凌空：无视占格\n    const innerBlocked = clamped.slice(1, -1).some((c) => occupied.has(`${c.q},${c.r}`));\n    if (!innerBlocked)\n        return clamped;\n    return bfsMovePath(from, to, occupied);\n}\n/** 移动演出位置（像素空间插值：from_px→…→pos_px 沿路径点列线性——像素直线恒直，无错位网格锯齿） */\nfunction moveAnimDrawPosPx(ma) {\n    const p = Math.min(1, ma.t / ma.duration);\n    const pts = ma.pathPx.length >= 2 ? ma.pathPx : [(0, battle_hex_1.hexToWorld)(ma.from.q, ma.from.r), (0, battle_hex_1.hexToWorld)(ma.pos.q, ma.pos.r)];\n    const segs = pts.length - 1;\n    if (segs < 1)\n        return { x: pts[0].x, y: pts[0].y };\n    const f = p * segs;\n    const i = Math.min(segs - 1, Math.floor(f));\n    const lp = f - i;\n    const a = pts[i];\n    const b = pts[i + 1];\n    return { x: a.x + (b.x - a.x) * lp, y: a.y + (b.y - a.y) * lp };\n}\nfunction drawPosXY(ma) {\n    return moveAnimDrawPosPx(ma);\n}\n/** 镜头跟随聚焦包围盒：可动区 FIELD + 窄边（L 环二反馈①：土黄外围自然推出视口，绿区铺满主体） */\nfunction movableBounds() {\n    const c0 = (0, battle_hex_1.hexToWorld)(battle_hex_1.FIELD.colMin, battle_hex_1.FIELD.rowMin); // 可动区西北格\n    const c1 = (0, battle_hex_1.hexToWorld)(battle_hex_1.FIELD.colMax, battle_hex_1.FIELD.rowMax); // 东南格\n    const pad = battle_hex_1.TILE_W / 2 + battle_hex_1.CAMERA.followPad; // 窄边余量\n    return {\n        minX: c0.x - battle_hex_1.TILE_W / 2 - pad,\n        minY: c0.y - battle_hex_1.TILE_H / 2 - pad,\n        maxX: c1.x + battle_hex_1.TILE_W / 2 + pad + battle_hex_1.TILE_W * 0.5, // 东南奇数行错位半格余量\n        maxY: c1.y + battle_hex_1.TILE_H / 2 + battle_hex_1.SIDE_DEPTH + pad,\n    };\n}\n/** env 世界锚定矩形（T24 方案 §2.1）：movableBounds 外扩 (width/2+M, height/2+M)，M=ENV_WORLD.margin。\n * width/height 必须用**当帧实际屏尺寸**（预览窗可拉伸，写死基准屏大窗露边——易错 1）：\n * 镜头被钳制在 movableBounds 内 → 屏面在 world 系的投影必然 ⊆ envRect，任意拖动位置不露边。 */\nfunction envWorldRect(width, height) {\n    const b = movableBounds();\n    const mx = width / 2 + battle_hex_1.ENV_WORLD.margin;\n    const my = height / 2 + battle_hex_1.ENV_WORLD.margin;\n    return { x: b.minX - mx, y: b.minY - my, w: b.maxX - b.minX + 2 * mx, h: b.maxY - b.minY + 2 * my };\n}\n/** 镜头：跟随 cameraTargetId（MVP 简化：恒跟主角/行动者）+ 拖动偏移 + 聚焦包围盒 clamp（旧 T06 口径） */\nfunction computeCamera(snapshot, camDrag, width, height) {\n    const target = snapshot.actors.find((a) => a.id === snapshot.cameraTargetId) ??\n        snapshot.actors.find((a) => a.side === 'player') ??\n        snapshot.actors[0];\n    const base = target ? (0, battle_hex_1.hexToWorld)(target.renderPos.q, target.renderPos.r) : { x: 0, y: 0 };\n    const b = movableBounds();\n    const clampAxis = (v, min, max, span) => span >= max - min ? (min + max) / 2 : Math.max(min + span / 2, Math.min(max - span / 2, v));\n    return {\n        x: clampAxis(base.x + camDrag.x, b.minX, b.maxX, width),\n        y: clampAxis(base.y + camDrag.y, b.minY, b.maxY, height),\n    };\n}\n/** 镜头策略（L 环追加③）：\n * - 非主角条满时镜头静止（敌方行动不牵引镜头；拖镜 delta 即时叠加保证跟手）\n * - 主角行动条满（等待输入）→ 镜头以指数平滑向主角理想机位回拉\n * - 跳跃表现位置参与取景（主角跳跃时镜头平滑跟随表现位） */\nfunction updateCamera(view, snapshot, dt, width, height) {\n    const hero = snapshot.actors.find((a) => a.side === 'player');\n    const ma = hero ? view.moveAnims.get(hero.id) : null;\n    const draw = ma && ma.t < ma.duration ? moveAnimDrawPosPx(ma) : hero ? (0, battle_hex_1.hexToWorld)(hero.renderPos.q, hero.renderPos.r) : null;\n    const ideal = computeCamera({ ...snapshot, cameraTargetId: hero ? hero.id : snapshot.cameraTargetId }, view.camDrag, width, height);\n    // 拖镜 delta 即时叠加（跟手）；首帧直接定位理想机位\n    if (!view.camInit) {\n        view.camera.x = ideal.x;\n        view.camera.y = ideal.y;\n        view.camInit = true;\n        view.lastCamDrag = { ...view.camDrag };\n        return;\n    }\n    const dx = view.camDrag.x - view.lastCamDrag.x;\n    const dy = view.camDrag.y - view.lastCamDrag.y;\n    view.camera.x += dx;\n    view.camera.y += dy;\n    view.lastCamDrag = { ...view.camDrag };\n    // 主角条满 → 平滑回拉理想机位（目标点含跳跃表现位置）\n    const heroTurn = snapshot.phase === 'fighting' && hero && snapshot.turnActorId === hero.id;\n    if (heroTurn && draw) {\n        const drawAxial = worldToHex(draw.x, draw.y);\n        const dest = computeCamera({ ...snapshot, cameraTargetId: hero.id, actors: [{ ...hero, renderPos: drawAxial }] }, view.camDrag, width, height);\n        const k = 1 - Math.exp(-dt / Math.max(0.001, battle_hex_1.CAMERA.smoothingSec));\n        view.camera.x += (dest.x - view.camera.x) * k;\n        view.camera.y += (dest.y - view.camera.y) * k;\n    }\n}\n// ============ 演出状态推进（动画钟/特效/弹出/镜头；只消费快照，不改快照任何字段） ============\nfunction easeOutCubic(p) {\n    const c = 1 - Math.max(0, Math.min(1, p));\n    return 1 - c * c * c;\n}\n/** 拒绝轻提示（T15 R3 rejected 事件消费）：单位头顶冒小字，上浮渐隐 */\nfunction spawnNoteFx(view, x, y, text) {\n    view.fx.push({ kind: 'note', x, y, t: 0, sec: 1.1, text });\n}\n/** 受击反馈入队（T21 §2.2/§一：入队机制归渲染层，宿主白名单判定后调用）。\n * 入队不立即冒字——updateView 按四条件冲刷（§2.3）；t 取 view.time 同源（冲刷超时/错位窗口判定基准）。 */\nfunction enqueueHit(view, attackerId, targetId, text, shake) {\n    view.pendingHits.push({ attackerId, targetId, text, shake, t: view.time });\n}\n/** 每帧推进视图状态。dt 秒。帧组播报铁律：状态切换=新组从组首帧重放（组间不跨）。 */\nfunction updateView(view, snapshot, dt, width, height) {\n    view.time += dt;\n    const riseToAttack = new Set(); // T21 冲刷条件 a：本帧切到 basic/strike 的攻击者（演出循环后统一冲刷）\n    for (const a of snapshot.actors) {\n        const prev = view.anim.get(a.id);\n        const prevAnim = view.moveAnims.get(a.id);\n        // ---- 移动演出启动（查修一体定位：普通移动此前无演出插值、跳跃演出期帧组随快照切 idle——\n        // 统一为演出计时主导：上升沿锁 from/pos/duration/hop，演出期位置与帧组全由演出决定） ----\n        const jumpRise = a.isJump && !prevAnim; // 无演出中才启动（AnimClock 不再承担跳跃标记）\n        const walkRise = !prevAnim && !view.moveDone.has(a.id) && a.animState === 'walk' &&\n            (a.renderPos.q !== a.pos.q || a.renderPos.r !== a.pos.r);\n        if (jumpRise || walkRise) {\n            // from 取整到最近格：walkRise 可能在 session lerp 中途触发（小数 renderPos），\n            // 小数起点会让 hexLerp/cubeRound 甩出界外乱路径（L 环终验实证）\n            const from = { q: Math.round(a.renderPos.q), r: Math.round(a.renderPos.r) };\n            const pos = { q: a.pos.q, r: a.pos.r };\n            const dist = (0, battle_hex_1.hexDist)(pos, from);\n            const jp = (0, battle_hex_1.jumpParamsFor)(dist);\n            const occupied = new Set(snapshot.actors\n                .filter((u) => u.id !== a.id && u.animState !== 'dead')\n                .map((u) => `${u.pos.q},${u.pos.r}`));\n            const path = computeMovePath(from, pos, occupied, a.isJump);\n            const dur = a.isJump ? jp.duration : battle_hex_1.PIECE.moveLerpSec * Math.max(1, dist);\n            view.moveAnims.set(a.id, {\n                from,\n                pos,\n                path,\n                pathPx: path.map((c) => (0, battle_hex_1.hexToWorld)(c.q, c.r)),\n                t: 0,\n                duration: dur,\n                hopHeight: a.isJump ? jp.height : 0,\n            });\n        }\n        // ---- 移动演出推进（演出计时主导：到时不删、定格终点等快照到位/离开 walk 才释放） ----\n        const anim = view.moveAnims.get(a.id);\n        if (anim) {\n            if (anim.t < anim.duration)\n                anim.t = Math.min(anim.duration, anim.t + dt);\n            // 终点变更（session 中途改目标）：以快照为准续画（无瞬移；此为快照真值变更非跳变）\n            if (anim.pos.q !== a.pos.q || anim.pos.r !== a.pos.r)\n                anim.pos = { q: a.pos.q, r: a.pos.r };\n            // 演出时长已满：定格终点；快照位移到位（或 session 已离开 walk）才释放——\n            // 释放即 moveDone：session 移动动画未结束时禁止 walkRise 重启（时长不同步防抖）\n            if (anim.t >= anim.duration) {\n                const settled = (a.renderPos.q === anim.pos.q && a.renderPos.r === anim.pos.r) || a.animState !== 'walk';\n                if (settled) {\n                    view.moveAnims.delete(a.id);\n                    view.moveDone.add(a.id);\n                }\n            }\n        }\n        // 移动演出单位离开 walk（session 侧到位/转向）→ 解除防重启\n        if (a.animState !== 'walk')\n            view.moveDone.delete(a.id);\n        // ---- 动画钟（帧组播报：组切换=新组从组首帧重放） ----\n        if (!prev || prev.state !== a.animState) {\n            const w = (0, battle_hex_1.hexToWorld)(a.renderPos.q, a.renderPos.r);\n            if (prev && (a.animState === 'strike' || a.animState === 'basic')) {\n                view.fx.push({ kind: 'slash', x: w.x, y: w.y, t: 0, sec: battle_hex_1.FX.slashSec });\n                riseToAttack.add(a.id); // T21：与 slash 同沿收集，pendingHits 在演出循环后按 §2.3 冲刷\n            }\n            else if (prev && a.animState === 'hit') {\n                // 【T21 受击反馈互指】受击反馈已改走事件驱动路径（main.ts 白名单入队 → pendingHits 冲刷 +\n                // shakes 震动，见本文件 §T21 注释）；下面这个 animState==='hit' 分支是休眠钩子——session 从不\n                // 产生 'hit' 态，属永不执行的既有验收代码，维持休眠不删不接。与 drawPieces 内同名休眠行互指。\n                view.fx.push({ kind: 'hit', x: w.x, y: w.y, t: 0, sec: battle_hex_1.FX.hitSec });\n            }\n            view.anim.set(a.id, { state: a.animState, t: 0 });\n        }\n        else {\n            prev.t += dt;\n        }\n    }\n    // 特效寿命推进（含 note 冒字）\n    const aliveFx = [];\n    for (const f of view.fx) {\n        f.t += dt;\n        if (f.t < f.sec)\n            aliveFx.push(f);\n    }\n    view.fx = aliveFx;\n    // 震动衰减计时推进（T21 §2.5：到期删除——判定用历时≥shakeSec，禁 sin 幅度归零判定防浮点尾数；\n    // 先推进后冲刷：冲刷帧写入的起振值 0 不被同帧 dt 提前消耗）\n    for (const [id, elapsed] of view.shakes) {\n        const next = elapsed + dt;\n        if (next >= battle_hex_1.DMG.shakeSec)\n            view.shakes.delete(id);\n        else\n            view.shakes.set(id, next);\n    }\n    // ---- 受击反馈挂起冲刷（T21 §2.3：四条件满足其一即冲刷出队；定位按当帧快照 renderPos） ----\n    if (view.pendingHits.length > 0) {\n        const stillPending = [];\n        for (const ph of view.pendingHits) {\n            const attacker = snapshot.actors.find((a) => a.id === ph.attackerId);\n            const inAttackState = attacker ? attacker.animState === 'basic' || attacker.animState === 'strike' : false;\n            // a 上升沿 / b 已在态兜底 / c 超时（攻击者挂死防御）/ d 攻击者已 dead（hp 减少是既成事实）\n            const flush = riseToAttack.has(ph.attackerId) ||\n                inAttackState ||\n                view.time - ph.t > battle_hex_1.DMG.flushDeadlineSec ||\n                attacker?.animState === 'dead';\n            if (!flush) {\n                stillPending.push(ph);\n                continue;\n            }\n            const target = snapshot.actors.find((a) => a.id === ph.targetId);\n            if (target) {\n                const w = (0, battle_hex_1.hexToWorld)(target.renderPos.q, target.renderPos.r);\n                // 同位错位（§2.4 · PM 裁决②+Q2 滑动窗口）：窗口内连续 spawn 按命中序横移，不合并；过期归零。\n                // 时间基准恒 view.time（与冲刷时钟同源，外部时钟会漂）。\n                const prev = view.dmgStagger.get(ph.targetId);\n                const seq = prev && (view.time - prev.at) * 1000 <= battle_hex_1.DMG.staggerWindowMs ? prev.seq + 1 : 0;\n                view.dmgStagger.set(ph.targetId, { at: view.time, seq });\n                view.fx.push({ kind: 'dmg', x: w.x, y: w.y, t: 0, sec: battle_hex_1.DMG.sec, text: ph.text, dx: seq * battle_hex_1.DMG.staggerPx });\n                // 震动：只震受击者且死亡不震（击杀一击=冒数字不震动，§2.3 单点判定；session doAttack 同步置 dead 不可逆）\n                if (ph.shake && target.animState !== 'dead')\n                    view.shakes.set(ph.targetId, 0);\n            }\n        }\n        view.pendingHits = stillPending;\n    }\n    // 弧形四钮弹出进度（目标：主角行动条满等待输入）\n    const popHero = snapshot.actors.find((a) => a.side === 'player');\n    const popTarget = snapshot.phase === 'fighting' && snapshot.pendingInput && popHero && snapshot.turnActorId === popHero.id ? 1 : 0;\n    const popK = Math.min(1, (dt / battle_hex_1.ARC_BTNS.popSec) * 3);\n    view.skillPop += (popTarget - view.skillPop) * popK;\n    if (Math.abs(view.skillPop - popTarget) < 0.02)\n        view.skillPop = popTarget;\n    // 镜头策略（L 环追加③）：镜头静止；仅主角行动条满时向主角平滑回拉；拖镜 delta 即时跟手\n    updateCamera(view, snapshot, dt, width, height);\n}\n// ============ 绘制工具 ============\nfunction drawImg(ctx, img, x, y, w, h) {\n    // 整数像素定位（亚像素抖动防虚影，home_demo 已验口径）\n    ctx.drawImage(img, Math.round(x), Math.round(y), Math.round(w ?? img.width), Math.round(h ?? img.height));\n}\n/** 圆角矩形路径（A06 能力检测）：优先 ctx.roundRect；旧基础库无此方法时走 arcTo 手工等价路径\n * （手法与 ui/battle-render.ts / ui/render.ts 既有私有副本同式，各自独立不跨文件 import）。\n * 调用方负责 beginPath；本函数只产路径（fill/stroke 仍归调用方）。 */\nfunction roundRectPath(ctx, x, y, w, h, r) {\n    if (typeof ctx.roundRect === 'function') {\n        ctx.roundRect(x, y, w, h, r);\n        return;\n    }\n    const rad = Math.min(r, w / 2, h / 2);\n    ctx.moveTo(x + rad, y);\n    ctx.arcTo(x + w, y, x + w, y + h, rad);\n    ctx.arcTo(x + w, y + h, x, y + h, rad);\n    ctx.arcTo(x, y + h, x, y, rad);\n    ctx.arcTo(x, y, x + w, y, rad);\n    ctx.closePath();\n}\n/** 尖角压扁六边形路径（pointy-top：上下尖角、左右竖直边；宽 TILE_W × 高 TILE_H） */\nfunction tilePath(ctx, cx, cy, w, h) {\n    const hw = w / 2;\n    const hh = h / 2;\n    const q = h / 4; // 竖直边半高（正六边形拓扑：竖边端点在 ±h/4）\n    ctx.beginPath();\n    ctx.moveTo(cx, cy - hh); // 上尖\n    ctx.lineTo(cx + hw, cy - q); // 右上\n    ctx.lineTo(cx + hw, cy + q); // 右下（右竖直边）\n    ctx.lineTo(cx, cy + hh); // 下尖\n    ctx.lineTo(cx - hw, cy + q); // 左下\n    ctx.lineTo(cx - hw, cy - q); // 左上（左竖直边）\n    ctx.closePath();\n}\n// ============ L1 立体瓦片 / L2 高亮 ============\n/** 色值明度缩放（#rrggbb → rgb() 字符串；噪点 ±amp 用）。带缓存：帧内重复色不重算字符串。 */\nconst tintCache = new Map();\nfunction tintRgb(hex, mul) {\n    const key = hex + '@' + mul;\n    const hit = tintCache.get(key);\n    if (hit)\n        return hit;\n    const v = parseInt(hex.slice(1), 16);\n    const ch = (shift) => Math.max(0, Math.min(255, Math.round(((v >> shift) & 0xff) * mul)));\n    const out = `rgb(${ch(16)}, ${ch(8)}, ${ch(0)})`;\n    tintCache.set(key, out);\n    return out;\n}\n/** 顶面静态噪点（T24 方案 §2.2；纯代码零素材，tileset 贴图到位后被 sprite 分支整体替换）：\n * 候选网格 ∩ 六边形内 → 格哈希闸门（density）→ ±amp 明度 2px 点。\n * mulberry32 序列以格哈希为 seed——同格逐帧同序列，静止不闪（易错 5，禁逐帧随机）。 */\nfunction drawTopNoise(ctx, cx, cy, baseHex, seed) {\n    const hw = battle_hex_1.TILE_W / 2 - 1;\n    const hh = battle_hex_1.TILE_H / 2 - 1;\n    const q4 = battle_hex_1.TILE_H / 4; // 竖直边半高（tilePath 同构：|dy|>q4 后宽度线性收缩到尖角）\n    let s = (seed ^ 0x9e3779b9) >>> 0;\n    const rnd = () => {\n        s = (s + 0x6d2b79f5) >>> 0;\n        let t = s;\n        t = Math.imul(t ^ (t >>> 15), t | 1);\n        t ^= t + Math.imul(t ^ (t >>> 7), t | 61);\n        return ((t ^ (t >>> 14)) >>> 0) / 4294967296;\n    };\n    for (let gy = -hh + battle_hex_1.TILE_NOISE.step / 2; gy <= hh; gy += battle_hex_1.TILE_NOISE.step) {\n        for (let gx = -hw + battle_hex_1.TILE_NOISE.step / 2; gx <= hw; gx += battle_hex_1.TILE_NOISE.step) {\n            const ax = Math.abs(gx);\n            const ay = Math.abs(gy);\n            if (ay > q4 && ax > (hw * (hh - ay)) / (hh - q4))\n                continue; // 斜边收缩段外\n            if (rnd() >= battle_hex_1.TILE_NOISE.density)\n                continue;\n            const light = rnd() < 0.5 ? 1 + battle_hex_1.TILE_NOISE.amp : 1 - battle_hex_1.TILE_NOISE.amp;\n            ctx.fillStyle = tintRgb(baseHex, light);\n            ctx.fillRect(Math.round(cx + gx - battle_hex_1.TILE_NOISE.dotPx / 2), Math.round(cy + gy - battle_hex_1.TILE_NOISE.dotPx / 2), battle_hex_1.TILE_NOISE.dotPx, battle_hex_1.TILE_NOISE.dotPx);\n        }\n    }\n}\n/** 单块压扁立体瓦片（临时代码版；TILE_SPRITES 素材到位即贴 sprite）：\n * 顶面尖角压扁形 + 下尖角/下斜边深色侧面（厚 SIDE_DEPTH）+ 上暗下亮光照描边。\n * T24：dirt 顶面两档色（fieldRing ≤ dirtInnerRings 用内档亮色，更远外档暗色——由内向外渐暗，\n * 色值 env 离线采样见 config TILE 注释）；顶面平色后叠静态噪点（seed=格哈希）。 */\nfunction drawTile(ctx, cx, cy, movable, ring, seed) {\n    const sprite = movable ? battle_hex_1.TILE_SPRITES.grass : battle_hex_1.TILE_SPRITES.dirt;\n    if (sprite) {\n        const img = tileSpriteImg(sprite);\n        if (img) {\n            drawImg(ctx, img, cx - battle_hex_1.TILE_W / 2, cy - battle_hex_1.TILE_H / 2 + (battle_hex_1.TILE_H - img.height) / 2 + 0, battle_hex_1.TILE_W, battle_hex_1.TILE_H);\n            return;\n        }\n    }\n    const hw = battle_hex_1.TILE_W / 2;\n    const hh = battle_hex_1.TILE_H / 2;\n    const q = battle_hex_1.TILE_H / 4;\n    const P = (dx, dy) => [cx + dx, cy + dy];\n    // 顶点序：上尖(0,-hh) 右上(hw,-q) 右下(hw,q) 下尖(0,hh) 左下(-hw,q) 左上(-hw,-q)\n    const top = P(0, -hh);\n    const rt = P(hw, -q);\n    const rb = P(hw, q);\n    const bot = P(0, hh);\n    const lb = P(-hw, q);\n    const lt = P(-hw, -q);\n    // 侧面：下尖角与两条下斜边向外挤出（深色/受光分段——底部光源氛围）\n    const sidePolys = [\n        { pts: [rt, bot, [bot[0], bot[1] + battle_hex_1.SIDE_DEPTH], [rt[0], rt[1] + battle_hex_1.SIDE_DEPTH]], color: battle_hex_1.TILE.side }, // 右下斜边\n        { pts: [bot, lb, [lb[0], lb[1] + battle_hex_1.SIDE_DEPTH], [bot[0], bot[1] + battle_hex_1.SIDE_DEPTH]], color: battle_hex_1.TILE.sideShade }, // 左下斜边（背光）\n        { pts: [lb, lt, [lt[0], lt[1] + battle_hex_1.SIDE_DEPTH], [lb[0], lb[1] + battle_hex_1.SIDE_DEPTH]], color: battle_hex_1.TILE.sideShade }, // 左竖直边下段\n    ];\n    for (const poly of sidePolys) {\n        ctx.beginPath();\n        poly.pts.forEach(([x, y], i) => (i === 0 ? ctx.moveTo(x, y) : ctx.lineTo(x, y)));\n        ctx.closePath();\n        ctx.fillStyle = poly.color;\n        ctx.fill();\n    }\n    // 顶面（T24：dirt 拆 inner/outer 两档）\n    const baseHex = movable ? battle_hex_1.TILE.topGrass : ring <= battle_hex_1.TILE.dirtInnerRings ? battle_hex_1.TILE.topDirtInner : battle_hex_1.TILE.topDirtOuter;\n    tilePath(ctx, cx, cy, battle_hex_1.TILE_W - 1, battle_hex_1.TILE_H - 1);\n    ctx.fillStyle = baseHex;\n    ctx.fill();\n    drawTopNoise(ctx, cx, cy, baseHex, seed); // 顶面质感（描边之下）\n    // 光照描边：上尖两斜边暗（上暗）/ 下尖两斜边亮+底缘受光线（下亮）\n    ctx.lineWidth = battle_hex_1.TILE.strokeWidth;\n    ctx.strokeStyle = battle_hex_1.TILE.edgeDark;\n    for (const [a, b] of [\n        [top, rt],\n        [top, lt],\n    ]) {\n        ctx.beginPath();\n        ctx.moveTo(a[0], a[1]);\n        ctx.lineTo(b[0], b[1]);\n        ctx.stroke();\n    }\n    ctx.strokeStyle = battle_hex_1.TILE.edgeLight;\n    for (const [a, b] of [\n        [rb, bot],\n        [bot, lb],\n        [rt, rb],\n        [lt, lb],\n    ]) {\n        ctx.beginPath();\n        ctx.moveTo(a[0], a[1]);\n        ctx.lineTo(b[0], b[1]);\n        ctx.stroke();\n    }\n}\n/** 瓦片 sprite 解析（素材键 → 已加载图；未加载返回 null 走代码绘制） */\nfunction tileSpriteImg(_key) {\n    return null; // 素材未到位；loader 接入时按键取已加载瓦片图\n}\nfunction fillHex(ctx, cx, cy, fill, edge) {\n    tilePath(ctx, cx, cy, battle_hex_1.TILE_W - 2, battle_hex_1.TILE_H - 2);\n    ctx.fillStyle = fill;\n    ctx.fill();\n    ctx.strokeStyle = edge;\n    ctx.lineWidth = 1.5;\n    ctx.stroke();\n}\n/** L1+L2：格子与高亮（仅绘视口内；数据来自快照，渲染只画不算；moveKind 换色——绿=走 / 金=轻功跳）。\n * 【Leo 09-04 翻案 v8 缺角】旧「战区矩形裁剪：错位行半格出界部分裁平 → 边缘整齐长方形战区」已撤——\n * 原 rect clip 逻辑整段删除，改为 isBoardCell 逐格判定：最外两圈非可动 dirt 格坐标哈希剔除 ~20%，\n * 边缘=不规则六边形齿边咬进 env 地形（被剔格露 env），画布自然出屏即裁。\n * 落地阴影（T24 方案 §2.2）：边缘格（六邻有缺）在格体之前画下偏软阴影两层（SHADOW 组，底侧重）。 */\nfunction drawCells(ctx, snapshot, cam, width, height, selected) {\n    const center = worldToHex(cam.x, cam.y);\n    const span = battle_hex_1.CAMERA.viewportCells + 2;\n    const keyOf = (c) => `${c.q},${c.r}`;\n    const jump = snapshot.moveKind === 'jump';\n    const moveFill = jump ? battle_hex_1.HIGHLIGHT.jump : battle_hex_1.HIGHLIGHT.move;\n    const moveEdge = jump ? battle_hex_1.HIGHLIGHT.jumpEdge : battle_hex_1.HIGHLIGHT.moveEdge;\n    const moveSet = new Set(snapshot.moveCells.map(keyOf));\n    const attackSet = new Set(snapshot.attackCells.map(keyOf));\n    const selKey = selected ? keyOf(selected) : null;\n    for (let r = center.r - span; r <= center.r + span; r++) {\n        for (let q = center.q - span; q <= center.q + span; q++) {\n            if (!isBoardCell(q, r))\n                continue;\n            const w = (0, battle_hex_1.hexToWorld)(q, r);\n            const sx = Math.round(w.x - cam.x + width / 2);\n            const sy = Math.round(w.y - cam.y + height / 2);\n            if (sx < -battle_hex_1.TILE_W * 2 || sx > width + battle_hex_1.TILE_W * 2 || sy < -battle_hex_1.TILE_H * 2 - battle_hex_1.SIDE_DEPTH || sy > height + battle_hex_1.TILE_H * 2)\n                continue;\n            const movable = isMovableCell({ q, r });\n            if (!movable && isEdgeCell(q, r))\n                drawEdgeShadow(ctx, sx, sy); // 台面向 env 投软阴影（格体之前）\n            const off = axialToOffset({ q, r });\n            drawTile(ctx, sx, sy, movable, fieldRing(off.col, off.row), cellHash(q, r));\n            const key = `${q},${r}`;\n            if (moveSet.has(key))\n                fillHex(ctx, sx, sy, moveFill, moveEdge);\n            else if (attackSet.has(key))\n                fillHex(ctx, sx, sy, battle_hex_1.HIGHLIGHT.attack, battle_hex_1.HIGHLIGHT.attackEdge);\n            if (selKey === key)\n                fillHex(ctx, sx, sy, battle_hex_1.HIGHLIGHT.selected, battle_hex_1.HIGHLIGHT.selectedEdge);\n        }\n    }\n}\n/** 边缘格落地阴影（T24 方案 §2.2）：下偏两层软阴影——近层主影（offsetPx/alpha）+ 深偏影\n * （offsetPx×bottomMul/alphaDeep，底侧重）；上大半随后被本格体覆盖，露出=下缘月牙，整板浮起。\n * r 从本格升序绘制，下方格后画自然盖住上溢阴影——单循环内「先影后格」即得正确遮挡。 */\nfunction drawEdgeShadow(ctx, sx, sy) {\n    ctx.fillStyle = `rgba(${battle_hex_1.SHADOW.rgb}, ${battle_hex_1.SHADOW.alpha})`;\n    tilePath(ctx, sx, sy + battle_hex_1.SHADOW.offsetPx, battle_hex_1.TILE_W - 1, battle_hex_1.TILE_H - 1);\n    ctx.fill();\n    ctx.fillStyle = `rgba(${battle_hex_1.SHADOW.rgb}, ${battle_hex_1.SHADOW.alphaDeep})`;\n    tilePath(ctx, sx, Math.round(sy + battle_hex_1.SHADOW.offsetPx * battle_hex_1.SHADOW.bottomMul), battle_hex_1.TILE_W - 1, battle_hex_1.TILE_H - 1);\n    ctx.fill();\n}\n// ============ L3 棋子 / L4 HUD ============\n/** 当前帧号（帧组播报：循环组取模循环；单播组夹到组尾保持，组切换由 updateView 重置） */\nfunction frameOf(view, actor, stateOverride) {\n    const state = stateOverride ?? actor.animState;\n    const clock = view.anim.get(actor.id);\n    const group = battle_hex_1.ANIM_FRAMES[state] ?? battle_hex_1.ANIM_FRAMES.idle;\n    if (!clock || clock.state !== state)\n        return group[0];\n    const idx = Math.floor((clock.t * 1000) / battle_hex_1.PIECE.walkFrameMs);\n    if (battle_hex_1.ANIM_LOOP_GROUPS.includes(state))\n        return group[idx % group.length];\n    return group[Math.min(idx, group.length - 1)];\n}\n/** 跳跃抛物线高度（纯函数，导出供用例；联调 F1：跳跃真值=快照 isJump，禁启发式猜）。\n * done = 已走位移占比（相位基准 view.moveFrom，updateView 在跳跃上升沿记录）。 */\nfunction pieceHop(view, actor) {\n    const ma = view.moveAnims.get(actor.id);\n    if (!ma || ma.hopHeight <= 0)\n        return 0;\n    const p = Math.min(1, ma.t / ma.duration);\n    return Math.sin(Math.PI * p) * ma.hopHeight;\n}\nfunction drawPieces(ctx, snapshot, assets, view, cam, width, height) {\n    const hexH = battle_hex_1.TILE_H; // 压扁格高（棋子定尺基准）\n    // y 排序遮挡（移动演出期按演出位置排序，保证跃过单位时遮挡正确）\n    const sorted = [...snapshot.actors].sort((a, b) => {\n        const aa = view.moveAnims.get(a.id);\n        const ab = view.moveAnims.get(b.id);\n        const wa = aa ? drawPosXY(aa) : (0, battle_hex_1.hexToWorld)(a.renderPos.q, a.renderPos.r);\n        const wb = ab ? drawPosXY(ab) : (0, battle_hex_1.hexToWorld)(b.renderPos.q, b.renderPos.r);\n        return wa.y - wb.y;\n    });\n    const placed = [];\n    for (const actor of sorted) {\n        const ma = view.moveAnims.get(actor.id);\n        const draw = ma ? moveAnimDrawPosPx(ma) : (0, battle_hex_1.hexToWorld)(actor.renderPos.q, actor.renderPos.r); // 演出期=像素空间插值（双轨消灭）\n        const sx = Math.round(draw.x - cam.x + width / 2);\n        const syGround = Math.round(draw.y - cam.y + height / 2);\n        const scale = actor.isBoss ? battle_hex_1.PIECE.bossScale : 1;\n        const h = hexH * battle_hex_1.PIECE.heightPerTile * scale;\n        const frameState = ma && ma.t < ma.duration ? 'walk' : actor.animState; // 演出期帧组强制 walk（空中不站立滑行）\n        const img = assets.frames.get(actor.spriteKey)?.[frameOf(view, actor, frameState)] ?? null;\n        const w = img ? (h * img.width) / img.height : h * 0.5;\n        if (frameState === 'dead') {\n            // 阵亡：压扁淡出倒地\n            if (img) {\n                ctx.save();\n                ctx.globalAlpha = battle_hex_1.PIECE.deadAlpha;\n                drawImg(ctx, img, sx - w / 2, syGround - h * 0.3, w, h * 0.3);\n                ctx.restore();\n            }\n            continue;\n        }\n        const hop = pieceHop(view, actor); // 轻功抛物线（演出期参数随距离插值）\n        // 【T21 受击反馈互指】T21 震动=事件驱动（view.shakes，参数组 DMG.shake*，下方 shakeDmg 附加偏移）；\n        // 下面这行 animState==='hit' 是休眠钩子——session 从不产生 'hit' 态，永不执行，维持休眠不删不接。\n        // 与 updateView 内 hit fx 分支互指（同注）。\n        const shake = actor.animState === 'hit' ? Math.sin(view.time * 70) * 2 : 0;\n        const shakeElapsed = view.shakes.get(actor.id);\n        const shakeDmg = shakeElapsed !== undefined\n            ? Math.sin(shakeElapsed * battle_hex_1.DMG.shakeFreq) * battle_hex_1.DMG.shakePx * (1 - shakeElapsed / battle_hex_1.DMG.shakeSec)\n            : 0; // T21 §2.5：水平 ±3px 衰减 ~200ms，cx 附加偏移（不动 moveAnims 插值位）\n        const cx = sx + shake + shakeDmg;\n        const top = syGround - h - hop;\n        placed.push({ actor, cx, top, h, w });\n        if (img) {\n            ctx.save();\n            if (actor.facing === 'left') {\n                ctx.translate(Math.round(cx * 2), 0);\n                ctx.scale(-1, 1);\n            }\n            drawImg(ctx, img, cx - w / 2, top, w, h);\n            ctx.restore();\n        }\n        else {\n            // 占位降级：无帧资源画剪影椭圆（永不空窗，L 环反馈防御）\n            ctx.fillStyle = actor.side === 'player' ? 'rgba(90,160,90,0.9)' : 'rgba(170,80,70,0.9)';\n            ctx.beginPath();\n            ctx.ellipse(cx, top + h / 2, w / 2, h / 2, 0, 0, Math.PI * 2);\n            ctx.fill();\n        }\n    }\n    return placed;\n}\nfunction drawPieceHud(ctx, placed, snapshot, view) {\n    ctx.textAlign = 'center';\n    ctx.textBaseline = 'middle';\n    for (const p of placed) {\n        const a = p.actor;\n        if (a.animState === 'dead')\n            continue;\n        const y0 = p.top - battle_hex_1.HUD.aboveHead;\n        // 名字牌（我方淡绿/敌方淡红）\n        ctx.fillStyle = battle_hex_1.HUD.nameBg;\n        ctx.fillRect(Math.round(p.cx - battle_hex_1.HUD.barW / 2 - 2), Math.round(y0 - 7), battle_hex_1.HUD.barW + 4, 10);\n        ctx.font = `${battle_hex_1.HUD.nameFontPx}px ${battle_hex_1.FONT_STACK}`;\n        ctx.fillStyle = a.side === 'player' ? battle_hex_1.HUD.nameAlly : battle_hex_1.HUD.nameEnemy;\n        ctx.fillText(a.name, Math.round(p.cx), Math.round(y0 - 2));\n        // 绿行动条（0~100 展示口径）\n        ctx.fillStyle = battle_hex_1.HUD.barBg;\n        ctx.fillRect(Math.round(p.cx - battle_hex_1.HUD.barW / 2), Math.round(y0 + 4), battle_hex_1.HUD.barW, battle_hex_1.HUD.barH);\n        ctx.fillStyle = battle_hex_1.HUD.actionBarColor;\n        ctx.fillRect(Math.round(p.cx - battle_hex_1.HUD.barW / 2), Math.round(y0 + 4), Math.round(battle_hex_1.HUD.barW * Math.max(0, Math.min(1, a.actionBar / 100))), battle_hex_1.HUD.barH);\n        // 红血条\n        ctx.fillStyle = battle_hex_1.HUD.barBg;\n        ctx.fillRect(Math.round(p.cx - battle_hex_1.HUD.barW / 2), Math.round(y0 + 4 + battle_hex_1.HUD.barH + battle_hex_1.HUD.barGap), battle_hex_1.HUD.barW, battle_hex_1.HUD.barH);\n        ctx.fillStyle = battle_hex_1.HUD.hpBarColor;\n        ctx.fillRect(Math.round(p.cx - battle_hex_1.HUD.barW / 2), Math.round(y0 + 4 + battle_hex_1.HUD.barH + battle_hex_1.HUD.barGap), Math.round(battle_hex_1.HUD.barW * Math.max(0, Math.min(1, a.hp / Math.max(1, a.maxHp)))), battle_hex_1.HUD.barH);\n    }\n    // 主角弧形特绝轻毒四钮（行动条满弹出；置灰=内力/冷却/武器不匹配——联调 F2 会话真值）\n    const hero = snapshot.actors.find((a) => a.side === 'player');\n    const showPop = snapshot.phase === 'fighting' && snapshot.pendingInput && hero && snapshot.turnActorId === hero.id;\n    const pop = showPop ? easeOutCubic(view.skillPop) : 0;\n    view.layout.skillBtns = [];\n    if (!hero || pop <= 0.01)\n        return;\n    const p = placed.find((x) => x.actor.id === hero.id);\n    if (!p)\n        return;\n    const headW = p.w * battle_hex_1.ARC_BTNS.headWidthRatio;\n    const d = headW * battle_hex_1.ARC_BTNS.diameterPerHead;\n    const R = headW * battle_hex_1.ARC_BTNS.arcRadiusPerHead;\n    const headCx = p.cx;\n    const headCy = p.top + headW * 0.6;\n    const from = (battle_hex_1.ARC_BTNS.angleFromDeg * Math.PI) / 180;\n    const to = (battle_hex_1.ARC_BTNS.angleToDeg * Math.PI) / 180;\n    const skills = snapshot.heroSkills ?? [];\n    for (let i = 0; i < battle_hex_1.ARC_BTNS.ids.length; i++) {\n        const id = battle_hex_1.ARC_BTNS.ids[i];\n        const ang = from + ((to - from) * i) / (battle_hex_1.ARC_BTNS.ids.length - 1);\n        const bx = headCx + Math.cos(ang) * R * pop;\n        const by = headCy + Math.sin(ang) * R * pop;\n        const info = skills.find((sk) => sk.id === id);\n        const disabled = info ? info.disabled : false;\n        view.layout.skillBtns.push({ id, x: bx, y: by, r: d / 2, disabled });\n        ctx.save();\n        ctx.globalAlpha = pop * (disabled ? battle_hex_1.ARC_BTNS.disabledAlpha : 1);\n        ctx.beginPath();\n        ctx.arc(bx, by, d / 2, 0, Math.PI * 2);\n        ctx.fillStyle = battle_hex_1.ARC_BTNS.colorBg;\n        ctx.fill();\n        ctx.lineWidth = snapshot.selectedSkill === id ? battle_hex_1.ARC_BTNS.rimWidthSelected : battle_hex_1.ARC_BTNS.rimWidth;\n        ctx.strokeStyle = snapshot.selectedSkill === id ? battle_hex_1.ARC_BTNS.rimColorSelected : battle_hex_1.ARC_BTNS.colorRim;\n        ctx.stroke();\n        ctx.fillStyle = disabled ? battle_hex_1.ARC_BTNS.colorTextDisabled : battle_hex_1.ARC_BTNS.colorText;\n        ctx.font = `bold ${Math.round(d * 0.52)}px ${battle_hex_1.FONT_STACK}`;\n        ctx.fillText(battle_hex_1.ARC_BTNS.labels[i], Math.round(bx), Math.round(by + 1));\n        ctx.restore();\n    }\n}\n// ============ L5 定稿组件 ============\nfunction drawComponents(ctx, snapshot, assets, width, height, view) {\n    view.layout.plaqueRect = null;\n    view.layout.ctrlRect = null;\n    // 顶栏（T23 §2.1）：topbar_base 无字底图全宽贴屏顶 + 代码压暗层（现状保留）+ 代码条/名字/百分比叠绘；缺图时代码兜底\n    const tb = assets.topbar;\n    const topH = (width * (tb ? tb.height : battle_hex_1.TOPBAR.artH)) / (tb ? tb.width : battle_hex_1.TOPBAR.artW);\n    if (tb)\n        drawImg(ctx, tb, 0, 0, width, topH);\n    else {\n        ctx.fillStyle = '#4a3826';\n        ctx.fillRect(0, 0, Math.round(width), Math.round(topH));\n    }\n    ctx.fillStyle = `rgba(0,0,0,${battle_hex_1.TOPBAR.dimAlpha})`;\n    ctx.fillRect(0, 0, Math.round(width), Math.round(topH));\n    const k = width / battle_hex_1.TOPBAR.artW;\n    // 观测面 last-drawn 镜像（T23 §2.6：每帧先复位再按本帧实况覆写）\n    const hud = view.topbarHud;\n    hud.name = '';\n    hud.hpFrac = 0;\n    hud.neiliFrac = 0;\n    hud.hpPctText = '0%';\n    hud.neiliPctText = '0%';\n    hud.statusIcons = [];\n    const hero = snapshot.actors.find((a) => a.side === 'player');\n    if (hero) {\n        // 条填充：frac=clamp01(v/max)（max≤0 防除零，沿棋子 HUD :875 同防）；纵向采样渐变复刻原稿（开放点①默认）\n        const fracOf = (v, max) => Math.max(0, Math.min(1, v / Math.max(1, max)));\n        hud.hpFrac = fracOf(hero.hp, hero.maxHp);\n        hud.neiliFrac = fracOf(hero.neili, hero.maxNeili);\n        const drawBar = (rect, frac, colors) => {\n            const grad = ctx.createLinearGradient(0, rect.y * k, 0, (rect.y + rect.h) * k);\n            grad.addColorStop(0, colors[0]);\n            grad.addColorStop(1, colors[1]);\n            ctx.fillStyle = grad;\n            ctx.fillRect(Math.round(rect.x * k), Math.round(rect.y * k), Math.round(rect.w * frac * k), Math.round(rect.h * k));\n        };\n        drawBar(battle_hex_1.TOPBAR.redFill, hud.hpFrac, battle_hex_1.TOPBAR.hpGradient);\n        drawBar(battle_hex_1.TOPBAR.blueFill, hud.neiliFrac, battle_hex_1.TOPBAR.neiliGradient);\n        // 名字/百分比：宋体奶黄+深描边（ctrl_face_text_meta 同族），strokeText 先描后填（DMG 冒字同手法，禁 measureText）\n        hud.name = hero.name;\n        hud.hpPctText = `${Math.round(hud.hpFrac * 100)}%`;\n        hud.neiliPctText = `${Math.round(hud.neiliFrac * 100)}%`;\n        const drawTopText = (text, x, y, fontPx, align) => {\n            ctx.font = `${Math.round(fontPx * k)}px ${battle_hex_1.TOPBAR.fontStack}`;\n            ctx.textAlign = align;\n            ctx.textBaseline = 'middle';\n            ctx.lineJoin = 'round';\n            ctx.strokeStyle = battle_hex_1.TOPBAR.textStroke;\n            ctx.lineWidth = battle_hex_1.TOPBAR.textStrokeWidth * k;\n            ctx.strokeText(text, Math.round(x), Math.round(y));\n            ctx.fillStyle = battle_hex_1.TOPBAR.textFill;\n            ctx.fillText(text, Math.round(x), Math.round(y));\n        };\n        drawTopText(hero.name, battle_hex_1.TOPBAR.nameBox.x * k, (battle_hex_1.TOPBAR.nameBox.y + battle_hex_1.TOPBAR.nameBox.h / 2) * k, battle_hex_1.TOPBAR.nameFontPx, 'left');\n        // 百分比右对齐锚=填充末端 −pctPadRight（随填充末端移动=复刻烘焙稿位，§六-6）\n        drawTopText(hud.hpPctText, (battle_hex_1.TOPBAR.redFill.x + battle_hex_1.TOPBAR.redFill.w * hud.hpFrac - battle_hex_1.TOPBAR.pctPadRight) * k, (battle_hex_1.TOPBAR.redFill.y + battle_hex_1.TOPBAR.redFill.h / 2) * k, battle_hex_1.TOPBAR.pctFontPx, 'right');\n        drawTopText(hud.neiliPctText, (battle_hex_1.TOPBAR.blueFill.x + battle_hex_1.TOPBAR.blueFill.w * hud.neiliFrac - battle_hex_1.TOPBAR.pctPadRight) * k, (battle_hex_1.TOPBAR.blueFill.y + battle_hex_1.TOPBAR.blueFill.h / 2) * k, battle_hex_1.TOPBAR.pctFontPx, 'right');\n        // 状态图标×4（T23 §2.2：读冻结字段 statusIcons，key 命中映射才画、槽中心对齐、icon 实际尺寸×k；\n        // 空/未知 key 不画=base 空槽自然露出，不再画代码色块）\n        hud.statusIcons = hero.statusIcons.slice(0, battle_hex_1.TOPBAR.statusSlots.length);\n        for (let i = 0; i < battle_hex_1.TOPBAR.statusSlots.length; i++) {\n            const key = hero.statusIcons[i];\n            if (!key)\n                continue;\n            const iconImg = assets.statusIcons.get(key) ?? null;\n            const slot = battle_hex_1.TOPBAR.statusSlots[i];\n            if (!iconImg || !slot)\n                continue;\n            drawImg(ctx, iconImg, (slot.x + slot.w / 2) * k - (iconImg.width * k) / 2, (slot.y + slot.h / 2) * k - (iconImg.height * k) / 2, iconImg.width * k, iconImg.height * k);\n        }\n    }\n    // 左侧木牌挂串（左上锚；缺图时代码占位牌，热区照常产出）\n    const pl = assets.plaque;\n    const pw = Math.min(battle_hex_1.COMPONENT_LAYOUT.plaque.wRatio * width, (battle_hex_1.COMPONENT_LAYOUT.plaque.maxHRatio * height) / (pl ? pl.height / pl.width : 2.19));\n    const ph = (pw * (pl ? pl.height : 680)) / (pl ? pl.width : 310);\n    const px = battle_hex_1.COMPONENT_LAYOUT.plaque.leftRatio * width;\n    const py = battle_hex_1.COMPONENT_LAYOUT.plaque.topRatio * height;\n    if (pl) {\n        drawImg(ctx, pl, px, py, pw, ph);\n    }\n    else {\n        // 占位牌：深木双牌+金字（视觉降级，功能不缺位）\n        ctx.fillStyle = '#3a2c18';\n        ctx.strokeStyle = '#d4af37';\n        ctx.lineWidth = 2;\n        for (const b of battle_hex_1.PLAQUE_BUTTONS) {\n            const by = py + b.yRatio * ph;\n            const bh = b.hRatio * ph;\n            ctx.fillRect(Math.round(px + pw * 0.06), Math.round(by), Math.round(pw * 0.88), Math.round(bh));\n            ctx.strokeRect(Math.round(px + pw * 0.06), Math.round(by), Math.round(pw * 0.88), Math.round(bh));\n            ctx.fillStyle = '#ffd870';\n            ctx.font = `bold ${Math.round(pw * 0.22)}px ${battle_hex_1.FONT_STACK}`;\n            ctx.textAlign = 'center';\n            ctx.textBaseline = 'middle';\n            ctx.fillText(b.label, px + pw * 0.5, by + bh / 2);\n            ctx.fillStyle = '#3a2c18';\n        }\n    }\n    view.layout.plaqueRect = { x: px, y: py, w: pw, h: ph };\n    // 右下托管/加速/逃跑（右下锚：L 环反馈④——任何窗口比例恒贴右下可见；T23 §2.4 三钮独立脸+代码字+金框激活态；\n    // 布局/热区零改动：钮屏矩形换算与 CTRL_BUTTONS 标定同构，缺图逐钮代码占位兜底）\n    const faces = assets.ctrlFaces;\n    const artAR = battle_hex_1.CTRL_ART.h / battle_hex_1.CTRL_ART.w; // 448/223（与图片解耦，防异常尺寸）\n    const cw = Math.min(battle_hex_1.COMPONENT_LAYOUT.ctrl.wRatio * width, (battle_hex_1.COMPONENT_LAYOUT.ctrl.maxHRatio * height) / artAR);\n    const ch = cw * artAR;\n    const cx = Math.round(width - battle_hex_1.COMPONENT_LAYOUT.ctrl.rightRatio * width - cw);\n    const cy = Math.round(height - battle_hex_1.COMPONENT_LAYOUT.ctrl.bottomRatio * height - ch);\n    // 激活判定源=宿主每帧填充的 view.uiState（渲染层只收快照+view+assets，禁直调 session._debug）\n    const modeActive = view.uiState.mode === 'auto';\n    const speedActive = view.uiState.speed === true;\n    hud.ctrlActive.mode = modeActive;\n    hud.ctrlActive.speed = speedActive;\n    ctx.textAlign = 'center';\n    ctx.textBaseline = 'middle';\n    for (const b of battle_hex_1.CTRL_BUTTONS) {\n        const bx = cx + (b.x / battle_hex_1.CTRL_ART.w) * cw;\n        const by = cy + (b.y / battle_hex_1.CTRL_ART.h) * ch;\n        const bw = (b.w / battle_hex_1.CTRL_ART.w) * cw;\n        const bh = (b.h / battle_hex_1.CTRL_ART.h) * ch;\n        const face = b.action === 'mode' ? faces.tuoguan : b.action === 'speed' ? faces.jiasu : faces.flee;\n        if (face) {\n            drawImg(ctx, face, bx, by, bw, bh); // 切图=钮本体 1:1 预裁，铺满标定矩形（勿按 meta 252×164 canvas 缩放）\n        }\n        else {\n            // 占位钮：深木底圆角矩形+金字（视觉降级，功能不缺位；L④ 用例锁「热区照常产出」——沿现行降级路径原文）\n            ctx.fillStyle = '#3a2c18';\n            ctx.strokeStyle = '#d4af37';\n            ctx.lineWidth = 2;\n            ctx.beginPath();\n            roundRectPath(ctx, cx + cw * 0.04, by, cw * 0.92, bh, 6); // A06：能力检测内聚（缺 roundRect 走 arcTo 手工路径，几何等价）\n            ctx.fill();\n            ctx.stroke();\n            ctx.fillStyle = '#ffd870';\n            ctx.font = `bold ${Math.round(bh * 0.42)}px ${battle_hex_1.FONT_STACK}`;\n            const phLabel = b.action === 'mode' ? '托管' : b.action === 'speed' ? '加速' : '逃跑';\n            ctx.fillText(phLabel, cx + cw * 0.5, by + bh / 2);\n        }\n        // 激活态（判定=view.uiState；Leo 09-04 L环拍板：去叠亮/柔光，激活态=金框+换字；绿 rim 段亦已整段删除——激活无绿点/绿框）\n        const active = b.action === 'mode' ? modeActive : b.action === 'speed' ? speedActive : false;\n        if (active) {\n            ctx.strokeStyle = battle_hex_1.CTRL_ACTIVE.goldFrame;\n            ctx.lineWidth = battle_hex_1.CTRL_ACTIVE.frameWidthRatio * bw;\n            ctx.strokeRect(Math.round(bx), Math.round(by), Math.round(bw), Math.round(bh)); // 金框\n        }\n        // 代码字（仅托管/加速有脸时叠绘；逃跑=烘焙字静态图不叠字）：label=active?自动/两倍:托管/加速\n        if (face && (b.action === 'mode' || b.action === 'speed')) {\n            const label = active ? battle_hex_1.CTRL_TEXT.active[b.action] : battle_hex_1.CTRL_TEXT.normal[b.action];\n            const tx = bx + battle_hex_1.CTRL_TEXT.centerRatio.x * bw; // meta text_center [132,66]（右移避开骷髅/双刀，Leo 09-04）\n            const ty = by + battle_hex_1.CTRL_TEXT.centerRatio.y * bw;\n            const off = battle_hex_1.CTRL_TEXT.shadowOffsetRatio * bw;\n            ctx.font = `${Math.round(battle_hex_1.CTRL_TEXT.sizeRatio * bw)}px ${battle_hex_1.CTRL_TEXT.fontStack}`;\n            ctx.textAlign = 'center';\n            ctx.textBaseline = 'middle';\n            ctx.lineJoin = 'round';\n            ctx.strokeStyle = battle_hex_1.CTRL_TEXT.stroke;\n            ctx.lineWidth = battle_hex_1.CTRL_TEXT.strokeWidthRatio * bw;\n            ctx.fillStyle = battle_hex_1.CTRL_TEXT.stroke; // 阴影垫底（meta shadow_offset 深色错位）\n            ctx.fillText(label, Math.round(tx + off), Math.round(ty + off));\n            ctx.strokeText(label, Math.round(tx), Math.round(ty));\n            ctx.fillStyle = battle_hex_1.CTRL_TEXT.fill;\n            ctx.fillText(label, Math.round(tx), Math.round(ty));\n        }\n    }\n    view.layout.ctrlRect = { x: cx, y: cy, w: cw, h: ch };\n}\n// ============ L6 特效 + 结算遮罩 ============\nfunction drawFx(ctx, view, cam, width, height) {\n    for (const f of view.fx) {\n        const p = f.t / f.sec;\n        const sx = Math.round(f.x - cam.x + width / 2);\n        const sy = Math.round(f.y - cam.y + height / 2);\n        ctx.save();\n        ctx.globalAlpha = 1 - p;\n        if (f.kind === 'note') {\n            ctx.fillStyle = '#ffb84a';\n            ctx.font = `bold ${Math.round(width * 0.032)}px ${battle_hex_1.FONT_STACK}`;\n            ctx.textAlign = 'center';\n            ctx.textBaseline = 'middle';\n            ctx.fillText(f.text ?? '', sx, Math.round(sy - battle_hex_1.TILE_H - 26 - p * 22)); // 头顶上浮\n        }\n        else if (f.kind === 'dmg') {\n            // 伤害冒字（T21 §2.4）：白字深描边粗体，strokeText 先描 + fillText 后填（lineJoin='round' 防尖刺）；\n            // 定尺恒屏高 round(H×fontPerH)——数字挂角色走屏高/格高系，与 note 组件屏宽系刻意不同源（PM 裁决①）；\n            // 锚点沿 note 同族 TILE_H 抬升基准 + 上浮 risePx×p，渐隐 alpha 复用上方 1-p。\n            ctx.font = `bold ${Math.round(height * battle_hex_1.DMG.fontPerH)}px ${battle_hex_1.FONT_STACK}`;\n            ctx.textAlign = 'center';\n            ctx.textBaseline = 'middle';\n            ctx.lineJoin = 'round';\n            ctx.strokeStyle = battle_hex_1.DMG.strokeColor;\n            ctx.lineWidth = battle_hex_1.DMG.strokeWidth;\n            ctx.strokeText(f.text ?? '', sx + (f.dx ?? 0), Math.round(sy - battle_hex_1.TILE_H - p * battle_hex_1.DMG.risePx));\n            ctx.fillStyle = battle_hex_1.DMG.fillColor;\n            ctx.fillText(f.text ?? '', sx + (f.dx ?? 0), Math.round(sy - battle_hex_1.TILE_H - p * battle_hex_1.DMG.risePx));\n        }\n        else if (f.kind === 'slash') {\n            ctx.strokeStyle = battle_hex_1.FX.slashColor;\n            ctx.lineWidth = 3;\n            ctx.beginPath();\n            ctx.arc(sx, sy - 14, battle_hex_1.FX.maxRadius * (0.5 + p * 0.7), -0.9 + p * 1.6, 0.5 + p * 1.6);\n            ctx.stroke();\n        }\n        else {\n            ctx.strokeStyle = battle_hex_1.FX.hitColor;\n            ctx.lineWidth = 2.5;\n            ctx.beginPath();\n            ctx.arc(sx, sy - 10, battle_hex_1.FX.maxRadius * (0.3 + p * 0.9), 0, Math.PI * 2);\n            ctx.stroke();\n        }\n        ctx.restore();\n    }\n}\nfunction drawPhaseOverlay(ctx, snapshot, width, height) {\n    if (snapshot.phase === 'fighting')\n        return;\n    ctx.fillStyle = 'rgba(0,0,0,0.55)';\n    ctx.fillRect(0, 0, width, height);\n    ctx.textAlign = 'center';\n    ctx.textBaseline = 'middle';\n    ctx.fillStyle = '#ffe9b8';\n    ctx.font = `bold ${Math.round(width * 0.16)}px ${battle_hex_1.FONT_STACK}`;\n    const main = snapshot.phase === 'won' ? '胜' : snapshot.phase === 'lost' ? '败' : '已撤离';\n    ctx.fillText(main, width / 2, height / 2 - width * 0.04);\n    ctx.font = `${Math.round(width * 0.04)}px ${battle_hex_1.FONT_STACK}`;\n    ctx.fillStyle = '#d9c896';\n    ctx.fillText('点击任意处重开演示', width / 2, height / 2 + width * 0.07);\n}\n// ============ 主入口 ============\n/** 每帧绘制（L0→L6 顺序）。快照与资源只读；演出状态由 view 承载。 */\nfunction drawFrame(fc, snapshot, assets, view) {\n    const { ctx, width, height } = fc;\n    const cam = view.camera;\n    // L0 环境底图（T24 方案 §2.1 改**世界系**）：envRect=envWorldRect(当帧 width/height)，绘制\n    // sx=envRect.x−cam.x+width/2 与格子**同一 cam 同一变换式**（禁第三套换算——易错 3），env 随镜头 1:1 平移。\n    // 旧「屏幕空间静态、不随镜头（75 v2.3 已验口径）」已翻案：env 钉屏不随 cam=「拖动地不动、台面动」两张皮根因。\n    // 层级保持在最底且在一切 clip 之前（env 不受裁剪——易错 2）；缺失降级纯色。\n    if (assets.env) {\n        const er = envWorldRect(width, height);\n        drawImg(ctx, assets.env, er.x - cam.x + width / 2, er.y - cam.y + height / 2, er.w, er.h);\n    }\n    else {\n        ctx.fillStyle = '#1c2416';\n        ctx.fillRect(0, 0, width, height);\n    }\n    ctx.save();\n    ctx.beginPath();\n    ctx.rect(0, 0, width, height);\n    ctx.clip();\n    drawCells(ctx, snapshot, cam, width, height, view.selectedCell);\n    const placed = drawPieces(ctx, snapshot, assets, view, cam, width, height);\n    drawPieceHud(ctx, placed, snapshot, view);\n    drawFx(ctx, view, cam, width, height);\n    ctx.restore();\n    drawComponents(ctx, snapshot, assets, width, height, view);\n    drawPhaseOverlay(ctx, snapshot, width, height);\n}\n";
  // ---- ui/battle-hex-render ----
  __def("ui/battle-hex-render", new Function("require", "module", "exports", __mod_ui_battle_hex_render));
  var __mod_systems_battle_session = "\"use strict\";\n// T18 战斗 session 结构重构 + 交互语义定版（规格 v1.0 落地，全新重写）\n// 行为真源：《战斗交互行为规格》v1.0（§三 移动三态 / §四 状态机 / §五 矩阵 / D1~D4 定版）\n// 规则真源：战斗规则C案（F-06/F-05/R-05/R-08/R-09）；数值真值：battle-core（零改动）+ 本文件 MVP 常量\n//\n// ═══ 交互语义规格表（唯一真源，PM 指令随源码携带） ═══\n// | 移动类型               | 可达判定                                    | 路径穿越             | 落点约束 |\n// | 普通移动（无选中）     | BFS 连通，移动力=F-06                        | ✗ 不可穿任何单位     | 仅空格 |\n// | 轻功跳跃（激活态）     | cube 距离 ≤ ⌊F-06/2⌋，无连通性要求           | ✓ 可穿越任何单位     | 仅空格 |\n// | 轻功未激活/未解锁      | 同普通移动（无跳跃格）                       | ✗                   | 仅空格 |\n// 可动区口径：12 高 × 8 宽（row 2..13 × col 4..11，L 环追加；规格 BASE-1 勘误待登记）\n// ═══════════════════════════════════════════════════════════\n//\n// 五大病灶收敛对照（规格 §八 → 本文件落点，回执引用）：\n//   病灶① bar 四职责拆分 → 填充=tick clamp；消耗=commitTurn 显式 bar=0（BAR-3/D3）；\n//         回合判定=tick 轮转 bar≥BAR.max（BAR-2）；选中生命周期=selection 状态机（activate/clear，不读 bar）。\n//   病灶② isJump 单点 → doMove(intent) 唯一推导（AI/玩家只传 'walk'|'jump' 意图）。\n//   病灶③ moveCells 同源 → legalMoveCells() 唯一产生点，snapshot 显示与 submit 校验共用。\n//   病灶④ selectedSkill 收敛 → selection 对象（activate/clear 两写点，kind+legalCells 派生快取）。\n//   病灶⑤ pos 派生视图 → Runner 无几何 pos 字段（恒原点单例），hex 唯一真值；无 swap、无手工双写\n//         （Q1 批复适配：屏蔽 core 曼哈顿复核；posNeutral 常量，无 swap、无手工双写）。\n//\n// 确定性契约：单 rng 流（我方出生→敌方出生→战斗掷骰），同 seed + 同操作序列 → 事件流全等\n// （含 rejected 拒绝序列，SP-3）。\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ANIM_MS = exports.NEILI_INITIAL = exports.NEILI_COST_PER_CAST = exports.FIELD_ROW_MAX = exports.FIELD_ROW_MIN = exports.FIELD_COL_MAX = exports.FIELD_COL_MIN = exports.MAP_SIZE = void 0;\nexports.assembleRoster = assembleRoster;\nexports.createHexBattle = createHexBattle;\nconst battle_core_1 = require(\"./battle-core\");\nconst battle_1 = require(\"../config/battle\");\nconst hex_1 = require(\"./hex\");\n// ---------- 布局与经济常量（Q7 批复：session 本地导出，config/battle-hex.ts 归 FE 卡） ----------\n/** 棋盘 16×16（96 号）。可移动区 = **12 高 × 8 宽**（row 2..13 十二行 × col 4..11 八列【16 宽内居中】，\n * L 环追加口径：配合压扁瓦片形成纵向对峙纵深；规格 BASE-1 待勘误 12×12 → 12h×8w） */\nexports.MAP_SIZE = 16;\nexports.FIELD_COL_MIN = 4;\nexports.FIELD_COL_MAX = 11;\nexports.FIELD_ROW_MIN = 2;\nexports.FIELD_ROW_MAX = 13;\n/** 【Q2 批复 · MVP 内力口径】特/绝/轻每季释放消耗内力 1、我方初始内力 100。\n * 正式内力经济后置：替换点=本常量 + makeInitialPlayer 装配（实装配置时回填公式总览）。 */\nexports.NEILI_COST_PER_CAST = 1;\nexports.NEILI_INITIAL = 100;\n/** 表现时长 ms（展示参数，ADR-004 口径；驱动快照动画态） */\nexports.ANIM_MS = { walk: 300, charge: 100, strike: 300, basic: 300, hit: 300 };\n/** 【病灶⑤】core 兼容 pos 视图单例：恒原点、无几何语义（hex 才是真值、无手工双写）。\n * 所有 Runner 共享此引用；resolveAction 的曼哈顿复核在 hex 度量下恒通过（Q1 批复适配终态）。 */\nconst POS_NEUTRAL = { x: 0, y: 0 };\n// ---------- 便捷组装（core rollEnemyCount + makeEnemy 薄转发，零数值逻辑） ----------\nfunction assembleRoster(shizhan, template, rng) {\n    return Array.from({ length: (0, battle_core_1.rollEnemyCount)(shizhan, rng) }, (_, i) => (0, battle_core_1.makeEnemy)(i, template));\n}\n// ---------- 工厂 ----------\n/** 【体检 A01 · 快照出口防御性复制】数组浅拷贝 + 逐元素 {...p}——HexPos={q,r} 字面量两层即断\n * 引用（报告 A01：单纯浅复制不能隔离元素修改），禁深递归（快照每帧重建，渲染热路径零浪费）。\n * 只在快照出口调用；selection.legalCells 内部仍是唯一真值（病灶③「显示=校验=结算」三同源不\n * 破坏：submit 校验与 cast 结算照读内部数组，仅快照出口断引用，展示端无法反向影响合法性）。 */\nfunction copyCells(cells) {\n    return cells.map((p) => ({ ...p }));\n}\nfunction createHexBattle(opts) {\n    // SP-2 确定性：单 rng 流、消费顺序固定（我方出生→敌方出生→战斗掷骰）\n    const rng = (0, battle_core_1.makeRng)(opts.seed ?? 20260902);\n    const events = [];\n    let t = 0;\n    let mode = opts.mode;\n    let speedFast = false;\n    let phase = 'fighting';\n    let lastActedId = null;\n    const manual = { stage: 0, idleSec: 0 };\n    let selection = null;\n    // ---- 布局与出生（D1 · SP-1 锚点制） ----\n    const zoneCells = (cols, rows) => {\n        const cells = [];\n        for (let row = rows[0]; row <= rows[1]; row++) {\n            for (let col = cols[0]; col <= cols[1]; col++)\n                cells.push((0, hex_1.offsetToAxial)(col, row));\n        }\n        return cells;\n    };\n    /** 【D1 · SP-1 · L 环 12h×8w 重算】出生锚：我方=可动区左下极格 offset(4,13)、\n     * 敌方=右上极格 offset(11,2)；出生格=锚 hex 距离 ≤3 的可动区格 rng 洗牌\n     * （锚距 cube=12 > 3+3，两带天然不相交）。 */\n    const ANCHOR_PLAYER = (0, hex_1.offsetToAxial)(4, 13);\n    const ANCHOR_ENEMY = (0, hex_1.offsetToAxial)(11, 2);\n    const spawnBand = (anchor) => zoneCells([exports.FIELD_COL_MIN, exports.FIELD_COL_MAX], [exports.FIELD_ROW_MIN, exports.FIELD_ROW_MAX]).filter((p) => (0, hex_1.cubeDistance)(anchor, p) <= 3);\n    // ---- 体检 A03 入口运行时校验（PM 裁定：非法输入 fail-fast 抛 Error，不静默吞不兜底） ----\n    // 置于任何 rng 消费/出生结算之前：坏输入零副作用（确定性契约不受校验影响）。\n    // 合法 1~6 敌基线（core rollEnemyCount 上限）远小于出生带容量，运算次序零变更。\n    {\n        const seenIds = new Set();\n        for (const c of [opts.player, ...opts.enemies]) {\n            if (seenIds.has(c.id))\n                throw new Error(`createHexBattle: duplicate combatant id \"${c.id}\"`);\n            seenIds.add(c.id);\n        }\n        const enemyCapacity = spawnBand(ANCHOR_ENEMY).length;\n        if (opts.enemies.length > enemyCapacity) {\n            throw new Error(`createHexBattle: enemy count ${opts.enemies.length} exceeds spawn capacity ${enemyCapacity}`);\n        }\n    }\n    const shuffleTake = (cells, n) => {\n        const a = cells.slice();\n        for (let i = a.length - 1; i > 0; i--) {\n            const j = Math.floor(rng() * (i + 1));\n            [a[i], a[j]] = [a[j], a[i]];\n        }\n        return a.slice(0, n);\n    };\n    const mk = (c, spawn) => ({\n        id: c.id,\n        side: c.side,\n        name: c.name,\n        hp: c.hp,\n        maxHp: c.maxHp,\n        // 【Q2 · MVP 内力口径】我方未显式配置内力（maxNeili=0）时应用初始 100；调用方显式配置则尊重\n        neili: c.side === 'player' && c.maxNeili === 0 ? exports.NEILI_INITIAL : c.neili,\n        maxNeili: c.side === 'player' && c.maxNeili === 0 ? exports.NEILI_INITIAL : c.maxNeili,\n        atk: c.atk,\n        def: c.def,\n        neigongLevel: c.neigongLevel,\n        jimin: c.jimin,\n        danshi: c.danshi,\n        shizhan: c.shizhan,\n        weapon: c.weapon,\n        skills: c.skills,\n        cooldowns: new Map(c.skills.map((s) => [s.id, 0])),\n        pos: POS_NEUTRAL,\n        bar: 0,\n        hex: spawn,\n        hexFacing: { q: 1, r: 0 },\n        faceLeft: c.side === 'enemy',\n        renderQ: spawn.q,\n        renderR: spawn.r,\n        moveFromQ: spawn.q,\n        moveFromR: spawn.r,\n        moveT: 1,\n        movePath: [],\n        isJump: false,\n        dead: false,\n        animState: 'idle',\n        animLeftMs: 0,\n        pendingAnim: null,\n        barWasMax: false,\n    });\n    const playerSpawn = shuffleTake(spawnBand(ANCHOR_PLAYER), 1)[0];\n    const player = mk(opts.player, playerSpawn);\n    const enemySpawns = shuffleTake(spawnBand(ANCHOR_ENEMY), opts.enemies.length);\n    const enemies = opts.enemies.map((e, i) => mk(e, enemySpawns[i]));\n    const all = [player, ...enemies];\n    // ---- 基础查询 ----\n    const alive = () => all.filter((c) => !c.dead);\n    const foesOf = (c) => alive().filter((x) => x.side !== c.side);\n    const byId = (id) => all.find((c) => c.id === id);\n    const occupied = () => alive().map((c) => c.hex);\n    const hpSum = (side) => all.filter((c) => c.side === side).reduce((s, c) => s + Math.max(0, c.hp), 0);\n    const inField = (p) => {\n        const off = (0, hex_1.axialToOffset)(p);\n        return off.col >= exports.FIELD_COL_MIN && off.col <= exports.FIELD_COL_MAX && off.row >= exports.FIELD_ROW_MIN && off.row <= exports.FIELD_ROW_MAX;\n    };\n    const emit = (e) => events.push({ t: +t.toFixed(2), ...e });\n    // ---- 病灶③：合法移动格唯一产生点（显示与校验同源） ----\n    /** 普通可达（MV-1）：BFS 步数 ≤ F-06 移动力，不可进入/借道单位格，限可动区。 */\n    function walkCells(actor) {\n        return (0, hex_1.reachable)(actor.hex, (0, hex_1.movePower)(actor.skills), occupied(), inField);\n    }\n    /** 跳跃格（MV-2）：cube 距离 ≤ ⌊F-06/2⌋ 的可动区空格，可穿越任何单位（无连通性要求）。 */\n    function jumpCells(actor) {\n        return (0, hex_1.jumpReachable)(actor.hex, (0, hex_1.movePower)(actor.skills), occupied(), inField);\n    }\n    /** 【唯一产生点】合法移动格：snapshot（显示）与 submit（校验）共用。\n     * 无选中=普通绿格（MV-1）；轻功选中=金格（MV-2）；攻击技选中=空（高亮走 attackCells）。 */\n    function legalMoveCells() {\n        if (!selection)\n            return walkCells(player);\n        if (selection.kind === 'qing')\n            return selection.legalCells;\n        return [];\n    }\n    // ---- 选中态动作（病灶④：写点仅此两处） ----\n    /** 激活（SEL-2 互斥 toggle / SEL-6 置灰拒激活）。激活不消耗行动预算。 */\n    function activate(skillId) {\n        const s = player.skills.find((x) => x.id === skillId);\n        if (!s || player.dead)\n            return false;\n        if (selection?.skillId === skillId) {\n            selection = null; // SEL-5①：再点同钮=取消\n            return true;\n        }\n        if (s.kind === 'qingGong') {\n            // SEL-6/MV-2：一阶轻功（⌊power/2⌋=0）或内力不足 → 置灰拒激活\n            if (Math.floor((0, hex_1.movePower)(player.skills) / 2) < 1 || player.neili < exports.NEILI_COST_PER_CAST)\n                return false;\n            selection = { skillId, kind: 'qing', legalCells: jumpCells(player) };\n            return true;\n        }\n        // 攻击技（特/绝）：SEL-6 置灰 = 冷却中/内力不足/武器不匹配 → 拒激活\n        if (player.neili < exports.NEILI_COST_PER_CAST ||\n            (player.cooldowns.get(skillId) ?? 0) > 0 ||\n            (s.weapon !== null && s.weapon !== player.weapon)) {\n            return false;\n        }\n        const shape = (0, hex_1.rangeShapeOf)(s.weapon ?? player.weapon ?? 'fist');\n        selection = {\n            skillId,\n            kind: 'attack',\n            legalCells: (0, hex_1.rangeCells)(player.hex, shape, (0, battle_core_1.skillRange)(s), player.hexFacing, inField),\n        };\n        return true;\n    }\n    /** 清除（SEL-3 消耗 / SEL-4 回落 / SEL-7 切自动·逃跑·终局 / SEL-5①②取消）。 */\n    function clearSelection() {\n        selection = null;\n    }\n    /** 朝向更新：六向 facing = from→to 的 cube 最近方向（Q4 批复，锥形轴）。\n     * 【FACE-1 §二.4-A】吸附度量 = axial 平面 2D 点积 → **cube 3D 点积**（同 hex.ts dirIndexOf\n     * 度量、整数精确；session 自持 ~6 行不引其导出——hex 零改动红线）。axial 基非正交，2D 点积\n     * 不度量对齐度：(0,±1) 两族竖向邻格被误吸到斜向（§一.6-A 实证），cube 度量修复；\n     * 方向边界平局（如 (2,-1) 族两向等分点）以 dirs 序首胜（与 dirIndexOf 同判，§二.4 定版）。\n     * 【FACE-1 §二.4-B】立绘左右向 = 像素水平分量符号 sign(2Δq+Δr)：场内 r≥0 时像素 x/W =\n     * q + r/2 恒等，故 Δx 符号 = sign(2Δq+Δr)（奇偶行 (0,±1) 方向 Δcol=0 的「保持旧值」\n     * 缺陷由此消除）；=0（等分水平向量）保持旧值，防抖口径同 T06（faceToward 每行动一次，\n     * 非逐帧调用，无抖动面）。 */\n    function faceToward(c, to) {\n        const v = { q: to.q - c.hex.q, r: to.r - c.hex.r };\n        if (v.q === 0 && v.r === 0)\n            return;\n        const dirs = [\n            { q: 1, r: 0 },\n            { q: 1, r: -1 },\n            { q: 0, r: -1 },\n            { q: -1, r: 0 },\n            { q: -1, r: 1 },\n            { q: 0, r: 1 },\n        ];\n        const vx = v.q;\n        const vz = v.r;\n        const vy = -vx - vz; // axial→cube：x=q, z=r, y=-x-z\n        let best = dirs[0];\n        let bestDot = -Infinity;\n        for (const d of dirs) {\n            const dx = d.q;\n            const dz = d.r;\n            const dy = -dx - dz;\n            const dot = vx * dx + vy * dy + vz * dz; // cube 3D 点积（整数精确，零浮点）\n            if (dot > bestDot) {\n                bestDot = dot;\n                best = d;\n            }\n        }\n        c.hexFacing = best;\n        const sx = 2 * v.q + v.r; // 像素水平分量符号（∝ Δq + Δr/2）\n        if (sx > 0)\n            c.faceLeft = false;\n        else if (sx < 0)\n            c.faceLeft = true;\n    }\n    function setAnim(c, state) {\n        c.animState = state;\n        c.animLeftMs = exports.ANIM_MS[state] ?? 0;\n    }\n    /** R-08 冷却递减：每行动回合恰一次，且须在全部「读冷却」判定之后（镜像 core act() 读→减→写序）。 */\n    function tickCooldowns(actor) {\n        for (const s of actor.skills) {\n            const cd = actor.cooldowns.get(s.id) ?? 0;\n            if (cd > 0)\n                actor.cooldowns.set(s.id, cd - 1);\n        }\n    }\n    // ---- 出招（唯一数值真值 = core resolveAction；病灶⑤：pos=POS_NEUTRAL 单例派生） ----\n    /** core 兼容视图（病灶⑤收敛点）：resolveAction 读 pos 做（已废弃度量的）曼哈顿复核——\n     * hex 度量换轨后射程已由 session 按 cube 距离预筛（Q1 批复适配），此处以 posNeutral 屏蔽之；\n     * Proxy 仅拦截 get(pos)，其余读写全部转发真身（neili/cooldowns/hp 副作用契约原样生效）。\n     * 无 swap、无手工双写、core 零改动。 */\n    /** 单次出手结算。skill 非 null 时按 Q2 口径以「视图技能」传入（neiliCost 视图=1，SkillDef 真值不动）。\n     * 【ATK-2 v2.2 · T22】payCost（session 内部参数，非契约）：AOE 逐目标结算中首目标 true（视图=1，\n     * 走 resolveAction 真值扣费）、后续 false（视图=0，core:245 扣 0）——资源每次施放恰扣一次；\n     * 冷却 core:246 写初值幂等，重复调用无行为差异（方案 §二.1 改动一）。 */\n    function doAttack(actor, target, skill, quiet = false, payCost = true) {\n        const skillView = skill ? { ...skill, neiliCost: payCost ? exports.NEILI_COST_PER_CAST : 0 } : null;\n        const outcome = (0, battle_core_1.resolveAction)(actor, target, skillView, rng);\n        if (outcome.kind !== 'blocked') {\n            // 【T19/N1 主修 · 方案 §3.2】ATK-3 路径（quiet 且仍在移动演出中）不覆写 walk——攻击演出\n            // 写入延后到移动演出结束（tick 倒计时归零分支消费 pendingAnim）。\n            // 结算与事件 emit 不受影响（仍同步，绿锁「move 事件后跟 basic 事件」保住）；faceToward 不变。\n            if (quiet && actor.animState === 'walk') {\n                actor.pendingAnim = skill ? 'charge' : 'basic';\n            }\n            else {\n                setAnim(actor, skill ? 'charge' : 'basic');\n            }\n            faceToward(actor, target.hex);\n        }\n        for (const l of outcome.logs) {\n            if (quiet && l.action === 'blocked')\n                continue;\n            emit({\n                type: l.action === 'skill'\n                    ? 'skill'\n                    : l.action === 'basic'\n                        ? 'basic'\n                        : l.action === 'miss'\n                            ? 'miss'\n                            : l.action === 'fallback'\n                                ? 'fallback'\n                                : 'blocked',\n                actorId: l.actorId,\n                targetId: l.targetId,\n                skillId: l.skillId,\n                damage: l.damage,\n                crit: l.crit,\n            });\n        }\n        if (target.hp <= 0 && !target.dead) {\n            target.dead = true;\n            target.animState = 'dead';\n            target.animLeftMs = 0;\n            emit({ type: 'death', actorId: target.id });\n        }\n        checkEnd();\n    }\n    /** 【FACE-1 §二.1 · faceTarget 唯一产生点】受击敌中 cube 距离最小者（平局判距=逻辑格 hex，\n     * §六-2 PM 裁定，与 ATK-7 命中口径同源）。单次遍历求 min + 保序收集平局（保持 all 数组序，\n     * 禁 sort——SP-2 目标序锁）；勿复用 pickTarget（hp 次键且 sort 破序，§四-5）。\n     * rng 纪律（规格③ + §六-4 恒掷 1 次）：ties==1 零消费；ties≥2 恰掷 1 次。消费点由调用方\n     * 固定在逐目标循环之前（=本次施放首次掷骰之前，§二.2 SP-2 时序论证：消费计数为状态\n     * 确定函数，同 seed 双场逐位全同）。 */\n    function faceTargetOf(actor, targets) {\n        // 【体检 A02 · FACE-1 备忘合并】入口非空断言（PM 裁定：空放路径不得静默吞——空输入旧路径\n        // 会消费 1 次 rng 并在 resolveAoe 尾部取 .hex 崩溃）。正常两调用链均不可达空：cast 臂被\n        // submit 的 targets.length>0 守卫滤空（ATK-6 空放走 else 臂镜像结算，不经本函数）；AI 臂\n        // planSkill 只返回非空计划。两守卫与本断言互为注释锚点，禁把空 AOE 静默当空放。\n        if (targets.length === 0)\n            throw new Error('faceTargetOf: empty targets');\n        let dmin = Infinity;\n        for (const t of targets) {\n            const d = (0, hex_1.cubeDistance)(actor.hex, t.hex);\n            if (d < dmin)\n                dmin = d;\n        }\n        const ties = targets.filter((t) => (0, hex_1.cubeDistance)(actor.hex, t.hex) === dmin);\n        if (ties.length === 1)\n            return ties[0]; // rng 零消费\n        return ties[Math.floor(rng() * ties.length)]; // 平局恰掷 1 次（规格③随机取一）\n    }\n    /** 【ATK-2 v2.2 · T22】范围 AOE 逐目标结算（cast 与 aiAct 案 A 的单一产生点）：\n     * 首目标承担资源（R-09/R-08 经 resolveAction 真值路径，neiliCost 视图=1）；后续目标\n     * payCost=false（视图=0，core:245 扣 0）——资源每次施放恰扣一次。每目标独立掷骰\n     * （命中 F-04 → 闪避 → 暴击，1~3 次 rng）+ 各发 skill|miss 事件（既有形状）+ 各自死亡判定。\n     * targets 顺序由调用方定死（all 数组序一次快照，禁 sort——SP-2 确定性）。\n     * 循环内 doAttack 副作用幂等注记（锁）：setAnim('charge') 同值覆写=单次效果（animLeftMs\n     * 重置同值，同步循环等价单次）；faceToward 循环内逐目标为不可观测中间态（submit/aiAct\n     * 同步块无快照采样，§一.1 注）——【FACE-1 §二.1】入口先定 faceTarget（rng 在循环前），\n     * 循环后 faceToward(actor, faceTarget.hex) 单点定版（朝最近受击敌；v2.2 点击格定版废止）。 */\n    function resolveAoe(actor, skill, targets) {\n        const faceTarget = faceTargetOf(actor, targets); // ★ FACE-1：faceTarget 产生单点，rng 先于逐目标掷骰\n        for (let i = 0; i < targets.length; i++)\n            doAttack(actor, targets[i], skill, false, i === 0);\n        faceToward(actor, faceTarget.hex); // ★ FACE-1：朝最近敌定版（AI 与玩家同规则，案 A 五点⑤）\n    }\n    // ---- 移动（病灶②：isJump 唯一产生点） ----\n    /** 移动结算：intent 由调用方按选中态给出（'walk'=贴地 / 'jump'=轻功抛物线）；\n     * AI 恒 'walk'（AI 主动用跳留待 AI 技能决策卡）。事件 toX/toY=offset(列,行)。 */\n    function doMove(actor, to, intent) {\n        const isJump = intent === 'jump'; // 【病灶②收敛】isJump 唯一产生点\n        faceToward(actor, to);\n        // 【T19/N1 防御 · 方案 §3.3】改 hex 前计算回退轨：walk 沿合法路径（BFS，不可穿占格，\n        // 失败防御回退直线）；jump 凌空可穿（MV-2 语义）恒 [旧格,to] 直线，不套绕行。\n        const fromCell = { ...actor.hex };\n        actor.movePath = isJump ? [fromCell, to] : (0, hex_1.movePathCells)(fromCell, to, occupied(), inField);\n        actor.hex = to;\n        const off = (0, hex_1.axialToOffset)(to);\n        actor.moveFromQ = actor.renderQ;\n        actor.moveFromR = actor.renderR;\n        actor.moveT = 0;\n        actor.isJump = isJump;\n        actor.pendingAnim = null; // 新移动演出开始，旧延后攻击演出作废（防跨回合误播）\n        setAnim(actor, 'walk');\n        emit({ type: 'move', actorId: actor.id, toX: off.col, toY: off.row });\n    }\n    /** ATK-3 移动附带普攻特例：到位后最近敌在普攻射程内 → 自动普攻（不另耗行动、blocked 静默）。 */\n    function basicIfAdjacent(actor) {\n        const nearest = pickTarget(actor);\n        if (!nearest)\n            return;\n        if ((0, hex_1.cubeDistance)(actor.hex, nearest.hex) <= (0, battle_core_1.basicRange)(actor)) {\n            doAttack(actor, nearest, null, true);\n        }\n    }\n    // ---- AI（C 案 B2 五级优先表；与玩家同构，位移恒 'walk' 意图） ----\n    function pickTarget(actor) {\n        const foes = foesOf(actor);\n        if (foes.length === 0)\n            return null;\n        return foes\n            .slice()\n            .sort((a, b) => (0, hex_1.cubeDistance)(actor.hex, a.hex) - (0, hex_1.cubeDistance)(actor.hex, b.hex) || a.hp - b.hp)[0];\n    }\n    function targetInRange(actor, target, n, shape) {\n        if ((0, hex_1.cubeDistance)(actor.hex, target.hex) > n)\n            return false;\n        if (shape === 'cone')\n            return (0, hex_1.inCone)(actor.hex, actor.hexFacing, target.hex, n);\n        return true;\n    }\n    /** 【T22 案 A · 五点⑤「敌方同规则」】AI 代行出技计划：按品阶降序取首个有射程内目标的技能，\n     * 收集射程内**全体**存活敌（foesOf 保持 all 数组序，禁 sort——resolveAoe 目标序=SP-2 确定性）；\n     * 1v1 局 targets 退化单目标（既有 AI 用例零改保持绿）。 */\n    function planSkill(actor) {\n        const usable = actor.skills\n            .filter((s) => (s.weapon === null || s.weapon === actor.weapon) &&\n            (actor.cooldowns.get(s.id) ?? 0) <= 0 &&\n            actor.neili >= exports.NEILI_COST_PER_CAST)\n            .sort((a, b) => b.grade - a.grade);\n        for (const s of usable) {\n            const n = (0, battle_core_1.skillRange)(s);\n            const shape = (0, hex_1.rangeShapeOf)(s.weapon ?? actor.weapon ?? 'fist');\n            const targets = foesOf(actor).filter((f) => targetInRange(actor, f, n, shape));\n            if (targets.length > 0)\n                return { skill: s, targets };\n        }\n        return null;\n    }\n    function aiAct(actor, forcedTrust) {\n        if (forcedTrust)\n            emit({ type: 'trust', actorId: actor.id });\n        const target = pickTarget(actor);\n        if (!target)\n            return;\n        const plan = planSkill(actor);\n        const basicOk = targetInRange(actor, target, (0, battle_core_1.basicRange)(actor), (0, hex_1.rangeShapeOf)(actor.weapon ?? 'fist'));\n        tickCooldowns(actor);\n        if (plan) {\n            resolveAoe(actor, plan.skill, plan.targets); // 【案 A】AI 代行出技同构 AOE（1v1 退化单目标）\n            return;\n        }\n        if (basicOk) {\n            doAttack(actor, target, null);\n            return;\n        }\n        // 第 4 级：位移进射程（普通移动，'walk' 意图——AI 主动用跳留待 AI 技能决策卡）\n        const best = walkCells(actor)\n            .slice()\n            .sort((a, b) => (0, hex_1.cubeDistance)(a, target.hex) - (0, hex_1.cubeDistance)(b, target.hex) ||\n            (0, hex_1.cubeDistance)(a, actor.hex) - (0, hex_1.cubeDistance)(b, actor.hex))[0];\n        if (best) {\n            doMove(actor, best, 'walk');\n            basicIfAdjacent(actor);\n        }\n        // 第 5 级：无格可动 → 莽（原地结束回合）\n    }\n    // ---- 回合提交（病灶①：bar 消耗=显式清零；选中清除挂 selection.clear） ----\n    function commitTurn(c) {\n        c.bar = 0; // 【BAR-3 / D3】显式清零重置（不依赖 clamp+减法隐式配合）\n        c.barWasMax = false;\n        if (c.side === 'player') {\n            manual.idleSec = 0;\n            clearSelection(); // 【SEL-3 / BASE-6】行动提交 → 选中清除（无连放）\n        }\n        lastActedId = c.id;\n    }\n    function checkEnd() {\n        if (phase !== 'fighting')\n            return;\n        if (foesOf(player).length === 0) {\n            phase = 'won';\n            clearSelection(); // SEL-7 终局清输入态\n            emit({ type: 'win' });\n        }\n        else if (player.dead) {\n            phase = 'lost';\n            clearSelection();\n            emit({ type: 'lose' });\n        }\n    }\n    /** BAR-5：90s 总时长未分胜负 → 存活 hp 总量高者胜，同量判玩家负（常量=core 唯一真值）。 */\n    function settleTimeout() {\n        if (phase !== 'fighting')\n            return;\n        emit({ type: 'timeout-hp' });\n        phase = hpSum('player') > hpSum('enemy') ? 'won' : 'lost';\n        clearSelection(); // SEL-7\n        emit({ type: phase === 'won' ? 'win' : 'lose' });\n    }\n    /** SEL-1 输入态：条满（clamp 后恒 ≤100，满即 100）+ 手动 + 我方存活。 */\n    const pendingInputNow = () => phase === 'fighting' && mode === 'manual' && !player.dead && player.bar >= battle_1.BAR.max;\n    function tick(realDtSec) {\n        // 【体检 A03 入口校验 · PM 裁定 fail-fast】非有限或负 dt 直接抛（宿主边界坏输入禁静默禁兜底：\n        // NaN 曾直乘速率把 clock/actionBar 写成 NaN 且不可恢复）。校验先于 phase 门——终局后坏输入\n        // 同样暴露；合法 0 dt 不受影响（零前进仍走后续逻辑，既有行为零变更）。\n        if (!Number.isFinite(realDtSec) || realDtSec < 0) {\n            throw new Error(`tick: dt must be a finite non-negative number, got ${realDtSec}`);\n        }\n        if (phase !== 'fighting')\n            return;\n        const speed = speedFast ? battle_1.SPEED_FACTOR.fast : battle_1.SPEED_FACTOR.normal;\n        const dt = realDtSec * speed;\n        if (!pendingInputNow())\n            t += dt; // BAR-4：等待期冻结总时钟（不烧思考时间）\n        for (const c of all) {\n            if (!c.dead)\n                c.bar = Math.min(battle_1.BAR.max, c.bar + (0, battle_core_1.fillRate)(c) * dt); // BAR-1 clamp 封顶\n            if (c.moveT < 1) {\n                c.moveT = Math.min(1, c.moveT + (dt * 1000) / exports.ANIM_MS.walk);\n                // 【T19/N1 防御 · 方案 §3.3】沿 movePath 分段等时插值（回退轨不穿占格）：\n                // moveT=1 时 f=segs → i 收敛到末段、lp=1 → renderPos 精确等于 path 末位=hex\n                //（整数坐标 IEEE 精确，FE settled 释放条件保持成立）；movePath 空/单点回退现直线公式。\n                const path = c.movePath;\n                if (path.length >= 2) {\n                    const segs = path.length - 1;\n                    const f = Math.min(segs, c.moveT * segs);\n                    const i = Math.min(segs - 1, Math.floor(f));\n                    const lp = f - i;\n                    c.renderQ = path[i].q + (path[i + 1].q - path[i].q) * lp;\n                    c.renderR = path[i].r + (path[i + 1].r - path[i].r) * lp;\n                }\n                else {\n                    c.renderQ = c.moveFromQ + (c.hex.q - c.moveFromQ) * c.moveT;\n                    c.renderR = c.moveFromR + (c.hex.r - c.moveFromR) * c.moveT;\n                }\n            }\n            if (c.animLeftMs > 0) {\n                c.animLeftMs -= dt * 1000;\n                if (c.animLeftMs <= 0 && !c.dead) {\n                    if (c.animState === 'charge') {\n                        c.animState = 'strike';\n                        c.animLeftMs = exports.ANIM_MS.strike;\n                    }\n                    else if (c.pendingAnim) {\n                        // 【T19/N1 主修 · 方案 §3.2】移动演出结束 → 补播延后的攻击演出（优先级 charge→strike→pendingAnim→idle）\n                        setAnim(c, c.pendingAnim);\n                        c.pendingAnim = null;\n                    }\n                    else {\n                        c.animState = 'idle';\n                        c.animLeftMs = 0;\n                    }\n                }\n            }\n        }\n        // BAR-2 轮转：bar 高 → fillRate 快 → 玩家先\n        const ready = all\n            .filter((c) => !c.dead && c.bar >= battle_1.BAR.max)\n            .sort((a, b) => b.bar - a.bar || (0, battle_core_1.fillRate)(b) - (0, battle_core_1.fillRate)(a) || (a.side === 'player' ? -1 : 1));\n        for (const c of ready) {\n            if (phase !== 'fighting')\n                break;\n            if (!c.barWasMax) {\n                c.barWasMax = true; // SEL-1：一次性 bar-max（等待期不重复发）\n                emit({ type: 'bar-max', actorId: c.id });\n            }\n            if (c.side === 'player' && mode === 'manual') {\n                const { state, event } = (0, battle_core_1.stepManualTimeout)(manual, dt);\n                Object.assign(manual, state);\n                if (event === 'trust') {\n                    aiAct(c, true);\n                    commitTurn(c);\n                }\n                else if (event === 'switchAuto') {\n                    mode = 'auto';\n                    clearSelection(); // SEL-7 切自动清输入态（AI 代行当前回合）\n                    emit({ type: 'switch-auto', actorId: c.id });\n                }\n                continue; // 未到托管：条保持满值等操作（输入态）\n            }\n            aiAct(c, false);\n            if (phase !== 'fighting')\n                break;\n            commitTurn(c);\n        }\n        if (t >= battle_core_1.TOTAL_TIME_LIMIT_S)\n            settleTimeout();\n    }\n    // ---- 玩家输入（§3.3 ActionRequest；拒绝可观测 SP-3） ----\n    function submit(req) {\n        if (req.type === 'toggleSpeed') {\n            speedFast = !speedFast;\n            return true;\n        }\n        if (req.type === 'setMode') {\n            mode = req.mode;\n            if (req.mode === 'auto')\n                clearSelection(); // SEL-7\n            return true;\n        }\n        if (req.type === 'flee') {\n            if (phase !== 'fighting')\n                return false;\n            phase = 'fled';\n            clearSelection(); // SEL-7\n            emit({ type: 'flee' });\n            return true;\n        }\n        if (phase !== 'fighting')\n            return false;\n        if (req.type === 'selectSkill') {\n            return activate(req.skillId); // SEL-2 互斥 toggle / SEL-6 置灰拒激活（不消耗预算）\n        }\n        if (req.type === 'cancelSkill') {\n            clearSelection(); // SEL-5\n            return true;\n        }\n        // move / attack：仅输入态受理（O1 二选一）\n        if (!pendingInputNow()) {\n            emit({ type: 'rejected', actorId: player.id, reason: 'bar' });\n            return false;\n        }\n        if (req.type === 'move') {\n            // 【SEL-5②】攻击技态点非射程格 = 取消选中回无选中（无事件）\n            if (selection?.kind === 'attack') {\n                clearSelection();\n                return false;\n            }\n            // 【MV-2 · 优先判定】轻功态：仅受理金格（selection.legalCells，与快照同源）；\n            // 集合外任何格（敌格/非金空格）→ rejected(invalid) 且不取消激活（ATK-4 不涉 move）\n            if (selection?.kind === 'qing') {\n                if (!selection.legalCells.some((p) => (0, hex_1.hexEq)(p, req.to))) {\n                    emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                    return false;\n                }\n                if (player.neili < exports.NEILI_COST_PER_CAST) {\n                    emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                    return false;\n                }\n                player.neili -= exports.NEILI_COST_PER_CAST; // 【Q2】跳跃释放扣内力 1\n                doMove(player, req.to, 'jump');\n                basicIfAdjacent(player);\n                tickCooldowns(player);\n                commitTurn(player);\n                return true;\n            }\n            // 【MV-1 无选中】绿格=移动；可达集外分两类：可动区内空格=无操作（ATK-5，无事件）、\n            // 出区/占格=rejected(invalid)\n            if (!walkCells(player).some((p) => (0, hex_1.hexEq)(p, req.to))) {\n                const off = (0, hex_1.axialToOffset)(req.to);\n                const isFieldEmpty = off.col >= exports.FIELD_COL_MIN && off.col <= exports.FIELD_COL_MAX && off.row >= exports.FIELD_ROW_MIN && off.row <= exports.FIELD_ROW_MAX &&\n                    !occupied().some((o) => (0, hex_1.hexEq)(o, req.to));\n                if (isFieldEmpty)\n                    return false; // ATK-5：无操作（不移动不取消，无事件）\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            doMove(player, req.to, 'walk');\n            basicIfAdjacent(player);\n            tickCooldowns(player);\n            commitTurn(player);\n            return true;\n        }\n        if (req.type === 'attack') {\n            // 【ATK-4 · Q4 session 守卫】轻功态点敌=无操作：false、无事件、选中保持\n            if (selection?.kind === 'qing')\n                return false;\n            const target = byId(req.targetId);\n            if (!target || target.dead || target.side === player.side) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            if (req.skillId === null) {\n                // 【ATK-1 普攻】合法性：cube ≤ basicRange 且（锥形）在 facing 扇区；非法 → rejected(invalid)\n                if (!targetInRange(player, target, (0, battle_core_1.basicRange)(player), (0, hex_1.rangeShapeOf)(player.weapon ?? 'fist'))) {\n                    emit({ type: 'rejected', actorId: player.id, targetId: target.id, reason: 'invalid' });\n                    return false;\n                }\n                tickCooldowns(player);\n                doAttack(player, target, null);\n                commitTurn(player);\n                return true;\n            }\n            // 【ATK-2 技能施放（Q1 定版：无降级）】四查全过才结算，否则 rejected（R-08/R-09 → SEL-6 置灰同源）\n            const s = player.skills.find((x) => x.id === req.skillId);\n            if (!s) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            if ((s.weapon !== null && s.weapon !== player.weapon) || // R-05 武器匹配\n                (player.cooldowns.get(s.id) ?? 0) > 0 || // R-08 冷却\n                player.neili < exports.NEILI_COST_PER_CAST // R-09 内力（Q2 常量口径）\n            ) {\n                emit({ type: 'rejected', actorId: player.id, targetId: target.id, reason: 'invalid' });\n                return false;\n            }\n            if (!targetInRange(player, target, (0, battle_core_1.skillRange)(s), (0, hex_1.rangeShapeOf)(s.weapon ?? player.weapon ?? 'fist'))) {\n                emit({ type: 'rejected', actorId: player.id, targetId: target.id, reason: 'range' });\n                return false;\n            }\n            tickCooldowns(player);\n            doAttack(player, target, s); // resolveAction 按 R-09 视图扣内力（neiliCost 视图=1）、R-08 写冷却\n            commitTurn(player);\n            return true;\n        }\n        // 【P-2=c 格子施放 · 方案 §3.2 单点插入（T20-BE）】cast 对格施放（ATK-2/6/7 v2.0）：\n        // 与 move/attack 同受上方 pendingInputNow 共享门（分支内不重复查门）。attack 分支保留不动\n        // （单位目标兼容入口）；input 层派发归 T20-FE。\n        if (req.type === 'cast') {\n            // 选中态门：无选中 / qing 态 / 陈旧 skillId → rejected(invalid)（cast 不得激活技能，激活只走 selectSkill）\n            if (!selection || selection.kind !== 'attack' || selection.skillId !== req.skillId) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            const s = player.skills.find((x) => x.id === req.skillId);\n            if (!s) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            // 四查之提交时重查（防激活后漂移）：R-05 武器匹配 / R-08 冷却 / R-09 内力（Q2 常量口径，禁 SkillDef.neiliCost）\n            if ((s.weapon !== null && s.weapon !== player.weapon) ||\n                (player.cooldowns.get(s.id) ?? 0) > 0 ||\n                player.neili < exports.NEILI_COST_PER_CAST) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'invalid' });\n                return false;\n            }\n            // 四查之射程：源=selection.legalCells（与快照 attackCells 同一产生点，显示=校验）+\n            // Q2 自己格特判【并联不并入】（高亮不含自己格，rangeCells 冻结不动）；射程外=rejected(range)，\n            // 不清选中（取消是 input 层 SEL-5② 职责，直接 cast=非法，SP-3）\n            const inRange = (0, hex_1.hexEq)(req.to, player.hex) || selection.legalCells.some((p) => (0, hex_1.hexEq)(p, req.to));\n            if (!inRange) {\n                emit({ type: 'rejected', actorId: player.id, reason: 'range' });\n                return false;\n            }\n            // ★【ATK-2 v2.2 · T22】受击目标集合 = 射程形态格（selection.legalCells，显示=校验=结算三同源，\n            //   激活快照禁 cast 时重算——病灶③不复发）内的全体存活敌；all 数组序一次快照（SP-2：\n            //   rng 消费顺序 = 目标序 × 每目标独立掷骰 1~3 次，禁 sort/tie-break）。\n            //   演出位与点击格解耦（ATK-7 v2.2 简化）：命中只看射程成员，不依赖点击格与逻辑位。\n            const legalCells = selection.legalCells; // 激活快照局部引用（选中态门已保证非 null；回调内窄化失效）\n            const targets = all.filter((x) => x.side !== player.side && !x.dead &&\n                legalCells.some((p) => (0, hex_1.hexEq)(p, x.hex)));\n            tickCooldowns(player); // 读后递减，与 attack 分支同位（四查后、结算前）\n            if (targets.length > 0) {\n                // 空集禁入 resolveAoe（faceTargetOf 入口断言 fail-fast）——空放=下方 else 臂（ATK-6 合法施放），两处互指\n                resolveAoe(player, s, targets); // ★ 逐目标独立掷骰全额伤害，资源只在首目标扣一次；\n                // ★ FACE-1：朝向由 resolveAoe 内 faceTarget 定版（朝最近受击敌）——v2.2 T22 落库的\n                // faceToward(player, req.to)「点击格定版」随规格 v2.3 FACE-1 废止，此处不再覆盖\n            }\n            else {\n                // ATK-6 空放=合法施放（v2.2 平移：射程形态内无任何存活敌）：resolveAction 不调\n                // （battle-core 零改动红线），session 逐字段镜像资源三件\n                // （与 AOE 施放资源终态四项全等：neili/冷却/bar/选中——用例 [ATK-6] 逐项锁）\n                player.neili -= exports.NEILI_COST_PER_CAST; // R-09 镜像（skillView 同值，禁用 SkillDef.neiliCost）\n                if (s.cooldownTurns > 0)\n                    player.cooldowns.set(s.id, s.cooldownTurns); // R-08 镜像 core:246 条件式（写初值）\n                setAnim(player, 'charge'); // Q3：施放演出照播（charge→strike 既有 anim 链，无受击 FX）\n                faceToward(player, req.to); // 朝目标格出招（自己格=v{0,0} 早退，朝向保持）\n                emit({ type: 'skill', actorId: player.id, skillId: s.id }); // 空放事件=skill 无 targetId 无 damage\n            }\n            commitTurn(player); // BAR-3 清零 + SEL-3 选中清除\n            return true;\n        }\n        return false;\n    }\n    // ---- 快照（session → render，每帧重建；渲染只读） ----\n    function snapshot() {\n        const pending = pendingInputNow();\n        const turnId = pending ? player.id : lastActedId;\n        // 【体检 A01】moveCells/attackCells 出口防御性复制（:267/:843 原直泄 selection.legalCells\n        // 内部引用——展示端篡改快照曾可反向影响 submit 校验与 cast 结算目标）。\n        // 其余数组字段既有事实=每帧新建对象（actors/heroSkills 均 map 产生、pos={...hex}、\n        // statusIcons=[] 字面量），本就无引用外泄，用例 A01-6 锁现状防回归。\n        const moveCells = pending ? copyCells(legalMoveCells()) : [];\n        const attackCells = pending && selection?.kind === 'attack' ? copyCells(selection.legalCells) : [];\n        const heroSkills = player.dead\n            ? []\n            : player.skills.map((s) => {\n                const isQing = s.kind === 'qingGong';\n                const disabled = isQing\n                    ? Math.floor((0, hex_1.movePower)(player.skills) / 2) < 1 || player.neili < exports.NEILI_COST_PER_CAST // SEL-6/MV-2\n                    : player.neili < exports.NEILI_COST_PER_CAST || (player.cooldowns.get(s.id) ?? 0) > 0 || (s.weapon !== null && s.weapon !== player.weapon);\n                return { id: s.id, label: s.name, disabled };\n            });\n        const actors = all.map((c) => ({\n            id: c.id,\n            side: c.side,\n            name: c.name,\n            pos: { ...c.hex },\n            renderPos: { q: c.renderQ, r: c.renderR },\n            hp: Math.max(0, c.hp),\n            maxHp: c.maxHp,\n            neili: Math.max(0, c.neili),\n            maxNeili: c.maxNeili,\n            actionBar: Math.min(battle_1.BAR.max, c.bar),\n            facing: c.faceLeft ? 'left' : 'right',\n            animState: c.animState,\n            statusIcons: [],\n            isBoss: false,\n            spriteKey: c.side === 'player' ? 'hero' : c.name, // F3：敌方 spriteKey=configId\n            isJump: c.isJump && c.moveT < 1, // 病灶②：doMove 单点真值，仅 lerp 窗口内\n            configId: c.side === 'player' ? undefined : c.name,\n        }));\n        return {\n            phase,\n            turnActorId: turnId,\n            pendingInput: pending,\n            moveCells,\n            moveKind: selection?.kind === 'qing' ? 'jump' : 'walk',\n            attackCells,\n            selectedSkill: selection?.skillId ?? null,\n            heroSkills,\n            actors,\n            cameraTargetId: turnId ?? player.id,\n        };\n    }\n    // 出生朝向：全员转向最近敌（锥形轴开局即有意义）\n    for (const c of alive()) {\n        const foe = pickTarget(c);\n        if (foe)\n            faceToward(c, foe.hex);\n    }\n    return {\n        tick,\n        submit,\n        snapshot,\n        get events() {\n            return events;\n        },\n        get phase() {\n            return phase;\n        },\n        _debug: {\n            units: all,\n            mode: () => mode,\n            clock: () => t,\n            player: () => player,\n            selection: () => selection,\n            // 【体检 A02 测试钩子】faceTargetOf 是闭包内函数、生产调用面不可达空（两守卫滤空）——\n            // 暴露 player 侧单参视图供守卫直呼断言（空数组抛错/非空返回本体），零生产行为面变化。\n            faceTargetOf: (targets) => faceTargetOf(player, targets),\n        },\n    };\n}\n";
  // ---- systems/battle-session ----
  __def("systems/battle-session", new Function("require", "module", "exports", __mod_systems_battle_session));
  var __mod_systems_battle_core = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.MANUAL_TIMEOUT_S = exports.TOTAL_TIME_LIMIT_S = void 0;\nexports.makeRng = makeRng;\nexports.rollEnemyCount = rollEnemyCount;\nexports.fillRate = fillRate;\nexports.rangeTier = rangeTier;\nexports.skillRange = skillRange;\nexports.basicRange = basicRange;\nexports.stepManualTimeout = stepManualTimeout;\nexports.makeInitialPlayer = makeInitialPlayer;\nexports.makeEnemy = makeEnemy;\nexports.resolveAction = resolveAction;\nexports.runBattleHeadless = runBattleHeadless;\n// ---------- 常量（均为公式编号拍板值，禁止改动） ----------\nconst BAR_MAX = 100; // F-05 行动条满值（docs/80 §4：1000 作废）\n// F-05 防死循环：战斗总时长 90s。\n// 2026-09-02 export（T15/Q2 批复）：主架构方案 §3.1「抽取导出」级零行为变更，\n// battle-session 90s 总时长判定消费；值与逻辑零改动，历史 14 用例背书。\nexports.TOTAL_TIME_LIMIT_S = 90;\nexports.MANUAL_TIMEOUT_S = 90; // docs/80 §4：手动 90s 托管 → 再 90s 切自动\nconst DT = 0.05; // 仿真步长（秒）\nconst BASE_HIT = 0.85; // F-04\nconst CRIT_MULT = 1.5; // F-04\nconst DODGE_CAP = 0.25; // F-04\n// R-05 攻击范围档位：武器形态 × 武功等级档（Lv 20/40/60）→ 曼哈顿半径（P2-11）\nconst RANGE_BY_WEAPON = {\n    sword: [1, 2, 3], // 剑：移动小十字 3×3/5×5/7×7\n    fist: [1, 2, 3], // 拳：同剑\n    staff: [1, 2, 3], // 棍：中心大十字 每向 3/5/7\n    club: [1, 2, 3], // 棒：同棍\n    whip: [3, 4, 5], // 鞭：正前方扇形 纵深 3/4/5\n    blade: [3, 4, 5], // 刀：同鞭\n    hidden: [4, 6, 9], // 暗器：射程 4/6/9（Leo 2026-08-20 定）\n};\nfunction makeRng(seed) {\n    let a = seed >>> 0;\n    return () => {\n        a |= 0;\n        a = (a + 0x6d2b79f5) | 0;\n        let t = Math.imul(a ^ (a >>> 15), 1 | a);\n        t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;\n        return ((t ^ (t >>> 14)) >>> 0) / 4294967296;\n    };\n}\n// ---------- R-07 敌方数量（按玩家实战档位，docs/80 §2） ----------\nfunction rollEnemyCount(shizhan, rng) {\n    if (shizhan < 100)\n        return 1;\n    const roll = rng();\n    if (shizhan < 10000) {\n        // 随机 1-3：50/35/15\n        if (roll < 0.5)\n            return 1;\n        if (roll < 0.85)\n            return 2;\n        return 3;\n    }\n    // 随机 1-6：10/25/30/20/10/5\n    if (roll < 0.1)\n        return 1;\n    if (roll < 0.35)\n        return 2;\n    if (roll < 0.65)\n        return 3;\n    if (roll < 0.85)\n        return 4;\n    if (roll < 0.95)\n        return 5;\n    return 6;\n}\n// ---------- F-05 行动条 ----------\nfunction fillRate(c) {\n    // 填充速度(/秒) = (100 + 内功等级×3 + 机敏×1) / 10；峨眉 +10% 后置（门派字段未进 MVP 输入）\n    return (100 + c.neigongLevel * 3 + c.jimin) / 10;\n}\n// ---------- R-05 范围 ----------\nfunction rangeTier(level) {\n    if (level < 20)\n        return 0;\n    if (level < 40)\n        return 1;\n    return 2;\n}\nfunction skillRange(skill) {\n    return RANGE_BY_WEAPON[skill.weapon ?? 'fist'][rangeTier(skill.level)];\n}\n/** 普攻射程：取该武器下等级最高武功的档位（无匹配武功按档 1）。\n * 2026-09-02 export（T15/Q2 批复）：主架构方案 §3.1「抽取导出」级零行为变更——\n * battle-session hex 度量预筛/高亮/AI 位移进射程消费，数值唯一真值仍在本文件；\n * 仅加 export 关键字，函数体零改动，历史 14 用例背书。 */\nfunction basicRange(c) {\n    if (!c.weapon)\n        return RANGE_BY_WEAPON.fist[0];\n    const tier = c.skills\n        .filter((s) => s.weapon === c.weapon)\n        .reduce((m, s) => Math.max(m, rangeTier(s.level)), 0);\n    return RANGE_BY_WEAPON[c.weapon][tier];\n}\nfunction manhattan(a, b) {\n    return Math.abs(a.x - b.x) + Math.abs(a.y - b.y);\n}\nfunction stepManualTimeout(s, dt) {\n    const next = { stage: s.stage, idleSec: s.idleSec + dt };\n    if (next.idleSec >= exports.MANUAL_TIMEOUT_S) {\n        next.idleSec = 0;\n        if (next.stage === 0 || next.stage === 1) {\n            next.stage = 2;\n            return { state: next, event: 'trust' }; // 本回合 AI 代行 + 提示\n        }\n        return { state: next, event: 'switchAuto' }; // 切回自动模式 + 提示\n    }\n    return { state: next, event: null };\n}\n// ---------- 玩家初始（R-11 / docs/80 §1，PlayerConfig 消费方） ----------\nfunction makeInitialPlayer() {\n    return {\n        id: 'player',\n        name: '少侠·无名',\n        side: 'player',\n        hp: 100,\n        maxHp: 100, // 基础气血常量 100（F-07）\n        neili: 0,\n        maxNeili: 0, // 初始未装配内功 → 内力上限 0 → 仅普攻（R-09 天然引导）\n        atk: 119, // 30 + 27×2 + 木剑20 + 野猫10×1.5（F-02）\n        def: 70, // 20 + 27×1.5 + 短褐10（F-02）\n        neigongLevel: 0,\n        jimin: 27,\n        danshi: 15,\n        shizhan: 0,\n        pos: { x: 4, y: 10 }, // P2-5 我方固定站位\n        weapon: 'sword',\n        skills: [\n            {\n                id: 'yemao-jianfa',\n                name: '野猫剑法',\n                kind: 'waiGong',\n                weapon: 'sword',\n                grade: 0.5, // 门派一阶 1.0 × 50%（docs/80 §1.1）\n                growth: 1.5,\n                level: 10, // 默认 10 级，锁级不可提升\n                cooldownTurns: 0, // 普通外功无冷却\n                neiliCost: 10, // 一阶 20 × 50%\n            },\n        ],\n    };\n}\n/** 生成敌方单位（从场景 mob 模板按布点顺序循环填充，R-07；MVP 敌方仅普攻） */\nfunction makeEnemy(index, template) {\n    return {\n        id: `enemy-${index}`,\n        name: template.name,\n        side: 'enemy',\n        hp: template.hp,\n        maxHp: template.hp,\n        neili: 0,\n        maxNeili: 0,\n        atk: template.atk,\n        def: template.def,\n        neigongLevel: 0,\n        jimin: template.jimin,\n        danshi: template.danshi,\n        shizhan: template.shizhan,\n        pos: { x: 1 + (index % 6), y: 1 }, // 敌方 y=1 行（P2-5），x 依次排开\n        weapon: 'fist', // MVP 敌方空手普攻，射程 1\n        skills: [],\n    };\n}\n// ---------- 出手结算（F-01/F-03/F-04） ----------\n// 参数类型放宽到 CombatantInput（resolveAction 导出供演出层同源调用；Runner 结构兼容，零行为变更）\nfunction resolveDamage(actor, target, gradeFactor, rng) {\n    // 判定顺序（F-04）：命中 → 闪避 → 暴击 → 破防保底\n    const hitRate = Math.min(1, BASE_HIT + (actor.shizhan / 1000000) * 0.01);\n    if (rng() >= hitRate)\n        return { damage: 0, crit: false, missed: true };\n    const dodgeRate = Math.min(DODGE_CAP, target.jimin * 0.002);\n    if (rng() < dodgeRate)\n        return { damage: 0, crit: false, missed: true };\n    const crit = rng() < actor.danshi * 0.003;\n    const base = Math.max(actor.atk - target.def, 1); // 破防下限 1（✅）\n    const damage = Math.floor(base * gradeFactor * (crit ? CRIT_MULT : 1));\n    return { damage, crit, missed: false };\n}\n/** 单次出手结算：skill=null 走普攻（射程不足返回 blocked，不消耗不结算） */\nfunction resolveAction(actor, target, skill, rng) {\n    const mkLog = (over) => ({\n        actorId: actor.id,\n        actorSide: actor.side,\n        action: 'basic',\n        targetId: target.id,\n        damage: 0,\n        crit: false,\n        ...over,\n    });\n    if (skill) {\n        actor.neili -= skill.neiliCost;\n        if (skill.cooldownTurns > 0)\n            actor.cooldowns.set(skill.id, skill.cooldownTurns);\n    }\n    else if (manhattan(actor.pos, target.pos) > basicRange(actor)) {\n        // 射程外：本次行动无法出手（MVP 静止站位语义，F-06 移动后置）\n        return {\n            kind: 'blocked',\n            damage: 0,\n            crit: false,\n            logs: [mkLog({ action: 'blocked', note: '目标超出射程' })],\n        };\n    }\n    else if (actor.skills.length > 0) {\n        // 有武功但均不可出（内力/冷却/匹配）→ 普攻兜底（提示性日志，随后紧跟实际结算）\n        const fallbackLogs = [\n            mkLog({ action: 'fallback', note: '武功不可出，普攻兜底' }),\n        ];\n        const r = resolveDamage(actor, target, 1.0, rng); // 此分支 skill=null，普攻无品阶加成\n        target.hp = Math.max(0, target.hp - r.damage);\n        if (r.missed) {\n            return { kind: 'miss', damage: 0, crit: false, logs: [...fallbackLogs, mkLog({ action: 'miss', note: '未命中/被闪避' })] };\n        }\n        return { kind: 'basic', damage: r.damage, crit: r.crit, logs: [...fallbackLogs, mkLog({ action: 'basic', damage: r.damage, crit: r.crit })] };\n    }\n    const r = resolveDamage(actor, target, skill ? skill.grade : 1.0, rng);\n    target.hp = Math.max(0, target.hp - r.damage);\n    if (r.missed) {\n        return { kind: 'miss', damage: 0, crit: false, logs: [mkLog({ action: 'miss', skillId: skill?.id, note: '未命中/被闪避' })] };\n    }\n    return {\n        kind: skill ? 'skill' : 'basic',\n        damage: r.damage,\n        crit: r.crit,\n        logs: [mkLog({ action: skill ? 'skill' : 'basic', skillId: skill?.id, damage: r.damage, crit: r.crit })],\n    };\n}\n// ---------- 主入口：runBattleHeadless ----------\nfunction runBattleHeadless(config) {\n    const rng = makeRng(config.seed ?? 20260821);\n    const logs = [];\n    let round = 0;\n    let t = 0;\n    let mode = config.mode;\n    const manual = { stage: 0, idleSec: 0 };\n    const mk = (c) => ({\n        ...c,\n        bar: 0,\n        cooldowns: new Map(c.skills.map((s) => [s.id, 0])),\n    });\n    const player = mk(config.player);\n    const enemies = config.enemies.map(mk);\n    const all = [player, ...enemies];\n    const aliveEnemies = () => enemies.filter((e) => e.hp > 0);\n    const hpSum = (side) => all.filter((c) => c.side === side).reduce((s, c) => s + Math.max(0, c.hp), 0);\n    const log = (l) => logs.push({ round, t: +t.toFixed(2), ...l });\n    /** 一次行动：选武功（R-05 匹配 + R-08 冷却 + R-09 内力）→ 不行则普攻兜底 → 自动寻敌 */\n    function act(actor, forced = null) {\n        round++;\n        const foes = all.filter((c) => c.side !== actor.side && c.hp > 0);\n        if (foes.length === 0)\n            return;\n        const dist = (f) => manhattan(actor.pos, f.pos);\n        const nearest = () => foes.slice().sort((a, b) => dist(a) - dist(b))[0];\n        // 选武功：数组顺序 = 优先级；需武器匹配、冷却 0、内力足够、射程内有敌\n        let chosen = null;\n        let target = null;\n        for (const s of actor.skills) {\n            if (s.weapon !== null && s.weapon !== actor.weapon)\n                continue; // R-05 不匹配 → 无法出招\n            if ((actor.cooldowns.get(s.id) ?? 0) > 0)\n                continue; // R-08 冷却中不选\n            if (actor.neili < s.neiliCost)\n                continue; // R-09 内力不足不选\n            const inRange = foes.filter((f) => dist(f) <= skillRange(s));\n            if (inRange.length === 0)\n                continue;\n            chosen = s;\n            target = inRange.sort((a, b) => dist(a) - dist(b))[0];\n            break;\n        }\n        // 冷却随自身行动递减（R-08：行动一次计 1 回合）\n        for (const s of actor.skills) {\n            const cd = actor.cooldowns.get(s.id) ?? 0;\n            if (cd > 0)\n                actor.cooldowns.set(s.id, cd - 1);\n        }\n        if (forced === 'trust') {\n            log({\n                actorId: actor.id,\n                actorSide: actor.side,\n                action: 'timeout-trust',\n                damage: 0,\n                crit: false,\n                note: '久未操作，本回合已自动托管',\n            });\n        }\n        // 结算段（A1-T06 v2 方案 A：零变更搬运至导出函数 resolveAction，act 只补 round/t）\n        const finalTarget = target ?? nearest();\n        const outcome = resolveAction(actor, finalTarget, chosen, rng);\n        for (const l of outcome.logs)\n            logs.push({ round, t: +t.toFixed(2), ...l });\n    }\n    // ---------- 主循环（离散步进至 90s 防死循环，F-05） ----------\n    while (t < exports.TOTAL_TIME_LIMIT_S) {\n        for (const c of all)\n            c.bar += fillRate(c) * DT;\n        t += DT;\n        // 行动顺序：行动条高者先，同条速度（fillRate）快者先，再同则玩家先\n        const ready = all\n            .filter((c) => c.hp > 0 && c.bar >= BAR_MAX)\n            .sort((a, b) => b.bar - a.bar || fillRate(b) - fillRate(a) || (a.side === 'player' ? -1 : 1));\n        for (const c of ready) {\n            if (c.hp <= 0 || aliveEnemies().length === 0 || player.hp <= 0)\n                break;\n            if (c.side === 'player' && mode === 'manual') {\n                // 手动：无人操作 → 累计 90s 托管代行，再 90s 切自动（状态机可独立复用）\n                const { state: ms, event } = stepManualTimeout(manual, DT);\n                Object.assign(manual, ms);\n                if (event === 'trust') {\n                    act(c, 'trust');\n                    c.bar -= BAR_MAX;\n                    manual.idleSec = 0;\n                }\n                else if (event === 'switchAuto') {\n                    mode = 'auto';\n                    log({\n                        actorId: c.id,\n                        actorSide: c.side,\n                        action: 'timeout-auto',\n                        damage: 0,\n                        crit: false,\n                        note: '久未操作，已切回自动模式',\n                    });\n                }\n                continue; // 未到托管时间：行动条保持满值等待操作\n            }\n            act(c);\n            c.bar -= BAR_MAX;\n            if (c.side === 'player')\n                manual.idleSec = 0; // 玩家行动重置计时\n        }\n        if (player.hp <= 0 || aliveEnemies().length === 0) {\n            return {\n                winner: player.hp <= 0 ? 'enemy' : 'player',\n                reason: 'annihilate',\n                logs,\n                duration: +t.toFixed(2),\n                finalHp: { player: hpSum('player'), enemy: hpSum('enemy') },\n            };\n        }\n    }\n    // 防死循环：90s 未分胜负 → 存活 hp 总量高者胜，总量相同判玩家负（F-05 ✅）\n    const pHp = hpSum('player');\n    const eHp = hpSum('enemy');\n    return {\n        winner: pHp > eHp ? 'player' : 'enemy',\n        reason: 'timeout-hp',\n        logs,\n        duration: exports.TOTAL_TIME_LIMIT_S,\n        finalHp: { player: pHp, enemy: eHp },\n    };\n}\n";
  // ---- systems/battle-core ----
  __def("systems/battle-core", new Function("require", "module", "exports", __mod_systems_battle_core));
  var __mod_systems_hex = "\"use strict\";\n// T15 六边形数学纯函数层（T-B1 需求表 #1/#2）\n// 依据：《战场布局规格-六边形战棋》v0.9（96 号，odd-r 换算/flat-top/地图 16×16）\n//      + 《战斗规则C案》A3（F-06 移动力/不可穿单位/二阶跳跃）+ O2 裁决（射程三形态几何）\n// 架构：纯函数、零渲染依赖、零 import battle-core（引擎零改动红线）；数值真值仍归\n//      battle-core（skillRange/basicRange）与公式总览（F-06），本文件只做几何换算。\n// 确定性：全部输入→输出纯映射，无随机、无时钟，node 直接可测。\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.MOVE_BASE = exports.HEX_DIRS = void 0;\nexports.offsetToAxial = offsetToAxial;\nexports.axialToOffset = axialToOffset;\nexports.hexNeighbors = hexNeighbors;\nexports.cubeDistance = cubeDistance;\nexports.hexEq = hexEq;\nexports.movePower = movePower;\nexports.reachable = reachable;\nexports.jumpReachable = jumpReachable;\nexports.movePathCells = movePathCells;\nexports.rangeShapeOf = rangeShapeOf;\nexports.rangeCells = rangeCells;\nexports.inCone = inCone;\n// ---------- 坐标换算（存储 = axial(q,r)，地图 = offset odd-r 行×列，96 号 §2.1） ----------\n/** offset(列,行) → axial。odd-r 左移奇数行：q = col - ⌊row/2⌋，r = row（方案 §2.1 定式） */\nfunction offsetToAxial(col, row) {\n    return { q: col - Math.floor(row / 2), r: row };\n}\n/** axial → offset(列,行)（换算唯一逆，往返恒等） */\nfunction axialToOffset(p) {\n    return { col: p.q + Math.floor(p.r / 2), row: p.r };\n}\n// ---------- 邻接与距离 ----------\n/** 六方向（axial 单位向量，索引即方向序；0=东，逆时针）。锥形/射线/朝向共用此序。 */\nexports.HEX_DIRS = [\n    { q: 1, r: 0 },\n    { q: 1, r: -1 },\n    { q: 0, r: -1 },\n    { q: -1, r: 0 },\n    { q: -1, r: 1 },\n    { q: 0, r: 1 },\n];\n/** 六邻接（cube 距离全为 1） */\nfunction hexNeighbors(p) {\n    return exports.HEX_DIRS.map((d) => ({ q: p.q + d.q, r: p.r + d.r }));\n}\n/** cube 距离（R-05 换轨后的战斗度量：axial→cube(x=q, z=r, y=-x-z)，距离=(|dx|+|dy|+|dz|)/2）。\n * session 一律用本函数与 skillRange/basicRange 的数字半径比较——core 内部曼哈顿不再参与\n * 射程判定（见 battle-session.ts 调 resolveAction 处的注释）。 */\nfunction cubeDistance(a, b) {\n    const dx = a.q - b.q;\n    const dz = a.r - b.r;\n    const dy = -dx - dz;\n    return (Math.abs(dx) + Math.abs(dy) + Math.abs(dz)) / 2;\n}\n/** 位置等价（HexPos 为普通对象，集中一处比较） */\nfunction hexEq(a, b) {\n    return a.q === b.q && a.r === b.r;\n}\n// ---------- F-06 移动力（公式总览：基础 2 + 装配轻功加成 + ⌊轻功等级/5⌋） ----------\n/** F-06 基础移动力（公式总览定值；T15/Q7 批复：几何常量本地导出，config/battle-hex.ts 归 FE 卡） */\nexports.MOVE_BASE = 2;\n/** F-06 移动力：基础 2 + 装配轻功加成（一阶 grade 1.0 → +1 / 二阶 1.3 → +2）+ ⌊最高轻功等级/5⌋。\n * 轻功数据来源 = 单位 skills 里 kind='qingGong' 的武功（T06 moveRange 已验证的通路，\n * CombatantInput 无需新增字段）；无轻功 → 基础 2。 */\nfunction movePower(skills) {\n    let bonus = 0;\n    let qinggongLevel = 0;\n    for (const s of skills) {\n        if (s.kind !== 'qingGong')\n            continue;\n        qinggongLevel = Math.max(qinggongLevel, s.level);\n        bonus = Math.max(bonus, s.grade === 1.0 ? 1 : s.grade === 1.3 ? 2 : 0);\n    }\n    return exports.MOVE_BASE + bonus + Math.floor(qinggongLevel / 5);\n}\n/** 普通移动可达格：从 from 起 BFS 步数 ≤ power 的格子（F-06 移动力即步数预算）。\n * 阻挡规则（C 案 A3，Leo 08-31 定）：普通移动不可穿过任何单位（友/敌）——occupied 格\n * 既不能落脚也不能借道；inBounds 给定时界外格同样不可进入/借道（棋盘外不存在绕行）。\n * 返回不含起点。 */\nfunction reachable(from, power, occupied, inBounds) {\n    const blocked = (p) => occupied.some((o) => hexEq(o, p)) || (inBounds !== undefined && !inBounds(p));\n    const seen = new Set([`${from.q},${from.r}`]);\n    let frontier = [from];\n    const out = [];\n    for (let step = 0; step < power; step++) {\n        const next = [];\n        for (const cur of frontier) {\n            for (const n of hexNeighbors(cur)) {\n                const key = `${n.q},${n.r}`;\n                if (seen.has(key) || blocked(n))\n                    continue;\n                seen.add(key);\n                next.push(n);\n                out.push(n);\n            }\n        }\n        frontier = next;\n    }\n    return out;\n}\n/** 二阶轻功跳跃可达格（F-06，Leo 08-31 定）：跳跃范围 = ⌊移动范围/2⌋，跳跃可穿越单位。\n * 语义：无视阻挡（不需连通路径，穿越单位与界外借道均允许），落点仍须在界内且不与其他\n * 单位重叠、不含起点；⌊power/2⌋=0（power≤1）时无跳跃格。 */\nfunction jumpReachable(from, power, occupied, inBounds) {\n    const range = Math.floor(power / 2);\n    if (range <= 0)\n        return [];\n    const out = [];\n    // 范围极小（≤ 数格），直接半径扫描即可，无需 BFS\n    for (let dq = -range; dq <= range; dq++) {\n        for (let dr = Math.max(-range, -dq - range); dr <= Math.min(range, -dq + range); dr++) {\n            const p = { q: from.q + dq, r: from.r + dr };\n            if ((dq === 0 && dr === 0) || occupied.some((o) => hexEq(o, p)))\n                continue;\n            if (inBounds && !inBounds(p))\n                continue;\n            out.push(p);\n        }\n    }\n    return out;\n}\n/** 【T19/N1 防御 · 方案 §3.3】普通移动回退轨的合法路径（6 邻 BFS 最短路）。\n * 阻挡语义同 reachable()：occupied/inBounds 格不可进入与借道；差异：\n * - 起点格不作阻挡（session 的 occupied() 天然含移动者旧格）；\n * - 返回含首尾的完整格序列 [from, …, to]（供 tick 分段等时插值）；\n * - BFS 失败（终点被占/界内无路）防御回退 [from, to]（= 现直线几何，不劣化）。\n * 确定性：HEX_DIRS 方向序固定 + 队列序 → 同输入恒同输出（纯函数、无随机无时钟）。\n * 注：jump 不走本函数（凌空可穿恒直线，doMove 处分流）；终点被占时先于 BFS 直接回退\n *（occupied 不可落脚 ⇒ BFS 必失败，提前返回同时避免无界网格上的无效展开）。 */\nfunction movePathCells(from, to, occupied, inBounds) {\n    if (hexEq(from, to))\n        return [from];\n    const blocked = (p) => (!hexEq(p, from) && occupied.some((o) => hexEq(o, p))) || (inBounds !== undefined && !inBounds(p));\n    if (blocked(to))\n        return [from, to];\n    const keyOf = (p) => `${p.q},${p.r}`;\n    const prev = new Map(); // 邻格 key → 前驱 key（回溯用）\n    const seen = new Set([keyOf(from)]);\n    let frontier = [from];\n    while (frontier.length > 0) {\n        const next = [];\n        for (const cur of frontier) {\n            for (const d of exports.HEX_DIRS) {\n                const n = { q: cur.q + d.q, r: cur.r + d.r };\n                const k = keyOf(n);\n                if (seen.has(k) || blocked(n))\n                    continue;\n                seen.add(k);\n                prev.set(k, keyOf(cur));\n                if (hexEq(n, to)) {\n                    const path = [n]; // 回溯 to → from 再反转\n                    let cur2 = k;\n                    while (cur2 !== keyOf(from)) {\n                        cur2 = prev.get(cur2);\n                        const [q, r] = cur2.split(',').map(Number);\n                        path.push({ q, r });\n                    }\n                    return path.reverse();\n                }\n                next.push(n);\n            }\n        }\n        frontier = next;\n    }\n    return [from, to];\n}\n/** 武器形态 → 射程几何（O2 定版/96 号 R-05 换轨：剑拳暗器圆形，棍棒直线，鞭刀锥形） */\nfunction rangeShapeOf(weapon) {\n    if (weapon === 'staff' || weapon === 'club')\n        return 'ray';\n    if (weapon === 'whip' || weapon === 'blade')\n        return 'cone';\n    return 'circle'; // sword / fist / hidden\n}\n/** 方向向量 → 方向索引（六方向最近匹配；输入应是 HEX_DIRS 之一或其负）。\n * 用 cube 点积取最大：v 在某方向的扇区内即匹配（浮点零参与，整数运算）。 */\nfunction dirIndexOf(v) {\n    const vx = v.q;\n    const vz = v.r;\n    const vy = -vx - vz;\n    let best = 0;\n    let bestDot = -Infinity;\n    for (let k = 0; k < exports.HEX_DIRS.length; k++) {\n        const dx = exports.HEX_DIRS[k].q;\n        const dz = exports.HEX_DIRS[k].r;\n        const dy = -dx - dz;\n        const dot = vx * dx + vy * dy + vz * dz;\n        if (dot > bestDot) {\n            bestDot = dot;\n            best = k;\n        }\n    }\n    return best;\n}\n/** 方向环距（0~3；六方向首尾相接） */\nfunction dirRingDist(a, b) {\n    const d = Math.abs(a - b) % 6;\n    return Math.min(d, 6 - d);\n}\n/** 射程格集合（目标可选格，不含原点；R-05 半径 N 直接来自 core 的 skillRange/basicRange）：\n * - circle（剑/拳/暗器）：cube 距离 ≤ N 的全部格\n * - ray（棍棒，O2）：出发格起六方向射线各 N 格\n * - cone（鞭/刀，O2）：以 axis 方向为轴的 120° 扇区（轴 ±1 个方向步）内 cube 距离 ≤ N；\n *   axis 缺省视为东向（调用方必须传战斗语义轴——session 用单位六向 facing）。\n * 视线遮挡不做（Q5 批复）：射程形态只定义\"哪些格可选目标\"，射线/扇区不被中间单位截断；\n * 阻挡仅作用于移动（reachable 的 BFS），两者语义分离。 */\nfunction rangeCells(origin, shape, n, axis, inBounds) {\n    const ok = (p) => !inBounds || inBounds(p);\n    const out = [];\n    if (shape === 'circle') {\n        for (let dq = -n; dq <= n; dq++) {\n            for (let dr = Math.max(-n, -dq - n); dr <= Math.min(n, -dq + n); dr++) {\n                if (dq === 0 && dr === 0)\n                    continue;\n                const p = { q: origin.q + dq, r: origin.r + dr };\n                if (ok(p))\n                    out.push(p);\n            }\n        }\n        return out;\n    }\n    if (shape === 'ray') {\n        for (const d of exports.HEX_DIRS) {\n            for (let i = 1; i <= n; i++) {\n                const p = { q: origin.q + d.q * i, r: origin.r + d.r * i };\n                if (ok(p))\n                    out.push(p);\n            }\n        }\n        return out;\n    }\n    // cone：轴 ±1 方向步（120° 扇区）\n    const axisIdx = axis ? dirIndexOf(axis) : 0;\n    for (let dq = -n; dq <= n; dq++) {\n        for (let dr = Math.max(-n, -dq - n); dr <= Math.min(n, -dq + n); dr++) {\n            if (dq === 0 && dr === 0)\n                continue;\n            const p = { q: origin.q + dq, r: origin.r + dr };\n            if (!ok(p))\n                continue;\n            if (dirRingDist(dirIndexOf({ q: dq, r: dr }), axisIdx) <= 1)\n                out.push(p);\n        }\n    }\n    return out;\n}\n/** 目标格是否在锥形射程内（出招校验用，与 rangeCells('cone') 同几何、免建全格集合）。\n * 轴 = 出招者指向目标的方位不成立（那样恒真）——语义是\"以出招者六向 facing 为轴\"（Q4 批复）。 */\nfunction inCone(origin, axis, target, n) {\n    const dist = cubeDistance(origin, target);\n    if (dist === 0 || dist > n)\n        return false;\n    const vx = target.q - origin.q;\n    const vr = target.r - origin.r;\n    return dirRingDist(dirIndexOf({ q: vx, r: vr }), dirIndexOf(axis)) <= 1;\n}\n";
  // ---- systems/hex ----
  __def("systems/hex", new Function("require", "module", "exports", __mod_systems_hex));
  __req("proto/battle_demo/main", "./main"); // 入口
})();