# 放置武侠小游戏 · MVP 开发规格

> 状态：**草稿（关键决策已拍板：战斗规模 3v3 / 结算 A 方案，2026-08-19）**
> 日期：2026-08-14
> 定位：Codex 开工的合同，包含技术选型、施工守则、Demo 范围

---

## 0. 第一指导原则（Leo 定，2026-08-14）

> **制作不是瓶颈，效果满意 + 可长期维护 + 稳定不出问题，才是核心。**

1. **不评估时间**：制作快（AI 能快速产出），时间不是讨论重点
2. **只拆解工作**：三思的职责是把工作拆好，不预测工期
3. **落地靠 Codex**：真正开发等开始用 Codex 再说，纸上不空谈
4. **验收三关**：效果（Leo 试玩）+ 可维护（架构清晰）+ 稳定（不出问题）
5. **可维护为底线**：AI 产出的东西如果难维护，就重做或拆解，不硬撑

---

## 0a. 施工守则（Codex 必须遵守）

1. **增量开发**：一次只做指定功能，不顺手改其他代码
2. **数值集中**：结算公式只在云函数 `settle/core.js`（唯一真值）；客户端 `config/numbers.ts` 只放只读展示参数（ADR-004），其他文件禁止硬编码数值
3. **UI 只展示**：UI 层只读数据、画出来，不算数值
4. **接口先定**：云函数接口、数据结构在开工前定死，按契约写
5. **类型约束**：所有数据结构定义在 `types.ts`，模块间通过类型咬合
6. **不动历史**：验收过的代码不再整体重写，要改就小步改

---

## 1. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | TypeScript | Leo 熟悉，类型系统帮 Codex 少犯错 |
| 渲染 | 原生 Canvas 2D | 6v6 战棋无需引擎，Codex 最稳 |
| 后端 | 微信云开发（云函数+云数据库） | 数值结算防作弊，免自建服务器 |
| 构建 | 微信开发者工具 | 零配置 |
| 美术目标 | L2.5（精致 2D + 大招特效） | 老金庸光影级别，Canvas 轻松做到 |

---

## 2. 架构原则

- **客户端只展示，后端只结算**：数值计算唯一真值在云函数
- **公式只在后端**：前端只放展示字段
- **模块职责单一**：打开文件 10 秒能看懂

---

## 3. 项目结构

```
project/
├── game.ts            # 入口：主循环 + 场景切换
├── config/
│   └── numbers.ts     # ★ 所有数值（公式集中地）
├── systems/
│   ├── battle.ts      # 战斗结算（对接云函数）
│   ├── growth.ts      # 成长/学武功逻辑
│   └── map.ts         # 刷怪点
├── ui/
│   └── render.ts      # 画界面（只读数据）
├── net/
│   └── cloud.ts       # 云函数调用封装
├── types.ts           # 所有数据结构定义
└── cloudfunctions/
    ├── settle         # 数值结算：挂机收益/战斗结果/学点增减
    └── save           # 存档：读档/存档
```

---

## 4. 开发顺序（按依赖，逻辑先行）

| 步骤 | 功能 | 说明 |
|---|---|---|
| 1 | 最小工程跑通 | 空项目在微信里显示画面 |
| 2 | 后端 settle/save | 文字输出验证数值结算 |
| 3 | 挂机循环 | 学点跳动（极简 UI） |
| 4 | 学武功 | 属性/学点条件校验 |
| 5 | 战斗 | 1v 多战棋最小版（玩家 1 人，敌方按实战档位 1~6；**不做好友助战**，R-03 后置；6v6 为侠客系统上线后上限） |
| 6 | 刷怪点 | 解锁链 + 掉落 |
| 7 | 闭环验收 | 挂机→结算→存档→学武功→变强 |

**铁律：第 1 步没跑通，不做第 2 步；每步验收通过才进下一步。**

---

## 5. Demo 验收标准

- [ ] 挂机 5 分钟 → 数值可见增长
- [ ] 学到新武功 → 战力提升
- [ ] 能打到更高级怪
- [ ] 云函数结算正确、存档可读回
- [ ] 美术风格试验：10 张素材风格统一

---

## 6. 接口契约（草案，开工前定死）

### 数据结构（types.ts）
```
Player {
  id: string
  level: number
  stats: { 臂力/胆识/根骨/机敏/定力/悟性 }
  learnedSkills: Skill[]
  currentMap: string
  lastSettleAt: timestamp
}
```

### 云函数
```
POST /settle  { playerId, action } → 结算结果 + 新状态
POST /save    { playerId, snapshot } → 保存确认
GET  /load    { playerId } → 存档数据
```

---

## 7. 待定问题

- [ ] **独有战斗机制（核心卖点）**——最高优先级，状态：🟡 思考中，不阻塞 MVP
  - 思考框架：武侠独有 × 玩家深层幻想 × 技术可落地
  - 候选方向：内力/真气、招式相克、门派阵法、地形克制
  - 决策时点：v1.0 内容填充阶段前必须定下
- [ ] 具体数值初稿（AI 出，Leo 调）
- [ ] 云函数接口字段最终确认
- [ ] 生图 API 选型（即梦/可灵/其他）
- [ ] 风格规范模板

---

## 7b. 开发工具决策（已确认 ✅）

**主力工具：Codex（ChatGPT Plus $20/月）**
- 用 GPT-5.6 家族（Sol/Terra/Luna 自动路由），开发任务主要走 Sol/Terra
- 5 小时滚动窗口 + 每周上限（约 3000 次/周）
- **额度评估：按开发计划真实消耗 120-600 次/周，占周上限 4-20%，绰绰有余**

**并行策略（分阶段）：**
- Demo 阶段：前端/后端两个窗口，错峰使用（先串行搭地基，再考虑并行）
- v1.0 阶段：按模块加实例（接口冻结 + 量大才拆）
- 并行上限 2-3 个实例（再多收益递减）

**工具可替换性（关键认知 ✅）：**
- 换模型 = 改 config（model + base_url），无痛
- 协议转换可能需网关/代理（CC-Switch 类成熟方案）
- **我们的架构（模块化 + 文档化 + 接口定死）是工具无关的——代码/文档不绑定工具，换执行者不影响资产**
- 先用 Codex 跑 Demo，真需要再换（GLM-5.3/ZCode 等为备选）

---

## 8. 变更记录

| 日期 | 版本 | 变更 | 签字 |
|---|---|---|---|
| 2026-08-14 | v1 草稿 | 创建 | 待审 |
| 2026-08-20 | v1.1 | **Leo 拍板回填（来源 docs/80）**：战斗规模 **3v3 → 1v 多**（不做好友助战 R-03，敌方按实战档位 R-07）；§4 步骤 5 同步 | Leo ✅ |
