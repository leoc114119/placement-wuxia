# 放置武侠 · AGENTS.md(项目开发约定)

> 本文件是**所有 Agent（ZCode/Codex/CodeBuddy）开工时自动读取**的项目指令。
> 内容与 02-MVP开发规格-v1.md 保持一致;项目文档更新时,本文件同步更新。

## 〇、工具无关总览（所有 Agent 必读 · 2026-08-23 增 · 2026-08-31 更新）

- **项目**：放置武侠小游戏（微信小游戏），挂机 → 学武功 → 战斗 → 变强
- **技术栈**：TypeScript + 原生 Canvas 2D + 微信云开发（已定，不换）
- **四方分工 + 三个自动化**（2026-08-31 更新；机制详见 `docs/81` §7）：
  - **CodeBuddy** → 玩法沟通对齐、文档撰写维护、工作流程梳理、任务卡起草、C 环审查（不写游戏代码）
  - **ZCode** → 代码实现 + 单元测试 + 修 bug（走 81 任务卡制，即下方施工守则）
  - **Codex** → 非开发管线（调研/工具/浏览器桌面操作）、设计辅助、**文档二审**（🚫 不碰游戏代码、不改已验收设计文档）
  - **Leo** → 拍板、发车、L 环真机验收
  - 自动化（规划中，落地前不占编制）：**夜间看门人**（扫箱/跑测/文档一致性/晨报）、**数值平衡模拟器**（battle-core Monte Carlo）、**资产质检员**（素材入库自动检查）
  - **明确不设角色**：美术生成（走脚本管线）、世界观文案（CodeBuddy/Codex 顺手）、独立项目经理（CodeBuddy 兼）、发布运维（上线前再议）——禁止再造对话型"角色 agent"
- **常青文档铁律（2026-08-31 定）**：项目"现在状态"只写四份协作常青文档（本文件 / `docs/PROJECT-MEMORY.md` / `docs/81` / `docs/82`），持续更新；**设计内容只写 `docs/design/` 六分类夹**（一个细分玩法/系统 = 一个 md，加内容改文件不开新号）；**编号文档（84/86/95…）只做决策纪要留档**。**查文档一律先看 `docs/文档总索引.md`**（另有两份：`docs/美术资产索引.md`、`docs/代码结构索引.md`）
- **项目级记忆**：**`docs/PROJECT-MEMORY.md` = 单一真源**——项目级决策/约定只写那里；开工前先读它 + 本文件
- **当前设计状态**：R03 主玩法已拍板 = B 江湖衣钵·世代传承；🟢 **战斗机制已拍板（08-31）= C 轻战棋回合制 + 托管自动化**（平时挂机自动跑、关键战手动）；布局层输入 = `docs/96-战场布局规格`（六边形战棋 v0.9，含射程沿用 R-05 换轨方案）；⏳ 当前阶段 = 补全游戏侧设计文档（P0：91 定位机制总纲/代际传承/战斗 C 详案/回合分配），暂不发 ZCode 任务卡
- **文档索引**：**总入口 = `docs/文档总索引.md`**（含设计六分类、美术/代码索引、旧文档迁移映射）；战斗布局层 = `docs/design/03-战斗系统/战场布局规格-六边形战棋.md`

---

## 项目简介

放置武侠小游戏(微信小游戏):挂机 → 学武功 → 战斗 → 变强 的放置循环,自走棋战棋战斗(MVP **1v 多** = 玩家 1 人,敌方按实战档位 1~6),水墨国风。

## 技术栈(已定,不换)

- 语言:TypeScript
- 渲染:原生 Canvas 2D(1v多自走棋无需引擎)
- 后端:微信云开发(云函数 + 云数据库)
- 构建:微信开发者工具
- 美术目标:L2.5 精致 2D + 大招特效

## 任务工单机制(必读)

**每个任务 = 一张任务卡(`docs/81-研发协作机制-ZCode任务包-v0.2.md`,表格版)**。开工前先读任务卡,按卡施工:

1. **需求表 = 唯一真相**:只做任务卡 §1.2 需求表里的事,表外内容一律不碰
2. **架构/接口由你(ZCode)决策**:怎么做(模块划分/接口签名/实现方式)自行选择,但**提交时写明架构决策与理由**
3. **边界**:
   - ✅ 需求表定死(照做):规则、公式编号(F-xx/R-xx)、数值、坐标、交互流程
   - 🚫 禁止:改需求表、加未列功能、动历史验收代码、硬编码数值、引未批准依赖
4. **歧义必问(铁律)**:需求含糊/冲突 → **立即停下向 CodeBuddy 提问,禁止自行假设**;架构问题可自决但需记录理由
5. **交付(DoD)**:任务卡 §1.6 全过(编译/类型/lint/用例/文件清单+决策说明/未碰禁止区)才算完成

## 任务箱协议(tasks/,2026-08-21 起 · 必读)

**你与 CodeBuddy 之间的任务传递与沟通全部走 `tasks/` 目录**(协议详见 `tasks/README.md`):

1. **会话启动第一件事**:`cat tasks/LOG.md` 看最新事件 → 扫 `tasks/inbox/` 有无新任务卡;有 → 领单(移入 `working/`,在 `threads/Txx.md` 记 🤝 领单 + LOG 登记)
2. **提问**:`tasks/questions/Qn-Txx.md` 写清上下文,停下等 `answers/`;**禁止在疑问未答复前继续施工**
3. **沟通双写铁律**:任何任务相关沟通,写 `threads/Txx.md` 表格行 + `LOG.md` 一行(时间/方向/事件/详情)
4. **交付**:任务完成 → 任务卡移 `done/` + 写 `done/Txx-done.md` 回执(DoD 逐项勾选 + 架构决策说明 + 文件清单)
5. **Leo 全程可见**:所有沟通都是文件,Leo(老板)随时可读;`dashboard.html` 是他的看板,你不碰它
6. **箱外不生效**:会话里的口头承诺要落到 tasks/ 文件才算数

## 施工守则(必须遵守)

1. **增量开发**:一次只做指定功能,不顺手改其他代码
2. **数值集中**：结算公式只在云函数 `settle/core.js`（唯一真值，ADR-002）；客户端 `config/numbers.ts` 只放**只读展示参数**（倍速/文案阈值等，不含结算公式，ADR-004）；其他文件禁止硬编码数值
3. **UI 只展示**:UI 层只读数据、画出来,不算数值
4. **接口先定**:云函数接口、数据结构在开工前定死,按契约写
5. **类型约束**:所有数据结构定义在 `types.ts`,模块间通过类型咬合
6. **不动历史**:验收过的代码不再整体重写,要改就小步改

## 架构原则

- 客户端只展示,后端只结算:数值计算唯一真值在云函数
- 公式只在后端,前端只放展示字段
- 模块职责单一:打开文件 10 秒能看懂

## 目录结构(规划)

```
project/
├── game.ts            # 入口:主循环 + 场景切换
├── config/
│   └── numbers.ts     # ★ 只读展示参数（结算公式只在云端 core.js，ADR-004）
├── systems/
│   ├── battle.ts      # 战斗结算(对接云函数)
│   ├── growth.ts      # 成长/学武功逻辑
│   └── map.ts         # 刷怪点
├── ui/
│   └── render.ts      # 画界面(只读数据)
├── net/
│   └── cloud.ts       # 云函数调用封装
├── types.ts           # 所有数据结构定义
└── cloudfunctions/
    ├── settle         # 数值结算
    └── save           # 存档
```

## UI 素材约定

- 素材目录:`assets/ui/`(buttons / panels / icons),素材说明见 `assets/ui/README.md`
- 风格:活泼年轻的水墨国风,严格遵循 `04-UI风格规范-v0.1.md`(风格圣经,含生图 prompt 前置模板)
- UI 代码引用规范色值(墨 #2B2B2B / 宣纸 #F8F4EA / 朱砂 #E2574C / 竹青 #7FB069 / 淡金 #D4AF37),禁止引入规范外鲜艳色

## 文档索引(开工前阅读相关设计文档)

- `01-项目资源与分工-v1.md`
- `02-MVP开发规格-v1.md`(本文件依据)
- `03-战斗系统设计-v0.1.md`
- `04-UI风格规范-v0.1.md`
- `05-世界观设定-v0.2.md`
- `06-ZCode流程验证-v0.1.md`
- `docs/79-场景与战斗系统评审报告-v0.1.md`(评审结论)
- `docs/80-开工决策配置-v0.1.md`(Leo 拍板决策,含玩家初始/敌方档位/行动条)
- `docs/81-研发协作机制-ZCode任务包-v0.2.md`(任务卡机制,本文件依据)
- `docs/82-任务管理总表.md`(每期任务流转跟踪,当前第 1 期)
- `tasks/README.md`(任务箱协议——你与 CodeBuddy 的沟通渠道,必读)

## 开发节奏

- 文档驱动:先读相关设计文档再动手,不凭空实现
- 按 MVP 开发顺序逐步推进:最小工程跑通 → 后端 → 挂机 → 学武功 → 战斗 → 刷怪 → 闭环验收
- **铁律:上一步未验收,不进下一步**

## 验收三关

1. **效果**:Leo 试玩满意
2. **可维护**:架构清晰
3. **稳定**:不出问题

可维护为底线:AI 产出若难维护,重做或拆解,不硬撑。

## 安全约定

- 高风险动作(删除文件 / 修改配置 / 联网操作)先征求确认
- 不修改与当前任务无关的代码和历史验收过的模块
