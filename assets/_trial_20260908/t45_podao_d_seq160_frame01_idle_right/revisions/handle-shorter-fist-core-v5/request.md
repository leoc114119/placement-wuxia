# T45 seq160 · frame01 idle_right D 校准刀柄/遮挡修正版 v5

保留 v3/v4 的刀刃长度 95px 与握持点：甲 `(125,175)`、乙 `(116,165)`。刀柄收为 `[-20,14]`（34px）；护手仍在轴向 7px。A/B 遮挡 ROI 收紧为拳心/指节核心，排除前臂与拳后区域，D OCCLUSION 只覆盖拳与柄重叠段，柄前后露段清楚可见。

只处理 `甲/乙 battle_idle_right`，credits=0，未生成下一帧，未修改正式 runtime。
