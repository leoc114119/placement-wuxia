# T45 seq160 · frame01 idle_right D 校准刀柄/遮挡修正版 v4

保留 v3 的刀刃长度 95px 与握持点：甲 `(125,175)`、乙 `(116,165)`。刀柄改为紧凑段 `[-22,14]`（36px），并将护手置于轴向 7px；B 的原拳 ROI 收紧为仅拳头范围，排除前臂，确保 D OCCLUSION 只遮住拳与柄重叠段，后侧柄段可见。

只处理 `甲/乙 battle_idle_right`，credits=0，未生成下一帧，未修改正式 runtime。
