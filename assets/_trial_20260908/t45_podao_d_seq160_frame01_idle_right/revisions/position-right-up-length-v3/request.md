# T45 seq160 · frame01 idle_right D 校准位置/长度修正版 v3

沿用人物右手（本组右向/我方反面姿势投影为画面左侧拳）。相对 v2，甲乙握持点在同一拳 ROI 内右移 6px、上移 6px；刀轴仍为 `angleDeg=-55°`。刀刃轴向范围由 `[15,92]`（77px）加长至 `[15,110]`（95px），柄后段由 `[-16,20]` 延长为 `[-24,20]` 以保留 D 遮挡后的可见握柄。

只处理 `甲/乙 battle_idle_right`，credits=0，未生成下一帧，未修改正式 runtime。
