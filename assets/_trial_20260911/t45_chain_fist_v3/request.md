# T45 seq=354 · 拳方向锁死 v3

- 链：`idle_right → atk_right_1 → atk_right_2 → atk_right_3`。
- `atk_right_1` 复用 v2 已确认的正确右拳终点锚；本单只生成 `atk_right_2`、`atk_right_3` 两张新图。
- 两张新图均为连续右直拳：画面右拳向画面右水平伸出，同方向、同肩高、手臂完全伸直；不把换手当交替。
- 绝对目标（原始 1086×1448）：拳心距躯干中线 `+398±40px`，拳心 `y=751±40px`，不得低于 `y=820`，距中线至少 `350px`。
- 每步参照：Image 1=上一链产物；Image 2=PM2 提供的右拳姿势图；Image 3=`atk_right_1` 成品终点锚。
- 透明、背景、比例不判；runtime 与正式资产不改；最多 3 张新图，本次目标 2 张。
