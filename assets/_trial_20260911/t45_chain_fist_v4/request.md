# T45 seq=356 · 拳方向恒定 + 换手交替 v4

- 链：`idle_right → atk_right_1 → atk_right_2 → atk_right_3`，每步输入为上一帧产物。
- 方向恒定：角色朝画面右，出拳永远朝画面右；高度为肩高，手臂伸直。
- 变量只有出拳拳：拳 α → 拳 β → 拳 α；禁止把方向当变量，禁止把后手抬到画面左后方。
- 起点位置指称：拳 α=(183,193)，拳 β=(101,205)，均为 240×320 起点坐标；prompt 禁用 left/right hand 命名。
- Image 1=上一链产物；Image 2=v2 `atk_right_1` 成品，只提供右向前伸拳的形状、肩高和终点位置。
- C1：每帧恰好一只拳在前伸区 x≥190、y=150~180，另一只在腰侧区 x=85~135、y=185~215。
- C2：相邻帧上身躯干带像素差≥6%；C3=人工确认 α→β→α 换手；C4=每帧恰好两拳；C5=链式 SHA。
- 透明、背景、比例不判；runtime 与正式资产不改；本批最多 3 张原生图。
