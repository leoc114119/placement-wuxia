图1 = **要处理的唯一目标**：我们的 3D 角色渲染图。**图1 的一切几何是绝对真源。**
图2 = **画风参照**：中国画水墨淡彩人物。**只借它的"画法"**（墨线怎么勾、淡墨怎么染），**不要照抄它的内容、构图、人物、比例**。

【本次唯一改动】把图1 的明暗改成中国水墨淡彩的画法：
① **墨线勾结构**：用墨色的线勾勒转折 —— 发丝走向、衣纹、袖口与衣摆的边、腰带绑结、裤褶、靴子结构、脸部与五官轮廓。线条要有毛笔感（起笔重收笔轻、有粗细变化）。
② **淡墨染暗部**：在墨线内侧用**很淡的墨色**渲染暗部（暗部偏青灰，不是加黑），过渡柔和。
③ **原有色彩保留**：青绿上衣 / 白裤 / 棕靴 / 肤色 色相不变，只在上面薄薄罩一层淡墨。
④ **★明暗对比度保持图1 的量级**，不要把整体画淡、画灰（这一点很重要：图2 是宣纸白底的大留白，我们**不是**要那个亮度）。

【★★最重要的一条：形状一个都不许动】
- **脸的宽度绝对不许变**（不许变宽也不许变窄）、五官的位置与大小不许动
- 头、头发、马尾的**轮廓与体积**不许变（不许画大、不许画长、不许往外鼓）
- 身体各部位的宽度、长度、头身比、站姿、手臂位置 —— **全部与图1 完全一致**
- 衣服的轮廓与已有的白色描边、纹样 —— 原样保留，不增不减
- **判断标准**：把输出和输入叠在一起，人物轮廓应该完全重合。如果你画的轮廓比输入宽了或窄了，就是做错了。

【其它】
- 背景保持图1 的深色背景（不要改成白底）
- 输出与图1 同尺寸同构图（1:1）
- 不要加背景元素、泼墨点、文字、印章、特效光晕

只输出这一张：**形状完全不动、只把明暗换成水墨淡彩笔法**的同一张图。
