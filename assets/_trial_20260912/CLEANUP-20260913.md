# 过程素材清理（2026-09-13 · Leo 令）

## 删了什么
**3.08 GB `.raw` 中间产物（105 个）** —— 全部可由已提交脚本重出，且多数已被取代：

| 目录 | 大小 | 为什么可删 |
|---|---|---|
| `国风明暗试产/*.raw` + `view/*` | 963 MB | 单视角往返实验（已被「每方向一张 ratio 贴图」取代） |
| `明暗贴图试产/tex/` | 835 MB | 旧回填路径产物（`backfill_from_sheet.py`，已取代） |
| `明暗贴图试产/tex6/` | 805 MB | 旧六向贴图（被 `tex6r` 取代） |
| `明暗贴图试产/_hairmask/` | 97 MB | `desaturate_hair.py` 的工作目录（脚本会自建） |
| `model_v2/tex/`、`model_v3/tex/` | 151 MB | 旧模型贴图（被 v4 取代） |
| `明暗贴图试产/tex6b/` | 67 MB | 中途修正尝试产物（被 `tex6r` 取代） |
| `贴图回填验证/*.raw` | 67 MB | 验证用中间产物（**同名 PNG 保留**，文档仍引用） |
| `明暗贴图试产/views/` | 29 MB | 旧视角集（被 `views6` 取代） |
| `views6/*_comp*.raw` | 64 MB | 回填输入合成中间件（可由渲染重出） |

另删 ~34 MB 已被取代的试验预览图（`国风明暗试产` 的 `AI输入_*` / `配准后_*` / `成品_D2a1*` / `D2p` / `D2w`）。
**决策证据保留**：`对照_水墨cel.png`、`成品_{A,YI,C1,D1,D2}.png`、`输入/`、prompt、报告。

## 保留了什么（当前生产链）
| 路径 | 大小 | 作用 |
|---|---|---|
| `贴图回填_美术线输入/配准后/basecolor_detail_20views.raw` | 67 MB | **源头 albedo**（美术线交付，不可重出） |
| `贴图回填_美术线输入/配准后/basecolor_detail.raw` | 67 MB | 上者前身（留档） |
| `明暗贴图试产/tex6r/` | 403 MB | **当前定档的六向明暗贴图**（出帧直接用它） |
| `明暗贴图试产/views6/`（核心 sidecar） | 265 MB | 重出 `tex6r` 的输入（可由渲染重出） |
| `model_v4/tex/basecolor.raw` | 69 MB | v4 模型贴图（旧脚本引用） |

## 怎么重出
```bash
python3 tools/glb2d/build_6dir_shading.py        # 重渲 views6
python3 tools/glb2d/shading_ratio_transfer.py    # 重出 tex6r（六向明暗贴图）
python3 tools/glb2d/export_all_actions_ratio.py  # 重出 6 动作 × 6 向 × 5 帧
```

## 没有动的
`assets/_trial_20260903…20260911` 各目录、其它线的产物 —— **不属于本线，未动**。
如需继续清理请另行指示。
