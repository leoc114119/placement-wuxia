Native edit prompts used one target per call. Image 1 was the locked candidate; the attached two-image contact supplied the accepted head/body proportion; matching `shanzei_a_rightup` or `shanzei_a_rightdown` supplied direction-specific proportion for b variants. The only requested change was head and neck proportion. All outputs were raw first, then locally white-background cut and normalized.
