# T45 seq=304 · R1 右向 10 帧组系数钳制重做

依据：`tasks/handoff/T45-hero-weapon-recalib-20260910.md` §7、rd→art seq=294/295/296/297/298。

范围固定为右向 10 帧：`walk_right_1..3`、`atk_right_1..4`、`jump_right_1..3`。只使用 seq278/279 已确认 raw 文件直取，不从动作表重切，不启动右上/右下，不进入 Phase B，不写 runtime。

本批加工（承接 PM2 seq=302 组系数钳制裁定）：

- seq278 的 walk+jump 共用固定系数 `min(256/312, 238/299)=0.7959866221`；seq279 的 atk 共用 `256/214=1.1962616822`。
- 刚性尺度改用不饱和的手工 crown→jaw 两点标注；保留帧 12 张波动 2.33%，源图六帧与 atk 组逐帧标注，并在输出候选上回测 ±5%。
- 背景隔离只做边缘连通的中性底；游离块只按 `alpha>32` + 4 邻接连通域逐元素删除。
- 归一目标：240×320 RGBA、脚底 y=300、alpha>32 加权质心 x=120±1、四边零 alpha、主体单连通、宽度≤238；另加 `输出 bbox == round(源裁框×组系数)±1` 的不裁切门。
- 任何触边帧不通过；不通过时保留候选并停在本段，不通过删除主体像素来制造绿灯。

状态：candidate-only；生成积分：0；runtime：未触碰。
