# T45 seq=288 · Phase A right-facing new-frame normalization

Zero new generation. Normalize the Leo-selected right-facing `walk_right_1..3`, `atk_right_1..4`, and `jump_right_1..3` sources into the shared 240×320 coordinate system. Keep runtime untouched. Phase B weapon anchors/masks proceed one frame at a time after this Phase A round passes PM2.
