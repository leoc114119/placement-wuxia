# 战斗 UI 实装需求文档（T23）

> 分类：03-战斗系统 · 状态：🟢 v1.0（2026-09-04 · 三开放点按默认圈定定稿）· 优先级：P0
> 定位：**战斗界面可见 UI 全部实装化**（Leo 09-04 口径：「能看到的 UI 都要换成实装的，没实装的功能先占位」）；**不含人物帧**（45° 帧生成中，另行接续）、不含武功光影（挂起等参考素材）、不含毒
> 素材基础：三组件已切齐——`topbar_base.png`（无字底图）+ `icon_status_{skull,blood,poison}.png` + `ctrl_tuoguan_face/ctrl_jiasu_face`（干净脸）+ `ctrl_flee.png` + `topbar_meta.json`/`ctrl_face_text_meta.json`（坐标与文字样式元数据）
> 红线：battle-core/types 零改动；渲染层禁 import core（宿主中转）；数值零改动；既有绿锁零回归

## 一、需求表

| # | 需求 | 口径 |
|---|---|---|
| 1 | **顶栏数据真实化**：`topbar_base.png` 替换现整稿；代码绘制——名字（快照 hero.name）、血条填充（hp/maxHp 比例 × 条宽，红系）、内力条（neili/maxNeili，蓝系）、百分比数字（ Songti 代码字样式沿用 ctrl_face_text_meta） | 填充色采样自原整稿平均色（开放点①）；坐标读 topbar_meta.json |
| 2 | **状态图标槽**：四槽坐标按 meta 保留；MVP 无状态系统 → **图标默认不显示**（空槽占位），代码留显示接口（传入图标 key+开关即可点亮） | 开放点② |
| 3 | **plaque 木牌**：已上板（T16 起，实测 plaque_l 与在用 alpha 版 RGB 全等）→ **本卡零改动**；点击=吞不穿透、无提示（占位） | 开放点③已圈定 |
| 4 | **ctrl 三钮独立实装**：托管/加速 = 干净脸 + 代码字（常态「托管/加速」，激活「自动/两倍」+ 金框点亮 + 亮度提升，样式按 ctrl_face_text_meta：宋体 50/奶黄/描边 5/中心 **x=132**,66）；**激活态无绿点**；逃跑 = ctrl_flee.png 静态 | 已定稿样式 |
| 5 | **数据接线**：血/内力/名字读 battle-session 快照（宿主 evCursor/帧循环既有链路）；托管/加速激活态读 session 模式与 speed 状态（既有状态，无契约新增） | 零契约新增 |
| 6 | 集成收尾：bundle rebuild + verTag bump + preview 目验 | 交付惯例 |

## 二、开放点（Leo 09-04 已圈定：均按默认）

| # | 开放点 | 默认建议 |
|---|---|---|
| ① | 血/内力条填充色：采样原整稿填充平均色（红 #b3272a 系/蓝 #2a6fd4 系，代码取色） | 按采样色 |
| ② | 状态图标 MVP 全隐藏、留接口 | 按默认 |
| ③ | plaque 点击行为：无反应（纯占位） | 按默认（不做未开放提示） |

## 三、验收条款

| 编号 | 验收 |
|---|---|
| V1 | 顶栏显示真实名字与血/内力条：受击掉血、施放扣内力，条长实时变化、百分比数字同步 |
| V2 | 状态四槽空占位显示，接口留置 |
| V3 | plaque 装备/武功木牌上板，位置与 v8 对照一致 |
| V4 | 托管常态「托管」↔激活「自动」+金框；加速常态「加速」↔激活「两倍」+金框；逃跑可用 |
| V5 | 四门+三零+既有绿锁零回归；types/core 零 diff 自查 |

## 四、流程

开放点圈定 → v1.0 定稿 → 主架构 mini 方案（接线设计+易错点，单段式）→ PM 审阅 → Leo 对齐 → 发 T23（frontend 单卡）→ 领单 DoR → 施工 → PM 复验 → Leo 验收。

## 更新记录

| 日期 | 变更 | 签字 |
|---|---|---|
| 2026-09-04 | **v1.0 定稿**：三开放点按默认圈定（①条填充=采样渐变复刻原稿 ②状态图标全隐藏留接口 ③plaque 点击无反应吞不穿透）+勘误（文字中心 x=122→132，git ac10608）+plaque 零改动口径（§六-5）；技术方案=《战斗UI实装方案-v0.1》9 项确认点 PM 全部按默认背书 | Leo 口径 / ZCode 落盘 |
| 2026-09-04 | v0.9 骨架：需求表 6 行/开放点 3 项/V1-V5/流程（素材切图已齐备：topbar_base/状态图标×3/干净脸×2/逃跑钮/meta×2） | ZCode PM |
