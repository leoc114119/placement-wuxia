# T45 战斗帧 45° 俯视·需求文档 v2.0（六向制重构版）

> 状态：**研发线已确认（§8 四条全闭，2026-09-04）· 待 Leo 发单**
> 变更缘由：2026-09-04 Leo 依据战斗实况图二次拍板——站姿与全套动作帧改为**六向制**，取代 v1.x 的"三向实绘 down/up/side+对角吸附"口径。
> 上位文档：`角色帧规范.md` §4c v1.2（视角/画布/帧集真源）；本文档=T45 执行需求（批次/方法/预算/验收）。

## 1. 朝向口径（本次重构核心）

- 战棋六边形=**pointy-top（上下尖，压扁 1:0.7）+ 奇偶行错位半格布点**（config/battle-hex.ts TILE_SPEC/drawTile 顶点序为准）→ 中心格紧邻格仅 6 个方向：**左、右、左上、右上、左下、右下**——无正上、正下（同列隔行不相邻，非六边形朝向所致）。
- 战斗中**主角面向攻击者所在方向自动选帧**（FE 行为）；敌人同理面向主角。
- 因此站姿与**全套动作帧**（行走/普攻/施放/轻功/死亡）均按六向出帧，down/up 朝向帧不产。
- **右系三向实绘（右 / 右上 / 右下），左系三向（左 / 左上 / 左下）程序化水平镜像派生**，镜像零生成。

| 屏幕朝向 | 视角特征 | 生成方式 |
|---|---|---|
| 右 | 纯侧面，面朝画面正右，身体侧对镜头 | 实绘 |
| 右上 | 背斜侧，面朝画面右上方，以可见后脑与右肩为主 | 实绘 |
| 右下 | 正斜侧，面朝画面右下方，双眼与右颊可见 | 实绘 |
| 左 / 左上 / 左下 | 右系三向的水平镜像 | 程序化派生，零生成 |

## 2. 交付范围（帧集）

### 主角（空拳口径，武器由渲染层按锚点叠加 §4b）

| 动作 | 帧数/向 | 说明 |
|---|---|---|
| 战斗待机 | 1 | 空拳戒备（+呼吸帧程序化，零生成） |
| 战场行走 | 2 | 迈A/迈B 空拳 |
| 轻功跳跃 | 2 | 起跳蜷缩/腾空——仅主角 |
| 普攻 | 3 | 起手/挥击/收招 |
| 施放 | 3 | 挥砍连击：往左砍/往右砍/收回剑位（身体帧只画手臂轨迹，剑由渲染层叠加） |
| 死亡 | 1 | 躺平 |

→ **12 帧/向 × 3 向实绘 = 36 张生成**；左系镜像派生 36 张零生成。

### 敌人：山贼甲、山贼乙（持朴刀，武器固定入帧不剥离）

- 7 帧/向：战斗待机 1 · 行走 2 · 普攻 3 · 死亡 1（无轻功）
- **7 帧/向 × 3 向实绘 × 2 敌 = 42 张生成**；左系镜像派生零生成。

### 总量

- **生成 78 张**（主角 36 + 敌人 42）；镜像派生 78 张零生成；呼吸帧程序化零生成。

## 3. 批次结构与停点（批间 Leo 目验，不过不铺量）

| 批 | 内容 | 生成量 | 方式 |
|---|---|---|---|
| 批 0' | 主角 右/右上/右下 三向 45° 待机锚 | 3 | 多参照 img2img → Leo 目验定锚 |
| 批 1a/1b/1c | 主角 右系各向 11 帧（锚外铺量） | 33 | 锚定表法（左格=各向锚） |
| 批 2a' | 山贼甲/乙 × 三向 待机锚 | 6 | 多参照 img2img → Leo 目验 |
| 批 2b' | 山贼各向 6 帧 ×3 向 ×2 敌 | 36 | 锚定表法（左格=各向锚） |
| 合计 | | **78** | |

每批交付：contact sheet + 程序校验数据（check_left / 归一 / 宽高比）+ credits 记账 + 命名入库清单 → **Leo 目验通过才发下一批**。

## 4. 生成方法（已实证沉淀，执行代理不得更改）

1. **多参照 img2img（≤6 张）**：画风锚（家场景标准帧 `walk_q/frames/idle_down.png`，prompt 声明其佩剑不出现）+ 45° 视角基准帧 + **朝向/姿势参照**。
2. **朝向真源 = 白模站姿转面图**（Leo 提供，2048×2048 高清，09-04 重发正确版已入库目验）：右系三向的朝向以图对应姿势为准——右向=白模第 3 个（纯侧朝左）镜像、右下向=白模第 2 个（斜正面朝左下）镜像、右上向=白模第 4 个（背斜侧朝右上）直接参照。白模 5×8 跑表 `pose_run_5x8.png`（2400×1792，已验正确）作行走/跑动动作参照，整表不裁直接作参照。~~前置依赖卡点：原图待重发~~（09-04 已解除）。
3. **锚定表法铺量**：2 格表（左格=该向锚锁死，右格=目标姿势）img2img；姿势词条文本随批任务书给死，**禁代理自拟**。
4. **禁令**：禁纯文生图新起炉灶；禁链式多轮修补（轮廓收缩实证）；禁 AI 直出多帧网格；动作叙事词会诱导退化，姿势描述用纯静态画面+**画面坐标**（禁"身体左/右"类身体坐标）。
5. **执行纪律**（agent.md 四条，违反=汇报无效）：目测值必须标注"目测"；姿势类交付必须附画面内容逐项描述；同类失败连 2 次停路径升级上报；左右/方向姿势禁纯文字描述，索要示意图作参照。

## 5. 规格红线（校验门，程序化判定）

- 画布 240×320；人物视觉高 256px、脚底基线 y=300、质心 x=120（红线 9）。
- 宽高比带 0.38~0.55（待机/行走/施放）；普攻挥击/轻功/死亡免宽高比门，改高度一致性门（256±10%）。
- **横躺帧（死亡）按宽适配**：包络宽 ≤220、贴底 y=300、质心 x=120。
- 锚定表族一律 `--aspect 3:2`（2:1 被 gpt-image-2 拒绝）+ check_left_v2 **T=30**。
- 抠图统一走 mxai 语义通道（白底唯一通道）；白角≥250、通道差≤2、单连通主体、无文字、无武器（主角系）。
- 命名 `{动作}_{向}_{序号}.png`（如 `walk_right_1.png`、`atk_rightdown_2.png`、向名 right / rightup / rightdown）；落盘 `assets/characters/hero/battle45/`、`assets/characters/enemy/shanzei_a|b/battle45/`。

## 6. 已产帧处置（口径重构的存量处理）

| 存量 | 处置 |
|---|---|
| battle_idle_down（down 向锚） | **作废**（六向无正下），归档不删文件 |
| down 向动作帧 11 张（walk/jump/atk/cast/die） | **保留备用**（不属六向现役，未来演出/兜底可能复用） |
| 批 2a up/side 锚 2 张 | **作废**（up 向不产；side 朝向实测不符右系口径） |
| down 向锚定表法经验/check_left 校准/横躺口径 | **继续有效**（方法资产，与新方向集无关） |

## 7. 预算

- 生成 78 张 × 2 分（gpt-image-2 标准 1K，成本口径）= **156 分**。
- 重摇余量（重摇权归 Leo）30 分 → **本批新增预算上限 186 分**。
- T45 已耗 96 分（含作废帧沉没成本）→ **T45 项目总上限建议调至 290 分**，待确认。
- seedream 默认禁用；例外=Leo 显式点名实验（用后记账不构成解禁）。

## 8. 待研发管线确认的接口（09-04 晚对照 v2.3 FACE-1 后更新；**4/4 已闭 · 主架构 2026-09-04 定夺**）

1. **左系镜像交付形态**：✅ **已闭（主架构定夺 2026-09-04）= 方案 A：美术管线直接产出左系 PNG 成品文件**（磁盘 156 文件，渲染路径零新增逻辑、换表即接）。定夺理由（三面评估）：①**FE 接线复杂度**——A 六向统一按 `{动作}_{向}_{序号}.png` 命名查表，现有帧消费路径（`assets.frames.get(spriteKey)[帧号]`）零分支；B 需 drawPieces 朝向翻转分支 + weapon_anchor 运行时镜像（x′=W−x、angle 镜像）+ 呼吸帧派生与后续特效挂点全部携带翻转知识——横切决策点多点化，违背《战斗交互行为规格》§八「同一决策收敛单一产生点」教训。②**包体/加载**——实测帧均值 ≈47KB/张（battle45 现存 13 张），A 增 78 文件 ≈ +3.7MB；但六向全量 156 张 ≈ 7.4MB 本已超微信主包 4MB 限，A/B 同走分包/资源外置通道，差异仅为用户下载量 +3.7MB（挂账发布侧规划，不阻塞生产）。③**帧表数据结构**——A 六向平铺直接映射 ANIM_FRAMES，无 flip 语义与归一化映射；B 需 left→right 映射+翻转标志位。镜像确定性由管线程序化镜像 + check 校验门（左右对称性程序验证）在产线拦截。
2. **向名约定**：`right / rightup / rightdown / left / leftup / leftdown`。✅ **选帧映射规则已闭**——战斗规格 v2.3 FACE-1（09-04 Leo 口谕落盘）：朝向由受击敌位置决定（单敌朝该敌/AOE 朝最近/同距 rng 随机/空放保持点击格朝向待复核），施放者→目标世界向量**吸附六向**，转向完成后播帧；帧集映射按角色帧规范 v1.2 六向制。剩命名字面 FE 接线时按本表对齐即可。
3. **武器锚点元数据**：✅ **已闭（主架构定夺 2026-09-04）= 方案 B：代码常量表**——运行时真源 = FE 常量表（建议 `config/battle-frames-meta.ts`，ADR-004 只读展示参数口径；`WeaponAnchor{x,y,angle}` 类型进 types.ts，落码归战斗实装卡）。定夺理由：①「UI 只展示/数值集中」原则——锚点是渲染挂接参数、非结算数值，归 config 只读常量（同 numbers.ts 模式）；②类型咬合 + 三零门——常量表受 typecheck/lint/build 覆盖，伴随 json 无类型检查不进门；③FE 消费——渲染层棋子绘制内联同步查锚点，常量表零 IO；json 伴随 = 每帧一文件（叠加 §8.1 的 156 PNG 后 312 文件）+ 预读解析与缓存管理复杂度。**管线交付流不变**：按批产出 json 样例/manifest 作为交付源（批 0' 起并行给出），战斗实装卡誊录进常量表，接线卡 DoD 加「manifest vs 常量表逐帧 diff 对表」防誊录漂移。口径不变：angle 冻结垂向右下、出招跟手不转（§2.5-4）。
4. **接入方式**：✅ **已闭**——素材盘点 v0.3 同步：新帧接线归战斗实装卡（FE 任务，ANIM_FRAMES 换表+`PIECE.heightPerTile` 定尺接口已预留 config/battle-hex.ts:133），本批只交素材。

## 9. 验收标准（DoD）

1. 批 0' 三锚 Leo 目验定锚（朝向对白模图/俯视头身/铃兰式形象/空拳无武器）。
2. 每批程序门全过（§5）+ contact sheet 交 Leo 目验。
3. 78 张生成全部入库 + 78 张镜像派生入库（形态已闭 §8.1=方案 A 左系成品文件）+ credits 总账。
4. 三零：误带文件零 / 未批准依赖零 / 禁区改动零。
5. 全程四门自检报告（全量数据+目验记录），红名单制度同研发线。

---
签：ZCode（PM 美术管线）2026-09-04 · 待研发管线确认后由 Leo 发单
接口确认：ZCode（主架构·研发线）2026-09-04——§8 四条全闭（①勘误 FACE-1 术语 / §8.1=方案 A / §8.3=方案 B / §8.4 原闭），答复详 `tasks/answers/A1-T45-sixdir-interfaces.md`
