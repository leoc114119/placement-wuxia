# 放置武侠 · 明日执行清单（08-21 第 1 期开工日）

> 状态：**待执行（2026-08-20 晚制定）**
> 目标：跑通第 1 期最小可玩闭环——江湖场景（手动模式）+ 8×12 战棋战斗。
> 执行人：**Leo**（环境/素材/验收签字——本清单重点）· **ZCode**（T02~T07 开发，交给他处理）· **CodeBuddy**（发单/测试/进度更新）
> 依据：`78-明日开工计划` + `81-研发协作机制`（任务卡/DoR/DoD）+ `82/Excel 任务管理总表`（状态跟踪）+ `77-开工素材清单`（素材 prompt/命令）
> 铁律：上一步未验收不进下一步（07 审查链条）；DoR 不过不发单（81）。
> **分工说明（Leo 2026-08-20 定）**：ZCode 侧开发由 ZCode 自行处理，本清单不为 ZCode 细化实现步骤；**Leo 侧动作 = 前置准备 + 每步验收**。

---

## 0. 今日已完成的准备（2026-08-20 晚）

| 项 | 状态 |
|---|---|
| 设计定稿回填（13 处文档，P0 全清） | ✅ |
| 协作机制 81 v0.2.1（任务卡/DoR/DoD/边界/歧义必问/素材拆解铁律） | ✅ |
| Excel 任务管理总表（总表 + 第1期：素材总表 6 组 22 项 + T01~T07 任务卡） | ✅ |
| **`scripts/split_sheet.py` 帧表切割脚本**（2×4→8 帧，自测通过） | ✅ 新增 |
| 素材生成命令/prompt（77 清单 §2.1~2.5） | ✅ 就绪 |

---

## 1. 开工前置（必须先完成，阻塞 T03/T04/T06/T07）

### 前置 A · T01 环境确认（Leo，5 分钟）
- [ ] 微信开发者工具已安装（未装 → 装个人版）
- [ ] `node -v` ≥ 18
- **通过后**：Excel 总表 T01 状态 → ✅

### 前置 B · P0 素材生成（Leo，约 40 分钟，按顺序）
> prompt 与命令全部在 `77-开工素材清单 §2`，风格前缀在 `docs/76`。

| 序 | 素材 | 卡谁 | 命令（77 §2 有完整 prompt） |
|---|---|---|---|
| 1 | split_sheet.py 已就绪 ✅ | — | 已写好自测通过 |
| 2 | **hero 主角帧表**（2×4=8帧） | T03/T06 | `mxai_gen.py --aspect 1:1 --out assets/ui/frames/hero_sheet.png` → split_sheet |
| 3 | **山贼帧表** spr_shanzei | T04/T06 | 同上（spr_shanzei） |
| 4 | **野狼帧表** spr_lang | T04/T06 | 同上（spr_lang） |
| 5 | **狼王帧表** spr_boss_lang | T07/T06 | 同上（spr_boss_lang） |
| 6 | **scene_battle 战斗背景** | T06 | `mxai_gen.py --aspect 9:16 --resolution 2K --out assets/ui/scene_battle.png` |

- 每生成一项 → Excel 素材总表状态改 ✅ → 卡它的任务 DoR 解锁
- **兜底**：帧表不一致 → 退化为单 idle 帧 + 代码颠簸（77 §0.1），不阻塞开工

---

## 2. Leo 侧动作清单（明天你要做的）

> ZCode 侧：CodeBuddy 发单（81 任务卡）→ ZCode 完成 → 自动进 C 环；**Leo 只做验收签字**。

### 2.1 前置（开工前）
| # | 动作 | 说明 |
|---|---|---|
| L-1 | 确认微信开发者工具已装（T01） | 未装装个人版；node ≥18 |
| L-2 | 生成 P0 素材 ×5（约 40 分钟） | 按序：hero 帧表 → 山贼 → 野狼 → 狼王 → scene_battle；prompt/命令在 77 清单 §2；每张生成后跑 `split_sheet.py` 切割 |

### 2.2 验收签字（每个任务完成后，按任务卡「验证要求」点验）
| 任务 | Leo 验收动作 | 通过标准 |
|---|---|---|
| T02 骨架 | 开发者工具打开工程 | 显示画面；`npm run typecheck` 0 error |
| T05 headless | CodeBuddy 报 7 用例绿 | 确认即可（无界面） |
| T03 场景 | 点场景任意位置 | 角色走过去；场景名/三按钮显示；助战入口不可见 |
| T04 NPC | 进青牛山下观察 | 3 只怪走动；贴近触发确认；点它进战斗；击败消失 |
| T06 战斗 | 点 NPC 进战斗 | 见 8×12 棋盘；自动模式出胜负；手动出招有光影 |
| T07 数据 | 观察 | 场景名正确；角色头顶 Lv.1；Boss 入口 1000 置灰 |

> 每验收一项 → 我更新 Excel 状态（✅），收工汇总。

---

## 3. 依赖关系与执行节奏

```
[前置] T01 环境 + P0 素材（5 项）─── 上午完成
   │
   ├─ T02 工程骨架 ──────────────┐
   │      └─ T05 headless（并行） ├─ 上午/午后
   │                    │
   ├─ T03 场景系统 ← hero帧+背景 │
   │      └─ T04 NPC ← 山贼/野狼帧│
   │              └─ T06 战斗界面 ← T05+T04+scene_battle+狼王帧（午后~傍晚，最重）
   │
   └─ T07 数据接入（随 03/04/06 逐步接）
```

**推荐节奏（Leo 侧）**：
- 上午：L-1 环境确认 + L-2 P0 素材（约 40 分钟）→ 交给我发单 T02/T05
- 午后~傍晚：按 §2.2 逐项验收（我每个任务完成会提醒你点验）
- 收尾：首次完整试玩 + 三关签字

**里程碑**：
| 时点 | 里程碑 |
|---|---|
| 中午前 | 工程跑通 + headless 7 用例绿（Leo 只确认） |
| 下午 | 场景+NPC 可玩（Leo 验收 2 次） |
| 傍晚 | 战斗界面闭环 → 首次完整试玩 |

---

## 4. 验收节奏（三环，07 链条）

```
ZCode 提交（DoD 勾选 + 架构决策说明）
  → CodeBuddy C 环：编译/类型/静态/单测/模拟运行/设计一致性 + 任务卡用例
  → Leo L 环：按任务卡「验证要求」点验 → 签字
  → Excel 状态更新（📤→🔧→🧪→✅）
每完成一步，当天更新 Excel + progress/2026-08-21.md
```

---

## 5. 风险与兜底（PM 已登记）

| 风险 | 概率 | 兜底 |
|---|---|---|
| P0 素材帧表 AI 漂移/不一致 | 中 | 退化为 idle + 代码颠簸（77 §0.1），不阻塞 |
| 微信开发者工具未装 | 低 | T01 第一件事装（个人版） |
| T06 战斗界面过重拖到明天 | 中 | T05 先行（纯逻辑独立验收）；T06 可拆两步（先棋盘+自动，后手动+光影） |
| headless 用例与 75 规格冲突 | 低 | 歧义必问铁律（81 §6），先问再写 |
| 数值手感不佳（初稿🔧） | 确定 | 预期内，Sprint 内滚动调，不阻塞验收 |

---

## 6. 变更记录

| 日期 | 版本 | 变更 | 签字 |
|---|---|---|---|
| 2026-08-20 | v1 | 创建：明日（08-21）执行清单——前置 A/B + T02~T07 逐项细化（步骤/交付/验收/耗时）+ 依赖节奏 + 三环验收 + 风险兜底；split_sheet.py 已就绪 | CodeBuddy → 待 Leo 执行 |
