# 美术资产索引（常青更新）

> 查素材先来这里。新增素材当天登记；归档纪律：draft（草稿）→ 定稿 → `archive/`，不删文件。

## 1. 风格基准（定稿，img2img 参考优先用它们）

| 资产 | 路径 | 说明 |
|---|---|---|
| 江湖 UI 基准 | `assets/ui/ref_jianghu_ui_v2.png` | v6 定稿：右上两组圆形图标+纯数字（元宝/书卷），头部木质感保留 |
| 人物风格基准 | `assets/ui/ref_char_style_v1.png` | v3 国漫风（Leo 喜欢），新角色生成用它做 img2img 风格参考 |
| 战斗 UI 参考 | `assets/ui/ref_battle_ui_v4.png` | |
| 武学 UI 参考 | `assets/ui/ref_wuxue_ui_v2.png`（另有 v1） | |
| 无文字框架图 | `assets/ui/ref_jianghu_framework.png` | 布局参考专用（AI 稿禁文字） |

## 2. 场景

| 资产 | 路径 | 说明 |
|---|---|---|
| 战斗场景 | `assets/ui/scene_battle.png` | pro 档，帧表 00 帧统一 |
| 江湖主场景 | `assets/ui/scene_jianghu.png` | 标准版（不满意可 --only 升 pro 重出） |
| 主界面背景 | `assets/ui/bg_main_v2.png` | |
| liblib 背景 | `assets/ui/bg_liblib_3.png` | Q 版水墨 LoRA 试产 |

## 3. 角色

| 资产 | 路径 | 说明 |
|---|---|---|
| 主角立绘（闭关） | `assets/ui/hero_idle_transparent.png` | 打坐像，归位模块 09 闭关专用 |
| 帧表 | `assets/ui/frames/` | 4 张 sheet + 各 8 帧透明 PNG + contact 验收页 |
| **生成铁律** | — | NPC 帧表一律「模板换皮」（图1姿势+图2外观）；侧视战斗帧只左右两向；立绘纯白底全身无特效 |

## 4. UI 组件

`assets/ui/buttons/` · `assets/ui/panels/` · `assets/ui/icons/`（规范见 `assets/ui/README.md`；图标统一替换批次登记为 P1）

## 5. 草稿与归档

`assets/ui/drafts/`（所有生图先落这里）· `assets/ui/archive/`（被替换的旧基准）

## 6. 生成管线（scripts/）

| 脚本 | 用途 |
|---|---|
| `assets_pipeline.py` + `config/assets-p0.json` | 清单驱动批量：并行生图→下载→切帧→抠图→contact sheet |
| `mxai_gen.py` / `mxai_img2img.py` | mxai 文生图 / 图生图（帧表/构图=5.0-pro 档） |
| `liblib_gen.py` | liblib（风格锚定单图/图标；需 LIBLIB key） |
| `split_sheet.py` | 帧表切帧 |
| `cutout_white_bg.py` | 常规纯白底抠图（白系/复杂边缘走 XMAI AI 抠图） |
| `gen_icons_v2.py` | 图标生成 |
| `shot_genmode.js` / `ux_screenshot.js` | preview 生图模式框架截图 / UX 截图 |

## 7. 规范文档指针

`docs/04-UI风格规范-v0.1.md`（风格圣经）· `docs/73-UI设计元素统一规范` · `docs/74-UI沉浸感升级方案` · `docs/76-生图风格前缀` · `docs/77-开工素材清单` · `docs/82-素材管线迭代路线图`

## 更新记录

| 日期 | 变更 | 签字 |
|---|---|---|
| 2026-08-31 | 创建（基于 08-21/08-23 素材日成果盘点） | 游承峰 |
