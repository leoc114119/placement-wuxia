# MVP 三模块 · 素材需求草案（CodeBuddy 先行版）

> 状态：**🟡 草案 v0.1（2026-08-31 CodeBuddy 先行拆解）**
> 定位：回答"MVP 三模块各要输出哪些素材"。ZCode 的 T10 会出正式版（`docs/reviews/T10-mvp素材需求清单-v1.md`），本稿作为**交叉验证基线**——两版比对查漏后合并为正式清单。
> 口径：按 81 素材铁律拆到区域/层颗粒度；只列 MVP 范围（传承/木人巷计分/正邪香火不在列）。
> 风格：2.5D 像素（v8 风格锚 `ref_char_style_v1.png`）+ 活泼年轻水墨 UI（基准 `ref_jianghu_ui_v2.png`）；生成约定照 PROJECT-MEMORY §2（AI 稿禁文字、白底抠图、模板换皮帧表）。

---

## 模块① 基础界面设计与美术风格

> 验收口径：主界面+战斗界面风格成立、可看。**核心是"风格系统"而非单张图**。

### 1.1 已有（不重复生成）✅

| 素材 | 路径 | 状态 |
|---|---|---|
| 江湖场景背景 | `assets/ui/scene_jianghu.png` | ✅ v6 定稿 |
| 战斗场景背景 | `assets/ui/scene_battle.png` | ✅ v3 定稿（无棋盘横幅） |
| 主角帧表 8 帧 | `assets/ui/frames/hero/` | ✅ 已切帧 |
| 山贼/野狼/狼王帧表 | `assets/ui/frames/spr_*` | ✅ 已切帧（含侧身 idle 补帧） |
| 风格锚×2 | `ref_char_style_v1` / `ref_jianghu_ui_v2` | ✅ 定稿 |
| 七图标+底部按钮×3 | `assets/ui/icons/` `buttons/` | ✅ |
| 战斗 UI 基准 | `ref_battle_ui_v4.png` | ✅ 定稿 |

### 1.2 缺口（需新生成）🔧

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| UI-01 | **风格 token 视觉板**（色板+按钮三态+面板圆角+描边样式一页纸） | 统一全部 UI 生成风格锚 | 9:16 一页 | liblib 水墨 LoRA 单图 | **P0**（先于一切 UI） |
| UI-02 | 通用面板底 ×3（对话框/设置/背包） | 九宫格拉伸底板 | 750×未知，圆角水墨框 | liblib，纯底无文字 | P0 |
| UI-03 | 主菜单背景（LOGO 位留白） | 启动页 | 9:16 竖版水墨 | liblib 场景类 | P1 |
| UI-04 | 资源条底（银两/学点/体力三连条） | 顶栏 | 横条 600×90 | 代码绘制为主，出底板纹理 P2 | P2 |
| UI-05 | 加载转圈/进度条皮 | 过场 | 小件 | 代码绘制 | P2 |
| UI-06 | 设置页开关/滑条组件皮 | 设置面板 | 小件组 | 代码绘制 | P2 |

### 1.3 代码绘制清单（不出图）

顶栏数字排版、按钮文字、飘字样式、面板内边距——全部代码，只依赖 UI-01 定色值。

---

## 模块② 战斗手感打磨

> 验收口径：打起来"有反馈、不木"。**手感 70% 靠代码动效，素材只补代码做不了的**。

### 2.1 帧表缺口（沿用模板换皮管线）

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| FX-01 | 主角**攻击帧强化**（现有 2 帧 → 出招预备/挥出/收招 3-4 帧） | 手感核心：挥击节奏 | 侧视 2 向，模板换皮（图1=hero 现帧+图2=hero 立绘） | mxai seedream img2img | **P0** |
| FX-02 | 受击姿态帧 ×1（后仰） | 打击反馈 | 同上 | 同上 | P0 |
| FX-03 | 敌方受击帧（山贼/野狼/狼王各×1） | 同上 | 模板换皮批量 | 同上 | P1 |
| FX-04 | Boss 狼王**攻击帧**（现缺独立 attack，复用 idle） | Boss 战张力 | 模板换皮 | 同上 | P1 |

### 2.2 特效素材（代码动效的贴图原料）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| EF-01 | 剑光/刀罡/掌风粒子贴图 ×3 | 出招光影（75 §5b.1） | 小贴图组（白底） | **P0**（代码粒子需要贴图籽） |
| EF-02 | 命中爆点/闪白帧 | 受击反馈 | 2-3 帧小图 | P0 |
| EF-03 | 尘土/踏步粒子 | 移动反馈 | 1-2 帧小图 | P2 |
| EF-04 | 死亡消散烟雾 | 击杀反馈 | 3-4 帧小图 | P1 |

### 2.3 代码动效（Leo 已定，不占帧——列此备查）

呼吸浮动/脚下阴影/伤害飘字（竹青-我方·朱砂-敌方）/miss 飘字/反击复用——全部代码。**手感的主体在这里，素材只是原料**。

---

## 模块③ 放置无限流（剿匪刷刷刷）

> 验收口径：体力→连刷→掉落→变强的循环闭得起来且**爽**。素材重点=掉落演出+循环场景。

### 3.1 场景层

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| SC-01 | **剿匪地图 2-3 张**（不同地貌：树林/丘陵/山寨） | Boss 换图机制的视觉支撑 | 9:16 竖版，尺度铁律（物体≤角色1.5倍高） | mxai 5.0-pro，v6 草甸做构图参考 | **P0** |
| SC-02 | Boss 点位标记/巢穴入口小件 | 换图目标可视化 | 小件 | liblib 图标类 | P1 |

### 3.2 掉落演出层（爽感核心）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| DR-01 | **掉落物图标 ×8-12**（武器/防具/材料/银两袋/秘籍各档品质色） | 掉落实时弹出的主体 | 128px 图标，白底抠图 | **P0**（无限流没它就没爽感） |
| DR-02 | 品质光框 ×4（白/绿/蓝/紫） | 稀有度包装 | 代码画框+粒子即可 | P2 |
| DR-03 | **小箱/大箱各×1**（开箱双粒度包装） | 在线小时箱+离线日箱 | 256px 立绘 | P1 |
| DR-04 | 开箱金光burst 贴图 | 开箱瞬间 | 复用 EF-02 可降级 | P1 |
| DR-05 | 掉落弹窗底板（"获得新物品"半透明浮层） | 结算包装 | 水墨横幅底 | P1 |

### 3.3 剿匪专属 NPC（换图=换怪）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| NC-01 | 新怪 ×2（与山贼/野狼不同皮） | SC-01 新地图配套 | 模板换皮 8 帧表 | P1（P0 地图可先用既有怪） |
| NC-02 | 剿匪头目（小 Boss）×1 | 换图进阶 boss | 同上 | P1 |

### 3.4 数字与结算（代码）

体力条/掉落计数/收益浮层/年龄显示——全代码，无素材依赖。

---

## 汇总：MVP 最小开工包（P0 全集）

> **先做这 9 项，三模块都能开工渲染；其余 P1/P2 随迭代补。**

| 模块 | P0 项 |
|---|---|
| ① 界面 | UI-01 风格 token 板 · UI-02 面板底 ×3 |
| ② 手感 | FX-01 主角攻击帧强化 · FX-02 受击帧 · EF-01 出招粒子 · EF-02 命中反馈 |
| ③ 无限流 | SC-01 剿匪地图 ×2-3 · DR-01 掉落图标 ×8-12 |

## 与 T10 的关系

本稿 = CodeBuddy 先行基线；ZCode T10 产出正式清单后做**双向 diff**：
- 本稿有 T10 无 → ZCode 复核是否漏
- T10 有本稿无 → 采纳（ZCode 从实现者视角可能发现新依赖）
- 两版冲突项（规格/优先级）→ 交 Leo 拍板
