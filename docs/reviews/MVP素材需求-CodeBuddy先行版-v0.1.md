# MVP 三模块 · 素材需求草案（CodeBuddy 先行版）

> 状态：**🟡 草案 v0.3（2026-08-31 晚，UI 全线像素化拍板后修订）**
> **v0.2 重大变更（Leo 08-31 拍板）**：像素风定案后，**旧 Q版水墨角色帧表×4 + 场景×2 全部重生成**——新增 §0 全换清单。
> **v0.3 重大变更（Leo 08-31 拍板）**：**UI 也全面国风像素化，且 UI 布局重新设计**——旧 UI 基准（ref_jianghu_ui_v2/ref_battle_ui_v4/七图标/按钮）全部作废；"不在意美术资产重设计的成本，这是必要的"（Leo 原话）。UI 线进入"重设计"而非"换皮"。
> 定位：回答"MVP 三模块各要输出哪些素材"。ZCode 的 T10 会出正式版（`docs/reviews/T10-mvp素材需求清单-v1.md`），本稿作为**交叉验证基线**——两版比对查漏后合并为正式清单。
> 口径：按 81 素材铁律拆到区域/层颗粒度；只列 MVP 范围（传承/木人巷计分/正邪香火不在列）。
> 风格：**全线索国风像素（HD-2D）**——角色锚 `ref_pixel_char_v8.png`；场景/UI 锚待新立；生成约定照 PROJECT-MEMORY §2（AI 稿禁文字、白底抠图、模板换皮帧表）。

---

## 模块① 基础界面设计与美术风格

> 验收口径：主界面+战斗界面风格成立、可看。**核心是"风格系统"而非单张图**。

### 0. 全量换皮清单（v0.2 新增 · 旧水墨资产 → HD-2D 像素）

| # | 旧资产 | 现状 | 换皮动作 | 优先级 |
|---|---|---|---|---|
| SW-01 | 主角帧表 8 帧（`frames/hero/`） | Q版水墨 ❌ | **以 v8 像素锚为模板**出完整帧表（idle1/walk4/attack2/basic1，img2img 换姿势不换风格） | **P0** |
| SW-02 | 山贼/野狼/狼王帧表 ×3（`frames/spr_*`） | Q版水墨 ❌ | 模板换皮：图1=v8 像素锚姿势 + 图2=各角色像素外观（外观 prompt 重写为像素描述） | **P0** |
| SW-03 | 江湖场景 v6（`scene_jianghu.png`） | 水墨手绘 ❌ | **像素场景锚先行**（先出 1 张像素风场景定风格，过 L 目验再量产）+ **WALK_ZONE 重新标定**（v6 的 x[0.24,0.76] y[0.46,0.84] 随图作废） | **P0** |
| SW-04 | 战斗场景 v3（`scene_battle.png`） | 水墨手绘 ❌ | 同上，像素场景锚 PASS 后跟量 | P0 |
| — | 立绘 `hero_idle_transparent.png` / `ref_char_style_v1` | 水墨立绘 ✅ | **不换**：已转岗剧情对话立绘（08-23 定） | — |
| SW-05 | UI 基准 `ref_jianghu_ui_v2` / `ref_battle_ui_v4` | 水墨 UI ❌ | **作废**：UI 全面像素化+布局重设计（v0.3 拍板），旧基准不可作参考，防锚污染 | **P0** |
| SW-06 | 七图标+底部按钮×3（`icons/` `buttons/`） | 水墨 UI ❌ | **作废重做**：像素图标全套，随新 UI 布局定 | **P0** |

> 换皮管线照 08-23 教训：帧表全部模板换皮、禁独立生图；切帧走 split_sheet.py；白底抠图；旧资产移 `assets/ui/archive/`。

### 1.1 已有（不重复生成）✅ → v0.3 仅剩

| 素材 | 路径 | 状态 |
|---|---|---|
| 像素角色风格锚 v8 | `assets/ui/ref_pixel_char_v8.png` | ✅ T08 试水验收升正 |
| 水墨立绘（剧情线用） | `ref_char_style_v1` / `hero_idle_transparent` | ✅ 转岗剧情对话立绘，不参与 UI |

### 1.2 缺口（需新生成）🔧 → v0.3 全列表（UI 线=重设计）

**第一步：UI 重设计（先于一切 UI 素材生成）**

| # | 交付物 | 说明 | 优先级 |
|---|---|---|---|
| UX-01 | **主界面布局线框重设计**（顶栏/场景区/底部导航/浮层逻辑） | 像素 UI 的信息密度与边框语言和水墨完全不同，需从布局层重来；输出=低保真线框（可代码画）过 Leo → 再出视觉稿 | **P0（第一优先）** |
| UX-02 | **战斗界面布局重设计**（HUD/棋盘/操作栏三分区在像素语言下的重排） | 基于战场布局规格 v0.9 的分区框架不变，视觉层重设计 | **P0** |
| UX-03 | **像素 UI 风格锚**（token 视觉板：色板/面板样式/按钮三态/边框样式一页纸） | 国风像素 UI 的唯一真源；UX-01 过审后出 | **P0** |

**第二步：UI 素材生成（UX-03 定稿后）**

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| UI-01 | 通用面板底 ×3（对话框/设置/背包） | 九宫格拉伸底板，像素边框 | 像素网格对齐（3px/5px 边框） | liblib/手绘像素，纯底无文字 | **P0** |
| UI-02 | **像素图标全套**（银两/学点/体力/背包/好友/门派/地图/武学 + 底部导航 ×3-5） | 替代旧七图标 | 32×32 网格整数倍，像素风 | liblib 像素 LoRA 或 Aseprite 手绘 | **P0** |
| UI-03 | 按钮三态 ×2 组（主/次） | 全界面按钮 | 像素边框+三态（normal/pressed/disabled） | 同上 | **P0** |
| UI-04 | 主菜单背景（LOGO 位留白） | 启动页 | 9:16 像素场景 | mxai 5.0-pro（像素场景锚后） | P1 |
| UI-05 | 资源条/进度条/加载组件皮 | 顶栏与过场 | 像素组件 | 代码绘制为主 | P2 |

> ⚠ 像素 UI 铁律（新增）：所有 UI 位图必须**像素网格对齐**（禁非整数倍缩放）；9:16 定稿图只能当**布局参考**，视觉稿以组件级切图交付（面板/按钮/图标独立 PNG），禁整屏大图直接裁切。

### 1.3 代码绘制清单（不出图）

顶栏数字排版、按钮文字、飘字样式、面板内边距——全部代码，只依赖 UI-01 定色值。

---

## 模块② 战斗手感打磨

> 验收口径：打起来"有反馈、不木"。**手感 70% 靠代码动效，素材只补代码做不了的**。

### 2.1 帧表缺口（沿用模板换皮管线）

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| FX-01 | 主角**攻击帧强化**（现有 2 帧 → 出招预备/挥出/收招 3-4 帧） | 手感核心：挥击节奏 | 侧视 2 向，模板换皮（图1=hero 现帧+图2=hero 立绘） | mxai seedream img2img | **P0** |
| FX-02 | 受击姿态帧 ×1（后仰） | 打击反馈 | 同上 | 同上 | P0 |
| FX-03 | 敌方受击帧（山贼/野狼/狼王各×1） | 同上 | 模板换皮批量 | 同上 | P1 |
| FX-04 | Boss 狼王**攻击帧**（现缺独立 attack，复用 idle） | Boss 战张力 | 模板换皮 | 同上 | P1 |

### 2.2 特效素材（代码动效的贴图原料）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| EF-01 | 剑光/刀罡/掌风粒子贴图 ×3 | 出招光影（75 §5b.1） | 小贴图组（白底） | **P0**（代码粒子需要贴图籽） |
| EF-02 | 命中爆点/闪白帧 | 受击反馈 | 2-3 帧小图 | P0 |
| EF-03 | 尘土/踏步粒子 | 移动反馈 | 1-2 帧小图 | P2 |
| EF-04 | 死亡消散烟雾 | 击杀反馈 | 3-4 帧小图 | P1 |

### 2.3 代码动效（Leo 已定，不占帧——列此备查）

呼吸浮动/脚下阴影/伤害飘字（竹青-我方·朱砂-敌方）/miss 飘字/反击复用——全部代码。**手感的主体在这里，素材只是原料**。

---

## 模块③ 放置无限流（剿匪刷刷刷）

> 验收口径：体力→连刷→掉落→变强的循环闭得起来且**爽**。素材重点=掉落演出+循环场景。

### 3.1 场景层

| # | 素材 | 用途 | 规格 | 生成方式 | 优先级 |
|---|---|---|---|---|---|
| SC-01 | **剿匪地图 2-3 张**（不同地貌：树林/丘陵/山寨） | Boss 换图机制的视觉支撑 | 9:16 竖版，**像素风**（SW-03 锚定后跟量），尺度铁律（物体≤角色1.5倍高） | mxai 5.0-pro，像素场景锚做风格参考 | **P0** |
| SC-02 | Boss 点位标记/巢穴入口小件 | 换图目标可视化 | 小件 | liblib 图标类 | P1 |

### 3.2 掉落演出层（爽感核心）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| DR-01 | **掉落物图标 ×8-12**（武器/防具/材料/银两袋/秘籍各档品质色） | 掉落实时弹出的主体 | 像素图标，与 UI-02 同规格（32×32 网格倍数），白底抠图 | **P0**（无限流没它就没爽感） |
| DR-02 | 品质光框 ×4（白/绿/蓝/紫） | 稀有度包装 | 代码画框+粒子即可 | P2 |
| DR-03 | **小箱/大箱各×1**（开箱双粒度包装） | 在线小时箱+离线日箱 | 像素立绘 256px | P1 |
| DR-04 | 开箱金光burst 贴图 | 开箱瞬间 | 复用 EF-02 可降级 | P1 |
| DR-05 | 掉落弹窗底板（"获得新物品"半透明浮层） | 结算包装 | 像素横幅底（UI-01 同批） | P1 |

### 3.3 剿匪专属 NPC（换图=换怪）

| # | 素材 | 用途 | 规格 | 优先级 |
|---|---|---|---|---|
| NC-01 | 新怪 ×2（与山贼/野狼不同皮） | SC-01 新地图配套 | 模板换皮 8 帧表 | P1（P0 地图可先用既有怪） |
| NC-02 | 剿匪头目（小 Boss）×1 | 换图进阶 boss | 同上 | P1 |

### 3.4 数字与结算（代码）

体力条/掉落计数/收益浮层/年龄显示——全代码，无素材依赖。

---

## 汇总：MVP 最小开工包（P0 全集）

> **先做这四步，三模块都能开工渲染；其余 P1/P2 随迭代补。**

| 步骤 | P0 项 | 说明 |
|---|---|---|
| **第一步 UI 重设计** | UX-01 主界面线框 · UX-02 战斗界面线框 → UX-03 像素 UI 风格锚 | 布局先行，锁视觉语言；**阻塞全部 UI 素材** |
| **第二步 像素场景锚** | SW-03 江湖像素场景锚（含 WALK_ZONE 重标） | **阻塞场景量产**；SW-04 战斗场景、SC-01 剿匪图随其后 |
| **第三步 像素帧表** | SW-01 主角全帧表（含受击/攻击强化帧） · SW-02 NPC×3 | 与场景锚并行（角色锚 v8 已定） |
| **第四步 组件与图标** | UI-01 面板底 · UI-02 像素图标全套 · UI-03 按钮三态 · DR-01 掉落图标 · EF-01 出招粒子 · EF-02 命中反馈 | UX-03 定稿后批量 |

> ⚠ FX-01/02 已并入 SW-01（一张主角全帧表含受击帧，省一轮生成）。

## 与 T10 的关系

本稿 = CodeBuddy 先行基线；ZCode T10 产出正式清单后做**双向 diff**：
- 本稿有 T10 无 → ZCode 复核是否漏
- T10 有本稿无 → 采纳（ZCode 从实现者视角可能发现新依赖）
- 两版冲突项（规格/优先级）→ 交 Leo 拍板
