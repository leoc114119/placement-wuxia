# 美术管线 12 类最小试产走查（2026-09-03）

> 执行：art-pipeline 代理（无人值守）· 依据：《美术素材生成流程规范》v1.2（`docs/design/01-基础功能/美术素材生成流程规范.md`）+ skill《art-pipeline-execution》12 类文件 + 主会话任务书（12 类最小试产动作表）
> 目的：① SOP 命令级可复现检验 ② 代理侧 0 自由发挥是否成立。**试产非量产**：生图定死 9 张 = 18 分，一张未多。
> 试产隔离：一切产出落 `assets/_trial_20260903/`，唯一改动既有文件 = `scripts/gen_hex_tileset.py`（c08，diff 全文见 §类8）。
> 总 contact sheet：`assets/_trial_20260903/contact_sheet_all.png`（4 列×3 行，网格序见文末附表）。

---

## 全局环境记录（开工前置）

| 项 | 结果 |
|---|---|
| `source ~/.zshrc && echo ${MX_AI_API_KEY:+OK}` | OK |
| `python3 -c "import PIL"` | PIL OK |
| mxai 登录态 `/tmp/mxai_localstorage_full.json` | 存在且有效（c05 两次网页抠图成功） |
| E-ENV-01（1 次） | 首条生图命令因 Bash 跨调用不保留环境变量报 `ERR: MX_AI_API_KEY 未设置`；按 common-pitfalls E-ENV-01 处置（同命令 + source 重跑 1 次）成功。**此后所有生图命令前置 `source ~/.zshrc`**（无第二次发生） |
| E-ENV-03（记录，未处置） | 环境 numpy 缺失（c02 diff 统计时 `ModuleNotFoundError`）；未 pip install，改纯 PIL 直方图统计完成同一校验，无绕过 |
| 重摇次数 | **0**（全部 9 张一次过；所有门失败均按任务书停上报，未重跑） |

---

## 类 1 · 行走帧（无武器锚）—— ✅ 成功（机械门层）

**SOP 引用**：规范 §2.1 + skill `c01-walk-frames.md`

**实跑命令**：
```bash
# prompt 构造（程序化替换，断言唯一命中）
# 源 = assets/characters/hero/walk_v2/prompts/q_anchor_v5_front.txt
# 替换：「腰间挂剑」→「手持空/无武器，腰间无挂物」（任务书 b 案拍板口径原文）
source ~/.zshrc && python3 scripts/mxai_img2img.py \
  assets/characters/hero/walk_v2/raw/q_anchor_v5_clean.png \
  assets/_trial_20260903/c01/anchor_noweapon_raw.png \
  --prompt-file assets/_trial_20260903/c01/prompts/anchor_noweapon.txt
```

**结果**：成功。1088×1920，PIL verify 通过，四角色值 (253,253,255)/(255,255,255)/(254,254,253)/(253,253,253) 纯白门 PASS。prompt 程序化 diff 仅第 4 行武器条款一处变化。
**产物**：`c01/anchor_noweapon_raw.png` + `c01/prompts/anchor_noweapon.txt` · 成本 2 分（credits #1）

**自检五问**：
1. 命令与 skill 逐字一致，**但 prompt 改法与 skill 不一致**（见卡点 K1-1）：skill c01 步骤 1 写"只删「腰间挂剑、」5 字"，任务书 + 规范 §2.5-②-1（"prompt 必须显式「手持空/无武器」——否则残留剑柄（在册坑）"）要求显式无武器条款。按 skill 自身声明的优先级（规范=口径真源，冲突以规范为准）执行任务书/规范口径。
2. prompt 全文 = `q_anchor_v5_front.txt` 原文 + 唯一替换「腰间挂剑」→「手持空/无武器，腰间无挂物」（逐字含斜杠，未润色）。无自拟新表述。
3. 自决念头：①"斜杠读起来别扭，要不要改成顿号"——未执行（拍板原文逐字）；②"删除式改写（skill 版）与替换式（任务书版）取哪个"——未自行调和，按优先级链执行并登记。
4. 门：verify ✓ / 纯白角部门 ✓；武器是否真被移除属内容判读 → 交 Leo 目验（skill c01 停点定义）。
5. 路径/尺寸/成本见上；credits name=anchor_noweapon 完整。

**卡点**：
- **K1-1（skill 勘误建议）**：skill c01 步骤 1（删除式）与规范 §2.5-②-1（显式条款）矛盾，本次按规范执行；建议修订 skill c01。
- **目验点**：锚是否已无剑无腰挂、发型/服装与 v5 锚一致性。

---

## 类 2 · 呼吸帧 —— ✅ 成功

**SOP 引用**：规范 §2.2 + skill `c02-breath-frames.md`（脚本逐字照抄，禁改参数）

**实跑命令**：`python3 assets/_trial_20260903/c02/make_breath_once.py`（脚本 = skill 原文，SHIFT=3 / SPLIT=0.52 未动）

**结果**：成功。输出 `OK (240, 320)`；与底帧 diff 像素 **8565 > 0**（paste 生效），diff bbox (70,41,175,163)——全部落在 y<52%（=166px）上半区 ✓ 与规范口径自洽。
**产物**：`c02/idle_down_breath_trial.png` + `c02/make_breath_once.py` · 成本 0

**自检五问**：
1. 脚本逐字一致；diff 统计工具从 numpy 改纯 PIL（环境缺 numpy，E-ENV-03 记录，统计口径不变）。
2. 无 prompt（零 AI）。
3. 自决念头："3px 够不够可见、要不要调大对比"——未执行（参数禁改，抖不抖归渲染表现不判）。
4. 门：尺寸=底帧 ✓ / diff>0 ✓ 两项全过。
5. 产物 240×320，0 分，无 credits 条目（纯程序化）。

**卡点**：无。

---

## 类 3 · 战斗待机（锚定表）—— ❌ 门失败（样张仍交目验）

**SOP 引用**：规范 §2.3 + skill `c03-battle-idle.md`

**实跑命令**：
```bash
python3 assets/_trial_20260903/c03/make_sheet.py        # skill 原文 → sheet.png 640×320（左格=idle_down 原像素，右格纯白）
# prompt = q_battle_idle_sheet2.txt 两处机械替换（skill 步骤 2）：
#   ①「持剑战斗待机帧」→「战斗待机帧」  ②「长剑出鞘握在右手中（剑尖朝前斜下方），」→「双手握空拳成戒备姿势，」
source ~/.zshrc && python3 scripts/mxai_img2img.py assets/_trial_20260903/c03/sheet.png \
  assets/_trial_20260903/c03/battle_idle_fist_raw.png \
  --prompt-file assets/_trial_20260903/c03/prompts/battle_idle_fist.txt
python3 assets/_trial_20260903/c03/check_left.py        # skill 原文
```

**结果**：**门失败**。返回图 **1440×2560（9:16）**，非 640×320 / 非 2:1（E-GEN-06，c03 异常分支明文"锚定表被改布局→上报"）；`check_left.py` 输出 `DIFF=(0, 0, 240, 320)`（左格锁死校验不过）。skill 允许 E-ANCHOR-01 重跑 1 次，但任务书无人值守铁律"任何门失败不重摇"优先 → 停上报，未重跑。

**程序化布局实证**（非目测）：内容行 y744-1704（上下大白边）、两主体并列：左主体包络 (208,744)-(588,1684) h=940，右主体 (824,784)-(1256,1700) h=916，**高度比 R/L=0.974 ∈ [0.9,1.1]**——即：内容本身是"左右双格、比例一致"的合格形态，但被重排进了 9:16 画布，导致以 640×320 为前提的 check_left 结构性不可能通过。
**产物**：`c03/battle_idle_fist_raw.png` + sheet/脚本/prompt · 成本 2 分（credits #2）

**自检五问**：
1. 命令逐字一致（含默认 `--aspect 9:16` 未传——skill 命令即如此）。
2. prompt 全文见 `c03/prompts/battle_idle_fist.txt`：仅 skill 规定的两处机械替换，其余逐字；武器条款 = b 案口径（skill 版「双手握空拳成戒备姿势，」，任务书转述「双手握空拳戒备姿势」——以 skill 命令级文本为准，差异登记于自检①）。
3. 自决念头：①"要不要加 `--aspect 2:1` 重跑"——**未执行**（改参数重跑=重摇=越权，任务书明令）；②"要不要自行改 check_left 适配 9:16 缩放比对"——**未执行**（校验脚本逐字照抄）。
4. 门：check_left DIFF≠0 ✗ / 尺寸非 2:1 ✗ → 停上报（未重摇、未换工具、未回退废止脚本）。
5. 产物/成本见上；credits #2 完整。

**卡点**：
- **K3-1（根因，波及 c04/c09）**：`mxai_img2img.py` 默认 `--aspect 9:16`，对 2:1（640×320）与 24:5（1440×300）输入一律返回 9:16 重排版。锚定表族 SOP 命令（skill 未传 --aspect）与 API 行为结构性冲突 → **check_left 门在现行命令下不可能通过**。待 Leo 拍板修复口径（命令加 `--aspect` 对齐输入 / 或校验脚本改为缩放感知比对 / 或接受重排版并改门定义），修复前锚定表族量产不可排期。
- **K3-2**：check_left.py 的 `resize` 兜底分支（`ret.size != (640,320)` 时裁左上角再缩放）对重排图是无意义比对，属校验脚本缺陷。
- **目验点**：样张内容（空拳戒备姿势、双格比例、画风一致性）是否可接受。

---

## 类 4 · 动作帧（空拳出招）—— ❌ 门失败（同根因）

**SOP 引用**：规范 §2.4 + skill `c04-action-frames.md`（先读 c03）

**实跑命令**：c03 门失败 → 按 skill 步骤 1 明文回退路径**回退左格 = `idle_down.png`**（报告注明）；make_sheet 复用（仅改 SRC/OUT，skill 允许的唯一改动）；prompt = c03 产物 + 唯一替换「战斗待机帧」→「出招攻击帧：右拳向前快速挥出，身体微前倾」；img2img 同款命令。

**结果**：**门失败**。返回 **1152×2048（9:16）**，`DIFF=(0,0,240,320)` ✗。程序化占用网格（36×20）实证：模型画了一条**贯穿全高的中分竖线**，左右两个竖长面板各含一个主体——"左半/右半"语义被重释为竖分栏 + 9:16 重排。
**产物**：`c04/atk_fist_raw.png` + sheet/prompt · 成本 2 分（credits #3）

**自检五问**：
1. 命令逐字一致；**「左格=类 3 产物」读法歧义已按唯一脚本化路径执行**（见卡点 K4-1）。
2. prompt = `c04/prompts/atk_fist.txt`：c03 prompt 仅一处替换，无自拟。
3. 自决念头：①"要不要从 c03 返回图切出右格战斗 idle 作左格（更贴任务书语义）"——**未执行**（无给定切帧脚本，自写=自由发挥）；②"中分线是不是机械缺陷该重跑"——按同根因 K3-1 停上报，未重跑。
4. 门：check_left 同款 DIFF≠0 ✗；停上报。
5. 产物/成本见上；credits #3 完整。

**卡点**：
- **K4-1（读法歧义登记）**：任务书"左格 = 类 3 产出的空拳战斗 idle（或 idle_down）"与 skill"类 3 产物存在→用之"——但类 3 产物是 640×320 双格**表**而非单帧，无切帧脚本则无法作左格；且类 3 门失败触发明文回退。本次执行 = 回退 idle_down（唯一零裁量路径）。若 Leo 意图是"切出 c03 右格作左格"，需补切帧步骤入 SOP。
- **目验点**：右格出招姿势是否为空拳挥出、无凭空武器。

---

## 类 5 · 武器分层（剑 2 张）—— ✅ 成功（机械门全过）

**SOP 引用**：规范 §2.5（Leo 09-03 定稿）+ skill `c05-weapon-layers.md`

**实跑命令**：
```bash
source ~/.zshrc && python3 scripts/mxai_gen.py --prompt "$(cat assets/_trial_20260903/c05/prompts/sword_sheathed.txt)" --out assets/_trial_20260903/c05/sword_sheathed_raw.png
source ~/.zshrc && python3 scripts/mxai_gen.py --prompt "$(cat assets/_trial_20260903/c05/prompts/sword_held.txt)"     --out assets/_trial_20260903/c05/sword_held_raw.png
NODE_PATH=<repo>/node_modules node scripts/mxai_web_cutout.js <raw> <out>   # ×2
python3 assets/_trial_20260903/c05/mark_anchor.py                            # skill 原文 → anchors.json
```

**结果**：成功。双 raw 1088×1920 纯白角部门 PASS；mxai 统一通道抠图两次均 OK（登录态有效）；成品门全过：
- `sword_sheathed.png` 330×582 透明底，四角 alpha=0 ✓，主体 4.0% ✓，包络 (10,260)-(320,360) = **310×100 横置**（与 prompt A"侧面水平视角"自洽）
- `sword_held.png` 330×582 透明底，四角 alpha=0 ✓，主体 8.7% ✓，包络 294×495（斜置长条）
- `anchors.json`：sheathed {anchor:[10,310], angle:90, mode:left-center}；held {anchor:[166,541], angle:45, mode:bottom-center}
- 手持态主轴程序化实测：单位向量 (0.502, -0.865) = **右上↔左下对角族，偏离竖直约 30°**（prompt 要求 45°；接受与否归 Leo，禁自判）

**产物**：2 贴图 + anchors.json + 2 prompt · 成本 4 分（credits #4/#5）

**自检五问**：
1. 生成命令逐字一致；**抠图工具与 skill 不一致**（见卡点 K5-1）：skill 步骤 5 写 `cutout_white_bg.py`，任务书 + 规范 §2.5-②-2 写"mxai 统一通道抠图"→ 按 skill 自身优先级（规范为准）走 mxai。
2. prompt A/B = skill 定稿文本逐字（首次成文配方，无改动）。
3. 自决念头：①"抠图若失败要不要回退本地 flood 脚本"——未发生（且明令禁回退）；②"30°≠45° 要不要重生成"——无权重，数据交 Leo。
4. 门：纯白角部 ×2 ✓ / 四角透明 ×2 ✓ / 主体>1% ×2 ✓ / json 合法 ✓ 全过。
5. 产物/成本见上；credits #4/#5 完整。

**卡点**：
- **K5-1（skill 勘误建议）**：skill c05 步骤 5（cutout_white_bg.py）与规范 §2.5-②-2 mxai 统一通道口径矛盾，本次按规范执行，建议修订 skill。
- **目验点**：剑造型；手持态 45° 初值是否合意（规范 §2.5-4 度数以样张目验为准）；anchor 近似点口径。

---

## 类 6 · 敌人/NPC 检测走查 —— ⚠️ 部分完成（audit ✓ / triage 阻塞）

**SOP 引用**：规范 §2.6 + skill `c06-enemy-npc.md`（检测权行使，清除不做）

**实跑命令**：
```bash
python3 scripts/audit_warmwhite_blocks.py assets/characters/enemy/shanzei_a/frames/*.png   # exit=1
python3 scripts/audit_warmwhite_blocks.py assets/characters/enemy/shanzei_b/frames/*.png   # exit=0
python3 scripts/triage_enclosed_white.py --dirs assets/characters/enemy/shanzei_a/frames   # KeyError
```

**结果**：
- **山贼甲 audit exit=1（残留报警 = 正常实证数据，只记录不清除）**：11 帧各 1 块腿部带（55%-90%）≥400px 暖白不透明块：`atk_down_2`(457px@84%)、`die_1`(541px@78%)、`idle_down_breath`(621px@76%)、`idle_down`、`idle_left_breath`、`idle_left`、`idle_side_breath`、`idle_side`、`walk_down_1`、`walk_left_2`(487px@78%)、`walk_side_2`(487px@78%)——完整清单存 `c06/audit_shanzei_a.txt`。
- **山贼乙 audit exit=0（过门）**：仅 `walk_down_2` 1 块 409px @ 高度带 52%（非腿部带，审计规则内不算残留）；完整输出存 `c06/audit_shanzei_b.txt`。
- **triage 双批失败（E-GATE-01）**：`KeyError: 'assets/characters/enemy/shanzei_a/frames'`——`--dirs` 实收**方向名**（down/up/…）且 ROOT 硬编码 `assets/characters/hero/walk_q/cut/`，按 skill 异常分支读 `--help` 后确认**该脚本不支持敌人帧目录**，登记上报（禁改脚本、禁写替代）。

**成本**：0（纯检测）

**自检五问**：
1. 命令逐字一致；triage 按 skill 异常分支"读用法后执行"处置至"仍失败→E-GATE-01"为止。
2. 无 prompt。
3. 自决念头："要不要改 triage 的 ROOT/POSES 或写个临时脚本对敌人帧复刻分诊逻辑"——**未执行**（改脚本/自写工具均越权），上报。
4. 门：audit 甲 exit=1（= 残留在案的实证数据，正是本类要的产出）/ 乙 exit=0；triage 阻塞如实登记。
5. 产物 = c06/ 下 6 个记录文件（4 输出 + 2 exit 码）；0 分。

**卡点**：
- **K6-1**：`triage_enclosed_white.py` 为 hero walk_q 专用（ROOT+POSES 硬编码），敌人帧封闭白分诊在现行工具下**不可执行**——SOP c06 步骤 2 与工具能力不符，需 Leo 裁决（扩脚本 or 该步骤从敌人走查中移除）。
- **目验点**：甲批 11 帧腿部带残留是否走 mxai 统一通道清除（清除权在 Leo/mxai，本轮未动）。

---

## 类 7 · 场景底图压缩 —— ✅ 成功（P-4 实证）

**SOP 引用**：规范 §2.7 + skill `c07-scene-bg.md`（脚本逐字照抄）

**实跑命令**：`python3 assets/_trial_20260903/c07/compress_candidates.py`（skill 原文）

**结果**：
| 候选 | 工具/参数 | 尺寸 | 体积 | ≤300KB |
|---|---|---|---|---|
| 母版（不动） | — | 1088×1920 | **3,251,380 B（3.10MB）** | — |
| A | PNG 降采样宽 720，LANCZOS，optimize | 720×1271 | 1,465,950 B（1.40MB） | ✗ 超额 |
| B | **WebP quality=80 原尺寸** | 1088×1920 | **233,350 B（228KB）** | ✓ 达标 |

**产物**：`c07/env_pure_down720.png` + `c07/env_pure_q80.webp` · 成本 0

**自检五问**：
1. 脚本逐字一致；**产物份数与任务书字面不一致**（任务书"压缩 1 份"，skill 定稿"双候选供 Leo 比选、不替 Leo 定口径"）——按 skill 执行（P-4 本要双口径比选，0 成本），登记 K7-1。
2. 无 prompt。
3. 自决念头："A 超标了要不要再压一档更小的 png"——**未执行**（skill 明令禁继续压到失真，两候选如实交割）。
4. 门：两候选产出 + 体积记录齐全；无失败。
5. 产物/成本见上；0 分。

**卡点**：
- **K7-1（口径差异登记）**：任务书"1 份" vs skill"双候选"，按 skill 双候选执行（哪条路线能接受 = P-4 裁决）。另注：WebP 在微信小游戏 canvas 的兼容性需 FE 侧确认（美术侧无法验证）。
- **目验点**：B（webp q80）画质是否可接受。

---

## 类 8 · 六边形瓦片双样张 —— ✅ 成功（P-2 裁决素材齐）

**SOP 引用**：规范 §2.8 + skill `c08-hex-tiles.md` + 代码契约 `config/battle-hex.ts` TILE_SPEC / `ui/battle-hex-render.ts` tilePath（唯一真值）

**a) 程序化样张**（`scripts/gen_hex_tileset.py` flat→pointy 改写，diff 138 行全文见本节末）：
- 实跑：`python3 scripts/gen_hex_tileset.py` → 输出 4 件：`hex_grass.png` / `hex_dirt.png`（各 **88×69** = 顶面 62 + 侧面 7）、`hex_tiles_2x1.png`（176×69）、`hex_tiles_preview_4x4.png`（416×227，16 块交错）
- 几何程序化实测：y=0 顶尖单像素 (43,43) ✓ 尖朝上下；y=15/31/46 不透明行全宽 0-87 ✓ **左右竖直边贴 ±44（宽恰 88，tilePath 同构）**；y=61 底尖窄行 ✓；不透明行 0→63。
- 拼贴算式与 hexToWorld 同源：px=(col+奇行0.5)×88，py=row×46（=round(62×0.75)）。

**b) AI 样张**：`mxai_gen.py --prompt "$(cat assets/ui/pixel/battle/prompts/tileset_v2.txt)"`（原文逐字禁改 ✓）→ `tileset_ai_raw.png` **1152×2048 RGB 不透明**（prompt 要求"2×1 横排 + 透明背景"两项形态均未满足：9:16 竖版、无 alpha；skill 预告"AI 图透明度不保证"，如实记录；角部 (241,241,241)/(239,239,236)/(242,241,241)/(249,249,250) 近白灰非纯白）。
**对照板**：`compare_board.png`（6496×2128，左=程序化 2×1 NEAREST 放大至 AI 原生高，右=AI 原生）。
**产物**：c08/ 7 件 + diff · 成本 2 分（credits #6）

**自检五问**：
1. 改写严格按【改写规格表】4 项 + 输出落 trial 目录（隔离铁律要求）；**两处几何读法裁定登记**（K8-1/K8-2）。
2. AI prompt = tileset_v2.txt 逐字，未改一字。
3. 自决念头：①"字面 ANGLES(30..330) 配原参数化只得 76.2px 宽，要不要就按字面交差"——**未按字面**（与 skill 自称的契约"左右竖直边在 ±hw、宽 88"冲突，改用 RX=HW/cos30° 使角度制与契约顶点**同时**成立，diff 内注释在案）；②"尖端 1px 圆整截断要不要顺手修"——**未修**（常量是规格表定死的，CY=HH 使底尖落 y=62 恰在 62 高顶面画布外 1px，侧面带最深顶点 (44,69) 同理出画布 → 实测不透明行止于 63；量产版若采程序化路线应修，登记 K8-2）；③"compare board 同高锚 69 还是 2048"——排版细节选 AI 原生高（保目验细节），登记。
4. 门：脚本无异常退出 ✓ / 尺寸=规格算式 ✓ / 尖角朝上下逐像素实测 ✓（方向机械门全过）；观感 = Leo。
5. 产物/成本见上；credits #6 完整。

**卡点**：
- **K8-1（规格表文字 vs 契约几何）**：改写规格表第 1 条的字面角度值与第 2/代码契约的"宽 88 竖直边"在原参数化下不可兼得，本次以契约为准实现（RX 修正），建议 skill 修订文字。
- **K8-2（1px 圆整）**：见自检③；另 TEX_PERIOD=32 不再整除 88（纹理周期性声明弱化）、颗粒数按原逻辑在 88×62 画布上密度约为旧版 4.7 倍——均按"保留原纹理逻辑色值不动"未调，量产定稿时一并议。
- **目验点**：路线 a vs b（P-2 关闭动作）；程序化侧厚度/色值；AI 侧形态缺陷（竖版/不透明）是否直接否决 b 路线。

**`scripts/gen_hex_tileset.py` diff 全文**（= `c08/gen_hex_tileset.diff`）：

```diff
diff --git a/scripts/gen_hex_tileset.py b/scripts/gen_hex_tileset.py
index 5350e90..7141506 100644
--- a/scripts/gen_hex_tileset.py
+++ b/scripts/gen_hex_tileset.py
@@ -1,28 +1,33 @@
 #!/usr/bin/env python3
 """六边形战棋瓦片集生成（T16 拼贴贴图）——程序化绘制，几何精确。
 
-规格（Leo 2026-09-02）：平顶六边形纵向压扁（宽:高≈1:0.63），底部深棕立体侧面
-（厚≈总高13%），草地/土路两种顶面材质（布纹+颗粒+描边+上亮下暗），2×1 横排，
-轮廓逐像素一致、可无缝拼贴，透明背景。
-输出：瓦片集 + 单块 + 4×4 拼贴验证图 + 拼贴参数。
+规格（Leo 2026-09-03 试产改写：pointy-top，对齐 config/battle-hex.ts TILE_SPEC
+与 ui/battle-hex-render.ts tilePath）：尖角朝上/朝下的压扁六边形（上下尖角、左右
+竖直边），宽:高=1:0.7，底部深棕立体侧面（厚=顶面高×12%），草地/土路两种顶面材质
+（布纹+颗粒+描边+上亮下暗），2×1 横排，轮廓逐像素一致，透明背景。
+输出：瓦片集 + 单块×2 + 4×4 拼贴验证图 + 拼贴参数（试产期落 _trial 目录，不入库）。
 """
 import math, random
 from PIL import Image, ImageDraw
 
-# ---- 规格 ----
-TILE_W = 256                          # 单瓦片宽（2s，s=半宽=128）
-SQUASH = 0.63                         # 纵向压扁系数（宽:高=1:0.63）
-TOP_H = round(TILE_W * SQUASH)        # 顶面高 = 161
-SIDE = round(TOP_H * 0.13 / 0.87)     # 侧面厚 ≈ 总高13% → 24
-TILE_H = TOP_H + SIDE                 # 总高 185
+# ---- 规格（pointy-top，契约=config/battle-hex.ts TILE_SPEC）----
+TILE_W = 88                           # 单瓦片宽（横向平边间距，TILE_SPEC.w）
+SQUASH = 0.70                         # 纵向压扁系数（宽:高=1:0.7，TILE_SPEC.hRatio）
+TOP_H = round(TILE_W * SQUASH)        # 顶面高 = 62（尖到尖）
+SIDE = round(TOP_H * 0.12)            # 侧面厚 = 顶面高×12% → 7（TILE_SPEC.sideRatio）
+TILE_H = TOP_H + SIDE                 # 总高 69
+ROW_H = round(TOP_H * 0.75)           # 拼贴行距 = 46（TILE_SPEC.rowRatio）
 HW, HH = TILE_W / 2, TOP_H / 2        # 半宽/半顶面高
 CX, CY = HW, HH                       # 顶面中心（瓦片内）
 
-# 平顶六边形顶点角度（度）：右尖/右下/左下/左尖/左上/右上
-ANGLES = [math.radians(a) for a in (0, 60, 120, 180, 240, 300)]
+# 尖顶六边形顶点角度（度）：右下/下尖/左下/左上/上尖/右上
+# 注：x 半径需除以 cos30°，使左右竖直边落在 ±HW（宽恰为 TILE_W，与 tilePath 一致；
+# 若直接用 HW·cos(30°) 参数化会得到 76.2px 宽，与代码契约"左右竖直边在 ±hw"冲突）
+ANGLES = [math.radians(a) for a in (30, 90, 150, 210, 270, 330)]
+RX = HW / math.cos(math.radians(30))  # 顶点 x 半径（竖直边贴 ±HW）
 
 def hex_pts(cx, cy):
-    return [(cx + HW * math.cos(a), cy + HH * math.sin(a)) for a in ANGLES]
+    return [(cx + RX * math.cos(a), cy + HH * math.sin(a)) for a in ANGLES]
 
 def lerp(c1, c2, t):
     return tuple(round(a + (b - a) * t) for a, b in zip(c1, c2))
@@ -72,11 +77,11 @@ def make_top(material):
     overlay = Image.new('RGB', (TILE_W, TOP_H), edge)
     top.paste(overlay, (0, 0), edge_band)
     top.putalpha(mask)
-    # 顶缘受光亮线（顶面上边两条水平边）
+    # 顶缘受光亮线（尖顶版：上尖两侧的两条上斜边）
     dd = ImageDraw.Draw(top)
     pts = hex_pts(CX, CY)
-    dd.line([pts[4], pts[5]], fill=lerp(base_hi, (255,255,230), 0.35), width=2)
-    dd.line([pts[5], pts[0]], fill=lerp(base_hi, (255,255,230), 0.2), width=2)
+    dd.line([pts[3], pts[4]], fill=lerp(base_hi, (255,255,230), 0.35), width=2)
+    dd.line([pts[4], pts[5]], fill=lerp(base_hi, (255,255,230), 0.2), width=2)
     return top
 
 def make_side(material):
@@ -94,8 +99,8 @@ def make_side(material):
 def make_tile(material):
     tile = Image.new('RGBA', (TILE_W, TILE_H), (0, 0, 0, 0))
     pts = hex_pts(CX, CY)
-    # 侧面：下半轮廓（右尖→右下→左下→左尖）挤出
-    lower = [pts[0], pts[1], pts[2], pts[3]]
+    # 侧面：下半轮廓（尖顶版：右下→下尖→左下，两条下斜边）挤出
+    lower = [pts[0], pts[1], pts[2]]
     lower_bot = [(x, y + SIDE) for (x, y) in reversed(lower)]
     sd = ImageDraw.Draw(tile)
     sd.polygon(lower + lower_bot, fill=(0, 0, 0, 255))
@@ -108,40 +113,42 @@ def make_tile(material):
     # 顶面
     top = make_top(material)
     tile.paste(top, (0, 0), top)
-    # 侧面顶缘暗线（顶/侧交界的转折感）
+    # 侧面顶缘暗线（顶/侧交界的转折感；尖顶版两条下斜边）
     dd = ImageDraw.Draw(tile)
-    dd.line([pts[3], pts[2]], fill=(30, 22, 14), width=2)
     dd.line([pts[2], pts[1]], fill=(30, 22, 14), width=2)
     dd.line([pts[1], pts[0]], fill=(30, 22, 14), width=2)
     return tile
 
-# ---- 生成 2×1 横排 ----
+# ---- 生成 2×1 横排（试产输出落 _trial 隔离目录，不入库正式 tileset/）----
+import os
+OUT_DIR = 'assets/_trial_20260903/c08'
+os.makedirs(OUT_DIR, exist_ok=True)
 grass = make_tile('grass')
 dirt = make_tile('dirt')
 sheet = Image.new('RGBA', (TILE_W * 2, TILE_H), (0, 0, 0, 0))
 sheet.paste(grass, (0, 0))
 sheet.paste(dirt, (TILE_W, 0))
-import os
-os.makedirs('assets/ui/pixel/battle/tileset', exist_ok=True)
-sheet.save('assets/ui/pixel/battle/tileset/hex_tiles.png')
+grass.save(f'{OUT_DIR}/hex_grass.png')       # 单块 grass
+dirt.save(f'{OUT_DIR}/hex_dirt.png')         # 单块 dirt
+sheet.save(f'{OUT_DIR}/hex_tiles_2x1.png')
 print(f'tileset: {sheet.size}, 单块 {TILE_W}x{TILE_H}（顶面 {TOP_H}+侧面 {SIDE}），压扁比 1:{SQUASH}')
 
-# ---- 4×4 拼贴验证图（无缝性检查）----
-dx = TILE_W * 0.75          # 列间距 = 0.75 宽
-dy = TOP_H                  # 行间距 = 顶面高（奇数行偏移半宽）
+# ---- 4×4 拼贴验证图（契约拼贴算式：px=(col+奇行0.5)×TILE_W，py=row×ROW_H，hexToWorld 同源）----
 COLS, ROWS = 4, 4
-board = Image.new('RGBA', (int(dx * COLS + TILE_W * 0.25) + 20, int(dy * (ROWS - 1) + TILE_H) + 20), (40, 34, 26))
-bd = ImageDraw.Draw(board)
+MARGIN = 10
+board_w = MARGIN * 2 + int((COLS - 1 + 0.5) * TILE_W) + TILE_W
+board_h = MARGIN * 2 + (ROWS - 1) * ROW_H + TILE_H
+board = Image.new('RGBA', (board_w, board_h), (40, 34, 26))
 r = 0
 for row in range(ROWS):
-    for col in range(COLS + 1):
-        cx = 10 + col * dx + (dx / 2 if row % 2 else 0)
-        cy = 10 + row * dy
+    for col in range(COLS):
+        cx = MARGIN + (col + (0.5 if row % 2 else 0)) * TILE_W   # 顶面中心 x
+        cy = MARGIN + row * ROW_H                                # 顶面中心 y
         t = grass if (row + col) % 2 == 0 else dirt
-        board.paste(t, (int(cx - HW), int(cy - HH)), t)
+        board.paste(t, (int(cx - HW), int(cy - TOP_H / 2)), t)
         r += 1
-board.save('assets/ui/pixel/battle/tileset/hex_tiles_preview.png')
+board.save(f'{OUT_DIR}/hex_tiles_preview_4x4.png')
 print(f'拼贴验证: {board.size}（交错 {r} 块）')
 
-# ---- 拼贴参数（供 T16 渲染）----
-print(f'拼贴参数: 列步进 dx={dx:.0f}px（0.75宽），行步进 dy={dy}px（顶面高），奇数行偏移 {TILE_W//4}px（半宽/2）')
+# ---- 拼贴参数（供 T16 渲染；与 config/battle-hex.ts hexToWorld 一致）----
+print(f'拼贴参数: 列步进 dx={TILE_W}px（整宽），行步进 dy={ROW_H}px（0.75×顶面高），奇数行偏移 {TILE_W // 2}px（半格）')
```

---

## 类 9 · UI 组件（topbar 无字重切）—— ❌ 门失败（尺寸漂移）

**SOP 引用**：规范 §2.9 + skill `c09-ui-components.md`

**实跑命令**：prompt = `topbar_v4_base.txt` + 唯一替换（首句「小虾米」「辰时」「8岁 1月」和所有「8888」→「画面中的所有战斗界面文字与数字（回合/数值/按钮文字等）」）；`mxai_img2img.py assets/ui/pixel/battle/components/topbar.png c09/topbar_noword_raw.png --prompt-file <配方>`。

**结果**：**门失败（E-GEN-06）**。源 1440×300（24:5）→ 返回 **1088×1920（9:16）**，尺寸漂移；skill 允许重跑 1 次，任务书禁 → 停上报未重跑。逐像素 diff 校验因尺寸不符无法执行（记录）。
**产物**：`c09/topbar_noword_raw.png` + prompt · 成本 2 分（credits #7）

**自检五问**：
1. 命令逐字一致（默认 --aspect 9:16 同 K3-1 根因）。
2. prompt = `c09/prompts/topbar_battle_noword.txt`：仅首句一处替换，其余条款（含家场景组件清单）逐字保留——skill 明文"其余条款逐字不动"，家场景专属条目对战斗版的不匹配属 skill 预告的"去字不彻底→记录交 Leo"范畴。
3. 自决念头："要不要删掉后面明显是家场景的条目让 prompt 更贴合战斗版"——**未执行**（skill 禁自拟新配方，配方改写=拍板项）。
4. 门：尺寸=1440×300 ✗ → E-GEN-06 停上报，未重摇。
5. 产物/成本见上；credits #7 完整。

**卡点**：
- **K9-1**：K3-1 同根因（img2img 默认 9:16 重排横版组件）。修复口径同前：--aspect 对齐（源 24:5 → 传 `--aspect` 自定义比例是否被 API 接受需验证）。
- **目验点**：返回图内 topbar 是否已无字（横版组件被竖版画布包裹后的可用性）。

---

## 类 10 · 图标（背包样板）—— ✅ 成功（风格门客观通过）

**SOP 引用**：规范 §2.10 + skill `c10-icons.md`

**实跑命令**：prompt = `item_dao.txt` + 唯一替换（主体句→背囊句）；`mxai_gen.py --prompt "$(cat .../icon_beibao.txt)" --out c10/beibao_raw.png`；`process_item_icons.py c10/beibao_raw.png c10/beibao.png`。

**结果**：成功。raw 1088×1920；**加工风格门 exit=0**：`stats={'avg_rgb': (207,175,82), 'hue_diff_deg': 1.0, 'val_diff': 0.025, 'sat': 0.604, 'coverage': 0.209}`；成品 **128×128 RGBA 透明底，四角 alpha=0** ✓。
**产物**：`c10/beibao.png` + raw + prompt · 成本 2 分（credits #8）

**自检五问**：
1. 命令逐字一致，无偏差。
2. prompt = `c10/prompts/icon_beibao.txt`：族语言条款逐字，仅主体句替换（skill 给死的文本）。
3. 自决念头："风格门输出参数要不要人工复核一遍色值"——未加戏（门是客观的，exit=0 即过；好不好看归 Leo）。
4. 门：process 风格门 exit=0 ✓ / 128×128 透明 ✓ 全过，一次过无重跑。
5. 产物/成本见上；credits #8 完整。

**卡点**：无。
**目验点**：背包造型/族风格是否定视觉样板（过门≠定稿，规范 §2.10-1 样板先行）。

---

## 类 11 · 头像双路线 —— ✅ 成功（P-7 裁决素材齐）

**SOP 引用**：规范 §2.11 + skill `c11-avatars.md`

**实跑命令**：路线 A = `crop_avatar.py`（skill 原文：顶部 40% 头带 → 包络正方 → 256×256 LANCZOS）；路线 B = prompt C（skill 原文）`mxai_gen.py` → `cutout_white_bg.py` → 规格化（主体包络短边满 256 居中裁切）→ 对照板。

**结果**：
- 路线 A：`avatar_cut.png` 256×256 ✓（0 分）
- 路线 B：raw 1440×2560；角部上=米色 (247,222,182)（**prompt C 明文"纯色浅米背景"所致**，E-GEN-05 字面命中——见卡点 K11-1）、下=深色主体（胸像顶到底）；`cutout_white_bg.py` exit=0（透明 34.6%）；规格化 `avatar_gen_256.png` 256×256（包络 1440×2317 满全宽/90.5% 高，scale 0.178，规格化后主体覆盖 80.2%）
- 对照板：`compare_board.png` 584×304（左 A 右 B）
**产物**：A/B 两样张 + raw/中间件/prompt/对照板 · 成本 2 分（credits #9，A 记 0）

**自检五问**：
1. 脚本/prompt 逐字一致；规格化"第三函数"skill 文本不存在（crop_avatar.py 为顶层脚本无函数），按其给死规则文字（主体包络短边满 256）实现，登记 K11-2。
2. prompt C = skill 定稿逐字。
3. 自决念头：①"路线 B 抠图后包络贴满全宽、疑似边缘米色残留，要不要清"——**未清**（清除权废止归 mxai；数据交 Leo）；②"E-GEN-05 命中要不要中止路线 B"——未中止（米色底是 prompt C 自身要求，属 skill 内部矛盾而非生图异常，按"如实记录+登记"处置）。
4. 门：A 空图检查 ✓；B cutout exit=0 ✓ / 256×256 ✓；两路线优劣=纯审美 → 全部交 Leo（无合格判定权）。
5. 产物/成本见上；credits #9 完整。

**卡点**：
- **K11-1（skill 内部矛盾）**：prompt C 定死"纯色浅米背景"与 cutout_white_bg（纯白假设）/E-GEN-05（非纯白即上报）互相冲突。本次 cutout 实跑成功（米色在 flood 容差内），但口径需 Leo 定：生成底色改纯白，或头像类抠图也走 mxai 统一通道。
- **K11-2（skill 文本缺陷）**：c11 步骤 4"脚本同文件第三函数"指向不存在的函数，规则已按文字口径实现，建议修订 skill。
- **目验点**：量产路线 A vs B（P-7 关闭动作）；B 边缘残留情况。

---

## 类 12 · 特效 —— N/A

类 12 = 纯代码体系（`config/battle-hex.ts` CHOREO/FX + proto 在跑），无美术资产，本次 N/A，**0 生图 0 分**。美术侧职责 = 0；贴图类特效（EF-01/EF-02）立项前禁预写禁预产。

---

## 汇总

### 12 类走查总表

| 类 | 类别 | 结果 | 生图 | 卡点/登记数 | 关键登记 |
|---|---|---|---|---|---|
| 1 | 行走帧·无武器锚 | ✅ 成功 | 1（2分） | 1 | K1-1 skill 删除式 vs 规范显式条款（按规范执行） |
| 2 | 呼吸帧 | ✅ 成功 | 0 | 0 | — |
| 3 | 战斗待机锚定表 | ❌ 门失败（样张仍交目验） | 1（2分） | 2 | K3-1 **img2img 默认 9:16 重排（根因）**、K3-2 check_left 结构性失效 |
| 4 | 动作帧·空拳出招 | ❌ 门失败（同根因，已按明文回退左格） | 1（2分） | 1 | K4-1「类 3 产物作左格」读法歧义 |
| 5 | 武器分层·剑×2 | ✅ 成功 | 2（4分） | 1 | K5-1 skill 抠图工具行 vs 规范 mxai 口径（按规范执行） |
| 6 | 敌人/NPC 检测 | ⚠️ audit✓/triage 阻塞 | 0 | 1 | K6-1 triage 脚本不支持敌人目录；甲批 11 帧腿部带残留在案 |
| 7 | 场景压缩 | ✅ 成功 | 0 | 1 | K7-1 任务书"1 份" vs skill 双候选（按 skill 执行）；B=228KB 达标 |
| 8 | 瓦片双样张 | ✅ 成功 | 1（2分） | 3 | K8-1 角度字面 vs 契约几何（按契约）；K8-2 尖端 1px 圆整；AI 样张竖版无透明 |
| 9 | topbar 无字重切 | ❌ 门失败（尺寸漂移） | 1（2分） | 1 | K9-1 = K3-1 同根因 |
| 10 | 图标·背包 | ✅ 成功 | 1（2分） | 0 | 风格门 exit=0 一次过 |
| 11 | 头像双路线 | ✅ 成功 | 1（2分） | 2 | K11-1 浅米底 vs 纯白假设矛盾；K11-2"第三函数"不存在 |
| 12 | 特效 | N/A | 0 | 0 | — |

**生图合计：9 张 = 18 分**（gpt-image-2 标准版 1K，与任务书定死量分毫不差；重摇 0 次；明细见 `assets/_trial_20260903/credits.json`）。

### 「0 自由发挥是否成立」总结论

**成立的环节（命令逐字可复现、零裁量到底）**：类 2（纯程序化）、类 5（单体生成+mxai 抠图+程序化锚点）、类 7（压缩双候选）、类 8 程序化段（几何契约化）、类 10（客观风格门一次过）、类 11 双路线产出段、类 6 audit 段。**这些类的主客二分（冻结客观条款+数据化门+重摇权归 Leo）实测成立。**

**不成立的环节（SOP 照抄也过不了门，需先修口径）**：
1. **锚定表族（类 3/4）与横版组件重切（类 9）**：`mxai_img2img.py` 默认 `--aspect 9:16` 会把任意输入重排为 9:16 竖版——skill 命令未传该参数，check_left/尺寸门**结构性不可能通过**。这是本轮最大发现：**SOP 命令级可复现 ✓（同样输入同样输出），但可复现的是"失败"**。修复三选一归 Leo：命令显式传 `--aspect`（需验证 API 支持任意比）/ 校验脚本改缩放感知 / 门定义改为接受重排版。
2. **skill 与规范/任务书冲突 3 处**（K1-1/K5-1/K7-1）：均按 skill 自身声明的"规范为准"优先级执行并登记，未自行调和——冲突本身说明 skill 修订滞后于规范 v1.1/v1.2。
3. **工具能力缺口 1 处**（K6-1：triage 不支持敌人目录）。
4. **文本缺陷 2 处**（K4-1 左格读法歧义、K11-2"第三函数"不存在）。

**总评**：代理侧 0 自由发挥**在"能照抄的命令"上成立**（本轮流经的每一个"要不要 X"念头都已登记且均未执行）；但 SOP 的**完备性**不达标——9 张生图里 3 张（c03/c04/c09）因命令缺陷必然过不了门。修完 K3-1 一项即可救活三类。

### 拍板建议（待 Leo）

1. **P-2（瓦片路线）**：双样张已齐（程序化 4 件 + AI 1 件 + 对照板 `c08/compare_board.png`）。裁决时注意：AI 样张为 9:16 竖版、RGB 不透明、角部非纯白——"2×1 横排 + 透明背景"两项形态要求均未满足（与 K3-1 同根因 + 模型不输出 alpha）；程序化样张几何与代码契约逐像素一致（尖端 1px 圆整瑕疵在案 K8-2）。
2. **P-7（头像路线）**：对照板 `c11/compare_board.png` 已齐；附带需裁 K11-1（浅米底 vs 抠图纯白假设）。
3. **P-3（63→72 张 144 分重算）**：**先修 K3-1 再排期**——锚定表族是动作帧/战斗 idle 的唯一标准方法（规范 §2.3），当前命令下量产会系统性翻车；修复+复跑试产门后才可放量。
4. **P-4（压缩口径）**：候选 B（WebP q80 原尺寸 228KB）达标、候选 A（PNG 宽 720）1.40MB 超额近 5 倍——若必须 PNG 则需更激进的降采样/调色板（本轮禁继续压，未做）；WebP 兼容性需 FE 确认。
5. **K6（敌人残留）**：山贼甲 11 帧腿部带暖白块（442~621px）在案，清除走 mxai 统一通道（本轮只行使检测权）。
6. **武器规格 §2.5**：已定稿无需再议；本轮剑 2 张全门通过，45° 初值样张已出（实测主轴约 30° 偏竖直，度数取舍见样张）。

### 附：Leo 目验样张总索引（contact_sheet_all.png 网格序，行优先）

| 格位 | 文件 | 说明 |
|---|---|---|
| 1 | `assets/_trial_20260903/c01/anchor_noweapon_raw.png` | 无武器锚（b 案口径） |
| 2 | `assets/_trial_20260903/c03/battle_idle_fist_raw.png` | 空拳战斗待机锚定表（门失败样张） |
| 3 | `assets/_trial_20260903/c04/atk_fist_raw.png` | 空拳出招锚定表（门失败样张） |
| 4 | `assets/_trial_20260903/c05/sword_sheathed.png` | 剑·腰挂态（透明底） |
| 5 | `assets/_trial_20260903/c05/sword_held.png` | 剑·手持态（透明底，45° 初值） |
| 6 | `assets/_trial_20260903/c09/topbar_noword_raw.png` | topbar 无字重切（门失败样张） |
| 7 | `assets/_trial_20260903/c08/hex_tiles_2x1.png` | 瓦片·程序化 2×1 |
| 8 | `assets/_trial_20260903/c08/hex_tiles_preview_4x4.png` | 瓦片·程序化 4×4 拼贴 |
| 9 | `assets/_trial_20260903/c08/tileset_ai_raw.png` | 瓦片·AI 样张 |
| 10 | `assets/_trial_20260903/c10/beibao.png` | 背包图标成品（128×128） |
| 11 | `assets/_trial_20260903/c11/avatar_cut.png` | 头像·路线 A 切图 |
| 12 | `assets/_trial_20260903/c11/avatar_gen_256.png` | 头像·路线 B 生成 |

其余交付件：`assets/_trial_20260903/credits.json`（9 条 18 分）；各 `cNN/prompts/*.txt`（可复用配方）；`c05/anchors.json`；`c06/*.txt`（检测记录）；`c07/*`（压缩候选）；`c08/gen_hex_tileset.diff`；`c11/compare_board.png`。
