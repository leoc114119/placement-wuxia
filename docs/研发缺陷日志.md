# 研发缺陷日志（记录即素材）

> 规范建立：Leo 2026-09-09 令。每个 bug 结束追加一条，格式与特别要求见 §0。
> 定位：LOG.md 记事件流水，本文件记 bug 维度的深记录（根因/轮次/教训），二者双写。
> 红名单与《行为测试红名单》（活缺陷登记簿）互引，不许假装通过。

## 0. 条目格式规范

```markdown
### [日期] BUG-ID 描述
- 现象：（用户视角）
- 复现条件：
- 测试状态：单测 X/X —— 全绿但仍有问题必须标红
- 根因：文件:行号 + 原因
- 修复过程：第1次(commit)→结果→第2次→...
- 复发次数：
- 关键数字：轮次/用例数/耗时
- 可引用原文：
```

特别要求：
1. 🔴「测试全绿但缺陷仍在」单独标注 + 记录为什么没拦住
2. 记录 patch 冲突（修 A 破 B）
3. 截图命名 before-fix/after-fix
4. 未修 bug 进红名单（§1），不许假装通过
5. 记录 AI 的错误判断原文
6. 每日一行（§2）：修复轮次/新增/未修

## 1. 红名单（未修 / 挂账，不许假装通过）

| BUG-ID | 描述 | 状态 | 阻塞点 |
|---|---|---|---|
| BUG-20250909-07 | hero 出招剑层出画布截断 | 🔴 修复方向已定（v2.1 独立剑模运行时合成 3aa004dd 已并 main），**完整度待 Leo 长剑 L 环终验**；第二批 cast 帧已同接口接入 | 无（等终验） |
| BUG-20250908-06 | 段 1 致胜终局快照缺口（AS-9：scheduleSkillCast 在建 cast 前 return，表现层无 T 可读） | 兜底 TRIAL_FX_FALLBACK_DURATION_MS=3000+warn 运行中 | 根除需 session 侧补 cast 身份字段（结算域另账） |
| BUG-20250909-09 | preview loader 并发竞态（fetch 完成序抖动→偶发 module not preloaded） | 既有基线缺陷，非 T25~T29 引入；基线 stash 5 连跑第 5 次同现 | 另立卡 |
| 观感批次 | 名条字体大小 / T27 L3 月牙被爆焰余亮覆盖 | Leo 裁后续优化批次 | 排产 |
| BUG-20250910-10 | seq=287 三帧清杂以 ROI 外扩清零削到主体轮廓（美术素材） | 🔴 已打回美术线重做（PM2 seq=280）→ 三轮已修复（commit 642bee08） | 无 |
| BUG-20250910-11 | R1 归一交付同格重复（jump 取错格）+ 尺度未达标，自检仍报 29/30 通过 | 🔴 已打回美术线重做（PM2 seq=291） | 无（等重做交付） |
| BUG-20250912-17 | seq=372 AI 细节精修交付实测更钝（−10.86%，27/30 帧），自检仍报「visibly sharper」 | 🟢 **A 方案已试产验证**（idle 六向 30 帧，合计 **+23.1%**，方法固化见 `tasks/handoff/动作帧细节精修-A方案-方法固化-20260912.md`）；待 Leo 目验后批量 | 无（等 Leo 目验） |

## 2. 每日汇总

| 日期 | 修复轮次 | 新增缺陷 | 未修 | 备注 |
|---|---|---|---|---|
| 09-08 | T25-R2×3 缺陷一轮修 / T26-R1 一轮修 | 5（其中🔴全绿假象 2） | 红名单 3 | T25/T26 验收链完整走通 |
| 09-09 | T27 施法卡帧两轮（循环→降速）| 2（🔴截图假证据 1；剑截断 1→等素材） | 红名单+1 | T27 并 main 8b6d050a；T28/T29 验收链走通 |
| 09-10 | 清杂两轮（判据与门不符 → ROI 外扩削主体）| 1（🔴自检全绿仍削主体 1） | 红名单+1 | §3 门检口径身高豁免裁定落地（宽/腾空帧）；右上/右下 20 帧其余 17 帧门检全过 |
| 09-10 | R1 归一交付打回一轮（同格重复 + 尺度未达标）| 1（🔴自检报 29/30 通过但交付为坏 1） | 红名单+1 | 归一改「固定缩放」口径落地（Leo 裁 A）；宽度上限改判画布贴合 ≤238 |
| 09-10 | 链式验证 2 轮（格式门误判停链 → 轻功比例复核）| 1（🔴误诊：病根在归一化而非生成端 1） | 红名单+1 | **BUG-12 定案：轻功头偏大真因=包围盒自适应归一放大蹲姿帧 1.21×（生成端脸宽极差仅 1.88%）**；v2 单系数口径被实证为正确解；链式编辑实测无决定性优势（建议不作主力）；旧批 63+20 帧同病待重做 |
| 09-12 | aim3 一轮定位即修（无返工） | 1（🔴全绿假象 1：帧0校验+火柴人图全过仍交付坏件） | 0 | **BUG-13 定案：aim2 源/目标「骨骼指向」参照不同口径 → Head 恒定多拧 25.78°（轻功同病）**；新增门检判据「逐骨朝向残差」；轻功 30 帧已用 aim3 重渲；walk/run 不受影响（模型自带动画，不经重定向） |
| 09-12 | aim3→aim4 二次迭代（同日） | 1（🔴门检与算法同口径自证清白 1） | 0 | **BUG-14 定案：aim3 只对齐「骨轴」→ 源 Neck 骨轴与其「Neck→Head」位移差 16.92° → 头关节位置偏、观感「往后仰」**；aim4＝有子骨骼位移用「子−自身」、叶子骨用自身 Y 轴，两侧同口径 → 22/22 残差 0.000°；idle+轻功均已 aim4 重渲；**教训：门检不能与算法同口径自证，且「同口径 ≠ 选对量」** |
| 09-12 | L/R 互换一轮修 | 1（🔴门检前提未校验 1） | 0 | **BUG-15 定案：模型 L_/R_ 命名与角色真实左右相反 → 源右手动作落到角色左手（Leo 发现「用左手砍」）**；定向实验＝只改 R_Upperarm 渲正面视图看动的是哪只；修法＝MIXAMO_TO_MODEL 加左右互换（默认开）；idle/jump/atk 三组已重渲；**教训：门检的前提假设必须本身被校验 + 测左右必须用正面视图** |
| 09-12 | AI 细节精修一轮（打回） | 1（🔴无量化判据的目视假 PASS 1） | 红名单+1 | **BUG-17 定案：`image_gen` 输出预算 ≈1.5MP（994×1582 vs 输入 1224×1948 = 0.812×）→ 整图送 30 格，每格只分到 195×260 = 源 59% 像素 → 净降采样，实测锐度 −10.86%**；同时排除"PNG-8 量化"（−0.4%）与"我方后处理"（+19%）两嫌疑 |
| 09-12 | A 方案试产验证（1 轮即通过） | 0（BUG-17 同案闭环） | 红名单−1 | **A 方案＝一条方向一张图（3×2 网格 / 图纸 1024×1024 / 每格 341×512 = 源像素 2.27×）+ 锁尺寸 prompt + 逐帧轮廓配准 + 回填原 alpha**；模型回传 1280×1280（输入的 1.25×）；六向全部提升（+0.6%~+64.8%），合计 **+23.1%**；PNG-8 lossless 30/30；★**判据口径修正：主体内部须腐 3px 而非 1px**（1px 会把源帧的硬混叠轮廓算进去 → leftdown 误判 −6.8%，实为 +2.1%） |
| 09-12 | 渲染镜像 3 轮（前两轮误修已撤销） | 1（🔴"画出来反了"被当成"骨骼映射错了" 1） | 0 | **BUG-16 定案：渲染器投影左右镜像**（相机在世界 −Z 侧、相机右向量 = −X，而 `screen_x` 用了 +X）→ 一切不对称动作左右反（攻击"用左手砍"）；**修法＝`screen_x` 取负 + 方向表 yaw 左右对调**；idle/jump/atk 三组重渲；**前两轮误修（换骨名 / 源转 180°）已全部撤销并改为默认关**；**教训：先分清"几何错了"还是"画出来反了"** |

## 3. 缺陷明细

### [2026-09-08] BUG-20250908-01 T25 光影触发只挂 skill 事件，全闪避/空放/致死不启动
- 现象：山贼施法被全闪避、或空放、或段 1 直接致胜终局时，光影完全不启动。
- 复现条件：全 miss 施法（harness hitRate<0）→ 事件流只有 miss、零 skill 事件。
- 测试状态：🔴 T25 交付单测全绿但缺陷仍在——**为什么没拦住**：触发用例 harness 恒命中，无 miss/空放/致死路径用例；主架构验收（seq=156）补齐矩阵后立即暴露。
- 根因：ui/fx-player.ts `tryStartCastFx` 挂 `e.type==='skill'`；session 非空 cast 逐目标发 skill（命中）或 miss（闪避），全闪避时零 skill 事件可挂。
- 修复过程：R2 `c189741` → 新增 FxCastGate（skill+miss 双源+三拦截）→ 415 全绿+主架构复审 PASS（seq=162）。
- 复发次数：0（触发矩阵 4 路径回归锁）。
- 关键数字：1 轮修复；新增触发矩阵用例 4+gate 去重纯逻辑；seq=156/162。
- 可引用原文：方案 §5.1"非伤害事件监听——段 1 是否命中、目标是否为空、暴击与否都不改变光影时间轴"。

### [2026-09-08] BUG-20250908-02 段 2 结算事件二次误触发（伴生缺陷）
- 现象：一次施法光影启动 seq=2（应恰 1 次）。
- 复现条件：真浏览器同一事件流含段 1+段 2 全部事件。
- 测试状态：🔴 旧版（只挂 skill）同样潜伏——双源化后必现才暴露。
- 根因：段 2 结算事件（AS-4：t=finishAtSec=t0+T，逐目标再发 skill/miss，与段 1 事件形态完全同构）被 gate 当成新 cast；且该时刻 cast 已从 pendingCasts 移除，还会走进"无快照"分支。
- 修复过程：R2 `c189741` → 段 2 拦截 `|evT−(t0+T)/1000|≤0.011`（R2 复验：T=500ms 边界 ±0.011 拦/±0.012 放行实测正确）。
- 复发次数：0（"同一事件流 seq=1"真浏览器锁+单测锁）。
- 关键数字：与 BUG-01 同轮修复；主架构边界实测 T=500ms。
- 可引用原文：主架构复审"最短 T=500ms 实测边界正确"。

### [2026-09-08] BUG-20250908-03 光影预载 reject 未收敛，可打崩战斗入口
- 现象：任一帧图片加载失败 → loadFxFramePack 整链 reject → loadAssets 失败。
- 复现条件：注入含 reject 的 loadImage stub。
- 测试状态：无既有用例覆盖 reject 路径（新增负例后锁）。
- 根因：loadFxFramePack 内 `await loadImage(key)` 裸调，未逐帧收敛。
- 修复过程：R2 `c189741` → 逐帧 try/catch，reject 收敛 null 槽+warn；返回 expectedCount/loadedCount 使数量不符显式暴露（DoD 断言 40/0）。
- 复发次数：0。
- 关键数字：负例 3/3（错模型/错 SHA/角色矩形 clip）后续沿用至 T29。
- 可引用原文：方案 §4.2"缺图只允许记录告警并跳过该帧，不得抛异常打崩战斗"。

### [2026-09-09] BUG-20250909-04 自动模式同刻重入，光影名条哑火（P1）
- 现象：自动托管下连续施法数次后，光影与名条永久不再出现。
- 复现条件：auto 模式 + jimin=300（施法期行动条必满）→ 同 tick 内旧 cast 收口、新 cast 受理，新 t0==旧 t1。
- 测试状态：🔴 主架构上轮复审（seq=162）明确断言"当前行动条清零与施法锁保证同一施法者不会在旧 t1 同刻重入"——**该假设被自动模式实测证伪**（AI 错误判断原文已录）。交付时 manual 模式测试全绿。
- 根因：`drainDueCasts` 收口旧 cast（removePending 释放施法锁）→ 同 tick BAR-2 ready 轮转 aiAct 受理新 cast；旧 gate 时间窗启发式 `|evT−(t0+T)|≤0.011` 在事件四元组 {type,actorId,skillId,t} 下，旧 cast 段 2 与新 cast 段 1 不可区分。
- 修复过程：R1 `f8fb7100` → 主架构方向 3：宿主 find-first 用 findCastSnapshot（session 快照队列）配 cast 身份 → castGate.shouldStart(e, cast) 身份分支精确配对；无快照分支（终局放行/段 2 拦）按时窗+skillId 比对。
- 复发次数：0（真 session auto 连放 AI 出技 ≥6 次不哑火，相邻启动间隔==T±0.06 实证）。
- 关键数字：1 轮修复；新增同刻重入矩阵用例 5；seq=171（rejected 报告）/267 前序 261。
- 可引用原文：主架构验收报告 seq=171"当前行动条清零与施法锁保证同一施法者不会在旧 t1 同刻重入"（后被证伪）。
- patch 冲突记录：tests『gate 去重』1 条签名升级 shouldStart(e,cast)——按打回工单要求显式报备，8 断言语义一一对应，"无身份不放行"是段 2 拦截的强化非放宽。

### [2026-09-09] BUG-20250909-05 hero 六向截图脚本假证据
- 现象：shot_sixdir.mjs 六张"不同方向"的 hero 六向截图实为同一帧（哈希相同）。
- 复现条件：运行 shot_sixdir.mjs 旧版即可复现（确定性）。
- 测试状态：🔴 截图"全过"但证据为假——**为什么没拦住**：shot 脚本只断言 exit 0 不断言图像差异；T27 施工时顺带修复并发现。
- 根因：shot_sixdir.mjs setUnit 双参 evaluate（playwright 只转发单 arg → Object.assign 静默 no-op），unit 从未真正切换。
- 修复过程：T27 `4bc19b81` 内修复 → 11 状态哈希互异。
- 复发次数：0。
- 关键数字：修复前 6 张同图；修复后 11 状态互异。
- 可引用原文：T27 回执"修复前 main 上六向证据即 6 张同图（哈希实证）"。注：hero 六向**功能本体** Leo 09-06 L 环真人验收过，此为脚本证据层缺陷；第一段证据补复验挂账可选。

### [2026-09-09] BUG-20250909-06 山贼施法卡静帧 → 循环过快（两轮）
- 现象：①山贼施法期间卡 atk_1 帧不动；②R1 修复后循环翻帧过快（读作加速闪）。
- 复现条件：敌方 AI 施放任意技能，观察施法全程帧表现。
- 测试状态：R1 前无帧表现断言；R1 后新增相位交替断言；R2 增"+140ms 不翻相"回归锁。
- 根因：enemy profile 降级映射 charge→atk_1（静态）/strike→atk_2（单播）；R1 修复 strike 循环 140ms（walkFrameMs）过快。
- 修复过程：R1 `717b28d4`（loopStates 数据字段：施法循环 atk_1↔atk_2，Leo 裁"复用普攻帧循环"）→ R2 `8b6d050a`（strike 步频 140→280ms 统一施法节拍，Leo："要循环，不是要加速"）。
- 复发次数：1（R1 修卡帧引出 R2 节奏问题——表现层节奏类修复必须 Leo 真人动态复验）。
- 关键数字：2 轮；charge 280ms/strike 280ms 统一；帧频回归锁"+140 不翻相"。
- 可引用原文：Leo"要循环，不是要加速啊。。"

### [2026-09-09] BUG-20250909-07 🔴 hero 出招剑层出画布截断（红名单，等素材终验）
- 现象：出招帧剑只有零头，正确剑长（Leo 红线标注）约为显示的 3~4 倍。
- 复现条件：atk 帧出招即现（素材层缺陷，确定性）。
- 测试状态：🔴 **T28 交付四门全绿+PM 视觉复验 PASS 但缺陷仍在**——为什么没拦住：①preflight 校验 manifest/SHA/尺寸一致性，无"武器是否超出身体画布"语义检查；②PM 视觉抽样的三帧（idle/atk_right_2/body_front）中 atk 帧剑刃低对比度+缩略图下截断感不明显；③方案 v1 §4.2"同矩形绘制、零旋转"裁决本身存在设计缺陷（架构层），PM 复核时未识别。
- 根因：双层——素材层：剑层 PNG bbox=(190,141,240,192) 右缘顶在画布 x=239 平切（最右列 9 个不透明像素），长剑像素在美术生成时已被 240×320 画布截掉（候选目录全 240×320 无完整版本）；管线层：方案 §4.2 武器层与身体同矩形绘制，出界必截。
- 修复过程：①Leo 红线标注 → 诊断实证（bbox 平切+全目录扫描无完整像素）→ 修订请求 seq=248 → 主架构 v2.1 方案（seq=261，4ab50dd）→ R2 T29 `3aa004dd`：独立剑模（88×80 双系）运行时合成（握点 [23,57] 旋转+拳心对齐+动态扩展离屏层越界绘制 14 行）→ 主架构验收 accepted（seq=267）→ 已并 main。**完整度待 Leo 长剑 L 环终验**。
- 复发次数：1（v1 路线整体作废重做）。
- 关键数字：2 轮（T28→T29）；正确剑长/显示≈3~4 倍；越界 14 行（frame04 右越 29.7px/frame22 左越 33.3px）。
- 可引用原文：Leo"美术那边只确定了剑的位置和遮罩啊，研发这边需要把完整的剑显示出来的"+"完整版剑模型肯定是有的啊。。。美术给的资源，只是剑的位置，角度，层级和遮罩。。。美术不能重出素材，这样帧的图片大小就不就一致了"；PM 错误判断原文（复验报告）："atk_right_2 举拳出剑、剑随拳位 ✓"（c16ba433 上，实为截断状态）。
- patch 冲突记录：layerOrder 枚举扩 body_front（候选 manifest 本体含 10 行，方案草写仅 weapon_front——强锁单值 26/26 不可满足，以 manifest 为真源扩枚举并提请主架构复核）。

### [2026-09-09] BUG-20250909-08 剑模 mask 生成器 CRC 笔误，6 张 cast mask 浏览器拒载
- 现象：6 张第二批 cast mask 在浏览器加载失败（自研 PNG 解码器不校验 CRC 故 T28 v1 预检未拦）。
- 复现条件：浏览器 Image 加载对应 mask 文件。
- 测试状态：🔴 v1 preflight 全绿（自研解码器与浏览器校验面不一致——工具链盲区）。
- 根因：生成器 crc32 实现 `c>>>0` 应为 `c>>>8`。
- 修复过程：T29-C6 `ca82e5f3` → 修复+chunk 级 CRC 编码自锁（写出处即校验）。
- 复发次数：0。
- 关键数字：受影响 6 张；修复含自锁。
- 可引用原文：T29 回执"自研解码器不查 CRC 故预检未拦"。

### [2026-09-09] BUG-20250909-09 武器 loader modelImgOf 键值错配，48 行全空手
- 现象：全部 48 行武器层不显示（全空手）。
- 复现条件：任意施法/攻击帧。
- 测试状态： spy/单测在 C6 前未覆盖 modelImgOf 键路径（C5 测试重写时暴露并修）。
- 根因：modelImgOf 按错误键查模型。
- 修复过程：T29-C6 `ca82e5f3` → 按 weaponModelKey 查。
- 复发次数：0。
- 关键数字：影响面 48/48 行。
- 可引用原文：T29 回执"施工中截获两缺陷"。

### [2026-09-10] BUG-20250910-10 清杂 ROI 外扩清零削到主体，自检"单连通"仍判绿
- 现象：seq=287 三帧（`atk_rightdown_3`/`jump_rightup_1`/`jump_rightup_3`）清完游离杂点后，角色本体轮廓被连带削掉一小段（≤1px 深、约 3~5px 长的台阶缺口 + 185~187 抗锯齿边丢失），交付声明"主体 RGB 保持不变"不成立。
- 复现条件：对 a17ad62c → 5ee871fa 做逐像素 alpha 差分 + alpha>32 掩膜差分（20 帧里只有这 3 帧有本体删除）。
- 测试状态：🔴 **美术线自检全绿**（`qa/pm275_cleanup_alpha32_verification.json` 报 `singleComponentAlphaGt32=true`）但缺陷仍在。**为什么没拦住**：自检只验"清理后是否单连通"，未验"清理是否只删游离元素、主体零改动"；脚本以 bbox 外扩 2px 为清除单位，其正确性前提（框内无主体像素）从未被校验——紧贴主体的游离点，其外扩框必然覆盖主体边缘。
- 根因：`cleanup_pm275_alpha32.py` 用 2px 外扩 ROI 清零框内全部 alpha>0 像素（`ex=[x0-2,y0-2,x1+2,y1+2]` + `if px[x,y]>0: px[x,y]=0`），清除单位是"矩形框"而非"alpha>32 连通域"。
- 修复过程：第 1 轮（a17ad62c：alpha>0 + 8 邻接，移除 17/11/22 px）→ 门检口径 alpha>32 下残点仍游离（口径与门不符，未清）→ 第 2 轮（5ee871fa：alpha>32 ROI 外扩清零）→ 单连通达成但主体被削 6/8/13 px → 第 3 轮（PM2 seq=280 打回，要求主体零改动重做）。
- 复发次数：2（同一 3 帧连续两轮不合格）。
- 关键数字：两轮各删 50 px（17/11/22）与 57 px（18/19/20）；第 2 轮 alpha>32 主体像素被删 6 / 8 / 13 px（含 alpha 251~255 的实体与近黑轮廓像素，如 `(165,222)=RGB(11,14,14) a255`、`(165,223)=(1,0,0) a251`、`(166,223)=(8,10,11) a255`）。全量复扫：20 帧其余 17 帧门检全过。
- 可引用原文：美术线交付原文"仅保留主体最大 alpha>0 连通域，移除 17/11/22 个游离像素……三帧复核均单连通域"；PM2 复检原文"alpha>32 掩膜差分：三帧各被删主体像素 6 / 8 / 13 px，其中含 alpha 251~255 的实体与近黑轮廓像素"。
- 口径附加勘误：门检"主体单连通"判据为 **alpha>32**（非 alpha>0）——alpha 1~15 的微弱光晕会把 alpha 92~151 的游离点并入主体造成假单连通，已落盘 PM2 onboard §3。

### [2026-09-10] BUG-20250910-11 R1 归一交付同格重复 + 尺度未达标，自检仍报 29/30 通过
- 现象：R1「固定缩放」归一交付（commit d9bd4e31）中，右上/右下各有 3 对帧**整幅逐字节相同**（`walk_3==jump_1`、`atk_1==jump_2`、`atk_2==jump_3`）→ 10 帧实际只有 7 张不同内容，jump 三帧是错姿势；同时成品尺度严重不齐：`right_atk_*` 视觉高 175（基准 `battle_idle_right` 256 的 **68%**）、`rightup_atk_*` 275~282、`rightdown_atk_*` 270~273，而 walk 三向 ≈256 是对的。
- 复现条件：对交付帧两两比 SHA（重复立现）；量成品 bbox 高与基准帧对比（尺度立现）。
- 测试状态：🔴 **自检报 29/30 通过**（`qa/r1_normalize.json` 的 `allHardGatesPass`）但交付为坏。**为什么没拦住**：门检项里**没有任何尺度一致性检查**——而尺度一致恰恰是 R1 这次改动的唯一目的；checks 只有 尺寸/RGBA/alpha极值/宽≤236/脚底300/质心120/边缘透明。第三项"刚性特征"本应承担该职责，但它的基准测值已饱和（见下），等于没有判据。
- 根因：三处叠加——①`build_r1.py` 的 jump 取格写成 **row0** col2/3/4，而已过门 commit 642bee08 的 seq287 manifest 记的是 **row1** col2/3/4，row0 那三格恰是 walk_3/atk_1/atk_2 → 直接复制出重复帧；②R1 的『同一源批次共用一个系数』被落成『同一方向一个系数』，但**同一方向内 atk 源与 walk/jump 源的画风尺度本就不同**（right 向 atk 源人物仅约一半），单系数无法同时对齐；③`rigid_head` 扫描窗上限 = 0.55×身高，benchmark 记 `rigidHeadSpan=141` = 0.55×256 → **测值饱和到窗口上限，失去分辨力**，三个 batchScale（0.8198/0.7705/0.7157）遂硬编码且无推导表。
- 修复过程：第 1 轮（d9bd4e31）→ PM2 复检 4 项硬缺陷打回（projbus rd→art seq=291）→ 第 2 轮待交付。
- 复发次数：1（首轮即打回）。
- 关键数字：重复帧 6 对（右上/右下各 3）；尺度偏差 `right_atk_*` −32%（175 vs 256）、`rightup_atk_*` +8~10%；基准刚性测值 141 = 0.55×256（饱和）。
- 可引用原文：交付 `manifest.json` 自述 `"passedFrames": 29, "failedFrames": ["right_jump_3"], "status": "blocked_on_right_jump_3_width"`；PM2 复检原文"rightup/rightdown 各 3 对帧 byte-identical——jump 取格写成 row0，已过门 seq287 manifest 记的是 row1 col2/3/4"。
- 教训（与 BUG-20250910-10 同源）：**交付门检必须包含"本次改动的目的项"本身**。R1 的目的是尺度一致，门检却查了 6 项周边规格、独独没查尺度——于是"全过"与"目的达成"完全脱钩。已并入 PM2 onboard §3 门检四项（①尺度偏差 ≤±5%）。

### [2026-09-10] BUG-20250910-12 轻功「头偏大」被误诊为生成端比例漂移，真因是包围盒自适应归一放大蹲姿帧
- 现象：T45 轻功帧在动画里头部明显偏大（Leo 两次目视判定「头的大小比例差距太大，完全不可用」），jump 三帧之间头忽大忽小。
- 复现条件：以原始生成帧（未归一，`raw/jump_right_*_native.png`，1086×1448）量脸宽：三帧 = **158 / 159 / 161**，极差 **1.88%** → **生成端头一致**；再按「包围盒自适应」归一（每帧自身 bbox 顶到 256）复算：jump_1 系数 256/951=0.2692、jump_2 0.2535、jump_3 0.2488 → 归一后脸宽 **42.5 / 40.3 / 40.1**，极差 **6.05%**，且 jump_1 相对单系数被放大 **1.210×**。
- 测试状态：🔴 **多轮「自检全绿」但缺陷仍在**。**为什么没拦住**：①门检只有「输出高度=某值」这类**结果**判据，**没有「组内刚体量极差」**这个能直接抓缩放的判据；②诊断阶段**默认病在生成端**，围绕「头尺测量法」投入 7+ 种自动化方案（暗色跨度/肤色块/发冠/模板匹配/颅顶→脸底/双眼距/垂直投影 span），全部未通过保留帧自验证——**测量无效恰恰因为被测对象在生成端本来是好的**；③「包围盒归一」的隐含前提（每帧姿势高度可比）从未被校验。
- 根因：归一化实现 = **逐帧包围盒自适应**（把每帧自身 bbox 顶到固定高度）。下蹲/蜷缩帧 bbox 天然矮 → 被赋予更大系数 → **整帧连同头部一起被放大**。生成端无病。**误诊链**：把「归一放大」的表象读成「AI 生成比例漂移」，据此反复重生成，方向错了整轮。
- 修复过程：口径升级为 **v2 单系数**（`系数 = 256 ÷ 锚帧源 bbox 高`，全批共用，只由锚帧推导）→ 用同批源图复算：归一后脸宽 **35.1/35.4/35.8，极差 1.88%**（与生成端一致，完整保住）。已落规范红线 9（2026-09-10 v2 修订）。
- 复发次数：跨多批次（旧管线 63 张基准帧 bbox 高**恒 256** 即其指纹；已过门右上/右下 20 帧同为包围盒归一产物，**同病**）。
- 关键数字：生成端脸宽极差 **1.88%**（无病）；包围盒归一后 **6.05%** 且 jump_1 放大 **1.210×** / 头大 **+21%**；单系数归一后回到 **1.88%**。
- 可引用原文：Leo 判定「我确认一下，这里的轻功第一帧，看着人物特别大，是因为蹲下来，所以把帧放大了吗」「轻功在生成的时候头的大小是正常的，但在合并到一张大的帧图的时候，被放大了，因为轻功下蹲的时候，很矮，他就放大了」；PM2 此前误判原文「轻功头明显偏大」被归因为生成端比例漂移并据此重生成。
- 附带教训（PM2 自记）：**判据必须与命题同轴**——本日两次踩到（①拿文件格式当"链式是否成立"的停止条件 ②把锚帧混进链内组算极差）；**跨分辨率比比例必须按人物身高归一，禁按画布宽归一**。

### [2026-09-12] BUG-20250912-13 🔴 aim2 重定向「骨骼指向」参照不一致 → 头部被恒定多拧 25.8°（"莫名其妙低头"）

- 现象：Collada(Mixamo) 重定向出的动作帧**集体低头**——idle 六向 30 帧每帧都是勾着脖子、下巴朝胸；轻功（Jumping Up，同一套重定向）同样。Leo 09-12 目视原文：「为什么会莫名奇妙的低头，轻功也是。。。不正常啊」。
- 复现条件：`node tools/glb2d/collada2anim.mjs <模型> <动作.dae> <out>`（旧默认 `--mode aim2`）后渲任意帧即可见；数值上量「模型 Head 世界 Y 轴 vs 源 Head 世界 Y 轴」恒定差 25.78°（逐帧 25.52~25.88°，源自身整个循环只动 4.68°）。
- 测试状态：🔴 **当日全部自检均报绿但缺陷在**。**为什么没拦住**：①既有「帧 0 校验」只比**源自身** rest vs 帧 0（恒等），与**跨骨架**映射对不对无关；②既有验收项「火柴人对比图」只看整体形状，头 25° 的偏差在火柴人上被躯干/四肢的大位移掩盖；③**门检里没有任何「骨骼朝向保真度」判据**——而"姿态对不对"恰恰是重定向的唯一目的（与 BUG-20250910-11/12 同一类病：门检没查本次改动的目的项）。
- 根因：`tools/glb2d/collada2anim.mjs` aim2 分支的「骨骼指向」两侧定义**不同源**——
  - 源侧 `srcDirOf(j)`：`子骨骼位置 − 自身位置`；目标侧 `tgtDirRest[i]`：`自身位置 − 父骨骼位置`。
  - 二者只在「子骨骼正好落在骨骼末端」时等价。实测 22 根映射骨中**恰好 5 根不等价**：`Head` 25.78°、`NeckTwist01` 16.92°、`Hip` 50.65°、`L_Hand` 71.72°、`R_Hand` 48.14°，其余 17 根全部 ≤0.06°。
  - `Head` 特例：Mixamo 的 `HeadTop_End` 不在头骨的 Y 轴上（两者差约 26°），而模型 Head 是叶子骨、参照退化成「父→自身」＝头骨 Y 轴本身 → 恒被多拧。
  - **次因**：`Hip` 的 `Waist − Hip` 实测长度 **0.0000**（Tripo 绑定里 Waist 与 Hip 同位置）→ 参照是零向量 → aim2 里 `hypot(curDir) > 1e-9` 的守卫**直接跳过修正**，Hip 保持自身 rest 朝向，身体整体偏 50.65°。
- 修复过程：第 1 轮即定位并修（`--mode aim3`，两侧统一取「骨骼自身世界 Y 轴」——同口径、无歧义、无零向量退化）。A/B 实测：idle 22/22 根骨残差全部 **0.00°**（修前 5 根超阈值）；渲染对照头由「勾着」变为直立，与 rest 帧一致；轻功火柴人对比图（源 vs 我们，5 帧）姿态逐帧吻合。**aim3 对本来正确的 17 根骨零影响**（两定义等价时夹角为 0），改动面精确等于出问题的 5 根。
- 复发次数：1（新暴露，非复发）；影响范围 = **全部走 collada2anim 的产物**：idle（09-12 新产）、轻功（09-11 已交付的 30 帧全部带此缺陷）。`walk`/`run` 走的是模型自带动画（`preset:biped:*`，不经重定向）→ **不受影响**；`retarget.mjs`(BVH) 与 `fbx2anim.mjs` 不含该 aim 逻辑 → 不受影响。
- 关键数字：Head 恒定偏差 **25.78°**（源自身仅动 4.68°）；五根问题骨偏差 **25.78 / 16.92 / 50.65 / 71.72 / 48.14°**；其余 17 根 **≤0.06°**；Hip 参照向量长度 **0.0000**；aim3 修后 **22/22 = 0.00°**。
- 可引用原文：Leo 09-12「为什么会莫名奇妙的低头，轻功也是。。。不正常啊」；PM2 自记当日错误判断：起先按「源动作本身就是深蹲低头」解释了一轮，直到量出**源 Head Y 轴仅偏垂直 8.9°、我们的偏 33.5°** 才转向查重定向——教训＝**"源大概就是这样"不是判据，必须量源自己的数**。
- 附带教训：①**重定向类改动的门检必须含「逐骨朝向残差」**（本例 22 根骨、判据 `ang(模型Y轴, 源Y轴) == 0`，可机械计算）；②「两侧参照必须同口径」——只要一侧用"子−自身"另一侧用"自身−父"，就必然在**叶子骨**与**子骨骼不与父骨骼同段的骨**上炸；③零向量守卫会**静默跳过**修正，属"沉默失败"，必须配"被跳过计数"告警。


### [2026-09-12] BUG-20250912-14 🔴 aim3 只对齐「骨骼 Y 轴」→ 头关节位置偏 16.92°，观感「往后仰」（BUG-13 的二次迭代）

- 现象：用 aim3 重产的 idle（源＝Orc Idle）站姿已直，但 Leo 目视二次反馈「**就是直了，但怎么有点往后仰的感觉**」——头像是被拉在肩膀后面。
- 复现条件：`collada2anim --mode aim3` 出件后渲 `left` 向即可见；数值上量「渲染后水平质心：头 − 脚」相对 REST：**aim3 = +22.7px**，而 aim4 = +20.1px（数值差不大，但**头的落点**变了 → 观感差很多）。
- 测试状态：🔴 **aim3 当时用「逐骨朝向残差」门检是全绿的（22/22 = 0.000°）**。**为什么没拦住**：门检量的是**骨骼 Y 轴**，而 aim3 恰恰就是按 Y 轴对齐的——**门检与算法同口径，于是"自证清白"**。真正出错的是**关节位置**（Y 轴之外的第二个量），门检压根没测。与 BUG-11/12/13 同一类病：**判据只覆盖了改动的一个面**。
- 根因：**源骨架「骨轴 ≠ 子骨骼位移」**。实测 Mixamo 的 `Neck` 骨：其自身 Y 轴与「Neck→Head 实际位移」差 **16.92°**（源自身不标准）。aim3 把目标骨骼的 Y 轴对齐到源骨骼的 Y 轴 → 目标「Neck→Head 位移」跟着目标自己的 Y 轴走（本项目 rig 标准，两者一致）→ 于是头关节落点相对源**偏了 16.92°**，方向朝后。
  - 对照：**我们的模型** rest 里「骨轴 vs 子骨骼位移」**全部对齐**（仅 `Hip` 退化，见 BUG-13）——所以问题出在**源**，不是我们的 rig。
- 修复过程：第 1 轮（aim3）→ Leo 目视打回感觉不对 → 量化溯源到 Neck 16.92° → 第 2 轮 **aim4**（`--mode aim4`，设默认）：
  **有非退化子骨骼位移的骨 → 两侧都用「子骨骼位置 − 自身位置」；叶子骨 / 子骨骼与自身同位置 → 两侧都用「骨骼自身 Y 轴」。**
  同一根骨两侧必须同口径（这是 BUG-13 的教训）。修后门检（同口径）**22/22 = 0.000°**，同为 0.000° 的还有 aim3 做不到的 Neck。
- 复发次数：1（同日内二次迭代）。
- 关键数字：源 `Neck` 骨轴 vs `Neck→Head` 位移差 **16.92°**；aim3 头关节位置偏 **16.92°**；三模式在同一门检下最大残差 **aim2 = 71.72° / aim3 = 16.92° / aim4 = 0.000°**；渲染质心（头−脚，相对 REST）**aim3 +22.7px → aim4 +20.1px**（位置微调，观感差异主要来自头的落点）。
- 可引用原文：Leo 09-12「**是直了，但怎么有点往后仰的感觉**」。
- 教训（新增，比 BUG-13 更深一层）：
  ①**「两侧同口径」不等于「选对了量」**——aim3 两侧都是 Y 轴（同口径）却仍然错，因为**量选错了**：
    Y 轴决定**朝向**，子骨骼位移决定**位置**，姿态对不对**两个都要**；
  ②**门检不能与算法同口径自证**——门检若只测算法对齐的那个量，就永远全绿。判据要覆盖**改动会影响的所有面**；
  ③**源骨架本身可能不标准**（骨轴 ≠ 子骨骼位移），跨骨架搬运时不能假设"源是规范的"。


### [2026-09-12] BUG-20250912-15 🔴 模型 L_/R_ 命名与角色真实左右相反 → 源的右手动作落到角色左手（攻击「用左手砍」）

- 现象：攻击（Sword And Shield Slash）出件后 Leo 目视报「**不对，为什么用的是左手砍呢，源动作明明显右手啊**」——源用右手挥砍，成品是左手挥砍。
- 复现条件：`collada2anim` 出任意含左右不对称的动作，渲染**正面视图（yaw=180）**即可见挥砍手在错的一侧。
- 测试状态：🔴 **当时的全部门检都是绿的（22/22 骨残差 0.000°）**。**为什么没拦住**：逐骨朝向残差比的只是「**同名骨**的方向」——而 L/R 互换时，同名骨的**对应关系本身就错了**，残差再小也只证明"错得一致"。**门检的前提（名字能对上）本身没被校验过。**
- 根因：**我们模型的 `L_`/`R_` 骨名与角色真实左右相反**。两条独立证据：
  1. **定向实验**：只改 `R_Upperarm` 一根骨、渲染**正面视图**（yaw=180）→ 动的是**画面右侧**那条手臂；正面视图里「画面左＝角色右」⇒ 我们的 `R_Upperarm` 实为**角色左臂**。
  2. **源侧交叉验证**：源 Mixamo 的 `RightHand` 位移幅度 **Y 138 / X 96**，`LeftHand` 仅 **Y 62 / X 52** ⇒ 源确实是**右手**挥砍（与 Leo 目视一致）。
  两条叠加 ⇒ `RightArm → R_Upperarm` 这条映射把源的右手动作送到了角色的左手上。
- 修复过程：`MIXAMO_TO_MODEL` 增加**左右互换**（默认开；`--no-swap-lr` 可复现 09-12 之前的旧产物），模型侧名字统一 `L_⇄R_`。**验证**：互换前位移最大的是我们的 `R_Hand`（X 0.379 / Y 0.370），互换后变成我们的 `L_Hand`（X 0.324 / Y 0.339）——而 `L_Hand` 正是角色右手所在的那根骨 ✓。
- 复发次数：1（新暴露）。**影响范围＝全部走 `collada2anim` 的产物**（idle / jump / atk 三组），已全部重渲；`walk`/`run` 是模型自带动画（不经重定向，模型自己动自己的骨，与命名无关）→ 不受影响。
- 关键数字：源 `RightHand` 位移 Y **138** vs `LeftHand` Y **62**（2.2 倍）；互换前后我们手上位移最大的骨从 `R_Hand`(0.379) 变为 `L_Hand`(0.324)；三组重渲后门检仍 22/22 = 0.000°、地面锁定后脚底最深仍 = 299。
- 可引用原文：Leo「**不对，为什么用的是左手砍呢，源动作明明显右手啊**」。
- 教训（新增，与 BUG-13/14 同类但更前一层）：
  ①**门检有"前提假设"时必须校验前提**——逐骨残差默认「同名骨该对应」，这个前提本身没人验过；
  ②**测左右必须用正面视图**：侧视图（左/右向）里"哪只手"根本无从判断，本项目已多次在左右上翻车；
  ③**判据要能机械化**：新增一条「**源位移最大的手，必须落在角色右手所在的那根骨上**」——
     实现＝先做一次「正面视图 + 单骨旋转」的**骨骼左右标定**（一次性），把它固化成常量；
     本项目标定结论：**模型的 `L_*` = 角色右侧、`R_*` = 角色左侧**。


### [2026-09-12] BUG-20250912-16 🔴 渲染器投影**左右镜像** → 不对称动作左右手反（攻击「用左手砍」／头胸朝向怪）· 前两轮误修已撤销

- 现象（Leo 三次目视，逐轮升级）：
  ①「**不对，为什么用的是左手砍呢，源动作明明显右手啊**」→ 我误判为"骨名左右反"，只换骨名 → ②「**整个身体的扭屈了**」→ 我又加"源绕 Y 转 180°" → ③「**现在头和胸的方向是反的了**」「**不是缓存，你现在渲染的就是胸和头部跟其他部位是反着的**」。
- **真因：渲染器 `render.mjs` 的投影是左右镜像的**，重定向本身一直是对的。证据链：
  1. **相机在世界 −Z 侧**：`yaw=0` 只见**后脑**（头部肤色像素 **130**）／`yaw=180` 见**整张脸**（**1004**）；
     深度缓冲实证：`yaw=0` 时把模型沿 **+Z** 平移，平均深度 **−0.054 → +0.446**（变**远**）。
  2. 相机在世界 −Z、up=+Y ⇒ **相机右向量 = f×u = (0,0,1)×(0,1,0) = (−1,0,0) = −X**。
  3. 而原式 `screen_x = W/2 + (q[0]−cx)·scale` 用的是 **+X** ⇒ **屏幕右 = 相机的「左」** ⇒ 整幅渲染镜像。
  4. 后果：一切**左右不对称**的内容都会反——攻击的挥砍手落到镜像侧（"用左手砍"）；
     侧向视图看起来"对着"，其实是**把角色的另一侧翻过来**（所以 2D 素材的 left/right 与我们的并不真同侧）。
- **修复**：`toScreen` 的 x 项**取负**（`W/2 − (q[0]−cx)·scale`）；同时因为侧向朝向跟着翻，**方向表 yaw 左右对调**
  （`270↔90 / 225↔135 / 315↔45`）。
  修后实测：3D `left` 脸在画面左、`right` 脸在画面右——**与 runtime 2D 素材同向** ✓；攻击正前视角里挥砍手出现在**画面左 = 角色右** ✓。
- 复发次数：同日内 **3 轮**（前两轮方向都错，已全部撤销：`MIXAMO_TO_MODEL` 的左右互换、`--src-yaw` 的 180° 转**均改为默认关**）。
- 关键数字：头部肤色像素 yaw0=**130** / yaw180=**1004**；+Z 平移后平均深度 **−0.054→+0.446**；相机右向量 **−X** vs 代码用的 **+X**。
- 可引用原文：Leo「不对，为什么用的是左手砍呢，源动作明明显右手啊」／「整个身体的扭屈了。。。你肯定漏改了东西」／「现在头和胸的方向是反的了」／「不是缓存，你现在渲染的就是胸和头部跟其他部位是反着的」。
- 教训（本轮最大的一条）：
  ①**我把"渲染出来看着不对"当成"骨骼映射不对"**，在 rig 层面连改两轮——**问题在投影层**。
     姿态类的症状要先分清"**几何错了**"还是"**画出来反了**"：做一次"单骨旋转 + 正面视图"就能定位，
     但更该先问的是**"相机的右向量是什么"**（一行数学，成本近乎零）。
  ②**`yaw` 这类对称参数很容易骗过目视**：`left(270)` 与 `right(90)` 各自看都"像"，只有**与真值素材（runtime 2D 帧）比朝向**才能发现镜像。
  ③**每一轮修改前先确认上一轮的诊断**——我第二轮是在第一轮的错判上叠加的，等于把错误放大。


### [2026-09-12] BUG-20250912-17 🔴 AI 细节精修交付实测「更钝」，自检仍报「visibly sharper」（分辨率天花板：模型输出图纸比输入小 0.812×）

- 现象（Leo 目视）：seq=372 idle 精修试产交付后，「**只是优化了色调，确实自然了，但精细度没任何变化**」。
- 复现条件：把 1224×1948 的 30 格精灵图整张交给 `image_gen` 做"只提清晰度"编辑，再按原网格切回 240×320。
- 测试状态：🔴 **自检全绿仍交付无效件**。QA `idle_refinement_qa.json` 的 `/fileGate/*` 全 True（尺寸/模式/alpha 逐字节一致——这部分确实做对了），但 `/clarity/internalVisualCheck = "pass: refined panel is visibly sharper"` 是**无量化判据的目视断言**。**为什么没拦住**：我发的任务书 5 条验收里**没有一条是可机械计算的清晰度判据**（只有"轮廓更锐利"这类形容词）；门检缺"本次改动的目的项本身"（AGENTS 施工守则 7 / 素材加工门检口径 验⑤）。
- 根因（实测，非推测）：**生成模型的工作分辨率低于我们的源图**。
  1. 输入图纸 1224×1948 = **2.384 MP**；`raw/refined_idle_sheet.png` 实际输出 **994×1582 = 1.573 MP**（长宽比完全一致 ⇒ 等比缩小 **0.812×**）。
  2. 1.5 × 1024² = 1,572,864 ≈ 实测 1,572,508 ⇒ **模型输出预算 ≈ 1.5 MP**，超出即等比缩。
  3. ⇒ 每格 240×320 实际只被画在 **≈195×260 = 源像素数的 59%** 上，再被 LANCZOS **放大回** 240×320 ⇒ **净降采样，没有像素预算承载新细节**。
  4. 佐证（分辨率天花板的指纹）：损失幅度与源帧本身锐度成正比——最锐的 `leftdown/rightdown`（源 2.6~2.8）掉 **13~22%**，最软的方向（源 1.33~1.4）只掉 **0~8%**。
- 关键数字（口径：主体内部 alpha>32 腐蚀 1px 后相邻像素梯度均值；30 帧全量）：

  | 项 | 值 |
  |---|---|
  | 源帧平均 | **1.840** |
  | 精修候选平均 | **1.641** = **−10.86%**，**27/30 帧更钝** |
  | 输入图纸 → 模型输出图纸 | 1224×1948（2.384 MP）→ 994×1582（1.573 MP）= **0.812×** |
  | alpha 一致性 | 30/30 逐字节一致（轮廓约束达成） |

- 归因边界（同时排除两个"看着像"的嫌疑）：
  - **不是 PNG-8 量化**：同一份影调后 RAW，PIL 256 色量化只掉 **0.4%**（2.599→2.590），64 色也只掉 0.8%。
  - **不是我们自己的后处理**：全链逐级实测（主体内部口径）RAW 1.015 → 影调 1.139 → 后处理 **1.360**（后处理反而**升**——描边 + 无抖动量化把边缘做硬了）。
  - ⚠️ **口径自查**：我第一版用"整幅含透明区"的梯度口径量出"后处理掉 41.3%"，是**口径错误**（透明区残值 RGB 污染），结论已作废；本条所有数字一律用主体内部口径。**记录在案：判据实现必须锁死坐标系/区域边界，见《素材加工类交付门检口径》验⑪。**
- 复发次数：1（首例）。
- 教训：
  ① **「只提升清晰度、不许改任何东西」与生成模型的机制相冲突**——模型只有在能被分到 ≥ 源分辨率的像素时才能加细节；图纸格子越多、单格分到越少。
  ② **验收判据必须锁死在"本次改动的目的项"上并给数字**（锐度变化 %）；形容词（"更锐利""更清晰"）不算判据。
  ③ **发单前先算一遍分辨率账**：`输入图纸像素数 vs 生成模型输出预算`，输的那一方注定变钝。
- 修复方向（待 Leo 拍板后再发单）：把格数降到模型预算内、且**每格先放大**再送。
  - 方案 A：**一条方向一张图（5 格），每格先放大到 384×512** ⇒ 图纸 ≈1936×528 = **1.02 MP < 1.5 MP**，模型在每格 384×512（源像素 **2.56×**）上精修，缩回 240×320 才是真超采样；6 次调用。
  - 方案 B：**一帧一张图**（30 次调用，Codex 原生免费），单帧在 1.5 MP 预算内可渲染到 ~960×1280；风险＝跨调用细节漂移（alpha 复用已保轮廓）。
  - 发单第一步＝**分辨率探针**：先送一张 ≤1.5 MP 的图纸，确认输出不再被缩小，再谈细节。
- 可引用原文：Leo「**45 seq=372 的 idle 精修试产已完成并交付 我看了，只是优化了色调，确实自然了，但精细度没任何变化，为什么呢？**」

## 4. 历史缺陷回填说明

T25 之前的缺陷（N1 穿人三轮、BE4 相位 aliasing、D-14、GSG-1 等）记录散见 `tasks/LOG.md`、`tasks/threads/`、各 done 回执与《行为测试红名单》。按需回填至本格式；新缺陷自本规范发布起（2026-09-09）必须即时按 §0 追加。
