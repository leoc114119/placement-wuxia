# T03 · 场景系统（江湖主场景 · 手动移动）

> **状态：Q1 四轮·素材重生成需求已报（ZCode 08-21 23:00 · 等管线）** · 优先级：P0 · 依赖：T02 ✅ · 发单：2026-08-21 CodeBuddy
> 依据：modules/02-场景系统.md · 75 战斗界面规格（仅布局参考）· docs/77 素材清单 v0.7 · UI 基准 assets/ui/ref_jianghu_ui_v1.png

## 1. 目标

江湖 Tab 主场景可玩：2.5D 背景 + 主角点击移动（walk 4 帧动画）+ 场景名标签 + 底部三按钮。**无 NPC（T04）、无战斗（T06）、无挂机逻辑。**

## 2. 需求表（唯一真相）

| # | 需求点 | 依据 | 验收口径 |
|---|--------|------|----------|
| 1 | 场景渲染：`assets/ui/scene_jianghu.png` 全屏铺底（L0 背景 → L1 角色 → L2 UI 浮层） | 模块 02 §1 | 模拟器显示场景图，无拉伸变形（cover 裁切） |
| 2 | 主角渲染：帧表 `assets/ui/frames/hero/`（00=待机 01~04=行走循环 05+暂不用），脚底锚点贴地 | 77 §0.1 | 场景中有主角，待机帧正确 |
| 3 | 点击移动：点场景任意位置 → 主角向该点直线移动（walk 01→04 循环），到达停止回 idle | 模块 02 §2.2 | 点哪走哪，两脚交替动画可见 |
| 4 | 逻辑坐标 0~1，目标 clamp [0.05, 0.95]；朝向左右翻转（立绘水平翻转） | 模块 02 §1.1/2.2 | 点左半屏角色面左 |
| 5 | 点击命中优先级：UI 浮层 > 地面（点按钮不触发移动） | 模块 02 §2.2 | 点三按钮不移动 |
| 6 | 场景名标签：左上角「青牛山下 · 野径」小胶囊（代码绘制，墨底淡金描边） | 模块 02 §3 | 标签显示 |
| 7 | 助战入口：**MVP 隐藏**（不渲染） | 模块 02 §3 R-03 | 屏幕无此元素 |
| 8 | 底部三按钮：闭关/挂机/挑战 Boss——代码绘制圆形朱砂描边按钮，图标大文字小，点击仅 console.log 占位 | UI 基准 ref_jianghu_ui_v1 | 三按钮可见可点，无移动触发 |
| 9 | 资源加载：图片用 wx.createImage 预载，失败降级纯色+日志（不崩） | — | 断图名测试不崩溃 |
| 10 | 单元测试：坐标换算/clamp/朝向判定纯函数 ≥4 用例（vitest，node 可跑） | 81 机制 | npm run test 全绿 |

## 3. 边界

- ✅ 定死：上表交互流程与帧号映射；色值用规范色板（#F8F4EA/#E2574C/#2B2B2B/#D4AF37）
- 🟡 有限自由：移动速度（建议 0.4 逻辑坐标/秒，可调）；标签/按钮绘制细节；触摸事件封装方式
- 🚫 禁止：NPC/战斗/挂机逻辑；改帧表素材；动 docs/ tasks/ assets/；改已验收的 battle-core 与骨架主循环结构（可增量接入）

## 4. DoD

- [ ] typecheck/lint/build 0 error；vitest 新用例全绿（含既有 14 用例不回归）
- [ ] node 模拟验证（wx mock）：点击→移动状态机转移正确（C 环自测，截图/日志贴回执）
- [ ] 开发者工具：场景显示 + 点击移动 + walk 动画 + 三按钮（L 环 Leo 验收）
- [ ] 回执含：渲染分层说明 + 帧动画接入方式 + 触摸封装决策
- [ ] 未触碰禁止区

## 5. 素材（全部就绪）

| 素材 | 路径 |
|------|------|
| 场景背景 | `assets/ui/scene_jianghu.png` |
| 主角帧表 8 帧 | `assets/ui/frames/hero/00.png ~ 07.png`（透明 PNG） |
| UI 视觉基准（参考） | `assets/ui/ref_jianghu_ui_v1.png`（不进代码，对照用） |

## 6. 疑问通道

歧义 → `tasks/questions/Qn-T03.md`，停下等待。
