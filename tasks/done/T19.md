# T19 · 战斗缺陷批一修复（N1 穿人 + D-14 DBG 残留 + 普攻态演出位拦截）

> **状态：📤 已发单待领** · 优先级：P0 · 发单：2026-09-03 ZCode PM（研发管线窗口）
> **执行者：backend 子智能体（单一领单人；跨栈授权文件清单见 §4）**
> 依据：《战斗缺陷修复方案-N1N2-v0.1.md》（`docs/design/03-战斗系统/`，**本卡需求表 = 该方案 §3.2/§3.3/§4(收窄版)/§五 逐条**）+ Leo 六点评审（2026-09-03，方案更新记录行）+《战斗缺陷全景复盘-2026-09-02》
> 关联：T15/T16 为缺陷载体卡（本卡修复不改其状态）；红名单登记簿 = `tests/battle-behavior.test.ts` 与 `proto/battle_demo/behavior_e2e.mjs` 文件头

## 1. 批次口径（Leo 六点评审落地，唯一口径）

| 项 | 定版 |
|---|---|
| 批一（本卡） | §3 N1（P-1 C 案=A 变体+P-5 最小切口）+ §五 D-14 + §4 演出位拦截**收窄至普攻态** |
| 二批（另卡，不在本批） | 特/绝改**点射程内任意格施放**（P-2=c 格子目标化）+ D-13 热区（P-3=c）；规格补条先行，归文档管线窗口 |
| **skill 态（特/绝选中）点击行为本批零改动** | 既有取消/受理语义不动 → **N2-② 与 BE3 保持登记红**（二批按格子施放新语义重写断言）；**N2-① 与 BE2 保持登记红**（二批转绿方向=施放受理） |
| 已确认定版（不再开放） | ①移动中敌=演出位不可点、**逻辑格可点**（D-06 绿锁 :337 保持）②绿格/金格移动意图优先 ③ATK-3 动画延后 ~300ms 可接受（事件/伤害时机不变）④P-6 hexToWorld quirk 登记不修 |

## 2. 需求表（唯一真相；方案条文与本表冲突时以方案原文为准并立即上报 PM）

| # | 需求 | 方案依据 | 验收口径 |
|---|---|---|---|
| 1 | **N1 主修**：Runner 新增 `pendingAnim`；doAttack 在 `quiet && animState==='walk'` 时不 setAnim、记 `pendingAnim = skill?'charge':'basic'`；tick 动画倒计时分支优先级 charge→strike→**pendingAnim**→idle。**明确不变**：resolveAction 结算与事件 emit 仍同步（basic 事件同步紧跟 move 的绿锁不得破）；faceToward 不变；非 quiet 路径零变化；AI basicIfAdjacent 同路径受益 | §3.2 | 行为套件 N1-1 转绿；既有"move 事件后跟 basic 事件"同步断言保持绿 |
| 2 | **N1 防御**：hex.ts 新增纯函数 `movePathCells(from, to, occupied, inBounds?)`（6 邻 BFS、HEX_DIRS 方向序固定、起点格不作阻挡、BFS 失败回退 `[from,to]`）；Runner 新增 `movePath`；doMove 改 hex 前计算：walk 走 path、**jump 恒 `[旧格,to]` 直线**；tick 位移插值改沿 movePath 分段等时插值，**moveT=1 时 renderPos 精确等于 hex**、moveT=0 从 path[0] 起、movePath 空/单点回退现直线公式 | §3.3 | 行为套件 N1-2 转绿；BE4 转 `crossed=false`；battle-session 既有 148 用例零回归（无 renderPos 形状断言，已 grep 实证） |
| 3 | **普攻态演出位拦截**（收窄版）：仅当**无技能选中（普攻意图）**时，点击格命中某存活敌方棋子的移动演出位（可见位≠其逻辑格）且该格非合法移动格 → 不派发任何请求、选中保持、`onBlocked('目标移动中')`。判定=双通道 `moving(a) = renderPos≠pos ∪ view.moveAnims.has(id)`；可视位量化=`worldToHex(hexToWorld(renderPos))` 与绘制同链，禁自造取整（input 需从 config/battle-hex 增 import `hexToWorld`）。**分支顺序（关键）**：弧钮/ctrl/plaque → 出界取消 → pos 命中（普攻/施放，既有）→ 轻功金格（既有）→ 无选中绿格（既有）→ **【新增】普攻态演出位反馈** → `skill && !inAttack` 取消（既有）。排除 dead 与我方单位；**零新增 dispatch**；**skill 态各分支零改动** | §4.1-4.3 + 评审②收窄 | 新增 input 用例（无选中点演出位反馈、绿格优先变体）绿；既有 :337、:331-334、BE1 全保持绿；N2-②/BE3 保持登记红 |
| 4 | **D-14**：删除 `ui/battle-hex-render.ts` 两处 DBG console.log 块（move-start ≈:440-442、move-end ≈:456-458），**只删日志，walkRise/moveDone 防抖与 from 取整逻辑一行不动**；build.mjs 重建 + verTag bump | §五 | 重建后 `grep 'DBG\[' proto/battle_demo/bundle.js` = 0 条 + behavior_e2e BE4 输出「DBG残留 0 条」；main.ts `__demo` 取证挂载段保留不动 |
| 5 | **红名单登记**（登记簿机制，不属断言改写）：N1×2 与 BE4 转绿 → 两处文件头红名单表回填修复 commit 并转绿锁；behavior_e2e **仅 BE4 expectRed 翻转**（BE2/BE3 保持预期红） | §八 | N1×2/BE4 绿且登记回填；无新红 |
| 6 | **新增用例最小集**（先交清单后编码，见 §6）：a) 既有红例 N1×2 原样转绿（断言零改动）b) session 例：submit 相邻落点后紧接快照 `animState==='walk'` c) session 例：同排隔敌移动逐帧采样 renderPos 不进入敌占格 d) hex 纯函数例：movePathCells 直连/绕行/不可达回退/起点自身格 e) input 例：no-skill 点演出位反馈变体 + 绿格优先变体 f) 既有 :337 / :331-334 / BE1 回归哨 | §十-2 | 全绿；行为套件终态 = **2 红（N2-①、N2-②）** |

## 3. 领单第一交付（铁律 4，先交后码）

按序交付，**未获 PM「✅确认开工」前禁止写任何实现代码**：
1. 四门基线复跑原文（`npm run test:battle` / `npm run test:behavior` / `node proto/battle_demo/shot.mjs` / `node proto/battle_demo/behavior_e2e.mjs` / `grep -c 'DBG\[' bundle.js` / `npm run typecheck`）——基线应为 148 绿 14 跳 / 4 红 10 绿 / 16 PASS / 4 MATCH / 1 / 0 error，不符立即停并上报。
2. 口径逐条复述（§1 批次口径 + 需求表 6 行），疑义逐条列出（无则写「无」）。
3. 完整测试用例清单（需求表 #6 最小集展开到断言级）。
4. **由用例自行总结的易错点**（与方案 §九 18 条互补，不重复抄写）。

## 4. 授权文件清单（跨栈授权，仅限以下文件）

- `systems/hex.ts`、`systems/battle-session.ts`（§3.2/§3.3 本体）；`tests/hex.test.ts`（movePathCells 纯函数用例归属，09-03 PM 批复增补）
- `ui/battle-input.ts`（§4 新分支 + hexToWorld import）、`ui/battle-hex-render.ts`（**仅删两处 DBG 块**）
- `tests/battle-session.test.ts`、`tests/battle-hex-render.test.ts`（新增用例）、`tests/battle-behavior.test.ts`（红名单登记簿回填）
- `proto/battle_demo/behavior_e2e.mjs`（仅 BE4 expectRed 翻转）、`proto/battle_demo/bundle.js`（build.mjs 重建产物）+ verTag
- 🚫 **禁碰**：`systems/battle-core.ts` 零改动；`config/` 数值零新增零改动；`mock_session.ts` 不碰；`main.ts` `__demo` 调试挂载段不碰；D-15 不碰；P-2/P-3/D-13 面不碰；事件流形状/types.ts 快照字段零变化（发现"需要加字段"=方向错了，停手上报）

## 5. 易错点对照

方案 §九 18 条为架构层清单，领单后由自己的用例清单另行总结互补（§3-4）；两份都须在施工中逐条自查。

## 6. DoD（全过才算完成）

1. 三零：typecheck / lint 0 error，主套件全绿（148+新增，**登记新基线数**）。
2. 四门终态（输出原文）：①主套件全绿 ②行为面 N1×2 转绿、N2-①/N2-② 保持登记红（4 红→2 红）③e2e shot 16/16 + behavior_e2e BE1 绿锁、BE2/BE3 预期红、**BE4 转预期绿**、全符合预期 exit 0 ④bundle grep DBG=0 + verTag bump + preview 刷新目验。
3. 文件清单 + 架构决策说明 + 红线逐条自查（§4 禁碰项零触碰）。
4. 回执写 `tasks/回执总表.md`（铁律 6），任务箱三处同步（卡状态/threads/T19.md/LOG）。
5. 提交信息附 grep=0 与用例数证据（D-14 反例教训：声明必须挂 diff 证据）。

## 7. 施工顺序建议（方案 §十-5）

hex.ts 纯函数+用例 → session §3.2/§3.3+用例 → input §4+用例 → D-14 删除 → 红名单两处登记+BE4 expectRed 翻转 → 四门+三零 → 回执。

## 8. PM 行为测试接口（铁律 8，C 环实测用，非仅复跑脚本）

现成挂载点 = `window.__demo.{session.snapshot()/session.submit()/session.events/session._debug.units、getView()（moveAnims/fx/skillPop/layout）、cellCss(q,r)、btnCss(id)、sampleHeroDraw()、startFrameLog()}`；`placeFoe/clearEnemyBars` 白盒布点用法见 `behavior_e2e.mjs:79-134`（可作范例）；toast 反馈观察口径 = onBlocked 元素 1400ms 渐隐。

## 更新记录

| 日期 | 变更 | 签字 |
|---|---|---|
| 2026-09-03 | 创建：批一拆单（方案 §十建议 T19 单卡双线），范围按 Leo 六点评审收窄（拦截限普攻态、N2-②/BE3 保持红待二批重写） | ZCode PM |
