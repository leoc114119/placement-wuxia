# T24 · 战斗场景融合（env 入世界系 + v8 缺角边缘 + 台面落地）

> 状态：已批（Leo 三点裁定 09-04）· owner：frontend（派单施工）· 方案真源：`docs/design/03-战斗系统/战斗场景融合方案-v0.1.md`（§六已裁）
> 红线：types / battle-session / battle-core / battle-input / systems/hex / config/battle.ts / 云函数 **零碰**；`computeCamera` / `updateCamera` / `clampAxis` **零 diff**（拖动手感已验收，禁顺手优化）；e2e 既有 11 项与登记簿零改

## §1.2 需求表（只做表内事项）

| # | 事项 | 锚点 |
|---|---|---|
| 1 | **env 入世界系**：envRect = movableBounds 外扩 (width/2+80, height/2+80)（当帧实际屏尺寸）；与格子同一 cam 变换式；绘制层级最底且在战区 clip 之前 | 方案 §2.1 |
| 2 | **v8 有机缺角边缘**：isBoardCell(q,r)——棋盘矩形最外两圈非可动 dirt 格按坐标哈希剔除（确定性、逐帧稳定），密度 ~20%；**FIELD 内格/出生锚区格绝不剔除**；撤战区矩形 clip，旧「整齐矩形」注释改写标注翻案 | 方案 §2.2 |
| 3 | **落地阴影**：边缘格逐格下偏阴影六边形（offset≈6px、alpha≈0.22、底侧重），参数进 config SHADOW 组 | 方案 §2.2 |
| 4 | **dirt 取色对齐 env**：离线采样 env 图两档土色落 config（topDirtInner/topDirtOuter），由内向外渐暗；禁运行时 getImageData | 方案 §2.2 |
| 5 | **顶面静态噪点**：同色系 ±3% 明度伪随机点阵，seed=格坐标哈希（静态不闪），参数 TILE_NOISE 组 | 方案 §2.2 |
| 6 | **验收断言 V1-V6** + shot 拖动位留档（拖动中一帧，证明 env 随动） | 方案 §三 |

## §4 DoD

1. 主套件全绿（基线 199，新增 probe 用例登记新数）
2. 行为面 14/14；shot 16 PASS；e2e 11 MATCH（既有段零改）
3. DBG=0 + verTag bump + preview 目验（拖动时背景随动/缺角边缘/阴影）
4. 三零；红线文件+镜头三函数 git diff=0 自查
5. commit 显式清单不 push，停等 PM 复验；回执含方案 §三 V1-V6 对表
