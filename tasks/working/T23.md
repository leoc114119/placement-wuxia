# T23 · 战斗 UI 实装（顶栏真实化 + ctrl 三钮独立 + 代码字）

> **状态：📤 已发单待领** · 优先级：P0 · 发单：2026-09-04 ZCode PM（研发管线窗口）
> **执行者：frontend 子智能体（授权文件清单见 §4）**
> 依据：《战斗UI实装方案-v0.1》（`docs/design/03-战斗系统/`，**本卡需求表=方案 §一.2 授权表/§二设计/§三 V1-V5 逐条**，9 项确认点 PM 已按默认背书）；需求真源=《战斗UI实装需求文档》v1.0
> 关联：FE 单卡（无 backend 卡——零契约、battle-session 零改动）；人物帧/光影/毒不在本卡

## 1. 批次口径（唯一口径）

| 项 | 定版 |
|---|---|
| 本卡范围 | 顶栏实装（topbar_base+代码条+名字+百分比）+ 状态图标接口（MVP 全隐藏）+ ctrl 三钮独立贴图+代码字+金框激活态 + bundle/ver 收尾 |
| 三零红线 | `battle-core.ts`/`types.ts`/`systems/battle-session.ts`/`ui/battle-input.ts` **零 diff**（方案 §一.1 零改动清单）；渲染层禁直调 `_debug`（激活判定读 `view.uiState` 宿主镜像） |
| 已批口径 | 条填充=采样渐变（红 #e22a23→#931a15、蓝 #1a94f4→#064faf）；状态图标 MVP 全隐藏留接口（key 词表 poison/blood/skull）；plaque 零改动+点击吞不穿透无提示；百分比随填充末端右对齐；名字/百分比奶黄+深描边；激活亮度=叠亮法（无绿点无绿框）；**meta 252×164 canvas 不用于代码**（切图=钮本体 1:1 换图） |

## 2. 需求表（唯一真相=方案条文）

| # | 需求 | 方案依据 |
|---|---|---|
| 1 | 顶栏重写：base 贴图+压暗层（现状保留）+渐变条填充（fill 区 meta 转换、maxHp≤0 防除零）+名字（hero.name，左对齐 44art）+两百分比（右对齐随填充末端、34art）；TOPBAR 常量组按 topbar_meta 重标定（统一 {x,y,w,h}，禁运行时读 json） | §2.1 |
| 2 | 状态图标接口：读 `hero.statusIcons` 冻结字段，映射表 STATUS_ICONS（poison/blood/skull），命中才画、恒空=空槽 | §2.2 |
| 3 | plaque：零改动；main.ts 移除 onPlaque toast 回调（吞点击不穿透，input 层零 diff） | §2.3 |
| 4 | ctrl 三钮：贴图切独立脸（drawImage 铺满钮矩形）；托管/加速叠代码字（label=active?自动/两倍:托管/加速；样式/中心位按 ctrl_face_text_meta **x=132**）；激活态=uiState 判定+金框 strokeRect+叠亮+柔光；**现行绿 rim 段整段删除**；逃跑=静态图不叠字 | §2.4 |
| 5 | 宿主接线：loadAssets 换图+增三脸/三图标；onPlaque 移除；ver `t16v2`→`t23v1`+rebuild+preview 目验；speedOn 镜像/uiState/evCursor 零改动 | §2.5 |
| 6 | 观测面：view 增渲染私有 `topbarHud` 镜像（V1/V4 e2e 断言出口，不进 types.ts） | §2.6 |
| 7 | 测试：V1-V5 断言（§三 表：e2e+单测两列）+ BattleHexAssets mock 构造 3 处同步+fillText 计数断言按新语义更新（先例 :330） | §三 |

## 3. 领单第一交付（先交后码）

未获 PM「✅确认开工」前禁止写实现：
1. 基线七项复跑原文（主套件 **194 绿 14 跳**/行为 **14/14**/shot 16/e2e **9 MATCH**/DBG 0/tsc+lint 0）——不符即停。
2. 口径逐条复述（§1+需求表 7 行），疑义清单（无则写「无」）。
3. 完整用例清单：V1-V5 断言级展开（e2e 步骤+单测 mock 采样点）+ 绿锁牵连面清单（§一.3-8 点名的 mock 3 处/fillText 4 处/绿 rim 段）。
4. 由用例自行总结的易错点（与方案 §四 10 条互补，禁照抄）。

## 4. 授权文件清单（仅限）

- `ui/battle-hex-render.ts`（drawComponents 顶栏段+ctrl 段+BattleHexAssets+topbarHud+休眠钩子零触碰）
- `config/battle-hex.ts`（BATTLE_HEX_RES 换图+ver bump+TOPBAR 重标定+CTRL_TEXT/CTRL_ACTIVE 常量）
- `proto/battle_demo/main.ts`（**仅** loadAssets 换图加载+onPlaque 回调移除；speedOn/uiState/evCursor/`__demo` 段零改动）
- `tests/battle-hex-render.test.ts`、`tests/battle-hit-feedback.test.ts`（mock 同步+断言更新+新增）
- `proto/battle_demo/behavior_e2e.mjs`（**仅追加** V1/V4 观测段；既有 9 项与登记簿零改动）
- `proto/battle_demo/bundle.js`（rebuild）；shots 产物随交付注明
- 🚫 **禁碰**：types/battle-core/battle-session/battle-input/hex/mock_session；休眠 hit 钩子；input 层热区判定

## 5. DoD（四门终验）

① 主套件全绿（**194+新增**，登记新基线数）② 行为面 **14/14 保持 0 红** ③ shot 16/16 + e2e **9 项保持+V1/V4 观测段 MATCH** ④ DBG=0+verTag bump+preview 目验（含多敌局顶栏数据对照）；三零；§一.1 零改动清单 git diff=0 自查；文件清单+决策说明+回执总表；commit 显式清单**不 push** 停等 PM 复验。

## 更新记录

| 日期 | 变更 | 签字 |
|---|---|---|
| 2026-09-04 | 创建：方案 §五拆 T23 frontend 单卡（9 确认点 PM 按默认背书） | ZCode PM |
