# A3-T45 · R1 固定缩放下 right_jump_3 宽度超限 · 裁定

> 2026-09-10 · 美术 PM2 裁定 · 对应提问 `tasks/questions/Q3-T45-r1-right-jump3-width.md`
> 关联打回单：rd→art seq=291（R1 交付 4 项硬缺陷）

## 裁定

1. **不接受擅自缩小 / 裁切 / 改动作**——你们停下来的做法是对的，继续禁用这三条。
2. **本轮不放宽 `≤236` 上限，也不新增逐帧特例。**为单帧放宽硬约束等于把规格当补丁，会把"定尺一致"这个 R1 的目的抹掉。
3. **该 `238px` 是在错误系数下测得的**，不能作为口径依据。R1 交付另有 4 项硬缺陷（详见 seq=291）：
   - jump 取格写成 row0 → 与 walk_3/atk_1/atk_2 **逐字节相同**（内容级错误）；
   - 尺度未达标（`right_atk_*` 仅 175 = 基准 256 的 68%；`rightup/rightdown_atk_*` 反而 270~282）；
   - 门检无尺度一致性项 → 29/30「通过」不构成 R1 证据；
   - `rigid_head` 基准值 141 = 扫描窗上限 0.55×256，测值已饱和、无分辨力。
   系数修正后，**该帧宽度必须在新的固定系数下重测**。
4. **若重测后仍 >236**：由 PM2 呈 Leo 裁定「维持 236（留 2px 余量）」还是「改判画布贴合（≤238，1px 余量，四边仍零 alpha）」——**不在执行侧自行取舍**，也不得先按其中一种做下去。
5. **Phase B 继续冻结**，runtime 不动。

## 备注

- 本帧不因宽度问题单独放行或单独驳回；它与其余 29 帧同批，按 seq=291 的打回项一起修完再整体复检。
- 修完后交付请附：新系数下的逐帧尺度实测表（vs 基准帧 `battle_idle_right`）、帧间 SHA 互异断言、以及本帧的宽度实测值。
