# T31 seq=425–428 · R2与局域网下载验收口径

2026-09-15，主架构。seq=426已替代425的缓办决定。

## 裁定

局域网记录认可为**真机下载链局部通过**，不能据此完成生产CDN验收或S1收口。原因是实际源为HTTP局域网IP，关闭域名/TLS校验，且GLB命中缓存；domainBlocked=false仅表示本轮未被阻止，不能证明白名单生效。四动作下载合计1,546,249B，5资产严格摘要与长度均匹配，20u 3607样本/60.015s、62.5fps、P95=18/P99=33ms，确认这些事实成立。

## 最小补采

1. 使用可公网访问的HTTPS资源域名、有效且主机名匹配的证书与完整证书链；域名满足小游戏后台实际配置要求（备案要求按该平台执行），配置downloadFile合法域名。推荐443直链，不引入跨域重定向；不要求购买特定CDN品牌或新建专用域名。腾讯CloudBase官方指引：https://docs.cloudbase.net/lowcode/practices/miniapp-guide/downloadfile-guide 。
2. 关闭开发工具/真机调试的跳过域名、TLS、证书校验；保留配置与运行模式证据。用蜂窝网络或非服务端局域网访问确认公网可达。
3. 在**该生产形态**下清理本任务5资产缓存，取一次5/5全冷下载记录（5,586,977B、downloads=5/cacheHits=0、逐件严格SHA/长度全过），紧接一次热启动（cacheHits=5/downloads=0，摘要仍实测）。不要求在局域网额外补一轮，也不重复要求3冷3热或整套20u压测；同渲染版本可沿用已有效的容量证据。
4. 补一次真实断网/不可达源失败与恢复重试记录，证明错误页/重试无隐形战斗；域名拦截的sim可保留为回归，但不能称真机白名单负例。
5. 原始JSON入实质提交，附build版本与bundle SHA映射；当前LAN JSON仍未跟踪。`env.build.commitSha=8dc2e3f5`是构建基线，需映射到交付1d6784ff的产物，不能当整份新代码已由该SHA唯一标识。

## seq=427 R2

审查对象1d6784ffd7f2b0a7610678d66e9492e0a104c2b4：严格身份校验恢复、热缓存绑定清单版本且复算摘要、structural降诊断的源码整改认可；独立五门及build --check通过（754 passed/14 skipped、behavior14/14）。本轮不复跑sim，不追认包内.bin真机字节保真：LAN记录的四动作来自网络，payloadSuffix=.bin不是包内实际读取证据。包内保真仍以local-subpackage模式的逐资产observed/expected全匹配原始记录补齐。

可记“R2代码整改通过；LAN真机网络下载通过；生产CDN与包内.bin真机证据待补”。S1维持未收口，S2施工不放行。没有新增第二台安卓门，原豁免保持。
