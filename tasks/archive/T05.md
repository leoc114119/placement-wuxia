# T05 · 战斗核心（headless 纯逻辑）

> **状态：✅ 已验收归档（C 环 PASS 08-21 21:05 + Leo L 环确认 08-22 11:02 · headless 无 GUI）** · 优先级：P0 · 依赖：无（可与 T02 并行，纯逻辑 + 单测）· 发单：2026-08-21 CodeBuddy
> 遗留钩子：峨眉+10% 门派加成未实现（门派字段不在 MVP 输入，见回执#10，后续门派系统任务接入）
> 依据：modules/01-最小自走棋盘.md · 03-战斗系统设计 · 13-战斗数值设计 · config/公式总览（F-05/R-07/R-08/R-09）· docs/80 拍板

## 1. 目标

实现**可脱离界面运行**的战斗结算核心 `runBattleHeadless()`：输入双方阵容/属性，输出逐步战斗日志与胜负。供 T06 界面与后续挂机复用。

## 2. 需求表（唯一真相）

| # | 需求点 | 依据 | 验收口径 |
|---|--------|------|----------|
| 1 | 行动条机制：fillRate=(100+内功×3+机敏)/10，**满值 100 即行动** | F-05（80 拍板，1000 作废） | 单测：不同属性的行动顺序正确 |
| 2 | 我方 1 人（MVP 1v 多）；敌方数量按实战档位 R-07：<100→1；100~1W→随机1-3；≥1W→随机1-6 | 80 拍板 | 单测：档位边界与概率分布 |
| 3 | 出手判定：R-08 冷却（不同武功类型冷却回合不同）+ R-09 内力消耗（内力不足→普攻兜底） | 公式总览 | 单测：冷却/内力边界 |
| 4 | 武器范围 R-05：暗器射程 4-6-9；普攻自动寻敌 | 80 拍板 | 单测：射程内/外 |
| 5 | 胜负判定 + 防死循环：自动模式无超时；**手动 90s 超时当前回合自动代行，再 90s 切回自动** | 80 拍板 | 单测：超时状态转移 |
| 6 | 玩家初始（PlayerConfig 消费方）：100 气血 / 0 内力 / 野猫剑法 Lv10 锁级（内力耗 10） | 80 §1.1 | 单测：初始仅普攻可用（0 内力） |
| 7 | 输出：`BattleLog[]`（回合/行动者/动作/伤害/结果），类型定义进 `types.ts` | — | 类型检查通过 |
| 8 | headless 可测：不依赖 Canvas/微信 API，node 直接跑 | — | `npm run test:battle` 全绿 |

## 3. 边界

- ✅ 定死：上表公式与数值（F/R 编号）；接口形态 `runBattleHeadless(config): BattleResult`
- 🟡 有限自由：内部数据结构/结算顺序细节/单测框架选择（写明理由）
- 🚫 禁止：改公式数值；做任何 UI；改 docs/ tasks/；引入未批准依赖（vitest/jest 二选一可自决）

## 4. DoD

- [ ] `npm run test:battle` ≥ 7 个用例全绿（对应需求表 1-6 + 类型）
- [ ] `npm run typecheck` 0 error
- [ ] done 回执含：模块划分说明 + 单测框架选择理由 + BattleLog 类型示例
- [ ] 未触碰禁止区

## 5. 素材

无需素材（headless）。

## 6. 疑问通道

公式歧义 → `tasks/questions/Qn-T05.md`。**特别注意**：若发现公式总览与 03/13 文档冲突，以 config/公式总览 + docs/80 为准，仍不确定就问。
