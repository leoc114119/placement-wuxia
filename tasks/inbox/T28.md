# T28 · hero 武器层接线（剑层逐帧锚点+遮罩，第一批 26 帧）

> 状态: 待领单
> owner: frontend-battle
> 发卡: 研发线 PM 2026-09-09
> 优先级: 高（Leo 令：idle/walk/atk 先通出效果；施法帧第二批到后同接口扩展）

## 1. 基座与阵地

- 阵地：`/tmp/placement-wuxia-rd-cast`
- 新分支：`task/hero-weapon-layer`，从 `origin/main`（8b6d050a）切
- 施工蓝图：主架构方案 v1.0（commit `dc0d14ed`）——**从主目录拷贝** `docs/design/03-战斗系统/主角武器层接线实施方案.md` 进任务分支同路径（主目录=该分支检出，文件已在工作区），蓝图逐条执行

## 2. 需求表（照做，方案为唯一细粒度真源）

| # | 需求 | 方案节 |
|---|---|---|
| 1 | 素材正式落库：主目录 `assets/_trial_20260909/t45_hero_weapon_left_derivation_seq232/` 逐字节复制 → `assets/characters/hero/weapon45/`（26 张剑层 + masks/ 18 张 + manifest.runtime.json status=runtime-release + 路径级 SHA 清单）；`_trial/` 禁止 production loader 引用 | §2.1 |
| 2 | 双阶段校验：`tools/preflight_hero_weapon_layer.mjs`（CI/预览启动前：14 字段严核/26 行 bodyFrame 无重复/candidate-runtime body SHA 相等/剑层遮罩存在性/240×320 RGBA/SHA/angleDefinition/layerOrder/mask 策略/runtime 索引双向一致，失败非零退出）；runtime loader 不做 SHA 运算 | §2.2 |
| 3 | `config/hero-weapon-layer.ts`：WEAPON_LAYER_PROFILES（spriteKey→bodySrc→WeaponLayerFrame，仅 hero 条目；NPC/敌方条目需后续需求卡授权）；maskPolicy 枚举化（body-alpha-tight-fist-roi→kind），禁字符串自由发挥 | §3 |
| 4 | Canvas 管线：武器覆盖层与身体同 left/top/w/h 绘制（**零 translate/rotate**）；有 mask 离屏预合成（source-over 剑层 + destination-out mask 挖拳区孔）；禁 destination-in；绘制次序=地形→hero 身体→武器层→HUD→world FX→UI（T25/T27 时序零改） | §4 |
| 5 | 状态映射：idle 6+walk 10+atk 10 接线；**未覆盖 4 帧（walk_rightdown_2/walk_leftdown_2/atk_right_1/atk_left_1）显式空手+开发诊断**，禁借邻帧/镜像/静态剑；jump/die 显式无武器；cast 第二批 9 帧同接口预留 | §5 |
| 6 | 缺图降级：只绘身体+`hero-weapon-missing:<bodyFrame>` 写 preview asset gate；预检失败 CI 不得判过 | §2.2 |
| 7 | tests：bodySrc 键唯一/26 行映射/左右六向/mask 与无 mask/缺层降级/jump·dead 无武器/第二批 cast 可扩展/NPC 无 overlay；spy canvas 断言 right/rightup/rightdown/left 四例（身体武器矩形相同/destination-out 仅离屏/主 canvas 无 rotate） | §7.3-4 |
| 8 | 截图：375×667、560×700、900×560 三视口 idle/walk/atk 已覆盖帧各≥1 张 + 缺口诊断图 1 张；preview bundle/assetGate 更新 | §7.5 |

## 3. 禁碰区

`systems/battle-core.ts`、`cloudfunctions/settle/core.js`、数值公式、`config/numbers.ts`、session 行为、敌方 profile 与资源（**本卡禁给山贼/朴刀建运行时武器层**）、T25/T26/T27 已验收语义（TRIAL_FX_01/WF_BANNER/FxCastGate/directional profile 选帧）；主目录只读。

## 4. DoD（方案 §7 全项 + 以下）

- [ ] preflight 26 行全过；任一 SHA/缺失/尺寸/字段不符可复现实红
- [ ] 运行时资源零 `_trial/` 引用；候选/正式路径与 SHA 回执逐项列明
- [ ] 单测与 spy canvas 覆盖（§7.3-4 全项）；battle 全量+behavior 全绿零回归（fx-player 30/wf-banner 23/battle-hex-render 102 零回归）
- [ ] `battle-core.ts` diff=0；三视口截图+缺口诊断图存 shots/
- [ ] 回执：commit/文件清单/架构决策/无疑义声明/视觉项标"待 PM 复验"（Leo 要亲看有剑帧 idle/走动/普攻）

## 5. 交付

分支 push；回执返回文本（PM 管任务箱流转）；PM 四门+视觉复验 → 主架构技术验收 → Leo L 环（有剑帧 idle/走动/普攻效果）。
