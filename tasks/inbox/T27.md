# T27 · 山贼六向身体帧接线（第二段：enemy directional profile，不含武器层）

> 状态: 待领单
> owner: frontend-battle
> 发卡: 研发线 PM 2026-09-08
> 优先级: 高（Leo 令：山贼六向帧先拼到战场；朴刀武器层 Leo 与美术处理中，另卡）

## 1. 基座与阵地

- 阵地：`/tmp/placement-wuxia-rd-cast`
- 新分支：`task/shanzei-2b-wiring`，从 `origin/main`（b0ba3a79）切
- 施工蓝图：主架构方案 §9（commit `ebee5ec2`，分支 `origin/codex/arch-enemy-frame-map`）——**先把该 commit 的方案文档更新（§9 节）cherry-pick/拷贝进任务分支**，蓝图逐条执行

## 2. 需求表（照做，方案 §9 为唯一细粒度真源）

| # | 需求 | 方案节 |
|---|---|---|
| 1 | 素材拷贝（开工第一步）：主目录 `/Users/leochen/WorkBuddy/Claw/placement-wuxia/assets/characters/enemy/shanzei_a|b/battle45/` 各 31 帧+manifest → 任务分支同路径；生成 SHA256SUMS；die_common 三处（hero/甲/乙）SHA 一致性校验（§9.3 基线 ae5a6ac7…） | §9.3 |
| 2 | battle45→动画状态唯一映射（idle 1/walk 1↔2/basic atk_1→2/charge→atk_1/strike→atk_2/dead→die_common）；cut/ 目录禁接线禁 fallback | §9.1 |
| 3 | enemy directional profile：稳定键 `npc-shanzei-a|b`（+`npc-shanzei-legacy` 显式回退），预载期一次性解析，逐向逐 clip 预检（manifest/尺寸/RGBA/SHA），失败 CI 报红+预览可显式指 legacy 诊断，禁静默混用 | §9.2 |
| 4 | 敌人生成/快照输出稳定 spriteKey（不按数组序/显示名猜）；demo 敌编成用甲/乙两键 | §9.2.1 |
| 5 | 死亡白骨：animState==='dead' 优先全覆盖，die_common 单帧静态不镜像不循环，沿 PIECE.deadAlpha；不挂武器锚 | §9.3 |
| 6 | 渲染统一 spriteKey→profile→facingHex→clip→ordinal 取帧；零运行时镜像；dead 优先级最高 | §9.4 |
| 7 | tests：profile/状态映射/死亡/legacy 回归锁+旧用例持续绿；shot 六向+敌型+死亡证据（多视口） | §9.4 |
| 8 | 预览 bundle/FILES 更新；六向走位+死亡白骨演示可交互 | §9.4 |

## 3. 禁碰区

`systems/battle-core.ts`、`cloudfunctions/settle/core.js`、结算公式、`config/numbers.ts`、T25/T26 已验收产物（TRIAL_FX_01/WF_BANNER/FxCastGate 语义）、音效；cut/ 不得变运行时目录；`config/battle-frames-meta.ts` 朴刀锚点接口只预留不实现（另卡）；主目录只读。

## 4. DoD（方案 §9.5 + 以下）

- [ ] 预检机制落地：31×2 张逐张校验输出；缺帧报红路径可见
- [ ] typecheck/build/lint/全量单测/behavior 全绿零回归（含 T25/T26 的 fx-player 30+wf-banner 23 用例）
- [ ] 六向走位/攻击/死亡白骨截图证据（多视口）存 shots/；敌我同屏
- [ ] legacy 回退路径可用（显式指定诊断）
- [ ] SHA256SUMS 核验 62/62（31×2）；die_common 三处一致
- [ ] 回执：commit/文件清单/架构决策/无疑义声明/视觉项标"待 PM 复验"

## 5. 交付

分支 push；回执返回文本（PM 管任务箱流转）；PM 复验 → 主架构验收 → Leo L 环。
