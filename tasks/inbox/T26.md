# T26 · WF-2 武功名条（纯 Canvas 代码字，与光影同刻）

> 状态: 待领单
> owner: frontend-battle
> 发卡: 研发线 PM 2026-09-08
> 优先级: 高（Leo 实测 T25 光影后点名补名条）

## 1. 基座与阵地

- 阵地：`/tmp/placement-wuxia-rd-cast`；分支 `task/fx-trial-01`（顶=`c189741`，T25 技术验收已 PASS；主架构名条方案 `726fa75` 已在分支内）
- 施工蓝图（逐条执行）：分支内 `docs/design/03-战斗系统/武功名条方案-v0.1.md`
- 需求真源：`武功名弹出与施法特效需求文档.md` v1.2（WF-2/WF-3/WF-5/WF-6/WF-10）

## 2. 需求表（照做，表外不碰）

| # | 需求 | 依据（方案节） |
|---|---|---|
| 1 | 新建 `ui/wf-banner.ts`：屏幕空间名条层（t0 世界锚定格+camera 投影，禁复制 hexToWorld；非 lighter） | §2/§3.3 |
| 2 | `config/battle-hex.ts` 新增 `WF_BANNER` 只读参数组：字号=伤害数字屏高定尺×1.5、特金 #D4AF37、绝学金红渐变 #FFD66B→#E2574C、墨描边 #2B2B2B+线宽 max(2,fontPx×0.12)、投影 rgba(43,43,43,.45)(0,2)4px、时长 1.000s、上浮 24px easeOutCubic、淡入 0~10%/保持 10~65%/淡出 65~100%、maxWidth=0.82W 压缩不截字 | §4 |
| 3 | `battle-hex-render.ts`：世界 clip restore 后、drawComponents 前调用名条 draw；禁改既有 HUD/DMG 语义 | §3.3 |
| 4 | `main.ts` 触发收敛为 fan-out：accepted cast t0 同刻调 fxPlayer.start + wfBanner.start；**FxCastGate 三拦截语义与 (t0) 去重键不变，T25 既有 25 用例必须持续全绿**（重构只收敛调用点，不改 gate） | §3.2 |
| 5 | 技能白名单：special/ultimate 才弹（te 金/jue 金红），qingGong/普攻/暗器/外功/rejected 不弹；`SkillDef.name` 原样传字符串，渲染层不查配置；敌方 AI 施法同弹（颜色按 tier 不按阵营） | §2/§3.2 |
| 6 | 动画：固定 1.000s 演出钟（view.time），不随 T 缩放；x2 只加速演出钟；死亡/终局播完不截断；reset/逃跑/session 重建 clearAll | §4.2 |
| 7 | 字体：FONT_STACK（宋体族）700 加粗，不新增字体资产；wx.loadFontFace 仅未来可选，失败回退不阻塞 | §5 |
| 8 | glyph 字效素材：仅未来可选背光（默认关），本卡不接入不预载 | §6 |
| 9 | preview FILES + dist/ui/wf-banner.js；tests/wf-banner.test.ts（曲线/字号/颜色/去重/清理/敌我/白名单/全闪避空放入口） | §7 |

## 3. 禁碰区

`cloudfunctions/settle/core.js`、`systems/battle-core.ts`、`systems/battle-session.ts`、`types.ts`（不加事件字段）、结算逻辑、`config/numbers.ts`、T25 光影素材与配方（TRIAL_FX_01 与 40 张 PNG 不动）、音效；UI 层禁 import battle-core；主目录只读。

## 4. DoD（逐项勾选进回执）

- [ ] typecheck/构建/lint 过；wf-banner 单测绿；**T25 既有 fx-player 25 用例零回归**；原 battle/render/DMG 零回归
- [ ] accepted 特技/绝学 t0 同帧出字；普攻/轻功/暗器/rejected 不出；敌方同规则
- [ ] 六档截图（x1/x2 × 375×667、560×700、900×560）存 proto/battle_demo/shots/：名字完整可读、字号=同屏伤害数字 1.5 倍、特金/绝学金红可辨、描边投影在草地/土路/T25 加色层上不丢字
- [ ] 两种 T 回放（快招 0.5s/慢招 3s）：名条均 1.000s 结束不随 T 缩放
- [ ] 全闪避/空放/致死终局均出一条名条（复用 T25-R2 触发矩阵 harness）；(actorId,t0) 不重复；重开/逃跑无残留
- [ ] FILES 无 module not preloaded、无字体/资源错误
- [ ] git diff 自查禁碰区零改动；回执附 commit/文件清单/测试命令输出/架构决策/无疑义声明；预览视觉标注"待 PM 复验"

## 5. 交付

分支追加 commit 并 push；回执直接返回文本（tasks/ 流转由 PM 管）；PM 四门复验+视觉复验 → 主架构技术验收 → Leo L 环（名条+光影+L3 月牙观感一起验）。
