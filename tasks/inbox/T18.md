# T18 · 战斗 session 结构重构 + 交互语义定版（backend · 主架构 T-B1 后续）

> **状态：📤 已发单待领（先答后工）** · 优先级：P0 · 发单：2026-09-02 ZCode PM（主架构任务书转卡）
> **执行者：backend 子智能体**
> 依据：主架构《战斗交互行为规格》v1.0（`docs/design/03-战斗系统/战斗交互行为规格.md`，commit 15dde99，Leo 批复定版）——**需求表=规格 §八（五大病灶）+§五（26 组测试矩阵），行为定版=规格 D1/D3/D4**
> 前情：T15/T16 交付后 Leo L 环三轮试玩暴露移动可达语义反复失控（穿模/轻功跳不了/普通移动又能穿/时好时坏），根因=补丁修症状不灭决策点（规格 §八机制总结）。本卡=结构重构终结此循环。
> 📌 卡号说明：主架构任务书原编号 T17 已被「NPC 配置加载器」卡占用，按递增顺延为 T18（PM 分配）。

## 1. 目标

按规格 §八收敛要求**重写 battle-session 内部结构**：每个交互语义决策收敛到规格指定的唯一产生点。外部契约（types.ts 快照/submit/events）保持稳定；行为变更仅 D1/D3 两处。

## 2. 需求表（唯一真相 = 规格 §八→收敛目标）

| # | 需求 | 收敛目标 |
|---|------|----------|
| 1 | bar 拆职责：填充/消耗/回合判定/选中门控分离 | 选中生命周期归状态机对象，不再读 bar 推导 |
| 2 | isJump 归 doMove 单点推导 | 调用方传意图，消灭 AI/玩家两套公式 |
| 3 | moveCells 显示与校验同源 | 单一 legalCells 函数，snapshot 与 submit 共用 |
| 4 | selectedSkill 建模为选中态对象（skillId+kind+legalCells 派生快取） | 写点收敛 activate/clear 两动作 |
| 5 | hex 唯一真值，pos 改为调 core 前的派生视图 | 消灭手工双写与 doAttack swap-hack |

## 3. 行为定版（随重构落地）

| 项 | 定版 |
|---|---|
| D1 出生 | 锚点制：我 offset(2,13)/敌 offset(13,2) 为锚，hex 距离 ≤3 可动区内随机（规格 SP-1） |
| D3 行动提交 | bar 显式清零（规格 BAR-3，不再依赖 clamp+减法隐式配合） |
| D4 input 轻功激活态点敌 | 无操作拦截（规格 ATK-4，**ui/battle-input.ts 一处三行——主架构授权的渲染侧例外**，避免拆卡对契约） |
| D2 | clamp 复核已对齐，无工作量 |

## 4. 边界（禁区）

- 🚫 battle-core 零改动 / hex.ts 零改动
- 🚫 渲染层零改动——**唯一例外：ui/battle-input.ts 的 D4 三行**（主架构授权，超范围即停工上报）
- 🚫 scene/npc/growth 零接触
- ✅ 外部契约（types.ts 快照/submit/events）保持稳定

## 5. DoD（硬项）

1. 三零 + 全量用例不回归（59 用例；残量口径断言允许按 BAR-3 改写，**须引用规格条目编号**）
2. 规格 §五 测试矩阵 **26 组逐条对号**（用例名可不同，断言方向不得改写）
3. 结构收敛断言（病灶①~⑤各至少一条：isJump 单点=AI/玩家路径同一来源、selectedSkill 写点=activate/clear 两处、moveCells 显示=校验同函数）
4. 事件流确定性：同 seed 同操作序列全等（含 rejected）
5. 回执含：§五矩阵对号表 + 病灶收敛对照表（§八→代码位置）

## 6. 流程

81 v0.4 全流程：起草 → inbox → **backend 答疑（问题清单）→ Leo「确认开工」** → 施工 → done 回执（含 §五矩阵对号表 + 病灶收敛对照表）→ C 环 → 主架构复核 → L 环。一任务一提交 + push origin main。
