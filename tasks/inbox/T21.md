# T21 · 受击反馈（掉血数字 + 受击震动）

> **状态：📤 已发单待领** · 优先级：P1 · 发单：2026-09-03 ZCode PM（研发管线窗口）
> **执行者：frontend 子智能体（授权文件清单见 §4，严格限内）**
> 依据：《受击反馈技术方案-v0.1》（`docs/design/03-战斗系统/`，**本卡需求表 = 方案 §一~§六逐条**，Leo 放行 09-03）；需求真源=《受击反馈》v1.0
> 关联：零契约变更（types/session/core 三零），领单即开工无冻结等待

## 1. 批次口径（唯一口径）

| 项 | 定版 |
|---|---|
| 本卡范围 | 事件消费扩展（basic/skill/miss 入队）+ view 三件新状态（pendingHits 挂起冲刷队列/shakes 震动 Map/dmgStagger 错位序号）+ dmg 冒字绘制 + DMG 参数组落 config + R1-R10 用例 |
| 三零红线 | `battle-core.ts`/`types.ts`/`systems/battle-session.ts` **零改动**；休眠 hit 钩子（battle-hex-render.ts:465-467/:735）**不删不接**，双处注释互指防误判 |
| 已批口径 | miss 不震动只冒「闪避」；击杀一击冒数字不震动；数字随 strike/basic 上升沿冒（挂起冲刷）；自己格/空放天然不冒字；DMG 参数组按方案 §2.6 默认值（L 环可调） |

## 2. 需求表（唯一真相=方案条文，冲突以方案原文为准并上报）

| # | 需求 | 方案依据 |
|---|---|---|
| 1 | main.ts 事件消费段扩展：`basic/skill 且有 targetId 且 damage>0` → 入队 `{text:String(damage), shake:true}`；`miss 且有 targetId` → `{text:'闪避', shake:false}`；其余一切不入队；resetDemo 清理新增 view 状态；**数字=text 直读禁任何换算** | §2.2 |
| 2 | view 三件新状态+冲刷机制：挂起队列四条件冲刷（上升沿/已在态兜底/超时 1.5s/攻击者 dead）；冲刷定位按**当帧快照**；dmg fx 绘制（屏高定尺+白字深描边 lineJoin round+上浮渐隐+同位错位 6px×序号）；震动 cx 附加偏移 ±3px 衰减 200ms（只震受击者，dead 不震） | §2.1/2.3-2.5 |
| 3 | config/battle-hex.ts 新增 DMG 参数组（§2.6 全表）；休眠钩子双处注释互指 | §2.6/§一 |
| 4 | R1-R10 用例全绿（命名沿方案 §五）；V1-V5 可观测断言落 e2e/单测 | §三/§五 |
| 5 | bundle rebuild + verTag bump + preview 刷新目验 | 交付惯例 |

## 3. 领单第一交付（先交后码）

未获 PM「✅确认开工」前禁止写实现：
1. 基线七项复跑原文（主套件 **170 绿 14 跳**/行为 **14/14 0 红**/shot 16/e2e 5 MATCH 0 不符/DBG 0/tsc+lint 0）——不符即停。
2. 口径逐条复述（§1+需求表 5 行，重点：三零红线、白名单事件过滤、冲刷四条件、屏高定尺与 note 屏宽不同源理由），疑义清单（无则写「无」）。
3. 完整用例清单：R1-R10 展开到断言级（每条：编号/文件/断言内容/方案锚/观测面）。
4. 由用例自行总结的易错点（与方案 §四 E1-E10 互补，禁照抄）。

## 4. 授权文件清单（仅限）

- `proto/battle_demo/main.ts`（**仅**事件消费段+resetDemo 清理；`__demo` 调试挂载段不碰）
- `ui/battle-hex-render.ts`（view 三件+冲刷+dmg 绘制+震动+休眠钩子注释互指）
- `config/battle-hex.ts`（DMG 参数组）
- `tests/`（新建 battle-hit-feedback.test.ts 或扩 battle-hex-render.test.ts）
- `proto/battle_demo/bundle.js`+`index.html`（rebuild 产物）；shots 产物随交付注明
- 🚫 **禁碰**：battle-core/types/session/mock_session；渲染层禁 import battle-core/session；input 层零触碰；休眠钩子零删改

## 5. DoD（四门终验）

① 主套件全绿（**170+新增**，登记新基线数）② 行为面 **14/14 保持 0 红** ③ shot 16/16 + e2e 5 MATCH 保持 + V1/V2/V4 的 e2e 观测断言（方案 §三）④ DBG=0+verTag bump 目验；三零；文件清单+决策说明+红线自查（§4 禁碰项零 diff）+回执总表；commit 显式清单**不 push** 停等 PM 复验。

## 更新记录

| 日期 | 变更 | 签字 |
|---|---|---|
| 2026-09-03 | 创建：方案 §五拆单 T21 frontend 单卡（Leo 放行） | ZCode PM |
