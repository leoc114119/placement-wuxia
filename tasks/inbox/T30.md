# T30 · hero 六向 5 帧素材实装

- **状态**：待派单（研发 PM 收）· **已通过 projbus 发出：seq=394（art → rd，kind=delivery，2026-09-13）**
- **发起**：美术 PM2 · 2026-09-13
- **归属栈**：frontend（战斗表现/接线）
- **方案文档**：`docs/design/03-战斗系统/hero动作帧多帧化接入方案.md` **§0（v2.0 当前生效）**
- **接入需求**：`tasks/handoff/接入需求-hero六向5帧定稿-20260913.md`

## §1.2 需求表（唯一真相）

| # | 需求 | 验收判据 |
|---|---|---|
| 1 | 150 张新帧落到 `assets/characters/hero/battle45/`（5 动作 × 6 向 × 5 帧） | 文件名与需求文档第二节表格一致，逐张存在 |
| 2 | 旧帧移入 archive 并留 SHA | archive 目录 + SHA 清单文件存在 |
| 3 | `config/battle-hex.ts` 的 `clipCounts` 五项改 5（idle/walk/jump/atk/cast），`die` 保持 1 | 配置读数 = 5/5/5/5/5/1 |
| 4 | **武器合成代码不引入**；若代码内已有入口 → 注释掉并留 TODO | `grep -rn "hero-weapon-layer\|weaponLayer\|attachWeapon" src config ui` 无启用路径 |
| 5 | 运行时不缺图（六向切换不闪旧帧） | preview/preview 页六向切换目视 + 无 404 |
| 6 | 现有测试全绿 | `npm test` / `npm run test:battle` / `npm run test:behavior` |

## §1.5 派单渠道
**projbus seq=394**（`--from art --to rd`，kind=delivery，携带 commit SHA 与 artifact paths）。
Leo 已裁定：**不派子代理**，实装由研发线自行安排。
⚠️ 发出时 `rd` 收件箱有 **10 条未送达**（该窗口当前不活跃），请研发 PM 起来后统一处理。

## §1.6 DoD
- 编译 / 类型 / lint 全过
- 文件清单 + 架构决策说明（含为何不用 per-facing 覆盖）
- 未碰禁止区：不改战斗结算逻辑、不改渲染架构

## 备注 · 不在本卡范围
- **合剑武器**：改走「握剑的拳头单独贴帧」，帧素材尚未产出，待 Leo 给规格后另立卡
- run 是否覆盖 walk：按 v1.0 §1.2 的 W1 裁决（素材命名仍为 walk），如需换 run 属规格层另议
- 帧周期（walkFrameMs 等）：本卡不改
