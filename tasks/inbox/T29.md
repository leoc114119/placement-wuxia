# T29 · hero 武器层 R2：独立剑模运行时合成（剑完整显示不裁剪）

> 状态: 待领单
> owner: frontend-battle
> 发卡: 研发线 PM 2026-09-09
> 优先级: 高（Leo L 环反馈剑截断；方案 v2.1 已交付）

## 1. 基座与阵地

- 阵地：`/tmp/placement-wuxia-rd-cast`
- 分支：**续用 `task/hero-weapon-layer`**（顶=c16ba433 T28），在其上 R2 改造
- 施工蓝图：方案 v2.1——`git -c core.quotepath=false show origin/codex/t45-shanzei-2b-pilot:"docs/design/03-战斗系统/主角武器层接线实施方案.md"`（或从该分支 cherry-pick 方案 commit `4ab50dd`/美术分支 tip 进任务分支），**v2.1 supersedes v1 预摆层路线**

## 2. 需求表（照做，方案 v2.1 为唯一细粒度真源）

| # | 需求 | 方案节 |
|---|---|---|
| 1 | 素材落库：`assets/characters/hero/weapon45/models/hero_sword_model_right|left.png`（88×80，源=`_trial_20260909/t45_hero_weapon_sword_model_seq252/normalized/`，主目录可读）+ `models/manifest.json`（模型握点 [23,57] 元数据）+ 48 行 `manifest.runtime.v2.json` + masks/（T28 已入库+第二批新增）+SHA256SUMS | §2.1-2.2 |
| 2 | 48 行 runtime projection 生成（第一批 26+第二批 22 整合；来源 SHA 可追溯；`HeroWeaponRuntimeRow` 含 weaponModelKey/gripPointPx/fistCenterPx/angleDeg/layerOrder/maskPath/status=runtime-release） | §2.2 |
| 3 | 运行时合成管线：`translate(fistX,fistY)→rotate(angleDeg)→drawImage(model,-modelGrip×s,…)`（旋转中心=模型握点[23,57]；屏幕角 0°=right/+Y down 锁死；**零运行时 mirror**）；`s=h/320` 与身体同 scale | §4.1 |
| 4 | 越界绘制：动态扩展离屏 layer（body rect + rotated sword bounds + 2px margin），身体与 mask 在 layer 中同 scale 偏移，source-over 贴回主 canvas；**主场景只保留屏幕 clip，禁角色矩形 clip** | §4.2 |
| 5 | 层序逐行：body_front=weapon→body；weapon_front=body→weapon（mask destination-out 挖拳，禁 destination-in）；全局顺序与 T25/T27 零改 | §4.3 |
| 6 | 状态覆盖 48 行：idle 6/walk 12/atk 12/cast 18 不得无剑；jump/die 不查询 overlay | §5 |
| 7 | T28 旧路线退役：预摆剑层/旧 HERO_WEAPON_FRAMES/同矩形入口从 production config 移除（历史文件留审计不删）；单测扫描 production source 无 _trial/旧预摆 png/旧 map/第二入口 | §6 |
| 8 | preflight：`tools/preflight_hero_weapon_model.mjs`（两 model 88×80 RGBA/SHA/握点在画布内；48 行唯一性/左右模型关联/mask 合法性/旋转后 bounds 越出身体矩形但不出 preview 主画布；负例：错模型/错 SHA/角色矩形 clip 非零退出） | §3 |
| 9 | tests 更新：battle-weapon-layer 用例按 v2.1 重写（身份制/旋转/越界/层序/48 行/降级）+既有全量零回归；合成对照截图（对照功能只消费运行时合成截图） | §6 |

## 3. 禁碰区

`systems/battle-core.ts`、`cloudfunctions/settle/core.js`、`config/numbers.ts`、session 行为、T25/T27 已验收语义（TRIAL_FX_01/WF_BANNER/FxCastGate/npc-shanzei profile）、NPC 独立武器层；主目录只读。

## 4. DoD（方案 v2.1 §DoD + 以下）

- [ ] preflight 全过+负例复现；48 行投影生成且来源可追溯
- [ ] 出招帧长剑**完整显示越出身体矩形**（旋转后 bounds 证据+截图）；Leo 验收点=刺击长剑不截断
- [ ] 四门全绿零回归（fx-player 30/wf-banner 23/battle-hex-render 102/behavior 14）
- [ ] 旧路线退役扫描零命中；SHA256SUMS 全过
- [ ] 回执：commit/文件清单/架构决策/无疑义声明/视觉项标"待 PM 复验"

## 5. 交付

分支 push；回执返回文本（PM 管任务箱流转）；PM 复验 → 主架构验收 → Leo L 环（长剑完整显示效果）。
