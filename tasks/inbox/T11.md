# T11 · 像素 UI 全量重设计（国风像素管线 · Codex 美术管线卡）

> **状态：📤 已发单待领** · 优先级：P0 · 依赖：无（v8 角色锚已定稿）· 发单：2026-08-31 CodeBuddy
> **执行者：Codex —— 美术管线卡（设计+素材产出），非编码施工卡；禁止写游戏代码**
> 依据：Leo 08-31 拍板"UI 全线国风像素化+布局重设计，不在意美术资产重设计成本" · `docs/reviews/MVP素材需求-CodeBuddy先行版-v0.1.md`（v0.3 草案 §UX/UI）· PROJECT-MEMORY §2 美术约定 · `tasks/README.md` v2 多执行者协议

## 1. 背景与目标

美术方向已定：**国风像素（HD-2D）全覆盖**。角色锚 v8 已定稿（`assets/ui/ref_pixel_char_v8.png`），但 UI 仍是无米之炊——旧水墨 UI 基准/图标已全部作废归档（`assets/ui/archive/ink_ui_20260831/`），需从**布局层重设计**。

本卡产出 MVP 主界面+战斗界面的**像素 UI 完整设计包**：布局线框 → 风格锚 → 组件切图，一步到位交到 ZCode 可接线状态。

## 2. 需求表（唯一真相）

| # | 需求点 | 依据 | 验收口径 |
|---|--------|------|----------|
| 1 | **UX-01 主界面布局线框**：顶栏（资源条×3+头像）/场景区/底部导航/浮层逻辑，低保真线框（标注区域尺寸占比） | MVP验收门 v0.2 模块① · 回合分配 v0.6.1 主循环入口 | 线框过 Leo 目验后才准进 UX-03 |
| 2 | **UX-02 战斗界面布局线框**：HUD/棋盘/操作栏三分区重排（像素语言下的信息密度与边框逻辑） | 战场布局规格 v0.9（分区框架不变，视觉层重设计） | 同上 |
| 3 | **UX-03 像素 UI 风格锚**：色板/面板样式/按钮三态/边框/描边一页纸 | MVP素材草案 v0.3 UI 铁律 | 成为后续全部 UI 素材唯一真源 |
| 4 | **UI 组件切图第一批**：面板底×3（对话框/设置/背包，九宫格）+ 按钮三态×2组 + 像素图标全套（银两/学点/体力/背包/好友/门派/地图/武学+底部导航） | 草案 UI-01/02/03 | 组件级独立 PNG，像素网格对齐 |
| 5 | **DR-01 掉落物图标 ×8-12**（武器/防具/材料/银两袋/秘籍，品质色区分） | 草案 DR-01，无限流爽感核心 | 与 UI-02 同规格同批 |
| 6 | 任务箱全链双写 + 产出落盘 + 一任务一提交 | tasks/README v2 | 先答后工流程完整 |

## 3. 硬约束（红线）

- 🚫 **像素网格对齐**：所有 UI 位图按基准网格整数倍设计，禁非整数倍缩放；9:16 整屏参考图只能辅助，**交付必须是组件级切图**（面板/按钮/图标独立 PNG，透明底）
- 🚫 **AI 稿禁文字**（文字全部代码绘制）；图标无水印
- 🚫 禁止写游戏代码/改 config（Codex 边界）
- ✅ 生成管线自选（Aseprite 手绘 / liblib 像素 LoRA / mxai 等），但**风格一致性锚 = UX-03**，批量组件必须同源
- 📐 尺寸基准：竖屏 9:16 · 设计稿 750×1334 · 图标 32×32 网格整数倍

## 4. 交付物与落盘

| 交付物 | 路径 |
|---|---|
| UX-01/02 线框（SVG/PNG/HTML 均可） | `docs/reviews/T11-ux-wireframes/` |
| UX-03 风格锚 | `assets/ui/ref_pixel_ui_v1.png` |
| 组件切图 | `assets/ui/pixel/buttons/` `panels/` `icons/` |
| 掉落图标 | `assets/ui/pixel/icons/drops/` |
| 交付说明（含生成 prompt 记录，供 77 清单回填） | `docs/reviews/T11-交付说明.md` |

## 5. 流程（两段门禁）

```
领单 → 先答后工（Q1-T11 理解复述+问题清单）→ ✅ 确认开工
    → 第一段：UX-01/02 线框 → Leo 目验（不过则打回重排）
    → 第二段：UX-03 风格锚 → Leo 目验 → 组件切图+掉落图标批量
    → 📦 交付 → C 环（CodeBuddy：像素对齐/风格一致性/禁文字/尺寸审计）→ L 环终验
```

## 6. DoD

- [ ] UX-01/02 线框过 Leo 目验
- [ ] UX-03 风格锚过 Leo 目验并转正 `ref_pixel_ui_v1.png`
- [ ] 组件切图第一批（面板×3/按钮三态×2组/图标全套）落盘且像素对齐审计通过
- [ ] DR-01 掉落图标 ×8-12 落盘
- [ ] 交付说明含全部 prompt 记录
- [ ] 任务箱双写完整，git 提交前缀 `T11：`

## 7. 疑问通道

`tasks/questions/Qn-T11.md`，停下等答复，禁止自行假设。
