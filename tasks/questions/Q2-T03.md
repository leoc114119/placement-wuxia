# Q2-T03 · 可走区限制：中央走廊方案（Leo 22:08 定稿 · 08-22 00:57 数值随 scene v6 定稿更新）

> 发单：CodeBuddy（Leo 拍板）· 处理人：ZCode · 状态：**已修复（08-22 01:30），待复验**
> 结果：WALK_ZONE 进 config（x[0.24,0.76]×y[0.46,0.84]）· clampTarget 走廊化（逐轴 clamp=矩形最近点，区外不拒绝）· HERO_START(0.5,0.72) 已在走廊内无需动 · 用例 4 组（区内/四边/角外/半出界）· 27/27 绿（既有 24 不回归）· node 模拟：tap(0.05,0.1)→(0.24,0.46) ✓

## 需求（唯一真相）
1. **可走区 = 中央走廊矩形**：x ∈ [0.24, 0.76]，y ∈ [0.46, 0.84]（数值进 config/numbers.ts 可调常量，命名 WALK_ZONE）——对齐 scene_jianghu v6 定稿：野径远端起于 y≈0.45 林带、底部路宽约占画面一半、底部 16% 留 UI 渐暗收边
2. `clampTarget` 改为：目标点 clamp 进走廊（点击区外 → 走到最近走廊边界点，不拒绝）
3. 角色初始位置放进走廊内（当前 HERO_START 若在走廊外，拉进来）
4. 用例更新：走廊 clamp 边界 ≥4 用例（四边+角+区外点击拉回），24 用例不回归
5. 场景图已定稿转正：assets/ui/scene_jianghu.png = v6（gpt-image-2，无河+宽路透视），路径不变代码无需改（CodeBuddy 已完成，你无需管素材）

## 边界
- ✅ 定死：WALK_ZONE 数值与 clamp 语义
- 🟡 自由：常量命名/是否加轻微远近收 taper（默认不加，矩形即可）
- 🚫 禁止：改渲染层结构/动其他模块

## DoD
- [ ] WALK_ZONE 进 config，clampTarget 走廊化，用例全绿
- [ ] node 模拟：点击 (0.05,0.1)（左上角区外）→ 目标被拉回走廊最近点
- [ ] threads/LOG 登记，回执写明改动文件
