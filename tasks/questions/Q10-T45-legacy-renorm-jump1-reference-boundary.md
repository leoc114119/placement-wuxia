# Q10-T45 · 组系数重跑后 jump_right_1 的 ±15% 边界

已按 A9 裁定完成右向 10 帧 R2 试点：

- walk+jump 组均值冠宽 `11.25`、组系数 `0.702077031660`；输出冠宽 `8/8/8/8/8/7`，极差 1px，组刚体硬门通过。
- atk 组均值冠宽 `7`、组系数 `1.128338086596`；输出冠宽 `8/8/8/8`，极差 0px，组刚体硬门通过。
- 10/10 帧几何、无裁切、单连通、加工安全、SHA 互异均通过。
- v2 ±15% 参考门仅 `jump_right_1` 失败：试点高 `180`、v2 高 `212`，差 `-15.094%`，超严格 `-15%` **0.094 个百分点**；其余 8 个可比帧通过，`atk_right_4` 无 v2 同帧，标 N/A。

请裁定：

1. `jump_right_1` 的 `-15.094%` 是否按像素取整/参考门测量误差接受为 R2 通过；或
2. 维持严格 ±15% 判定，R2 停在此处，不铺后续。

停点：不处理右向其余 20 帧、右上/右下、左系或 runtime。结果见 `tasks/handoff/T45-legacy-renorm-right-pilot-result-r2-20260910.md`，QA 见 `assets/_trial_20260910/t45_legacy_renorm_right_pilot_seq342/qa/right_legacy_renorm_pilot.json`。
