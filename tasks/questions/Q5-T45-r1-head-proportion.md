# Q5-T45 · R1 右向 jump 头部视觉占比偏大

## 新反馈

Leo 目测指出：R1 对照中 jump 三帧的头明显偏大。

## 复核结果

对 seq=300/304 候选做确定性复核：

- 基准 `battle_idle_right` 冠饰外框 `13×18`；
- seq278 的 walk 冠饰宽 `17/18/17`，相对基准约 `+31%/+38%/+31%`；
- seq278 的 jump 冠饰宽 `17/17/16`，相对基准约 `+31%/+31%/+23%`；
- seq279 的 atk 冠饰宽 `14/14/14/13`，相对基准约 `+8%/+8%/+8%/0%`。
- 但固定头部探针在 12 张保留帧上均为 `87px`，波动 `0%`；这说明 jump 没有被单帧机械放大。

证据：`assets/_trial_20260910/t45_hero_weapon_recalib_r1_right_seq300/qa/head-proportion-recheck.json`。

## 结论与停点

视觉偏大主要来自 jump 身体自然缩短：头部绝对尺度保持，但头在整个人中的占比更高。不能据此在归一阶段单独缩小 jump；那会破坏 R1 的固定组系数和动作比例。

请 Leo 裁定视觉门：若不接受 jump 的头身观感，需要退回/改选 jump 源图；若接受其自然蹲姿占比，则继续按 seq=302 组系数钳制和不裁切门处理。两种决定前均不启动 Phase B、不写 runtime。
