# Q1-T03 · L 环打回：walk 动画不自然 + 流程纠正

> 打回人：Leo（21:27）· 处理人：ZCode · 状态：**四轮：素材重生成需求已报（23:00），等管线执行**
> 一轮结果：抬剑帧已消（04 出招预备移出 walk）。「高抬腿帧」仍在 → 见下方复验反馈#2。
> 处理结果：两现象根因均为代码侧帧映射（素材实为六帧表，04=出招预备混入 walk 循环）→ 已修 `HERO_FRAME.walkEnd 4→3`；素材目验结论与可选项见 `Q1-T03-素材目验回报.md`

## 复验反馈#4（Leo 22:55 · 原话转述）

> 「感觉需要把抬脚这一帧改成小幅度的迈步才可以，现在还是不太自然」

- **结论：素材缺口坐实**。01↔02 交替后，01（设计即「起步抬左腿」）的抬脚观感仍不自然；全 8 帧排查（含 05/06/07：出招/普攻/空）无任何「小幅迈步」帧可选，代码侧本轮不改码。
- **已就绪**：`config/assets-p0.json` hero prompt 更新为「②③④ = 侧身向左·左右脚小步交替（低抬腿、脚贴地滑出、严禁高抬腿、剑垂身侧）」，出招帧规格不变。
- **待执行**：`python3 scripts/assets_pipeline.py --only hero`（CodeBuddy 管线；ZCode 侧无 key/Pillow 且 assets/ 属禁区）。
- 新素材落地后 ZCode 做：腿位核验 → `HERO_FRAME` 映射更新 → 用例/复测 → 复验。详见 `Q1-T03-素材目验回报.md` 四轮节。

## 复验反馈#3（Leo 22:35 · 原话转述）

> 「现在是滑动啊。。。就是左走只会左边出脚。。。右走只会右边出脚，需要有两只脚交替走的两帧，不是滑着走」

- 二轮的 (02,03) 对虽是「右/左」腿位，但两帧同为迈开姿势、步幅开合变化不足 → 交替感弱，观感单侧伸脚滑行。
- **定位方法升级**：01/02/03 拼图（/tmp/hero_walk_compare.png）+ 脚区像素对比（前景水平范围 01=[0.23,1] / 02=[0.06,1] / 03=[0,0.86]）严格核验腿位：**01=左腿在前(收步)、02=右腿在前(迈开)、03=左腿在前(收步变体)**。真正「左右脚交替 + 收张开合明显」的对 = **01↔02**。
- 修法：`HERO_FRAME.walkStart 2→1`（walk = 01↔02，160ms/帧）；03 弃用（与 01 同腿位的变体，无交替价值）。
- 复测：三零 + 24/24 + node 帧序列 `1→2→1→2` 交替，03/04 永不出现（全时间轴断言）。

## Leo 反馈（原话转述）
1. **人物走动不自然，有一个高抬腿的帧**
2. **走动后会出现"抬剑"的一帧**
3. （流程）bug 应该 ZCode 来修

## CodeBuddy 验收诊断（供 ZCode 定位，不代替修复）

### 现象 1：高抬腿帧
- walk 循环 = 帧 01~04（01=起步抬左腿，02=迈出落地，03=另一脚跟步，04=??）
- **疑点**：AI 生成的帧表 04 帧动作与 77 文档口径（抬腿/迈出/落地/跟步）可能不符——素材侧问题概率大于代码侧。请先目验 `assets/ui/frames/hero/hero_04_transparent.png` 是否为夸张高抬腿。
- 若是素材问题：报回 CodeBuddy 走素材重生成管线（`--only hero`），ZCode 侧无需改码。
- 若代码侧帧序错：修 `config/numbers.ts` HERO_FRAME 映射。

### 现象 2：走动后出现"抬剑"帧（帧 05）
- 代码只在 state='walk' 时用 01~04，到达后回 idle=00。但**到达瞬间**：`update()` 里 `avatar.moving=false` 后 `walkMs=0`，而下一帧渲染时 state 已是 idle → 应显示 00。
- **疑点**：到达判定 `stepAvatar` 返回新值中 state='idle' 是否与渲染同帧生效；或素材 **00 帧（待机）本身带剑上抬姿势**。请目验 `hero_00_transparent.png`。
- 若 00 帧姿势异常：同素材问题流程报回。

### 现象 3（流程，Leo 拍板）：**bug 一律 ZCode 修**
- CodeBuddy 只做验收诊断与打回，不再直接改码（今晚 T02/T03 热修×6 属越权，已反省；后续 CodeBuddy 的修复仅限"ZCode 无法处理的本地环境配置类"）。
- 打回流程：Leo 反馈 → CodeBuddy 诊断 → 工单（本文件）→ ZCode 修复 → 复验。

## 修复要求
- [ ] 定位现象 1/2 根因（素材 or 代码），给出结论
- [ ] 代码侧可修的修掉（帧映射/状态时序），素材侧的报回
- [ ] 回执写明根因与修法，更新 threads/LOG
