# Q8-T45 · V2 四样本中 `atk_right_1` 无法满足同一系数站立高

依据 seq=330：四样本必须共用一个由 idle 源人物 bbox 高推导的系数；`idle/atk` 属站立帧，输出人物 bbox 高须为 `256±1`；walk/jump 高度自然记录；通过后才生成剩余 6 帧。

实测系数为 `0.22241529105125976`（`256/1151`）：

- `idle_right` 源 bbox 高 `1151` → 输出 `256`，通过；
- `atk_right_1` attempt 1 源 bbox 高 `1113` → 输出 `248`，失败；
- `atk_right_1` attempt 2 源 bbox 高 `1108` → 输出 `246`，失败。

两次均为 Codex 原生单参考单帧生成，已达到普通原生两次上限。不能同时保持“同一系数”和“atk 站立高 256±1”；逐帧改系数会违反 seq=330 的同系数证明，强行纵向拉伸会改变动作比例。

请 PM2/Leo 裁定：

1. 是否调整/重定义站立帧高门或同一系数口径；
2. 是否作废本四样本包并重新拍样；
3. 若授权其他路线，请明确允许的确定性处理和验收标准。

停点：不生成剩余 6 帧，不改 runtime，不继续第三次原生尝试。
