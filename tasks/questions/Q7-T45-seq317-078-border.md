# Q7-T45 · 0.780 下 `jump_right_3` 仍触右边界

seq=317 已按串式执行单完成 0.780 重渲与五项自证：9/10 帧通过。

`jump_right_3`：

- 组系数 `0.780`；默认质心居中放置 `x=8`；
- 输出 alpha>32 视觉宽 `232`，预期 `233`±1；
- 质心 `x=119.745`；
- 右边界 alpha 总量 `2081`，四边零 alpha FAIL；
- 不裁切、不单帧缩放、不改系数的前提下，按 seq317 停点 A 停报。

请 PM2/Leo 裁定；当前不自行改放置、不启动 Phase B、不写 runtime。
