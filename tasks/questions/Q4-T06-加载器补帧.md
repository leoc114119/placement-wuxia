# Q4-T06 修复单 · 战斗帧加载器补 00~07（黑椭圆真因）

> 出单：2026-08-22 21:10 · CodeBuddy → ZCode · 关联：Q3-T06 接线轮 + 素材回报单（勘误）
> 优先级：P0 · 预计一行修复 + 断言

## 真因（CodeBuddy C 环定位，证据齐全）

1. `ui/assets.ts loadBattleAssets()`：`BATTLE_FRAME_COUNT = 7` **只加载 00~06 七帧**（该常量写于 idle=3 时代，⑧格入编后未同步）。
2. `BATTLE_FRAME.idle = 7` → 渲染取 `frames[7]` = **undefined** → 命中 `ui/battle-render.ts:347`「无帧降级：墨色胶囊」→ `ctx.ellipse(...)` 按棋子宽高画**墨色椭圆**。
3. Leo L 环截图实锤：主角+野狼**两颗棋子同为纯黑竖椭圆**（所有 kind 的 frames[7] 全缺，与素材内容无关）。

## 勘误（撤回 Q3-T06 素材回报单的重出请求）

- 「d2⑧帧素材跑偏成黑影」**不成立**——黑椭圆是降级占位，不是素材渲染结果。
- hero_07 文件实测前景亮度 125，略暗于站立帧基线 144~150（-13%），属轻微风格偏差**非黑影**；山贼/野狼/狼王⑧格亮度审计全部在各自角色正常带内（79/153/93）。
- 处置：hero_07 轻微偏暗转**观察项**——本单修复上线后 L 环复验，Leo 仍嫌暗再走素材重出，本轮不动。

## 修复要求

1. `BATTLE_FRAME_COUNT` 7→**8**（加载 00~07 全八帧）；建议改为由 `BATTLE_FRAME.idle + 1` 派生或加断言，防帧表扩充再犯。
2. `BATTLE_FRAME.idle` 保持 **7**（回退兜底解除）。

## DoD

- [ ] 三零 + 全部用例绿
- [ ] 新增断言：battle frames 每 kind `length === 8` 且 idle 帧已加载（防回归）
- [ ] node 渲染断言 idle=7 生效
- [ ] GUI 留 L 环点验：棋子正常显示、黑椭圆消失、⑧侧身待机观感（顺带评 hero_07 偏暗是否可接受）
- [ ] 交付纪律：回执 + threads/T06 + LOG 双写 + 任务箱 📦 + git 注明 Q4-T06
