# Q3-T03 · UI 层级修正：地图标签下移 + 三按钮上移（Leo 08-22 01:13 反馈）

> 发单：CodeBuddy（Leo 拍板）· 处理人：ZCode · 状态：**已修复（08-22 01:30），待复验**
> 结果：SCENE_LABEL.y 0.055→0.115、SCENE_BUTTONS.yRatio 0.885→0.82（各一行常量 + 注释预留说明）· 渲染/hit 逻辑零改动（布局函数自动跟随常量）· 27/27 绿 · node 模拟：标签中心 y=0.115 ✓ 按钮行 cy/H=0.820 ✓
> 背景：当前元素位置未预留系统组件区。权威基准 = `assets/ui/ref_jianghu_ui_v1.png`（UI 定稿 v6，CodeBuddy 已像素测量）。

## 需求（唯一真相）
全屏场景之上的浮层，自上而下堆叠顺序：
1. 微信胶囊（系统组件，右上 y≈0.04~0.10）——**本单不实现，仅预留，禁止遮挡**
2. 人物状态栏（木纹条 y≈0.03~0.085，头像+银两+学点）——**本单不实现，仅预留**
3. **地图标签下移**：`SCENE_LABEL.y` **0.055 → 0.115**（状态栏下沿之下；x 0.05 不变）
4. **三按钮上移**：`SCENE_BUTTONS.yRatio` **0.885 → 0.82**（四 Tab 栏上沿 0.92 之上、场景面板内，对齐定稿按钮行中心）
5. 四 Tab 导航栏（y≈0.92~0.995，武学/装备/江湖/门派）——**本单不实现，仅预留**

## 边界
- ✅ 定死：两个 y 常量数值（均在 config/numbers.ts）
- 🟡 自由：标签与按钮的间距微调 ±0.01（目测对齐定稿即可）
- 🚫 禁止：实现状态栏/Tab 栏本身（属后续任务）；改 hitSceneButton 点击优先逻辑（现行为正确）；动场景/角色渲染

## DoD
- [ ] numbers.ts 两常量更新，typecheck/lint/build 三零 + 24 用例不回归
- [ ] node 模拟断言：标签中心 y≈0.115±0.01、按钮 cy≈0.82±0.01
- [ ] threads/LOG 双写登记，回执写明改动文件
