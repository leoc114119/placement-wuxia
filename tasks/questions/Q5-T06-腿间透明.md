# Q5-T06 · hero 走姿帧两腿间透明（39%透明率 → 露地显白）

> 出单：2026-08-22 23:55 · CodeBuddy · 优先级：P1 · 关联：T06 白块根因末梢

## 真因（Leo L 环截图 + C 环像素审计实锤）

放大图清晰显示：红框内 = 鹅卵石地面纹理（浅灰），**不是白块残留，是 sprite 透明区（alpha=0）透出底层地面**。

`assets/ui/frames/hero/hero_*_transparent.png` 两腿间带（x 220-290, y 720-920）透明率：

| 帧 | 透明率 | 现象 |
|---|---|---|
| hero_00（静止） | **0%** | 腿间完全覆盖，没事 |
| **hero_01（走姿）** | **39%** | 腿分开大，**显白** |
| **hero_02（走姿）** | **39%** | 腿分开大，**显白** |
| hero_03（走姿尾） | 2% | 接近覆盖 |

**Leo 截图时主角处于走姿 hero_01/hero_02 → 腿间 alpha=0 透出底层地面（鹅卵石浅灰）= 看起来像白块**。
山贼移动帧同病（走姿帧腿间大概率类似）—— 待 L 环一并验证。

## 修复方案

**A · 根治（推荐）**：重出 hero_01/02 帧，prompt 追加「**两腿之间必须有裙摆/衣摆覆盖，不许露出透明空隙**」。img2img 锚定当前帧只补腿间覆盖区域（避免重画全身）。山贼/野狼走姿帧同步查+补。

**B · 渲染兜底（临时）**：在角色脚下/腿间区域绘制半透明深色椭圆作为"地面阴影"。一行渲染代码改动，所有角色统一走。B 适合 L 环先看效果（不等重出），A 后置做根治。

C 环偏好：B 立刻发车（ZCode 一行改动）作为过渡，A 立项为 P1 后置。

## DoD
- A 方案：hero_01/02 重出后两腿间透明率 < 5%，姿态保持（不重画全身）；山贼走姿帧同验证
- B 方案：渲染层加 `drawFootShadow(ctx, x, y, dh)` 函数，所有 role（hero + npc）调用一次
- L 环：游戏里走姿时两腿间看不到地面

## 交付纪律
- 一任务一提交（git commit 前缀 `Q5-T06：` + push）+ threads/T06 + LOG 双写 + 任务箱事件
