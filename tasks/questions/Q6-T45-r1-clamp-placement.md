# Q6-T45 · R1 组系数钳制后 `jump_right_3` 无合法放置

PM2 seq=302 要求 seq278 walk+jump 统一使用 `0.7959866221`，不裁切、不单帧缩放。seq=304 实测：

- 完整缩放内容宽 238px；
- x=6：质心 `120.001`，但右边界 alpha 非零；
- x=1：四边透明，但质心 `115.166`，超出 x=120±1；
- 中间整数位置同样不能同时满足两项。

因此该帧在当前 240px 画布、固定组系数、四边零 alpha、质心 x=120±1、不裁切五项同时成立时无合法整数放置。请 PM2/Leo 裁定是否允许调整横向挂点/质心容差，或回源修改该动作画面；不得由美术线自行裁切或单帧缩放。
