# Q1-T03 · 素材目验回报单（hero 帧表）

> 回报：2026-08-21 22:05 · ZCode → CodeBuddy/Leo · 关联工单：questions/Q1-T03.md
> 结论先行：**两现象直接根因均为代码侧帧映射错误（已修），素材无必须重生成的硬伤；两条可选项待 Leo 复验后拍板。**
>
> **→ 复验拍板（Leo 22:15）：素材不重生成。残余「高抬腿」= 01 起步帧，代码侧弃用，walk 收为 02/03 左右两帧交替（见工单复验反馈#2）。本回报单可选项 a/b 就此关闭。**

## 一、目验方法与逐帧结论

AI 视觉逐帧核验 `assets/ui/frames/hero/` 透明帧 00~04（放大看姿态/持剑/朝向）：

| 帧 | 目验结论 | 用途判定 |
|---|---|---|
| 00 | 正面站立，无朝向，自然待机 | ✅ idle（待机）正常 |
| 01 | 面左，左腿迈出在前，剑垂下 | ✅ walk（起步迈左脚） |
| 02 | 自然行走，右腿抬约 45°，剑垂下 | ✅ walk（右脚交替） |
| 03 | 自然行走，左腿抬约 45°，剑垂下 | ✅ walk（左脚交替） |
| 04 | **双腿站定不迈步，剑举向斜上方（出招预备姿势）** | ❌ 不是行走帧 = 出招预备 |

## 二、根因判定（对应工单修复要求 #1）

- **素材实际口径 = docs/77 §2.2 六帧表**（待机/左脚/右脚/举手预备/挥出/普攻），而非 §0.1 的八帧设计（idle1+walk4+attack2+basic1）。任务卡「01~04=行走循环」按八帧口径预写，与素材实物不符。
- **现象 2（走动后"抬剑"帧）**：04 号出招预备帧被映射进 walk 循环，每圈出现一次 → **代码映射错误，已修**（`walkEnd` 4→3）。
- **现象 1（"高抬腿"帧）**：大概率同一帧——03 抬左腿后接 04 突然站定举剑，步态被打断；且 04 单腿承重+举剑的姿势易读作"高抬腿"。01~03 本身是幅度一致的左右交替自然步态（抬腿约 45°，Q 版正常范围）→ **主因代码侧，已修**；若复验后仍觉不自然见下可选项 a。
- 另排除代码时序嫌疑：热修后主循环为 update→render 同帧生效，到达即显示 00 待机帧，无"多播一帧"问题（C 环复跑证据：帧序列 1→2→3 回卷，到达后 0）。

## 三、素材侧可选项（不阻塞复验，待 Leo 拍板）

- **a) 抬腿幅度**：02/03 抬腿约 45°，属 Q 版正常步态。若 Leo 复验后仍觉得"高抬腿"不自然，建议走重生成管线 `python3 scripts/assets_pipeline.py --only hero`，prompt 追加「行走帧抬腿幅度小、接近日常走路，不要夸张高抬腿」。
- **b) walk 帧数**：素材实物 walk 只有 3 帧（左/右/左交替），比 77 §0.1 设计的 4 帧（起步/迈出/落地/跟步）少"跟步"帧。3 帧循环视觉可接受（已按 3 帧接好）；若 Leo 要 4 帧步态，同走重生成管线并在 prompt 写明四帧行走分解。
- 重生成后 ZCode 侧只需把 `HERO_FRAME.walkEnd` 调回 4（一行配置），无代码风险。

## 四、代码侧修复摘要（详见 done/T03-done.md 打回修复记录）

`config/numbers.ts`：walk 循环 01~04 → **01~03**，预载 5 帧 → 4 帧（04 出招帧留给 T06）；`tests/scene.test.ts` 帧用例同步 + 全循环不越界断言。三零错误 + 24/24 用例 + node 帧序列 `1→2→3→回卷` 复测通过。
