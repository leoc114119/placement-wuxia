# T30 派单提示词（已封存，额度恢复后直接发）

> ## ⏭️ 下一次运行先读这里
>
> **当前状态：派单被额度阻塞，未派出。**
> 阻塞证据（连续两次）：`[1310] 您已达到每周/每月使用上限，您的限额将在 2026-09-14 10:51:46 重置`
>
> **额度恢复后要做的唯一一件事**：把下面 ```` ```text ```` 代码块**整段原样**作为 prompt，
> 用 Agent 工具、`subagent_type=frontend-battle` 派出去执行 T30。**不要改写、不要摘要、不要自己动手改代码。**
>
> 若再遇 `[1310]` ⇒ 如实登记 `tasks/LOG.md` 并把卡点写回本文件顶部，**不要假装已派发**。
> 派发成功后，等子代理回执（`tasks/done/T30-done.md`），逐条核对需求表六条证据后向 Leo 汇报。
>
> 关联：卡 `tasks/inbox/T30.md` · 需求 `tasks/handoff/接入需求-hero六向5帧定稿-20260913.md` ·
> 方案 `docs/design/03-战斗系统/hero动作帧多帧化接入方案.md` §0(v2.0)

- **封存时间**：2026-09-13
- **为什么没发出去**：子代理派单被额度挡住 —— `[1310] 您已达到每周/每月使用上限，限额将在 2026-09-14 10:51:46 重置`（连续两次确认，**账号级、非瞬时故障**）
- **恢复后怎么发**：把下面代码块整段作为 prompt 交给 `frontend-battle` 子代理

```text
你是 placement-wuxia 项目的战斗前端代理。执行任务卡 T30：把美术线交付的 hero 六向 5 帧素材实装进游戏。

工作目录：/Users/leochen/WorkBuddy/Claw/placement-wuxia（分支 codex/t45-shanzei-2b-pilot，不要切分支）

## 先读这三份（必读，不要凭上下文推断）
1. docs/design/03-战斗系统/hero动作帧多帧化接入方案.md 的 §0（v2.0 当前生效节，在最上面）
2. tasks/handoff/接入需求-hero六向5帧定稿-20260913.md
3. tasks/inbox/T30.md

## 要做的（严格按需求表 §1.2 六项，表外一律不碰）

① 素材落位：把下列源目录的 30 张/组，按目标名复制到 assets/characters/hero/battle45/
   idle  assets/_trial_20260912/glb2d_idle/idle_6dir_ratio/  → battle_idle_{向}_{1..5}.png
   walk  assets/_trial_20260912/glb2d_walk/walk_6dir_ratio/  → walk_{向}_{1..5}.png
   jump  assets/_trial_20260912/glb2d_jump/jump_6dir_ratio/  → jump_{向}_{1..5}.png
   atk   assets/_trial_20260912/glb2d_atk/atk_6dir_ratio/    → atk_{向}_{1..5}.png
   cast  assets/_trial_20260912/glb2d_cast/cast_6dir_ratio/  → cast_{向}_{1..5}.png
   向 = left/leftdown/leftup/right/rightdown/rightup
   各目录根的 PNG-8 件即交付件（_rgba/ 子目录是对照原件，不要接进游戏）
   源文件前缀分别是 battle_idle_ / walk_ / run_ / jump_ / atk_ / cast_，注意 walk 与 run 是两个不同目录，按上表取
   glb2d_run/run_6dir_ratio/ 本次不用（是否换 run 属规格层，未定）

② 旧帧归档：把现有 battle45/ 里被替换的旧帧（idle 1 / walk 2 / jump 2 / atk 2 / cast 3）
   移入 archive 子目录并输出 SHA 清单文件。die_common.png 不要动。

③ 配置：config/battle-hex.ts 的 SPRITE_PROFILES[*].clipCounts 改为
   { idle: 5, walk: 5, jump: 5, atk: 5, cast: 5, die: 1 }
   六向全齐，不要启用 clipCountsByFacing。

④ 武器（重要）：
   - 武器层代码（config/hero-weapon-layer.ts 等）只在 task/hero-weapon-layer 分支，本分支没有
     → 绝对不要合并那个分支、不要引入任何相关依赖
   - 若你在本分支代码里发现任何武器合成/挂载入口 → 注释掉，并写：
     // TODO(weapon-redo): 合剑武器改走「握剑的拳头单独贴帧」，见 tasks/handoff/接入需求-hero六向5帧定稿-20260913.md §四
   - 验收：grep -rn "hero-weapon-layer\|weaponLayer\|attachWeapon\|weaponImg" src config ui systems types.ts 应无启用路径

⑤ 验收：按 T30 需求表六条逐条跑证据；第 6 条跑 npm test / npm run test:battle / npm run test:behavior
   （若脚本名不同，先看 package.json）。如实报结果，不许把红说成绿。

## 硬约束
- 禁改战斗结算逻辑（systems/battle-core、battle-session、hex）
- 禁动历史验收过的模块做整体重写；要改就小步改
- 禁硬编码数值
- 遇到需求含糊/冲突立即停下，把问题写进 tasks/questions/，不要自行假设

## 交付
1. 代码/素材改动 commit（写明改了什么、为什么）
2. 回执写入 tasks/done/T30-done.md：需求表六条逐条勾选 + 证据（实际命令与输出摘要）
   + 架构决策说明（含为何不用 per-facing）+ 文件清单 + 未碰禁止区声明
3. 卡从 inbox 移到 done（python3 scripts/task.py move placement-wuxia T30 done --status "待研发验收"），
   并在 tasks/LOG.md 追加一行

最后用中文汇报：做了什么、六条需求逐条实际结果（含测试原文摘要）、有没有没做到或存疑的地方。
```
