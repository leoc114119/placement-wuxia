# T45 seq=387 · 全身 cel 明暗分块试产结果

日期：2026-09-13

状态：候选完成，待 PM2 目验；本次只做全身明暗分块，不做细节增强、不改 runtime。

## 执行事实

- 使用 Codex 原生 `image_gen` 1 次，cost=0；目标是把头、头发、衣服、腰带、裤子、靴子统一成三档 cel 明暗块。
- 参照 `头部_明暗分块.png` 复用同一套光照逻辑；没有加入发丝、衣纹、绣花、纹理或五官重绘。
- 原生输出 `900×1748` 与目标 `640×1243` 同比例；只做一次等比例缩回到 `640×1243`，没有裁切或非等比拉伸。

## 产物与 QA

- raw：`assets/_trial_20260912/明暗贴图试产/交付/raw_fullbody_shading_imagegen.png`
- raw SHA-256：`711cb151781f1f889de42c3afb81f9003c716e7a437469cc5652075e65b039b`
- 候选：`assets/_trial_20260912/明暗贴图试产/交付/全身_明暗分块.png`
- 候选 SHA-256：由 `qa_fullbody.json` 记录
- 候选尺寸/模式：`640×1243 RGB`
- QA：`assets/_trial_20260912/明暗贴图试产/交付/qa_fullbody.json`，`selfGatePass=true`
- 处理：仅 uniform resize；未做 detailBlend、alpha restore、局部重绘或颜色校正。

## 停点

等待 PM2 回填后 3D 重渲，并与线上 2D 做整帧并排对比。未通过前不改正式贴图、shader 或 runtime。

## 批次健康门

收尾线程健康读数：rollout `287.5 MB`、`39` 轮、压缩 `4` 次；未触发线程轮换阈值。本批图片读取 2 次（全身目标、头部候选），未超过 ≤3 上限。
