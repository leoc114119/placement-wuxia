# T45 seq=358 · 拳交替 v5 结果

日期：2026-09-11

状态：路径 A、路径 B 均机械失败；两条路线的全部候选均已交付；正式资产与 runtime 未改。按本单范围停止，不追加生成。

## 执行事实

- 路径 A：1 次原生生成得到横向双格图，随后用等宽、无插值裁切得到 α/β 两帧。
- 路径 B：2 次从同一 idle 独立原生生成，α/β 不互相参照、不走链。
- 总计 3 次 Codex 原生生成，provider=`codex-native`，cost=0，未超过本单上限。
- prompt 全部使用 idle 坐标位置指称 α=(183,193)、β=(101,205)，未使用解剖左右手命名；方向要求均为“朝画面右且恒定”。

## 路径 A：双格同图

| 判据 | 实测 | 结论 |
|---|---|---|
| D1 姿势正确 | 两格各检测到 2 拳；前拳中心归一化约 `(206.92,143.00)` / `(207.76,143.01)`，y 均低于规定前伸区 150~180 | **FAIL** |
| D2 换手 | 两格腰侧拳 x=`93.97 / 94.56`，未形成 β 原始位≈101 与 α 原始位≈183 的交换 | **FAIL** |
| D3 非手臂区差异 | `3.57% ≤ 8%` | PASS |
| D4 拳数 | `2 / 2` | PASS |
| D5 单一角色 | 接触表逐格目视为每格 1 个角色 | PASS |

路径 A 结论：双格图保持了两格同角色和主体连续性，但两格都生成了同一侧拳前伸，且前拳落在规定肩高区之上，不能作为合格换手帧。

## 路径 B：单帧直生

| 判据 | 实测 | 结论 |
|---|---|---|
| D1 姿势正确 | α/β 两帧均为 1 拳前伸区 + 1 拳腰侧区 | PASS |
| D2 换手 | 腰侧拳 x=`93.57 / 97.96`，两帧均回到同一位置，未交换 | **FAIL** |
| D3 非手臂区差异 | `22.28% > 8%` | **FAIL** |
| D4 拳数 | `2 / 2` | PASS |
| D5 单一角色 | 接触表逐帧目视为每帧 1 个角色 | PASS |

路径 B 结论：两张独立帧都复用了同一侧前拳；同时身体非手臂区域差异超过可交替播放阈值，不能作为合格备用路线。

## 画面逐项描述

- 路径 A 左格（目标 α）：画面右侧拳向画面右伸出，另一拳在腰侧；实际前拳 y≈143，低于 D1 肩高区，腰侧拳约 x=94。
- 路径 A 右格（目标 β）：画面右侧拳仍向画面右伸出，另一拳仍在腰侧；没有出现 β 出拳，腰侧拳仍约 x=95。
- 路径 B α：画面右侧拳在前伸区、画面右向，另一拳在腰侧；腰侧拳 x≈93.6。
- 路径 B β：画面右侧拳仍在前伸区、画面右向，另一拳在腰侧；腰侧拳 x≈98.0，未换到 α 原始位置。

接触表：`assets/_trial_20260911/t45_fist_v5/contact/fist_v5_compare.png`，包含 idle 基准、路径 A 两帧和路径 B 两帧，已人工查看。

## 产物与 SHA

产物包：`assets/_trial_20260911/t45_fist_v5/`

- idle：`5f417df27ecfdd4ae5c73280068296d61a703fb13f24ad9bdc60b472f5a29e7f`。
- 路径 A 双格图：`63c6a7a8672687de4f0734bf09343f188b8d56be69670ab757541f5fac068a85`。
- 路径 A α/β：`4370cb5fcdc1e1ad2e0b8c8109a81c5bbb13ac1b465ad03346a068ad9bf2545d` / `015772f90fd128a361260814623701e2c45dde4f20ccde677de9455035219b8e`。
- 路径 B α/β：`f4747f5cab8ff840fedf3cb80119a3978e34a88585687dab0751e0b5b1e91bc8` / `d6ad8be10108a2ffd1da9685cd3aa39730eff76d83de98a0c869b40a40af7e85`。
- QA：`qa/fist_v5.json`；接触表：`contact/fist_v5_compare.png`。
- `credits.json`：`generationCount=3`、`cost=0`、`runtimeTouched=false`。

## 结论与停点

双格同图和单帧直生两条未试路线均未产生可交付的 α/β 换手：A 失败 D1/D2，B 失败 D2/D3。按 v5 红线，两条结果全部交付后停止；后续若继续，需 Leo/PM2 另行裁定改动作设计或参数化路线。

## 批次收尾健康门

开工复跑：rollout 155.8 MB、28 轮、压缩 3 次，未触阈值。收尾复跑：rollout 160.5 MB、28 轮、压缩 3 次，仍未达到 SKILL §12 的轮换阈值（轮次≥150、rollout≥300 MB、压缩≥30 次）。
