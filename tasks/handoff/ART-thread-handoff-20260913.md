# 美术线线程交接（2026-09-13 · 线程 01a08a45 卡死，换新线程续作）

## 0. 为什么换线程

旧线程 `01a08a45-f703-7190-a484-442a2d0af631` **卡死**，实测证据：

| 检查 | 结果 |
|---|---|
| rollout 大小 | **313,943,258 字节（305 MB）** —— 超 SKILL §12 的 300MB 轮换线 |
| thread_items | **3012 条**（其中 reasoning 1473 / commandExecution 729 / imageView 129 / imageGeneration 78） |
| rollout 最后写入 | 01:36 |
| 新轮次 | 01:55:25 已接收（Submission/TurnInput），**之后 20+ 分钟零产出** |
| 全局日志 | 只有 UI 例行轮询，**没有模型在工作** |
| 投影状态 | 313,943,258 = 与 rollout 一致 ⇒ **不是"投影落后"故障**，是线程太长处理不动 |

**已知处置**：记录在库不在进程 ⇒ 重启 App / 杀进程都无效。**退役线程 + 交接上下文 + 开新线程**（Leo 令：裸重开丢上下文不可接受）。

## 1. 当前待办（新线程第一件事）

**projbus seq=392**：五张全身明暗分块图（left / leftdown / leftup / right / rightup）。

完整任务书：`assets/_trial_20260912/明暗贴图试产/dispatch_5dirs_fullsize.json`

要点：
- 输入：`assets/_trial_20260912/明暗贴图试产/其余五向输入/` 下 5 张（各方向画布尺寸不同，**按各自尺寸输出**）
- **光照参照**：把 `交付/全身_明暗分块.png`（已有的右下那张）一并送入，prompt 写"光从同一世界方向来，明暗分档方式与参照一致"
- 只改三档 cel 明暗；**不加细节 / 不重画五官 / 不改轮廓与袖子手臂位置 / 不改配色 / 不写文字**
- **盯住袖子与手臂轮廓不许动**（早先那张把手臂画动了 133px；头部那张做到过 ±3px）
- 交付到 `交付/全身_{left,leftdown,leftup,right,rightup}_明暗分块.png`

> ⚠️ **为什么改回"每方向一张"**：实测拼图版每格只剩 **17%** 的像素（单张全身 79.5 万 px vs 拼图一格 13.5 万 px），明暗画不细。
> 生成模型输出总量上限约 1.5MP，六格摊下来必然小 —— 光照一致与绘制精度不可兼得，**以质量优先**。

## 2. 角色与铁律（沿用）

- 你是**美术线（Codex art 窗口）**，服务 placement-wuxia 项目（微信小游戏，国风像素 HD-2D）。
- **开工必读**：`CODEX-ART-README.md`（入场须知）、`.zcode/skills/art-pipeline-execution/SKILL.md`（执行手册 12 类 + 异常码）、`docs/design/03-战斗系统/素材落地账.md`。
- **纪律**：规格未读完不生成；有停点就停、如实登记，禁自行补全；原生 `image_gen`（cost=0），**禁 mxai**；交付必带 commit + 路径 + QA，禁回执式提交。
- **行内已验证的工具**（沿用，别重造）：`tools/glb2d/`（渲染/回填/去饱和/度量）、`scripts/png8_encode.py`。

## 3. 最近脉络（2026-09-12 夜 ~ 09-13 01:36）

| seq | 任务 | 结果 |
|---|---|---|
| 372/373 | 整图 30 格 idle 细节精修 | 交付，但实测**更钝**（−10.86%）→ PM2 判打回（BUG-17） |
| 374/375 | 一条方向一张图 精修试产（yaw270） | 交付；PM2 回填后锐度 +25.8% |
| 376/377 | 其余 7 角度 | 交付 |
| 378/379 | 俯仰环 12 角度 | 交付 |
| 380/381 | **2D 重绘试产**（线上 2D 锁风格 + 3D 锁姿势） | 通过：风格追平（锐度 3.26 vs 3.45），比例误差 0.4~2.8% |
| 382/383/384 | idle 2D 重绘 → 收窄为每向 1 帧共 6 张 | 交付（Leo 指出方向感不足） |
| 385/386 | ★**头部 cel 明暗分块** | 交付，PM2 回填成立、头部对比度反超线上 2D ⇒ **路线突破** |
| 387/388 | 全身 cel 明暗分块 | 交付（但手臂被画动 133px） |
| 389 | 其余五向各一张 | **被 390 取代** |
| 390/391 | 六向拼成一张大图一次生成 | 交付；PM2 实测每格只剩 17% 像素、明暗偏粗 ⇒ 判回每方向一张 |
| **392** | **五向全身明暗图（当前待办）** | ⏳ 旧线程收下后卡死，**新线程接续** |

## 4. 配套事实（PM2 侧已做的加工，供理解下游）

- 明暗图的用法：**回填进贴图空间 → 3D 管线重渲**，比例由 3D 保证、观感由明暗图提供。
  工具：`tools/glb2d/backfill_tex.mjs`（`--mode best`）、`backfill_from_sheet.py`（拼图直接映射回填）。
- **明暗图是"跟视角"的**：一个方向一张；每张覆盖该方向所有动作所有帧（idle/walk/run/jump/atk/cast 共用）。
- 头发"发红"的真因**不在明暗图**，在 PM2 侧影调那步的 `--red 26`（已降到 10）；另新增 `tools/glb2d/desaturate_hair.py` 把模型自带的棕发去饱和成中性灰黑。

## 5. 完整时间线

`tasks/handoff/ART-thread-timeline-20260913.md`（226 条 发单/汇报，2026-09-10 15:44 → 09-13 01:36）
