# T45 · 右向尺度表圈定 + 左系改真左向生成（PM2 派单）· 2026-09-10

**依据：Leo 2026-09-10 两条裁定。本文档为执行口径真源。**

---

## 一、右向尺度表：Leo 已圈定（本条即冻结判据）

| 族（右向） | 表值 | 判据 |
|---|---|---|
| idle_right | **256** | 锚，`±1` |
| walk_right | **246** | `±1` |
| atk_right | **246** | `±1` |
| jump_right | **212** | `±1` |

- 单批唯一系数 **`0.22241529105125976`**（= 256 ÷ idle 源 bbox 高 1151）同步冻结。
- **你已交的四帧全部符合本表**（PM2 独立复测：146×256 / 149×246 / 181×246 / 153×212），无需回炉。
- **剩余 6 帧照此执行**，判据是「输出 bbox 高 == 上表 ±1」，不再有任何「动作帧也要 256」的要求——那条是我的口径错误，已作废（见 `T45-seq332-v2-four-frame-gate-RULING-20260910.md`）。
- 反向也成立：**禁**把动作帧顶到 256（= 逐帧自适应，Leo 已否决）。

## 二、左系路线：Leo 裁「真左向重新生成」→ 镜像路线整体作废

**立即停止并作废**（不再交付、不再补证据）：

1. Phase B+C 的**左系镜像 7 帧**（seq=323/324 产物）；
2. 左向的**锚点 / 遮罩 / 剑层**等一切由右向镜像派生的件；
3. 原红线 10「right = left 镜像（一张图翻转）」**已被 Leo 修订**：左向**不得由镜像派生**。理由：单手武器下镜像把右手兵刃换到左手（Leo 判"左手持剑"不可用），且真左向的远侧手与武器遮挡关系与镜像不同。

**左向新口径**：

- 左向 = **left / leftup / leftdown 三个方向各自独立生成**（不是镜像、也不是右向的翻转）；
- 每向按**三锁**独立走：唯一身份/比例锚 → 一族一表（本向 10 帧同组同系数）→ 输出规格锁；
- 每向**先交该向 idle 锚样张过样张门**，过门后再铺其余 9 帧；
- 尺度机制同右向：系数 = 256 ÷ 该向 idle 源 `alpha>32` bbox 高；该向尺度表由实测自然高登记后报 PM2 → Leo 圈定；
- **主角武器不进帧**（武器是独立剑层运行时合成，左右向一致）；帧内禁带武器、禁带文字/水印；
- 禁 AI 一次性直出多帧网格（红线 10 后半段未变）。

## 三、执行顺序（串行，一条线程）

1. **先收尾右向**：生成 `walk_right_2/3`、`atk_right_2/3`、`jump_right_2/3`（唯一系数 0.22241529105125976），整组（≤10 帧）交 PM2 门检。
2. **右向过门后**再开左向：先交 `left` 向 idle 锚样张（三锁 + 机械门 + 同类证据三件），过样张门后铺 `left` 其余 9 帧；三联方向（left / leftup / leftdown）**逐向串行**，不并发。

## 四、机械门（不变，逐帧照跑）

240×320 RGBA / 脚底 y=300 / 四边零 alpha / `alpha>32`+4 邻接单连通 / 质心 x=120±1（动作帧 ±20）/ **无裁切断言 `输出bbox == round(源bbox × 系数) ±1`** / 宽度 ≤238 / SHA 互异 / 帧间互异 / 对照图含基准帧且**交付前人眼过一遍并写画面逐项描述**。

## 五、状态

- runtime 不动；全部 candidate-only；生成额度与 SHA 记入 QA。
- 交付须声明落库状态与路径+SHA（素材落地纪律第 ① 条）。
- 本单与 `T45-seq332-...-RULING-20260910.md` 共同构成当前执行口径，冲突时以本单为准。
