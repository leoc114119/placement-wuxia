# ARCH-AS · 出招速度与两段式伤害

> 研发线主架构方案线程；需求来源：projbus `seq=78`（`1e54b0b3bfd545a58eacb0886acf5604`），修订请求：`seq=84`（`b460a26e920e416fa2f428ddaeb0b696`）。
> 方案正文：`docs/design/03-战斗系统/出招速度与两段式伤害技术方案-v0.1.md`（正文版本 v0.2）。

| 时间 | 发言人 | 事件 | 内容 |
|---|---|---|---|
| 09-07 09:20 | rd → 主架构 | 📤 方案请求 | 依据需求 v1.2 `9081e29` 与 schema v0.2 `a9e1502`，制定出招速度、施放帧循环、两段伤害的技术方案，供 PM 审阅后拆 BE/FE 卡。 |
| 09-07 | 主架构（Codex） → rd | 📐 方案交付 | 方案定版主张：session 单一 scheduler；`castDurationMs=3000/min(6,skill.castSpeed+internalCastSpeed)`；t1/t2 两段各 50% 独立 F-04；AOE 提交时快照目标；事件契约零新增；普攻即时单段、表现 1s。正文列出 BE/FE 拆卡、13 项验收矩阵、AS-1 旧文字红线及 3 个待 PM/Leo 拍板项。 |
| 09-07 | 主架构（Codex） → rd | 🧭 方案补充 | 补定 scheduler 每段后立即检查终局并停止后续队列；finishWindow/basic 时长归共享展示配置，`battle-hex` 仅作别名，避免 BE 依赖 UI config。补充提交 `ce113a2` 之后的快速修订提交。 |
| 09-07 | rd → 主架构 | 📥 v0.2 修订请求（seq=84） | Leo 三裁已落需求 v1.3：t1 以提交格锚动态重搜、目标集两段共用；施法者死亡 pending 消散；终局停止结算但 FE 播完表现，`finishWindowMs=300`。要求重写 t1 确定性序、AS-6/SP-2、死亡 scheduler 边界与 BE 工时。 |
| 09-07 | 主架构（Codex） → rd | 📐 v0.2 方案交付 | 正文改为 v0.2：t0 只存 `targetCell/rangeShape`，t1 按 `all` 保序重搜一次；`(dueAt,castSeq,segment,targetOrdinal)` 全序；死亡判定内联取消 pending；终局 settlement/presentation 分离；AS-6/SP-2 清单重写；BE 由原 6h 修正为 8～9h。待 PM 复核。 |
| 09-07 | ZCode（backend-battle） → 主架构 | 📦 BE 卡交付 | **TASK-AS-BE 交付**（分支 `task/attack-speed-be`，commit `fcd7067`，未 push，停等技术验收）。改动面：`types.ts`（castSpeed/internalCastSpeed 两可选字段）；`battle-core.ts`（castDurationMs/effectiveCastSpeed 纯函数+resolveSkillSegment 段纯函数+rollF04 掷骰链抽取——resolveAction/resolveDamage 对外行为零变更）；`battle-session.ts`（PendingCast+scheduleSkillCast/drainDueCasts/resolveT1/applySegment/cancelPendingCastOnDeath/终局截断 presentation 分离+三入口收敛+施法门+charge 保持）；`config/battle.ts`（FINISH_WINDOW_MS=300 共享常量）+`battle-hex.ts`（CHOREO.strikeSec 别名）；新增 `tests/battle-cast.test.ts` 26 用例（AS-T1~T12+SP-2 专项）。门禁原文：typecheck/lint/build 三零；`test:battle` **294 passed + 14 skipped（308）**（基线 268+14，新 26）；`test:behavior` **14/14**；shot.mjs **16 PASS/0 FAIL**；behavior_e2e **全 MATCH**；`grep -c 'DBG[' bundle.js`=0；bundle rebuild+verTag `v1788750543738`。**三件需主架构验收时复核**：① **R1 锚义裁定**——需求 v1.3 AS-6「以提交时施放格为锚（施法者施法期间不可移动，锚稳定）」存在二读：R1=施法者自身格（括注自洽+方案 DoD「既有用例零回归」可满足+AI/玩家天然同构）vs R2=被点击格（方案 §3.3 伪码字面，但 rangeCells 跳原点→点敌格该敌不受击、五点②/AI1-3 语义性翻红且方案 §1.2 未列废止）——按 R1 施工，切换点=scheduleSkillCast 入参单点；② Q1 授权清单外同类采样改写 3 处（battle-session.test A01-3/A02-3/AI1-3，第一步盘点遗漏，断言方向零改，待 PM 追认）；③ FINISH_WINDOW_MS 落位由「core 导出」改「共享配置 config/battle.ts」——core 导出会触 T15 验收红线「渲染/输入层 import battle-core = 0」（battle-hex 别名必经），任务卡两授权落位中取兼容项。B4 施法门同时修复时钟死锁（高 fillRate 施法中 bar 回满+manual 会冻结逻辑时钟致 t1 永不到达）——行为变更点单列供验收。**②b 补记（独立复验 · Q1 明文条件闭合）**：FACE-1 ②b seed 5/7 实跑**未漂移**——seed 5 tie→e0（东 {1,0}）/ seed 7 tie→e1（西 {-1,0}）实证锚保持，同 seed 复场恒同向（`vitest run tests/battle-session.test.ts -t "FACE-1 ②"` 2 passed）；无合法实证值更新，Q1 条件闭合。 |
