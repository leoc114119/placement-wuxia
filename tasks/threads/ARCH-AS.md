# ARCH-AS · 出招速度与两段式伤害

> 研发线主架构方案线程；需求来源：projbus `seq=78`（`1e54b0b3bfd545a58eacb0886acf5604`），修订请求：`seq=84`（`b460a26e920e416fa2f428ddaeb0b696`）。
> 方案正文：`docs/design/03-战斗系统/出招速度与两段式伤害技术方案-v0.1.md`（正文版本 v0.2）。

| 时间 | 发言人 | 事件 | 内容 |
|---|---|---|---|
| 09-07 09:20 | rd → 主架构 | 📤 方案请求 | 依据需求 v1.2 `9081e29` 与 schema v0.2 `a9e1502`，制定出招速度、施放帧循环、两段伤害的技术方案，供 PM 审阅后拆 BE/FE 卡。 |
| 09-07 | 主架构（Codex） → rd | 📐 方案交付 | 方案定版主张：session 单一 scheduler；`castDurationMs=3000/min(6,skill.castSpeed+internalCastSpeed)`；t1/t2 两段各 50% 独立 F-04；AOE 提交时快照目标；事件契约零新增；普攻即时单段、表现 1s。正文列出 BE/FE 拆卡、13 项验收矩阵、AS-1 旧文字红线及 3 个待 PM/Leo 拍板项。 |
| 09-07 | 主架构（Codex） → rd | 🧭 方案补充 | 补定 scheduler 每段后立即检查终局并停止后续队列；finishWindow/basic 时长归共享展示配置，`battle-hex` 仅作别名，避免 BE 依赖 UI config。补充提交 `ce113a2` 之后的快速修订提交。 |
| 09-07 | rd → 主架构 | 📥 v0.2 修订请求（seq=84） | Leo 三裁已落需求 v1.3：t1 以施法者提交时所在格（R1）锚动态重搜、目标集两段共用；施法者死亡 pending 消散；终局停止结算但 FE 播完表现，`finishWindowMs=300`。要求重写 t1 确定性序、AS-6/SP-2、死亡 scheduler 边界与 BE 工时。 |
| 09-07 | 主架构（Codex） → rd | 📐 v0.2 方案交付 | 正文改为 v0.2：t0 只存 `anchorCell/rangeShape`，其中锚=施法者提交时所在格（R1）；t1 按 `all` 保序重搜一次；`(dueAt,castSeq,segment,targetOrdinal)` 全序；死亡判定内联取消 pending；终局 settlement/presentation 分离；AS-6/SP-2 清单重写；BE 由原 6h 修正为 8～9h。 |
| 09-07 | rd → 主架构 | 📥 勘误请求（seq=91） | PM 终裁 R1：`提交格` 存在二读，统一勘误为“施法者提交时所在格”；点击格只作受理/演出朝向，三入口共用自身格锚。 |
| 09-07 | rd → 主架构 | 📥 BE 技术验收请求（seq=92） | 请求复核 `task/attack-speed-be`：`fcd7067` + `670bce9` + `d571eb7`；PM 预裁包含 R1、清单外采样改写追认、`FINISH_WINDOW_MS` 落 `config/battle.ts`，并要求重点核对 B4 施法门时钟死锁修复。 |
| 09-07 | 主架构（Codex） → rd | ✅ TASK-AS-BE 技术验收 PASS | 独立副本复验远端 tip `d571eb7`（含 `fcd7067`）：typecheck 通过、lint 通过、build 通过；`test:battle` 294 passed + 14 skipped（308），`test:behavior` 14/14；`shot.mjs` 16/16 PASS；`behavior_e2e.mjs` 11/11 MATCH、0 不符、exit 0；bundle `DBG[` 残留 0。代码复核确认 R1 自身格锚、t1 动态重搜/目标集缓存、B4 施法门与时钟推进、死亡消散、终局 presentation 分离均符合 v1.3/方案 v0.2；已同步修正文案并推送主架构修订。 |
| 09-07 | rd → 主架构 | 📥 FE 技术验收请求（seq=94） | 请求复核 `task/attack-speed-fe`：`fe37cf4` + `676c8c6`，基点 `d571eb7`；范围为 charge 循环、strike 单播、普攻 1s 保持窗、x2 逻辑 dt、T21 两段冲刷、legacy 降级；单列 `FX.slashSec` 口径与终局冻结快照表现。 |
| 09-07 | rd → 主架构 | 📥 v0.3 修订请求（seq=98） | Leo L 环二次终裁：段 1=t0 提交即时且出手必中；段 2=t1=整个出招时长末端，保留动态重搜/收招可躲；取消独立 300ms 收招结算；施法循环 280ms/帧；要求评估单 pending 简化、t0/t1 空集合、AS-T3/T4/T5/T6 与工时。 |
| 09-07 | 主架构（Codex） | ✅ 收到 seq=98 | 已确认变更范围，不改游戏代码；基于 v0.2 已验收 BE/FE 做 v0.3 方案修订。 |
| 09-07 | 主架构（Codex） → rd | ✅ TASK-AS-FE 技术验收 PASS（seq=94） | 独立副本锁定 `676c8c6`（origin/task/attack-speed-fe，基点 `d571eb7`）复核：typecheck/lint/build 通过；`test:battle` 303 passed + 14 skipped（317）；`test:behavior` 14/14；交付自带 `shot.mjs` 16/16、`shot_as_cast.mjs` 30 影+18 校验、`shot_sixdir.mjs` 48 张零 pageerror、e2e 11 MATCH、bundle `DBG[`=0/verTag `v1788755695162` 证据链。代码复核确认 charge cast1→3 循环、strike cast2→3、普攻 basic 1s 保持窗、宿主 x2 单一逻辑 dt、条件 e 收尾沿与 legacy 单帧幂等均符合方案 §4.4。`FX.slashSec=0.28s` 认定为独立瞬时斩击弧，不与普攻 1s 强绑；终局按冻结 `SnapshotActor.animState` 继续渲染，`presentationCasts` 仅作 session 内部只读审计面；两项均不阻塞技术验收，待 PM 需求门与 Leo L 环。 |
| 09-07 | rd → 主架构 | 📥 v0.2 修订请求（seq=84） | Leo 三裁已落需求 v1.3：t1 以提交格锚动态重搜、目标集两段共用；施法者死亡 pending 消散；终局停止结算但 FE 播完表现，`finishWindowMs=300`。要求重写 t1 确定性序、AS-6/SP-2、死亡 scheduler 边界与 BE 工时。 |
| 09-07 | 主架构（Codex） → rd | 📐 v0.2 方案交付 | 正文改为 v0.2：t0 只存 `targetCell/rangeShape`，t1 按 `all` 保序重搜一次；`(dueAt,castSeq,segment,targetOrdinal)` 全序；死亡判定内联取消 pending；终局 settlement/presentation 分离；AS-6/SP-2 清单重写；BE 由原 6h 修正为 8～9h。待 PM 复核。 |
| 09-07 | ZCode（backend-battle） → 主架构 | 📦 BE 卡交付 | **TASK-AS-BE 交付**（分支 `task/attack-speed-be`，commit `fcd7067`，未 push，停等技术验收）。改动面：`types.ts`（castSpeed/internalCastSpeed 两可选字段）；`battle-core.ts`（castDurationMs/effectiveCastSpeed 纯函数+resolveSkillSegment 段纯函数+rollF04 掷骰链抽取——resolveAction/resolveDamage 对外行为零变更）；`battle-session.ts`（PendingCast+scheduleSkillCast/drainDueCasts/resolveT1/applySegment/cancelPendingCastOnDeath/终局截断 presentation 分离+三入口收敛+施法门+charge 保持）；`config/battle.ts`（FINISH_WINDOW_MS=300 共享常量）+`battle-hex.ts`（CHOREO.strikeSec 别名）；新增 `tests/battle-cast.test.ts` 26 用例（AS-T1~T12+SP-2 专项）。门禁原文：typecheck/lint/build 三零；`test:battle` **294 passed + 14 skipped（308）**（基线 268+14，新 26）；`test:behavior` **14/14**；shot.mjs **16 PASS/0 FAIL**；behavior_e2e **全 MATCH**；`grep -c 'DBG[' bundle.js`=0；bundle rebuild+verTag `v1788750543738`。**三件需主架构验收时复核**：① **R1 锚义裁定**——需求 v1.3 AS-6「以提交时施放格为锚（施法者施法期间不可移动，锚稳定）」存在二读：R1=施法者自身格（括注自洽+方案 DoD「既有用例零回归」可满足+AI/玩家天然同构）vs R2=被点击格（方案 §3.3 伪码字面，但 rangeCells 跳原点→点敌格该敌不受击、五点②/AI1-3 语义性翻红且方案 §1.2 未列废止）——按 R1 施工，切换点=scheduleSkillCast 入参单点；② Q1 授权清单外同类采样改写 3 处（battle-session.test A01-3/A02-3/AI1-3，第一步盘点遗漏，断言方向零改，待 PM 追认）；③ FINISH_WINDOW_MS 落位由「core 导出」改「共享配置 config/battle.ts」——core 导出会触 T15 验收红线「渲染/输入层 import battle-core = 0」（battle-hex 别名必经），任务卡两授权落位中取兼容项。B4 施法门同时修复时钟死锁（高 fillRate 施法中 bar 回满+manual 会冻结逻辑时钟致 t1 永不到达）——行为变更点单列供验收。**②b 补记（独立复验 · Q1 明文条件闭合）**：FACE-1 ②b seed 5/7 实跑**未漂移**——seed 5 tie→e0（东 {1,0}）/ seed 7 tie→e1（西 {-1,0}）实证锚保持，同 seed 复场恒同向（`vitest run tests/battle-session.test.ts -t "FACE-1 ②"` 2 passed）；无合法实证值更新，Q1 条件闭合。 |
| 09-07 | ZCode（frontend-battle） → 主架构 | 📦 FE 卡交付 | **TASK-AS-FE 交付**（分支 `task/attack-speed-fe`，基点 d571eb7=BE 交付点，未 push，停等主架构技术验收）。**范围六项落地**：① **charge 帧循环**（AS-2/开放点①）——`config/battle-hex.ts` ANIM_LOOP_GROUPS=['walk','charge']+hero `stateMap.charge` cast1→1 定格改 **cast1→3 整套循环**（周期=3×walkFrameMs=420ms 由帧时钟定，循环持续到 session 切 strike=出招时长本身）；② **strike 单播核实**——`stateMap.strike` cast2→3 为现行行为零改动，既有用例（charge/strike/basic 钟驱动锁）+新增配置别名锁（CHOREO.strikeSec=FINISH_WINDOW_MS/1000）双锁；③ **普攻表现 1s**（口径③/开放点③）——`BASIC_DURATION_MS=1000` 落 `config/battle.ts`（与 FINISH_WINDOW_MS 同处），battle-hex `CHOREO.basicSec` 0.32→别名（BE 留的 FE 接线位），渲染层 **basic 保持窗**（view.basicHolds：basic 上升沿开窗 1s，session ANIM_MS.basic=300 冻结不动、快照回 idle 后由窗续播补足差额，atk1→2 无 2→1 回跳，到期惰性清理+resetDemo 跨局清理；legacy 单帧组幂等=帧 6 恒持）；④ **宿主逻辑 dt 唯一真源**（方案 §4.4）——BE 未暴露 presentationDt 走宿主传参：main.ts `dt=realDt×(speedOn?fast:normal)` 同传 updateView/drawFrame、session.tick 吃 realDt（session 内部同 SPEED_FACTOR 缩放→两路恒同值），x2 时 cast 帧循环/血条/行动条/移动演出同倍率（源码锁用例+mock_session 零改仅 basicSec 别名自动传导）；⑤ **两段演出核实+缺陷修复**——白名单无去重拦截（R4 既有锁），但**读码发现**：固定步长下 t2 与 strike 收招窗（=FINISH_WINDOW_MS）同刻到期、session tick 动画机先翻 idle 再 drain 段 2 事件→T21 冲刷条件 a/b 双缺、第二跳挂 1.5s 兜底，违反 AS-8「收势=段2 终点」——按 AS-8 语义补**冲刷条件 e 攻击态收尾沿**（strike/basic→他态转换沿同帧冲刷；既有 T21 用例零破坏：R8 恒 idle 案例不受影响）；终局表现帧=session 冻结快照下渲染持续消费不崩（e2e 11 MATCH+真 session 全链用例覆盖）；⑥ **legacy 敌型降级核实**——charge=[4]/strike=[5]/basic=[6] 单帧组循环语义幂等不坏（新用例锁）。**证据链**：新增 `proto/battle_demo/shot_as_cast.mjs`（x1/x2×375/560/900×五相位=**30 影+18 校验全 PASS**：施法相循环两帧互异 cast2/cast3→段1 冒字+血条首降→strike 收招帧→段2 冒字两跳同屏错位 dx 0/6、hp 70→65→60；宽窗 900x560 画布恒 9:16 竖条→attackCells 内安全格筛选防点击出画布）。**随卡回调 2 处既有用例**（battle-hex-render：hero stateMap charge 锁+charge 钟驱动锁——旧「定格 cast1」断言与本卡冲突，按卡改循环断言并标注【AS · TASK-AS-FE 随卡回调】，strike/basic 断言原文未动）。**新增 9 用例**（render 6：配置别名/charge 3s 采样帧序 [1,1,2,2,3,3,1]/保持窗 directional+legacy/legacy 循环降级/宿主 dt 源码锁；hit-feedback 3：段2 收尾沿当帧冲刷/两跳 300ms 错位/**真 session 两段全链**固定 50ms 步长——charge 保持+三帧均可见+两跳时刻差=收招窗±1 步+恰 2 条结算事件）。**门禁原文**：typecheck/lint/build 三零；`test:battle` **303 passed + 14 skipped（317）**（基线 294+14+新 9）；`test:behavior` **14/14**；shot.mjs **16 PASS/0 FAIL**；shot_sixdir **48 张零 pageerror**（⑤charge 相位适配：cast_1 取 [0,140ms) 首帧窗等待 90→30ms）；behavior_e2e **11 MATCH 不符合 0**；`grep -F -c 'DBG[' bundle.js`=0；bundle rebuild verTag `v1788755695162`。**两处单列待主架构/PM 复核**：① `FX.slashSec=0.28` 未动——任务卡「斩击弧时序只引用别名」按「方案 §2.2 只定『普攻帧表现时长 1s』、斩击弧为独立特效时长（strike/skill 共用出招弧，挂 1s 会改技能收招弧观感）」理解落线，若口径=弧随普攻 1s 则一行改别名即可；② 终局「播完」口径=终局时已接受的施放/收招帧由冻结快照持续渲染至收招帧定格（presentationCasts 只读面 FE 未另设时钟——session 冻结 animState 即表现真值），非终局施法者若冻结在 charge 相则循环帧在结算遮罩下持续（规格未覆盖该演出细节，未发明截断）。 |
| 09-07 19:59 | ZCode（backend-battle） → 主架构 | 📦 TASK-AS-v03 交付（续作接管） | **两段式伤害时序改写 v0.3 交付**（分支 `task/attack-speed-v03`，基座=合并提交 `0966ce7`（fe 全量 v0.2+main 方案 v0.3，Q1-TASK-AS-v03 裁定候选 A）+WIP `742f3a6` 续作闭合，未 push，停等技术验收）。**BE 面（WIP 闭合）**：`battle-session.ts`——段 1=t0 提交内联（scheduleSkillCast 内即时范围判定+逐目标 applySegment@t0，AS-3）、段 2 单 pending dueAt=t0+castDurationMs（resolveSegment2@t1 锚格重搜固定集合，AS-4/AS-6 分层）、空放=t0 一条无目标 skill+presentationCasts 保持至 t1（不建结算 pending）、死亡只消散段 2/段 1 已随 t0、段 1 致胜终局段 2 不入队、drain 全序降维 (finishAtSec,castSeq,targetOrdinal)、FINISH_WINDOW_MS 结算节点/ strike 收招相废止；core resolveSkillSegment 零改动（两段各自完整 F-04 含闪避，PM 修正裁定 forceHit 作废）。**FE 面（本续作）**：`config/battle-hex.ts` 新增 **CAST_FRAME_PERIOD_MS=280 独立常量**（charge 循环解耦 walkFrameMs，方案 v0.3 §4.4）+CHOREO.strikeSec 勘注（表现兼容暂留、非结算锚）；`ui/battle-hex-render.ts`——charge 循环步频改 280ms、冲刷条件 b'（施法相 charge 在态=t0 首跳当帧冲刷）+条件 e 扩展臂（charge→idle 收势沿=t1 二跳锚；strike/basic 臂保留），普攻 1s basicHolds 与 FX.slashSec=0.28 未动，main.ts 零改。**用例面（注明 v1.4 依据）**：battle-cast 29 用例（WIP 重写闭合）；battle-hex-render 2 处 charge 循环用例随 280ms 改写+别名锁加新常量；battle-hit-feedback R2/段2 对齐/真 session 两段全链 3 处随卡改写（spawn 探针改 fx 引用增量——v0.3 两跳间隔 3.0s≫DMG.sec 0.6s 旧存活计数探针失效）；battle-session AI1-3 AI 臂采样窗 8→32 tick（越 t1=t0+3.0s）+settleBoth 勘注；behavior 3 处仅注释勘新（断言体零改）。**证据链**：`shot_as_cast.mjs` 重写新时序（x1/x2×375/560/900×五相位 seg1/loop_a/loop_b/loop_late/seg2=30 影+24 校验全 PASS：段1@t0 charge 冒字 70→65、280ms 循环帧互异 cast2/cast3、循环持续 ≥1.8s 至 t1、段2@t1 回 idle 二跳 65→60）。**门禁原文**：typecheck/lint/build 三零；`test:battle` **306 passed + 14 skipped（320）**（v0.2 基线 303+14；battle-cast 26→29）；`test:behavior` **14/14**；shot.mjs **16 PASS/0 FAIL**；behavior_e2e **11 MATCH 不符合 0**；`grep -c 'DBG[' bundle.js`=0；bundle rebuild+verTag `v1788781961272`。**单列待复核 3 项**：① battle-session AI1-3/hit-feedback 3 处与 render 2 处=任务卡用例重写面内适配（断言方向零反转，均注明 v1.4/方案 v0.3 §4.4 条目）；② hit-feedback「两跳同位错位」mock 锁保留未改（stagger 机制锁，300ms 仅作喂入间隔非结算依赖）；③ shot_as_cast 循环步频页内常量 280 与 config 同源单测锁定（未改 main.ts 暴露面）。 |
| 09-07 21:30 | ZCode（backend-battle） → 主架构 | 📦 TASK-AS-v04 交付 | **普攻交互联合卡交付（规格 v2.5 三项+PRM-1/GSG-1 · 方案 v0.4 §9 唯一增量）**（分支 `task/attack-speed-v04`，基点=3b60e03（v0.3 交付点），未 push，停等技术验收；验收通过与 v0.3 合并一次 L 环关 seq=117+121）。**增量四项**：① **ATK-1 纯距离**（§9.2.1）——玩家 attack(skillId=null) 合法性改 `cubeDistance≤basicRange` 六向皆可，锥形 inCone/facing 扇区过滤移除（技能 ray/cone/circle 与 AI 出技/planSkill/targetInRange 零改动）；② **FACE-1 目标朝向**（§9.2.2）——普攻/空挥朝向单点=doAttack faceToward(实际受击敌)/空挥 faceToward(点击格)（现行 fallback，L 环确认后锁死），六向接线既有用例继续背书；③ **ATK-3 玩家侧废止**（§9.2.3）——submit walk/jump 两路径移除 basicIfAdjacent，AI 位移臂（aiAct→basicIfAdjacent，含托管代行）保留+pendingAnim 补播机制随 AI 臂存活；④ **PRM-1 攻钮选格+GSG-1 hover**（§9.3）——session 新增 basicSelecting 选中态（selectBasic toggle 进出、GATE-1 同门、与技能选中两向互斥 BASE-5）+快照 `basicCells`=六邻格∩可动区（HEX_DIRS 序，显示=校验同源 basicCellsOf 单点）+`basicAtCell(to)`（集合外 rejected:invalid 选中保持；有敌格=既有 doAttack F-04 全链零改动；无敌格=空挥：恰 1 条 basic 无 targetId 无 damage+零 RNG+零内力+零冷却写入+耗回合 BAR-3）；清理单点=clearSelection（commit/cancel/mode/end 四路共用，§9.3 红线）；FE：view.hoverCell 表现态（不进快照，§9.4）+basicCells 金格+三类可选格（轻功金/attackCells/basicCells）hover 红（HIGHLIGHT.cellHover，绿格不纳入）+ctrl「攻」钮（ctrl 正上方锚定同宽、待命期才产出热区=非待命 fall-through，HIT-1 同规）+input pickAtkButton/basicCells 点选/hover 事件+main.ts 最小接线（自由 pointermove→input.hover——§9.3「PC/预览实现 pointer hover」宿主落点，input 层 hover 事件的唯一来源；触屏不触发=自然退化）。**types.ts 仅授权两处**：ActionRequest 增 selectBasic+basicAtCell（普攻选格请求族）+BattleSnapshot.basicCells。**用例面（注明规格依据）**：新增 session 9（ATK-1 锥面反例回归+六向遍历、PRM-1 门/互斥/有敌/空挥零 RNG/集合外/清理单点/SP-2 全等）+render/input 4（攻钮命中/basicCells 派发/hover 翻译/渲染金红两态+攻钮热区四态）；**随卡改写 4 处**（规格依据=v2.5 ATK-3 玩家侧废止 09-07 Leo 裁+方案 §9.2.3）：battle-session [ATK-3] 玩家臂翻转+AI 位移臂新锁（pendingAnim 正面覆盖自 B-1 移交）、B-1 改移动纯移动锁、battle-behavior ATK-3 绿锁翻转、behavior_e2e HF3 改废止锁（文件头改写登记）；测试夹具补契约字段 3 处（basicCells/atkBtn，类型咬合非断言改写）。mock_session 补 basicCells:[]（契约咬合）。**证据链**：新 `shot_basic_cells.mjs`（x1/x2×375/560/900×四相位 select/hover/sixdir/whiff=24 影+36 校验全 PASS：攻钮→六邻格金色→hover 红态→点击立即执行；背向敌（朝东敌北+whip 白盒）受理+受击敌定朝向 leftup；空挥恰 1 条无目标 basic+rng 32→32+朝向=点击格 right）；`shot_as_cast` v0.3 时序回归 24 校验全 PASS；`shot.mjs` 16 PASS；e2e 11 MATCH（HF3 为改写后新锁）。**门禁原文**：typecheck/lint/build 三零；`test:battle` **320 passed + 14 skipped（334）**（v0.3 基线 306+14，净增 14：session 90→96、render 82→86、ATK-3 describe 1→2）；`test:behavior` **14/14**；shot.mjs **16 PASS**；shot_as_cast **24 PASS**；shot_basic_cells **36/36 PASS**；behavior_e2e **11 MATCH 不符合 0**；`grep -c 'DBG[' bundle.js`=0；bundle rebuild+verTag `v1788786384936`。**单列待复核 2 项**：① 攻钮布位=「ctrl 按钮区」（§9.3/L 环清单 §9.5-4）与「技能钮排下方」双短语取 ctrl 组件正上方锚定同宽（满足两短语的空间关系且不动已验收 ctrl 三钮布局/热区，代码绘制占位钮走既有降级模式无新素材）——若 Leo 目验要求异位，ATK_BTN 常量组单点可调；② main.ts 为 §9.3 预览 hover 管线最小接线（6 行级：pointermove 监听+resetDemo 清 hoverCell），§9.4 文件落点未列 main.ts 但预览 hover 无法绕开宿主转发，单列报备。 |
