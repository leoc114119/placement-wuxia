# 主架构（研发线）入职校准报告

> 日期：2026-09-05  
> 范围：只读校准；未修改代码、素材或需求文档。

## ① 当前研发状态复述

- **FACE-1 朝向规则**：数据层已完成并通过技术验收与 PM 四门复验（211 项绿、既有断言纯新增、红线文件零碰）。普攻/特绝/ATK-3 附带普攻均以受击敌位置定向；AOE 取最近受击敌，同距时在 seed 单流中恒消费一次 rng；空放仍保持点击格朝向，待 Leo 复核。`faceToward(req.to)` 的 AOE 点击格收尾已废止，同时修正了 axial 二维点积吸附和 `faceLeft` 判据。视觉层尚不能验收，须随 T45 六向帧接线卡的 L 环一起完成。
- **T45 六向制与素材两道关卡**：方向口径为 pointy-top（上下尖）六边形加奇偶行错位布点，因此使用右/右上/右下三向实绘与左系镜像派生；无上下向。当前普攻六向共 18 帧已入库，其他帧与敌方帧仍在产用队列中，未获第二道门检前不得接线。素材先由 Leo 作审美定稿，后由研发 PM 仅按尺寸、alpha、边缘、方向/姿势、色板、成套一致性等硬标准复核；第二道不过门即打回并附逐项缺陷清单。
- **projbus 消息总线基建**：工作区已有 `scripts/projbus/` 待纳入的基建产物；本岗位当前不将其视为已验收能力。实现验收复审尚待通过任务箱派发，届时按方案、接口契约、三零、既有用例与需求逐条对表执行。

## ② 风险与疑点清单

1. FACE-1 的数据层已闭合，但六向方向帧尚未接入，视觉层验收不可替代也不可提前宣告完成。
2. FACE-1 空放朝向仍是 Leo 悬置项；在获得明确裁定前，既有「朝点击格／点自己格保持原朝向」实现与断言均不得改写。
3. FACE-1 留有两项非阻断备忘：③b 的测试注释表述过度，以及 `faceTargetOf` 当前没有空数组守卫。两者均不可在无卡条件下顺手改动。
4. T45 素材生产与接线之间必须保持两道审查门；当前视觉通过或原始图可用不等于规格通过，更不等于允许接线。
5. 当前工作区已有与本次入职无关的修改和未跟踪产物（含 `scripts/projbus/`）；本次未触碰，后续验收须先以任务卡明确其授权范围和基线，避免将他人未提交状态误判为可验收交付。

## ③ 我理解的待办队列

1. FACE-1：仅在下一张获授权的同文件任务卡中处理两条备忘——③b 注释收敛、`faceTargetOf` 空数组守卫；空放朝向继续等待 Leo 裁定。
2. T45：待帧产出通过 Leo 目验与 PM 规格门检后，由研发 PM 发接线卡；该卡需完成六向帧表／契约导出与 L 环视觉验收，不能以当前 FACE-1 数据层通过替代。
3. projbus：等待研发 PM 从任务箱派发「实现验收复审」；届时依六步工作流完成答疑、方案核对、审查与需求验收，不预先改码或补充口径。

---

入职校准完成后停等任务箱或 Leo 直达指令。
