# 放置武侠 · 代码审查清单与记录 v0.1

> 状态：**生效**
> 日期：2026-08-18
> 定位：ZCode 产出的质量门禁。**ZCode 完成 → CodeBuddy 测跑通 → Leo 真实验收**，缺一不可。

---

## 1. 验收链条（固定流程）

```
ZCode 完成任务
   ↓
[1] CodeBuddy 工程层测试      ← 本文件约束这一环
    ├─ 编译/类型检查
    ├─ 静态检查
    ├─ 单元测试
    └─ 模拟运行核心逻辑
   ↓ 通过
[2] Leo 微信环境真实验收       ← 微信开发者工具打开试玩
   ↓ 通过
[3] 进入下一步开发（铁律：上一步未验收不进下一步）
```

**规则**：
- 第 1 环任何一项失败 → 记录问题 → 打回 ZCode 修改 → 重测
- 第 1 环通过 ≠ 可用，第 2 环（Leo 真实验收）是最终裁决
- 每步完成后，在本文件第 3 节追加一条审查记录

---

## 2. 审查标准（CodeBuddy 按此执行）

### 2.1 工程层检查（第 1 环，必须全过）

| 检查项 | 手段 | 判定 |
|---|---|---|
| 编译通过 | `tsc --noEmit` 或项目构建命令 | 0 error |
| 类型正确 | 同上（strict 下无 any 泄漏） | 0 error |
| 静态检查 | eslint / 项目 lint 命令 | 无 error |
| 单元测试 | 项目 test 命令 | 全绿 |
| 核心逻辑可跑 | node 模拟运行：数值结算、战斗、存档函数 | 输出符合设计文档 |
| 数值集中 | 检查 `config/numbers.ts` | 无其他文件硬编码数值 |
| 接口契约 | 对照 `types.ts` 和云函数约定 | 字段一致 |

### 2.2 设计一致性（对照文档）

- 实现是否符合对应设计文档（03 战斗 / 05 世界观等）
- UI 素材引用是否在 `assets/ui/` 且符合 04 风格规范色值
- 是否遵守 AGENTS.md 施工守则 6 条

### 2.3 可用性边界（诚实声明）

- **CodeBuddy 能测**：TypeScript 工程层（编译/逻辑/测试/模拟运行）
- **CodeBuddy 测不了**：Canvas 真实渲染效果、微信开发者工具预览、云函数部署后的线上行为 → 交给 Leo 第 2 环验收

---

## 3. 审查记录

### 记录格式

| 项 | 内容 |
|---|---|
| 日期 | |
| 审查对象 | （哪一步 / 哪个任务 / 涉及文件） |
| 编译/类型 | ✅ / ❌ + 问题 |
| 静态检查 | ✅ / ❌ + 问题 |
| 单元测试 | ✅ / ❌ + 问题 |
| 模拟运行 | ✅ / ❌ + 问题 |
| 设计一致性 | ✅ / ❌ + 偏差说明 |
| 结论 | 通过 / 打回 |
| Leo 验收 | 待验收 / ✅ / ❌ + 备注 |

### 记录表（按步骤追加）

#### 第 0 条 · 预置

> 尚未开始代码施工，无产出可审。首条记录将在 ZCode 完成第一步（最小工程跑通）后产生。

| 项 | 内容 |
|---|---|
| 日期 | 2026-08-18 |
| 审查对象 | 预置（无产出） |
| 结论 | 等待 ZCode 开工 |

---

## 4. 变更记录

| 日期 | 版本 | 变更 | 签字 |
|---|---|---|---|
| 2026-08-18 | v0.1 | 创建：审查清单与记录（ZCode → CodeBuddy → Leo 验收链条） | Leo |
