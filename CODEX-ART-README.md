# CODEX-ART-README · 美术管线入场须知（Codex 迁移版）

> 建立：2026-09-05 · 面向：在 Codex 中执行本仓库美术管线生产的会话/代理
> 推荐：独立 worktree 或独立 clone。若当前 Codex 会话只能使用共享工作区，则保留他人改动、只提交明确路径；git 纪律见 §四。

## 一、开工必读（按序，全部在仓库内）

1. `AGENTS.md` ——项目总约定（含素材审查门两道关卡）
2. `.codex/skills/art-pipeline-execution/SKILL.md` ——Codex 当前生产路线与停点规则
3. 本次任务明确引用的需求文档与资产规范；不为无关类别通读全部历史细则
4. `docs/design/03-战斗系统/战斗素材盘点与产用原则-v0.1.md` ——产用原则与素材审查门
5. 涉及角色帧时再读 `docs/design/01-基础功能/角色帧规范.md`；涉及 T45 时再读对应 T45 需求文档
6. `tasks/threads/ART-ARCH.md` ——接口咨询与主架构答复留痕

## 二、产线规矩（九条铁律，违反即打回）

1. **整图不裁切**：能整张直用的绝不裁切组件
2. **简单件才可生成**：组件级生成仅限少细节平涂粗边件
3. **锚定+单发**：多参照锚定+单发出图，禁链式修补
4. **质检门不过不接线**：尺寸/白底/边缘 alpha 程序化检查
5. **不可控项停报**：连续不过门时登记失败并提议降级路线；动作帧同一单帧同一手脚异常连续三次原生失败时，只有 Leo 已授权的 §五单图回退可以执行；代码降级由研发任务卡实施
6. **AI 稿禁文字**：文字一律代码绘制
7. **状态成套**：普通/激活变体同源生成，禁代码在美术件上叠装饰
8. **加工零副作用**（09-10 T45 清杂两轮实证）：清杂/抠底/归一/镜像等本地加工，除报"目标达成"外**必报"改动安全"**——对基线掩膜差分，**主体域删除像素数=0**；只报前者视为未过门。清除以**连通域像素集合**为单位，禁矩形/膨胀框批量清零；连通性判据与门检逐字一致（`alpha>32` + 4 邻接，`alpha>0` 会被光晕假性连通）。细则见 `.codex/skills/art-pipeline-execution/references/batch-closeout.md`「清杂」节
9. **线程轮换 + 上下文卫生**（09-10 实证新增，细则见 SKILL §12）：**触阈值时首选 `fork_thread`/`handoff_thread` 交接给新线程**（官内置，`codex_app` MCP；保留对话上下文、底层换新 rollout）——即"不换对话但重置记录"；不可用时才退回新线程+读交接文档。**一条线程只服务一个批次**，批次收尾即做上述交接。触发轮换的硬指标：单线程 **轮次 ≥150** 或 **rollout ≥300 MB** 或 **contextCompaction ≥30 次**。**程序化检查优先**：能出指标表的就不把图读进上下文；图只在必须目验时读，**一次一张按需**，禁批量灌入（contact sheet 每批读**一张**即可）。**机械执行（不是"记得看"）**：开工与收尾各跑一次 `python3 scripts/codex_thread_health.py`（**exit=2 即已触阈值**），读数写进交付回执；**硬上限**＝单批次 `imageView` **≤3 次**、单批次 **≤30 帧或 ≤1 个工作日**，到点即轮换、不因"快做完了"延期。**依据**：实测某线程 5 天被压缩 **68 次**、rollout 涨到 **1.65 GB** → 早先判据被摘要掉（反复重推已定事项）→ 最终会话库损坏、新轮次持久化失败、UI 永卡「思考中」（重启/resume 均无效）

## 三、交付与审查流程（两道关卡）

```
你产出一批 → Leo 目验（第一道关卡：审美定稿）
           → Leo 放行后 LOG 登记交付
           → ZCode PM 第二道审查（机械门检+规格符合性，见盘点 §2.5）
           → 过门：入库存档，研发发接线卡
           → 不过：打回单（逐条缺陷+违反门检条目），返工重过门
```

**交付登记最小集**：LOG 一行（批次/件数/消耗/Leo 目验结论）+ 产物入任务指定候选/正式路径 + 本任务自己的 thread/handoff。接口问题走 `tasks/threads/ART-ARCH.md`；不得修改他人任务内容。

## 四、git 提交纪律（撞车三次的教训）

- **禁 `git add -A` / `git add .` + 裸 commit**——会打包他人窗口已暂存内容
- **一律路径级**：`git commit <你自己的文件路径> -m "说明"`
- 提交前 `git status`：预暂存区有别人的东西，不动也不提交
- 自己 staged 的被别人提交走：内容没丢，`git log` 认领即可，**禁 revert / force-push**
- 交付只动**本任务自己的**路径：`tasks/LOG.md`、`tasks/threads/` 本任务线程、`tasks/handoff/`、`tasks/questions/`/`answers/` 本任务的问答件、素材路径——**他人任务文件、任务协议（box.db/index.json）与 docs/ 研发文档零沾**
- 首次创建新文件：先 `git add -- <明确文件>` 再路径级 commit（既有例外合并为主规则）

## 五、生图管线现状（09-07 条件回退修订）

- **默认：GPT 原生直出**（Codex/Plus，Leo 09-05 确认已跑通）——出图后仍须过机械门检（尺寸/白底/alpha），门检与生成源无关
- **严格例外：动作帧同一单帧同一手脚异常连续三次原生失败，且 Leo 已授权该条件时，可用 mxai API 的精确模型 slug `seedream-5.0-pro`（Seedream 5 Pro）做一次单图修正，固定 6 积分/张**；其他 Seedream 变体或厂商模型均禁用，必须单帧、单次、记账、保留原生失败证据，失败后停止，不得批量换源或顺带生成其他帧
- **其他情况：mxai 中转**仅供旧产物溯源；不得作为普通新任务、限流回退或替代生图通道
- 历史成本与失败案例：`docs/design/01-基础功能/` 各 T45 复盘文档（cast 五版教训/单发优于链式/多图参照 ≤6 张等，开工前必读——锚定参照方法论跨生成源通用）

## 六、边界

- 禁碰：`docs/design/03-战斗系统/` 研发规格文档、`systems/` `ui/` `config/` 代码、`tests/`、他人任务线程
- 美术需求文档（T45 需求文档等）归美术线维护，但**接口口径变更须走 ART-ARCH 通道经主架构确认**
- 规格、验收标准或预算缺失时，暂停依赖该口径的资产分支并走本任务 question/ART-ARCH 通道；继续其他独立检查与已授权产物。当前任务已明确的决定登记后直接执行，不重复确认
